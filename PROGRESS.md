# Voortgang & overdrachtsdocument

Dit bestand is bedoeld voor een AI-coding-agent (Claude Code, Codex, of wie
dan ook) die dit project overneemt in een nieuwe sessie zonder chatgeschiedenis.
Lees eerst `AGENTS.md` (de oorspronkelijke productspecificatie),
`PRODUCT_VISION.md` (het actuele productkompas), `DATAMODEL_AUDIT.md`
(toetsing van schema/architectuur tegen de visie), `WORKFLOW.md` (de
werkregels en Definition of Done — wordt automatisch geladen via
`CLAUDE.md`, dus vermoedelijk al bekend) en dan dit bestand voor de actuele
status. Loop je tegen een migratie-, test- of architectuurvraag aan, raadpleeg
dan ook `OPERATIONS.md`.

## Manier van werken

Zie `WORKFLOW.md` — die werkregels (branchstrategie, mergebeleid, wanneer
expliciet toestemming vragen, commit-taal) staan daar nu vast, zodat ze
automatisch geladen worden in elke sessie in plaats van dat je dit bestand
apart moet lezen.

## Status: work packages

Alle work packages hieronder zijn **gemerged in `main`** en staan live in
productie (Vercel + Supabase), tenzij anders vermeld.

| WP | Titel | Kern |
|----|-------|------|
| WP1 | Dieetbeperkingen | `src/lib/dietaryRestrictions.ts`, harde filtering in `ensureMealPlan` (`src/lib/mealPlan.ts`) — onbekende restrictie-tekst wordt nooit stilzwijgend genegeerd. |
| WP2 | Vaste boodschappen | `src/lib/fixedGroceries.ts`, sectie "Vaste boodschappen" op `/boodschappen`. |
| WP3 | Quantity-engine | `src/lib/quantity/*` — eenheden, verpakkingsberekening, voorraad-aftrek, tekstparser voor pakketgroottes. Pure, goed geteste module. |
| WP4 | Eenvoudig voorraadmodel | `InventoryItem`-model, `src/lib/inventory.ts`, sectie "Voorraadcontrole" op `/boodschappen`. Onbekende/lage voorraad wordt nooit als "genoeg" aangenomen. |
| WP5 | Productmatching als domein | `src/domain/product-matching/` (eerste map onder de doelarchitectuur `src/domain/`) — deterministische, uitlegbare matching, geen `Math.random()`. |
| WP6 | Controlepagina herbouwd | `/controle` in 3 secties (aandacht nodig / niet gevonden / vertrouwd), met verpakkingsberekening, hoeveelheid aanpassen, "alleen deze week", verwijderen. |
| WP7 | Picnic-mandje professionaliseren | `src/lib/picnic/cartService.ts` (idempotente add + clear), `PicnicNetworkError`/`PicnicApiError`, bevestigingsscherm vóór het vullen van het mandje (`src/lib/picnic/confirmationSummary.ts`), "mandje legen"-knop. |
| WP8 | Uitlegbare weekplanning-scoring | `src/domain/meal-planning/scoreMealPlanCandidate.ts` + `src/lib/mealPlan.ts` — `Math.random()` vervangen door deterministische score met stabiele tiebreak op variant-id, concrete redenen, recente-planning-signaal en variantvoorkeuren. |
| WP9 | Multi-household / authenticatie | `src/lib/auth.ts`, `/login`, `HouseholdSession` + toegangscode-hash — pagina's gebruiken het huidige huishouden uit een HttpOnly-sessiecookie en server actions valideren household-toegang voordat ze muteren. |
| WP10 | Persoonlijke gezinslogica | `Person.portionMultiplier`, `PersonPresenceOverride`, `src/domain/household/presence.ts` en bewerkbare gezinsleden op `/ons-gezin` — weekplanning filtert harde beperkingen per dag, boodschappenhoeveelheden schalen relatief op wie die dag mee-eet. |
| WP11 | Persoonsvoorkeuren per gerecht | Persoonlijke voorkeuren worden opgeslagen via bestaande `Preference(ownerType=PERSON, subjectType=RECIPE_VARIANT)` — op `/` kan per aanwezige eter favoriet/oké/liever niet/nooit worden gekozen; planning en `/gerechten` wegen dit per dag mee en sluiten persoonlijke `NEVER` uit. Geen nieuwe migratie nodig. |
| WP12 | Persoonsvoorkeuren per categorie/ingrediënt | De weekplanning kan per aanwezige eter nu ook voorkeuren voor receptcategorieën en ingrediënten vastleggen via dezelfde `Preference`-tabel. Planning en `/gerechten` wegen `RECIPE_CATEGORY` en `INGREDIENT` mee; persoonlijke `NEVER` sluit ook categorieën/ingrediënten uit. Geen nieuwe migratie nodig. |
| WP13 | Voorkeurenbeheer op Ons gezin | `/ons-gezin` toont nu een beheerblok voor alle persoonlijke voorkeuren per gezinslid, met leesbare labels voor gerecht/categorie/ingrediënt. Voorkeuren kunnen daar worden aangepast of verwijderd, met household-validatie in server actions. Geen nieuwe migratie nodig. |
| WP14 | Weekplanning opnieuw genereren | Op `/` staat een uitklapbare actie "Week opnieuw plannen". Deze verwijdert alleen de huidige weekplanning, bijbehorende boodschappenlijst en suggesties van het huidige huishouden, en bouwt de week opnieuw op met actuele voorkeuren. Geen nieuwe migratie nodig. |
| WP15 | Recepten/varianten beheren | Nieuwe pagina `/recepten` met navigatietab. Daar kan een recept met ingrediënten en eerste variant worden toegevoegd, kunnen titel/bron/categorie/status/eigenschappen/bereiding worden gecorrigeerd en kunnen varianten met context-signalen worden toegevoegd of bijgewerkt. Server actions valideren het huidige huishouden. Geen nieuwe migratie nodig. |
| WP16 | Ingrediëntenbeheer en receptingrediënten bewerken | `/recepten` heeft nu beheer voor ingrediënten: aanmaken, hernoemen, categorie/dieettags/voorraadcontrole aanpassen. Per bestaand recept kunnen ingrediëntregels en hoeveelheden worden vervangen; dubbele regels worden samengevoegd en de huidige boodschappenlijst wordt ongeldig gemaakt zodat nieuwe receptdata doorwerkt. Geen nieuwe migratie nodig. |
| WP17 | Productkeuzes per ingrediënt beheren | In `/recepten` kan per ingrediënt nu ook de productmatching worden beheerd: bekende producten bekijken, handmatig productkandidaten toevoegen, een standaardproduct vastleggen, producten uitsluiten of weer toestaan. Wijzigingen maken de huidige boodschappenlijst ongeldig zodat de volgende lijst de nieuwe standaardkeuze gebruikt. Geen nieuwe migratie nodig. |
| WP18 | Productvisie vastleggen | `PRODUCT_VISION.md` legt de aangescherpte richting vast: de app leert wat een huishouden eet en regelt boodschappen, met algemene slimme basis, huishouden-personalisatie, waarom-signalen, korte onboarding, expliciete bevestiging voor Picnic en beheer als secundaire route. Geen code- of migratiewijziging. |
| WP19 | Datamodel toetsen aan productvisie | `DATAMODEL_AUDIT.md` legt vast wat al goed staat en wat ontbreekt voor de zelflerende multi-household assistent: receptscope/eigenaarschap, typed tags, waarom-signalen, afgeleide patronen, rijkere onboarding, entry-context en provider-neutraliteit. Geen code- of migratiewijziging. |
| WP20 | Receptscope en basisdata scheiden | `Recipe.scope`, `householdId`, `originHouseholdId` en `promotedAt` scheiden globale basisrecepten van eigen huishoudrecepten. Seed-data blijft `GLOBAL`; nieuwe recepten op `/recepten` worden `HOUSEHOLD`; basisrecepten kunnen eerst naar een eigen kopie worden gekopieerd voordat ze bewerkt worden. Planning en gerechten tonen alleen globale/community-goedgekeurde recepten plus eigen huishoudrecepten. Migratie nodig: `20260727093000_recipe_scope`. |
| WP21 | Typed meal tags en slimme wens-invoer | `src/domain/meal-tags/mealTags.ts` introduceert een gecontroleerde taglaag boven bestaande categorieën/properties/contextFit, met herkenning van termen zoals AVG, snel, airfryer, kindvriendelijk, kip en sperziebonen. `/gerechten` heeft nu een wensveld ("AVG met sperziebonen en kip") dat suggesties filtert/rangschikt en kort uitlegt waarom iets past. De planner gebruikt dezelfde taglaag voor drukke dagen en kindvriendelijkheid. Geen nieuwe migratie nodig. |
| WP22 | Typed waarom-signalen bij vervangen | `FeedbackEvent.reason` en enum `FeedbackReason` leggen vast waarom een gebruiker een gerecht vervangt. `/gerechten` vraagt bij wisselen één korte reden, zoals "alleen nu iets anders", "niet lekker genoeg" of "te veel werk vandaag". `src/domain/learning/feedbackReasons.ts` zorgt dat redenen gecontroleerd blijven en dat bijvoorbeeld `TOO_MUCH_EFFORT` niet als smaakafkeur telt. Migratie nodig: `20260727104500_feedback_reasons`. |
| WP23 | Afgeleide patronen en leervragen | `LearnedPattern` en `LearningPrompt` leggen herhaalde gedragssignalen vast zonder meteen harde conclusies te trekken. Bij drie vervangingen van dezelfde receptcategorie op dezelfde dag ontstaat een pending leervraag. `/` toont maximaal twee slimme vragen tegelijk en antwoorden bevestigen of verwerpen het patroon. Migratie nodig: `20260727113000_learning_patterns`. |
| WP24 | Onboardingprofiel met snel/beter afstemmen | Onboarding heeft nu twee routes: `QUICK` voor snel starten en `DETAILED` voor meer afstemming. `Household.onboardingMode`, `planningStyle` en `maxSmartQuestionsPerSession` leggen het startprofiel vast. De planner gebruikt `planningStyle` als zachte score: veilig starten geeft bewezen/veilige gerechten voorrang, nieuwsgierig geeft nieuwe suggesties meer ruimte. Migratie nodig: `20260727133000_onboarding_profile`. |
| WP25 | Hoofdflow als assistent | `/` opent nu met een compacte assistentkaart die de eerstvolgende logische stap toont: leervraag beantwoorden, boodschappen voorbereiden, productkeuzes controleren of bevestigen. Weekstatistieken zijn compacter, leervragen gebruiken `maxSmartQuestionsPerSession`, en "Week opnieuw plannen" staat onder rustige meer-acties in plaats van bovenin als beheeractie. Geen nieuwe migratie nodig. |
| WP26 | Aanvullen en controle versimpelen | `/controle` toont nu eerst alleen uitzonderingen en onzekerheden; vertrouwde keuzes staan ingeklapt maar blijven bewerkbaar. `/boodschappen` toont vaste boodschappen en voorraadcheck als samenvattende uitklapsecties in plaats van grote open beheerblokken. Geen nieuwe migratie nodig. |
| WP27 | Directe knopfeedback op controle | Controle-acties gebruiken nu een client-side pending-knop (`PendingSubmitButton`) zodat klikken direct zichtbaar wordt met teksten zoals "Opslaan..." of "Kiezen...". Al opgeslagen/trusted producten tonen een groene "Opgeslagen"-status in plaats van opnieuw "Goed, onthouden", zodat de gebruiker ziet dat de keuze verwerkt is. Geen nieuwe migratie nodig. |
| WP28 | Echte product- en gerechtfoto's | `/controle` en `/boodschappen` verrijken bestaande productkeuzes automatisch met Picnic-afbeeldingen, prijs en verpakkingsinfo wanneer er een goede zoekmatch is, zonder het Picnic-id stilzwijgend als bevestigd product vast te leggen. Boodschappen tonen productminiaturen. Recepten hebben nu echte fotovelden (`imageUrl`, credit en bron), `/`, `/gerechten` en `/recepten` tonen alleen gekoppelde echte gerechtfoto's en geen nep-previewfoto's. Migratie nodig: `20260727162000_recipe_images`. |
| WP29 | Controle blijft op dezelfde plek | Acties op `/controle` redirecten nu terug naar dezelfde productregel via `?focus=<lineId>#line-<lineId>`, zodat zoeken of aanpassen midden op de pagina niet meer bovenaan uitkomt. Een gefocuste vertrouwde regel opent automatisch de ingeklapte sectie. Knoppen en de onderste navigatie hebben duidelijkere hover/focus/active feedback zodat selectie en klikken zichtbaar aanvoelen. Geen nieuwe migratie nodig. |
| WP30 | Vaste boodschappen zoeken en productkeuze sturen | Nieuwe vaste boodschappen worden niet meer uit een ingrediënt-dropdown gekozen: op `/boodschappen` kan de gebruiker live Picnic-producten zoeken (bv. "appels"), echte producten met foto/prijs/verpakking zien en een product als vaste boodschap kiezen. Die keuze maakt/hergebruikt een ingrediënt, voegt de vaste boodschap toe aan de huidige lijst en onthoudt het gekozen Picnic-product meteen als standaard voor dat ingrediënt. `/ons-gezin` heeft nu een productkeuze-instelling (`Gebalanceerd`, `Voordelig`, `Bekende verpakking`) die de automatische rangschikking stuurt wanneer er nog geen vertrouwde productkeuze is. Geen nieuwe migratie nodig; dit gebruikt `Household.deliveryPreference`. |
| WP31 | Navigatie en controle sneller maken | `/boodschappen` en `/controle` wachten niet meer op live Picnic-afbeeldingsverrijking tijdens paginaload; die draait via `after(...)` na de response. `/controle` haalt productkandidaten nu in één batch op in plaats van per regel apart. Er is een globale `loading.tsx` toegevoegd zodat routewissels meteen visuele feedback geven. Geen nieuwe migratie nodig. |
| WP32 | Vaste boodschappen wijzigen en klikfeedback | Vaste boodschappen op `/boodschappen` kunnen nu echt worden gewijzigd via dezelfde Picnic-zoekflow als toevoegen: de bestaande regel wordt vervangen, de standaard vaste boodschap wordt bijgewerkt en de pagina komt terug bij dezelfde regel. De boodschappenpagina maakt de Picnic-kopieerlijst uit de reeds opgehaalde regels in plaats van de lijst opnieuw op te vragen. De onderste navigatie toont direct een pending-status op de aangeklikte tab. Geen nieuwe migratie nodig. |
| WP33 | Performance quick wins zonder migratie | `/boodschappen` en `/controle` hergebruiken nu de shopping list die `ensureShoppingList` al ophaalt, in plaats van dezelfde lijst direct opnieuw te lezen. Picnic-fotoverrijking start alleen nog wanneer er daadwerkelijk producten zonder afbeelding zijn. `/controle` laadt standaard alleen productkandidaten voor aandacht-regels en een eventueel gefocuste vertrouwde regel. `/` hergebruikt het resultaat van `ensureMealPlan` en telt reviewregels met een lichte count-query. Geen nieuwe migratie nodig. |
| WP34 | Performance-indexen en product-upserts | Hot-path indexen toegevoegd voor reviewtellingen, vaste-regel lookups en feedbackgeschiedenis. `Product(ingredientId, externalRef)` is nu uniek, met een migratiestap die bestaande dubbele Picnic-producten eerst samenvoegt. Picnic-zoekresultaten worden nu met upserts opgeslagen in plaats van find-then-update/create. Migratie nodig: `20260727173500_performance_indexes`. |
| WP35 | Jouw week als assistent-flow | De homepage is vereenvoudigd: bovenaan staat de eerstvolgende assistentactie, direct daaronder een natuurlijke wens-invoer ("AVG met kip en sperziebonen") die naar passende gerechten leidt. Het weekmenu is scanbaarder gemaakt met duidelijke `Wissel`-acties per dag; uitleg en persoonlijke voorkeuren zitten rustig ingeklapt onder "Waarom dit?" en "Voorkeuren aanpassen". Geen nieuwe migratie nodig. |
| WP36 | Favorieten en productvoorkeuren aanpassen | `/ons-gezin` heeft nu zichtbare beheerblokken voor huishoudenvoorkeuren: maaltijdsoorten kunnen direct op `Favoriet`, `Oké`, `Liever niet` of `Geen voorkeur` worden gezet, en onthouden Picnic-productkeuzes kunnen per ingrediënt worden vergeten. Productkeuze-rangschikking blijft apart instelbaar. Geen nieuwe migratie nodig. |
| WP37 | Receptenpagina versimpeld | `/recepten` toont nu eerst een rustigere receptenbibliotheek: nieuw recept en receptenlijst komen vóór technisch ingrediënt/productbeheer, geavanceerd beheer staat onderaan ingeklapt en per recept zijn detailbewerking, ingrediënten en varianten ingeklapt. Geen nieuwe migratie nodig. |
| WP38 | Productkeuze en concrete maaltijdwens | `/controle` geeft na productacties een zichtbare statusmelding op dezelfde regel en vult bij regels met te weinig bekende keuzes automatisch live Picnic-alternatieven aan, met fallback als Picnic niet reageert. `/gerechten` kan een concrete wens met minimaal drie herkende ingrediënten (bijv. kip, rijst en paprika) direct als eigen huishouden-maaltijd plannen, terwijl bestaande recepten als alternatief zichtbaar blijven. Geen nieuwe migratie nodig. |
| WP39 | Maaltijdwens-parser aangescherpt | Concrete wensen zoals `kip rijst boontjes` kiezen nu één beste ingrediënt per genoemd product: algemene `kip` wordt standaard kipfilet in plaats van alle kipvarianten, `boontjes` wordt herkend als sperziebonen en specifieke woorden zoals `kipdijfilet` blijven specifiek. Tests dekken deze voorbeelden. Geen nieuwe migratie nodig. |
| WP40 | Bulk vaste boodschappen invoeren | `/boodschappen` heeft nu een plakveld voor vaste boodschappenlijsten. Regels of komma-gescheiden items zoals `2 pakken magere melk, drinkyoghurt framboos, bananen` worden gesplitst, per regel live bij Picnic gezocht en onder elkaar getoond met foto, prijs en verpakking. De gebruiker kan beste keuzes in één keer opslaan of per regel een alternatief kiezen; gekozen producten worden meteen als vaste boodschap en standaardproduct onthouden. Geen nieuwe migratie nodig. |
| WP41 | Bulk boodschappenlijst blijft staan | Als de gebruiker binnen de bulk vaste-boodschappen-preview één product los opslaat, blijft de originele geplakte lijst in beeld en wordt dezelfde preview opnieuw geopend. Daardoor kan de gebruiker de rest van de lijst verder afwerken zonder opnieuw te plakken of zoeken. Geen nieuwe migratie nodig. |
| WP42 | Bestellen in verpakkingen tonen | Vaste boodschappen gebruiken nieuwe productkeuzes voortaan als aantal verpakkingen (`1x pak melk`, `1x doos eieren`) in plaats van inhoudshoeveelheid (`1000 ml`, `10 stuks`). Bestaande vaste regels met bekende productverpakking worden in de UI omgerekend naar verpakkingen. De weeklijst toont bij receptproducten nu het aantal te bestellen verpakkingen (`4x aardappeltjes`) terwijl productnaam/verpakking zichtbaar blijft. Picnic-mandje gebruikt vaste `PIECE`-regels ook als pakket-aantal. Geen nieuwe migratie nodig. |
| WP43 | Per-dag boodschappencontrole | `/boodschappen` toont nu vóór de totaalbestelling een per-dag controleweergave: dag + gerecht, status compleet/controleren, producten met foto, verpakking, behoefte voor dat gerecht, totaal te bestellen verpakkingen en snelle links naar productcontrole of ander gerecht. De oude losse "Per maaltijd bekijken"-sectie is verwijderd zodat de route rustiger wordt. Geen nieuwe migratie nodig. |
| WP44 | Gekozen bulkregel verdwijnt | In de bulk vaste-boodschappen-preview verdwijnt een regel nu zodra de gebruiker daar een product voor opslaat. De originele lijst wordt automatisch zonder die regel opnieuw geopend, zodat alleen de nog te kiezen producten overblijven. Geen nieuwe migratie nodig. |
| WP45 | Dagcontrole met acties en kosten | `/boodschappen` toont per maaltijd nu een daginschatting van de kosten en per product de productkosten voor die maaltijd. Dagregels hebben directe acties voor weekaantal `+/-`, deze week verwijderen, huidige keuze opnieuw bevestigen, alleen deze week kiezen en product onthouden. Bekende alternatieven staan direct onder dezelfde dagregel en acties keren terug naar dezelfde plek met een zichtbare statusmelding. Geen nieuwe migratie nodig. |
| WP46 | Recept snel toevoegen | `/recepten` heeft nu bovenaan een snelle invoer met alleen receptnaam en een vrij tekstveld voor ingrediënten/hoeveelheden (`400g kipfilet`, `300 gram rijst`, `2 paprika`). De parser maakt of hergebruikt ingrediënten, combineert dubbele regels en zoekt bij gekoppelde Picnic-accounts automatisch productkandidaten met foto, prijs en verpakking zodat de boodschappencontrole daarna direct bruikbaar is. Het oude uitgebreide receptformulier staat onder geavanceerd. Geen nieuwe migratie nodig. |
| WP47 | Dagcontrole sluit beantwoorde keuzes | Op `/boodschappen` verdwijnt de huidige-product-keuzekaart zodra een product met `Onthouden` of `Alleen deze week` is bevestigd; alleen de statusmelding blijft staan. Productregels hebben naast `+/-` nu ook een direct invoerveld voor het aantal te bestellen verpakkingen/stuks, en de zichtbare bestelprijs rekent mee met het actuele weekaantal. Geen nieuwe migratie nodig. |
| WP48 | Zoeken overrulet gerecht-suggesties | `/gerechten` zoekt nu ook expliciet op recepttitel. Een zoekopdracht zoals `kofta` toont een eigen recept met die naam bovenaan, ook als er geen bekende tag of ingrediëntmatch is. Dieetrestricties en persoonlijke `nooit`-voorkeuren blijven wel harde filters. Geen nieuwe migratie nodig. |
| WP49 | Losse maaltijd per dag | Op `/` kan per dag nu een losse maaltijd worden ingevuld met naam en vrije productregels, bijvoorbeeld `Airfryeravond` met `Kai: frikandel` en `Ellen: mini kaassouffle`. Persoon-prefixen worden niet onderdeel van het ingrediënt, de app maakt/hergebruikt ingrediënten, zoekt bij gekoppelde Picnic-accounts productkandidaten en plant de losse maaltijd direct op die dag in. De boodschappenlijst wordt daarna opnieuw opgebouwd. Geen nieuwe migratie nodig. |
| WP50 | Maaltijdhoeveelheid versus weektotaal | `/boodschappen` toont in de per-dag controle nu het aantal verpakkingen en de kosten voor die specifieke maaltijd, bijvoorbeeld 800 g aardappelblokjes → 2x 750 g en 400 g boontjes → 1x 600 g. Het geaggregeerde weektotaal blijft apart beschikbaar onder "Weektotaal aanpassen", zodat dagcontrole en weekbestelling niet meer door elkaar lopen. Geen nieuwe migratie nodig. |
| WP51 | Vaste daggewoontes bovenop losse maaltijden | Nieuw `DayRoutine`-model: één expliciet onthouden daggewoonte per dag per huishouden. `ensureMealPlan` gebruikt een geldige gewoonte als voorstel voor die dag in plaats van de gewone scoring, maar valt terug op normale scoring zodra de gewoonte een harde beperking zou schenden. Op `/` staat per dag een "Onthoud voor elke [dag]" / "Stoppen"-toggle. Bijvangst: vaste boodschappen konden bij een actieve regel nog niet direct verwijderd worden (alleen via de omweg "deze week niet nodig" → "verwijder voorgoed"); dat kan nu rechtstreeks. Migratie nodig: `20260727180000_day_routines`. |
| WP52 | MealPlanEntry-context | `MealPlanEntry` uitgebreid met `source` (AUTO/MANUAL/ASSISTANT/REGENERATED), `status` (PROPOSED/ACCEPTED — `REPLACED` bewust nog niet in gebruik, vereist entry-geschiedenis), `reason`, `score`, `confidenceLevel`, `replacedFromRecipeVariantId`, gevuld op elk schrijfpunt (`ensureMealPlan`, handmatig wisselen, maaltijdwens, losse maaltijd). "Waarom dit?" op `/` gebruikt nu `entry.reason` rechtstreeks — handmatige wissels en losse maaltijden tonen daardoor nu ook een reden, wat voorheen leeg bleef. `getReasonsForPlan`/`MealSuggestion`-lookup is verwijderd. Migratie nodig (2 stappen): `20260727190000_meal_plan_entry_context`, `20260727190100_meal_plan_entry_updated_at_drop_default`. |
| UX WP1 | `/boodschappen` naar 1 samengevatte lijst | Gebruikersgevraagde kritische UX-review tegen `PRODUCT_VISION.md`: "Jullie boodschappenlijst" staat nu direct bovenaan als primaire, samengevatte weergave; de volledige per-dag uitsplitsing staat ongewijzigd maar standaard dicht in een uitklapper "Bekijk per dag". Paginahoogte in de standaardweergave met -72% (12.258px → 3.431px), zonder functieverlies. Geen nieuwe migratie nodig. |
| UX WP2 | Navigatie, titels, controle-teksten, startscherm compacter | Vervolg op de UX-review. Onderste navigatie gebruikt nu korte labels (`Lijst`, `Gezin`, …) met volledige tekst als `aria-label`, zodat labels niet meer afknippen. Recepttitels op `/` en `/gerechten` knippen niet meer af maar wrappen (`line-clamp-2`) naar twee regels. Productmatch-redenen op `/controle` zijn niet langer een letterlijke herhaling van de huishoudinstelling ("gebalanceerde productkeuze" op elke regel) maar leggen per product specifiek uit hoe het zich verhoudt tot de andere beschikbare opties (bijv. "Goedkoopste van 2 beschikbare opties"). Op `/` zijn de twee losse dagkaart-secties "Losse maaltijd invullen" en "Voorkeuren aanpassen" samengevoegd tot één uitklapper "Meer voor deze dag", zodat elke dag nog maar één actieblok toont in plaats van twee. Geen nieuwe migratie nodig. |
| Bugfix | Halve stuks op `/boodschappen` | Wanneer een product z'n `packageQuantity` onbekend is (alleen een vrije tekst-`packageSize`), viel de per-dag en weektotaal-weergave voor stuks-ingrediënten terug op de ruwe, door portieschaling soms gebroken receptbehoefte (bijv. "0.5x" bij 50% portie van een ingrediënt in stuks) — verwarrend, want dat oogt als een besteladvies terwijl je nooit een half stuk kunt bestellen. `formatQuantity` in `src/app/boodschappen/page.tsx` rondt stuks-hoeveelheden nu altijd naar boven af (`Math.ceil`), consistent met het "nooit afronden naar beneden"-principe van de verpakkingsengine. Losstaand gefixt: een pluralisatie-typefout "alternatiefven" (moest "alternatieven" zijn). Geen nieuwe migratie nodig. |
| Feature | Vangnet tegen stilzwijgend onder-bestellen | Een `ShoppingListLine` kan na het aanmaken handmatig verlaagd zijn (via "Weektotaal aanpassen") tot onder wat de geplande maaltijden deze week daadwerkelijk nodig hebben — de verpakkingsengine rondt altijd naar boven af, maar kan dat niet corrigeren als de behoefte zelf al te laag de berekening ingaat. `findShoppingListShortfalls` (`src/lib/shoppingList.ts`) telt de echte receptbehoefte per ingrediënt opnieuw op (gedeelde `aggregateMealNeeds`-helper, ook gebruikt door `ensureShoppingList`) en vergelijkt die met de huidige regel. Op `/boodschappen` toont een regel met een tekort nu expliciet "Dit is X g minder dan nodig..." met twee acties: "Aanvullen" (herberekent en zet de hoeveelheid recht) of "Toch doorgaan" (bewust accepteren, geen herhaalde melding tot de hoeveelheid weer handmatig wijzigt). Nieuw veld `ShoppingListLine.shortfallAcknowledged`. Migratie nodig: `20260728083705_shopping_list_line_shortfall`. |
| Bugfix | `/recepten`-wijzigingen leken te "reverten" | Root cause gevonden via live reproductie: server actions op `/recepten` (bijv. "Maak standaard" voor een productkeuze) sloegen wel correct op (geverifieerd in de database), maar riepen alleen `revalidatePath` aan zonder navigatie — de al open pagina bleef daardoor de oude data tonen totdat de gebruiker handmatig herlaadde. Dat voelde aan als "mijn keuze werd genegeerd", terwijl de data allang goed stond. Alle 13 server actions in `src/app/recepten/actions.ts` redirecten nu na een geslaagde opslag terug naar `/recepten?status=...`, wat een verse render afdwingt én een expliciete groene bevestiging toont ("Standaardproduct opgeslagen.", "Ingrediënten opgeslagen.", enz.) — dezelfde aanpak als het bestaande statusmeldingen-patroon op `/boodschappen`. Bijeffect: opengeklapte `<details>`-secties sluiten na het opslaan (geen gerichte heropen-op-dezelfde-plek zoals `/boodschappen` heeft — kan later worden toegevoegd). Geen nieuwe migratie nodig. |
| Feature | Bevestiging bij elke opslagactie (homepage + `/ons-gezin`) | Vervolg op de `/recepten`-fix: hetzelfde patroon (`revalidatePath` zonder redirect, dus geen zichtbare bevestiging) zat ook in `src/app/actions.ts` (8 functies, 0 redirects) en `src/app/ons-gezin/actions.ts` (11 functies, 1 redirect — alleen `logout`). Alle opslagacties op de homepage (`/`) en `/ons-gezin` redirecten nu terug naar dezelfde pagina met `?status=...` en tonen een expliciete groene bevestiging bovenaan ("Losse maaltijd ingepland.", "Onthouden als vaste gewoonte.", "Profiel opgeslagen.", "Toegangscode opgeslagen.", enz.) — dezelfde `redirectToHome`/`redirectToOnsGezin`-helpers als bij `/boodschappen` en `/recepten`. Geen nieuwe migratie nodig. |
| Feature | Recept verwijderen | `/recepten` had nergens een manier om een eigen recept te verwijderen. Nieuwe actie `deleteRecipe` (alleen voor eigen huishoudrecepten, nooit basisrecepten) blokkeert expliciet — met een leesbare melding, niet stilzwijgend — als het recept deze week nog op het menu staat of als vaste daggewoonte is ingesteld; anders worden bijbehorende (historische) `MealPlanEntry`/`MealSuggestion`-regels en het recept zelf in één transactie verwijderd. Bijvangst tijdens het testen: een geblokkeerde verwijdering toonde een generieke Next.js-crashpagina in plaats van de foutmelding, omdat `/recepten` geen `error.tsx` had — dat gold voor elke bestaande `throw new Error(...)` op deze pagina (bijv. ook de "titel bestaat al"-check), niet alleen voor deze nieuwe actie. Nieuwe `src/app/recepten/error.tsx` toont voortaan de eigen foutmelding netjes met een "Opnieuw proberen"-knop. Geen nieuwe migratie nodig. |
| Fix | Laatste opslagfeedback en algemene foutboundary | Codex-overname vanaf commit `7637057`: overgebleven `/boodschappen`-acties voor vaste boodschappen en voorraadstatus redirecten nu na succesvolle opslag terug naar de juiste sectie/regel met `?status=...`, openen de relevante `<details>` en tonen een groene bevestiging. De twee `/gerechten`-acties die een gerecht wisselen of een concrete maaltijdwens plannen redirecten nu naar `/` met een zichtbare statusmelding. Nieuwe `src/app/error.tsx` geeft alle routes zonder eigen error-boundary (o.a. `/`, `/gerechten`, `/boodschappen`, `/controle`, `/ons-gezin`) een nette "Dat lukte niet"-melding in plaats van de generieke crashervaring; `/recepten/error.tsx` blijft pagina-specifiek. Verificatie: `npm run lint` groen, `npx tsc --noEmit` groen na `npx prisma generate`, `npm run build` groen met netwerktoegang voor Google Fonts. `npm test` startte en 122/144 tests slaagden; de 22 failures waren allemaal integratietests met dezelfde lokale oorzaak: database `jurgen` bestaat niet op de lokale Postgres-server. Geen nieuwe migratie nodig. |
| WP53 | Leren van stil geaccepteerde weekplanning | Eerste toepassing van `MealPlanEntry.status` uit WP52: bij `confirmShoppingList` markeert `acceptProposedMealPlanEntries` alle nog-`PROPOSED` regels met source `AUTO`/`REGENERATED` als `ACCEPTED`, logt per regel een zachte impliciete `CHOSEN`-feedbackevent met context `source: "silent_week_acceptance"`, herberekent de variantconfidence en laat bestaande promotielogica voorzichtig meewegen. Handmatige en assistent-keuzes tellen hier niet dubbel mee, omdat die al expliciet gekozen zijn. De weekplanning gaat bij boodschappenbevestiging naar `GROCERIES_READY`, en `/boodschappen` toont een groene bevestiging dat het weekmenu als "goed genoeg" meetelt. Nieuwe pure helper + tests: `src/domain/meal-planning/silentAcceptance.ts`. Geen nieuwe migratie nodig. |
| WP54 | Leervragen uit herhaald stil accepteren | Stil geaccepteerde weekplanningen voeden nu voorzichtig dezelfde leerlaag als vervangingen: `recordRepeatedMealAcceptance` verhoogt per huishouden/categorie/dag een `MEAL_CATEGORY_ACCEPTED_ON_DAY`-patroon en maakt pas na 3 signalen een `CONFIRM_REPEATED_ACCEPTANCE`-leervraag. De homepage gebruikt nu antwoordopties per prompt, zodat acceptatievragen positieve opties tonen ("Vaker zo plannen", "Toeval") in plaats van alleen vervangingsredenen. Dit maakt WP53 bruikbaar zonder direct harde daggewoontes of voorkeuren te zetten. Migratie nodig: `20260728171000_silent_acceptance_learning`. |
| WP55 | Bevestigde acceptatiepatronen sturen planning zacht | De weekplanner leest nu bevestigde `MEAL_CATEGORY_ACCEPTED_ON_DAY`-patronen met `confirmedReason=ALWAYS_USE` en geeft die categorie op dezelfde dag een uitlegbare scorebonus ("past bij wat jullie vaker op dinsdag willen eten"). Dit is bewust een zachte bonus in `scoreMealPlanCandidate`, geen harde routine: persoonlijke `NEVER`-voorkeuren, dieetrestricties en vaste daggewoontes blijven winnen. Nieuwe scoringtests dekken zowel de bonus als het niet-overrulen van een persoonlijke nooit-voorkeur. Geen nieuwe migratie nodig. |
| WP56 | Geleerde weekpatronen beheren | `/ons-gezin` toont nu een compact uitklapblok "Wat ik over jullie week heb geleerd" met echte `LearnedPattern`-regels: type patroon, dag/categorie, status, aantal signalen, zekerheid en eventuele bevestigde reden. Per patroon kan de gebruiker het patroon vergeten; daarbij worden open leervragen voor dat patroon ook dismissed en redirect de pagina terug met een groene bevestiging. Geen nieuwe migratie nodig. |
| WP57 | Daggerichte gerechtvoorkeuren | Nieuw beheerblok `DayRecipePreferencesManager` op `/ons-gezin`: per gerecht kan het huishouden aangeven of het "vaak", "soms" of "liever niet" op een specifieke dag past. Hergebruikt bewust de bestaande `Preference`-tabel (`ownerType=HOUSEHOLD`, samengestelde `ownerId` via `dayRecipePreferenceOwnerId(householdId, dayKey)`, `subjectType=RECIPE_VARIANT`) — geen nieuw model, geen nieuwe migratie. `scoreMealPlanCandidate` geeft een uitlegbare scorebonus/-malus per stance ("staat in jullie vaste opties voor dinsdag" / "kiezen jullie minder graag op dinsdag"), en `/gerechten` kan nu ook op deze dagopties filteren, met een link vanaf het weekmenu die het filter direct opent. |
| Bugfix | Vaste boodschappen: geen productfoto's + onzinnige verpakkingsinfo | Twee gebruikersgerapporteerde problemen. (1) De sectie "Vaste boodschappen" op `/boodschappen` toonde nooit een productfoto — de backend-verrijking (`enrichShoppingListProductImages`) dekte deze regels al, maar de JSX had er simpelweg geen `<ProductThumb>`/`<ProductImage>` staan. Toegevoegd voor zowel actieve als "deze week niet nodig"-regels (het laatste met een extra query naar `HouseholdProductPreference` voor het onthouden product, want die regels hebben geen eigen `ShoppingListLine` meer). Bijvangst: de knoppenrij naast de naam kneep de productnaam op mobiel tot bijna 0px breed (bv. "Ri…" i.p.v. "Rijstwafels") — nu staat de naam/foto op een eigen rij en de acties op een wrappende rij eronder. (2) Op `/controle` toonde een vaste boodschap in stuks (bv. "1x rijstwafels") soms een onzinnige verpakkingsregel als "1 verpakking · 130x totaal · 129x over": `Product.packageQuantity` wordt altijd berekend in de eenheid van het ingrediënt (vaak gram/ml), maar `describeLinePackaging` labelde dat getal blind met `line.unit` — voor een vaste-stuks-regel klopt die eenheid niet. `PackagingLine` op `/controle` slaat de verpakkingsberekening nu over voor `FIXED`-regels in stuks, exact dezelfde regel als `formatOrderQuantity` op `/boodschappen` al hanteerde. Geen nieuwe migratie nodig. |
| Bugfix | Onderste navigatie verdween tijdens scrollen op mobiel | Gebruikersrapport: op telefoon verdween de vaste onderste menubalk kort tijdens actief scrollen door een lange lijst en kwam pas terug zodra het scrollen stopte. `NavBar.tsx` combineerde `position: fixed` met een semi-transparante achtergrond (`bg-surface/95`) en `backdrop-blur` — een bekend WebKit/iOS Safari-bugpatroon waarbij de browser een `backdrop-filter`-laag op een fixed element tijdens actief scrollen tijdelijk laat vallen. Vervangen door een volledig ondoorzichtige achtergrond (`bg-surface`, geen alpha, geen blur) plus een GPU-compositing hint (`translateZ(0)`) om de laag stabiel te houden. Geverifieerd: `tsc`/`eslint`/`npm test` (150/150)/`npm run build` groen; Playwright-controle bevestigt `position: fixed`, ondoorzichtige `rgb(255, 255, 255)`-achtergrond en correcte weergave van alle 6 items. Het exacte iOS-Safari scroll/toolbar-gedrag zelf kan niet 1-op-1 gereproduceerd worden in deze headless Linux-sandbox — dit is de standaard oplossing voor dit bekende bugpatroon, geen 100%-geverifieerde reproductie. Geen nieuwe migratie nodig. |
| Feature | Gerechtfoto's verwijderd + ingrediënten inzichtelijk op de week | Gebruiker vroeg eerst om automatisch gegenereerde/gezochte gerechtfoto's; na afweging van kosten (AI-generatie) en beperkte gratis limieten (stockfoto-API's) koos de gebruiker ervoor de hele fotofunctie te laten vallen ("voegt niet zoveel toe en kan alleen maar fouten opleveren"). `RecipePhoto`-component en alle gebruik ervan (`/`, `/gerechten`, `/recepten`) verwijderd, inclusief de losstaande "Echte foto-URL"-velden in de geavanceerde recepten-formulieren op `/recepten` (die zonder weergave nergens meer voor dienden) en de bijbehorende parsing in `src/app/recepten/actions.ts`. Als vervanging vroeg de gebruiker om op de hoofdpagina snel te kunnen zien waar een gerecht uit bestaat. Elke dag in "Jullie weekmenu" heeft nu een uitklapper "Ingrediënten (n)" met alle receptingrediënten (hoeveelheid + eenheid) en een badge per ingrediënt: "Vers" (vlees, vis, zuivel, groente, fruit) of "Houdbaar/pak" (graan, peulvrucht, voorraadkast) — gebaseerd op de bestaande `IngredientCategory`, geen nieuw model of migratie nodig. Nieuwe gedeelde helpers `UNIT_LABELS`/`ingredientFreshness` in `src/lib/categoryStyle.ts`; ongebruikte `CATEGORY_GRADIENT` (nooit geïmporteerd) in dezelfde opruiming verwijderd. Geen nieuwe migratie nodig; `Recipe.imageUrl`/`imageAttribution`/`imageSourceUrl` blijven ongebruikt in het schema staan (geen risico, geen migratie voor nodig). |
| Bugfix | Vers/Houdbaar-badge negeerde het echte gekozen product + wenszoekbox stond op onlogische plek | Twee gebruikersgerapporteerde problemen op de zojuist toegevoegde "Ingrediënten"-uitklapper. (1) De Vers/Houdbaar-badge was puur gebaseerd op de statische `IngredientCategory` van het ingrediënt (bv. "Aardappelen" = VEGETABLE = altijd "Vers"), zonder te kijken naar het daadwerkelijk gekozen product — een diepvriesvariant van hetzelfde ingrediënt kreeg dus onterecht "Vers". `ingredientFreshness` in `src/lib/categoryStyle.ts` kijkt nu eerst naar de productnaam van de regel op de actuele boodschappenlijst (`ShoppingListLine.product`), of anders de onthouden standaardkeuze (`HouseholdProductPreference.product`), en herkent daarin "diepvries"/"ingevroren" en "blik"/"pot"/"conserven"; alleen zonder bekend product valt hij terug op de categorie-inschatting. `/` haalt deze productnamen nu op per ingrediënt van het weekmenu. Geverifieerd door lokaal een diepvriesproduct als boodschappenlijst-regel te koppelen aan "Wortel": de badge wisselde correct van "Vers" naar "Diepvries" (testdata na verificatie weer teruggedraaid). (2) De wenszoekbox "Toch ergens anders zin in?" stond bóven "Jullie weekmenu", terwijl de tekst impliciet verwijst naar een menu dat de gebruiker op dat moment nog niet heeft gezien — logisch onlogisch, zoals de gebruiker zelf aangaf. Verplaatst naar direct onder het weekmenu, vlak vóór de "Naar boodschappenlijst"-knop: eerst het voorstel zien, dan pas de gelegenheid om iets anders te kiezen voor een specifieke dag. Geen nieuwe migratie nodig. |
| Bugfix | Vers/Diepvries-badge alsnog weggehaald | Ook met de link naar het echte product bleef de badge in de praktijk onbetrouwbaar aanvoelen (screenshot van de gebruiker: "Tomaten" als "Overig", "Wereldgerecht Kofta" als "Overig", terwijl losse producten als "vissticks" niet als diepvries herkend worden — productnamen zijn simpelweg te wisselend om hier hard op te vertrouwen). De gebruiker gaf aan dat het doel nooit de badge zelf was, maar puur het kunnen zien welke ingrediënten in een gerecht zitten (bv. "pot bolognesesaus" vertelt je al dat het gerecht snel klaar is, zonder dat daar een apart label bij hoeft). Vers/Diepvries/Houdbaar-badges volledig verwijderd uit de "Ingrediënten"-uitklapper op `/` — die toont nu alleen nog naam + hoeveelheid + eenheid per regel, geen classificatie. `ingredientFreshness` (met de diepvries/blik-woordherkenning) verwijderd uit `src/lib/categoryStyle.ts`; de twee extra queries op `/` (huidige boodschappenlijst-producten + onthouden standaardkeuzes) die alleen voor deze badge bestonden zijn ook weer weg. Geen nieuwe migratie nodig. |
| WP58 | Gestructureerde logging (Fase 16) | Nieuwe `src/lib/logger.ts`: `logEvent({ level, area, message, correlationId, meta })` schrijft één JSON-regel per gebeurtenis (makkelijk terug te vinden op `area`/`correlationId` in de servergegevens), plus `createCorrelationId()` en `errorMessage()`. `meta` wordt altijd gefilterd op gevoelige sleutels (token/wachtwoord/secret/cookie/toegangscode → `"[redacted]"`), ook als een aanroeper per ongeluk iets gevoeligs meegeeft — afgedekt met eigen unit tests. Toegepast op de zeven gebieden uit Fase 16: Picnic-fouten en verlopen sessies (`src/lib/picnic/client.ts`, bij elke `AUTH_ERROR`/API-fout/netwerkfout), mislukt vullen/legen van het mandje (`cartService.ts`, met correlation-ID over de hele operatie), productmatching (`ensureShoppingList` logt `notFoundCount`/`reviewCount` per gegenereerde lijst; `controle`-pagina logt mislukte alternatieven-aanvulling), weekplangeneratie (`ensureMealPlan` is nu een dunne wrapper om `ensureMealPlanInner` die start/einde/fout met correlation-ID en timing logt, zonder de bestaande 340-regelige functie zelf te hoeven herstructureren), en onverwachte hoeveelheidsdata (`productEnrichment.ts` logt wanneer een Picnic-verpakkingstekst niet naar een hoeveelheid kon worden herleid). Databasefouten worden alleen gelogd waar ze al binnen deze try/catch-blokken vallen (geen generieke Prisma-middleware toegevoegd — dat zou overengineering zijn voor de huidige schaal). Twee losse `console.warn`-calls vervangen door de nieuwe structured logger. Live geverifieerd: na het forceren van een nieuwe weekplanning/boodschappenlijst verschenen de verwachte `meal_plan`- en `product_matching`-logregels met correlation-ID's in de dev-serverconsole. Geen nieuwe migratie nodig. |
| WP59 | End-to-end testsuite (Fase 15) | Nieuwe `e2e/criticalFlow.e2e.ts` (`npm run test:e2e`) dekt de kritieke flow uit Fase 15 (onboarding, weekmenu, gerecht vervangen, boodschappen opbouwen, vaste boodschap via Picnic-zoeken, voorraad aanpassen, twijfelproduct corrigeren, mandje vullen, mandje legen) tegen een eigen productiebuild (`next build && next start`, bewust niet `next dev` — zie code-comment voor de reden) op een losse poort, met een lokale mock-Picnic-server (`e2e/fixtures/mockPicnicServer.ts`, vast poortnummer 4010) zodat er nooit een live Picnic-account nodig is. Elke run gebruikt een vers, geïsoleerd testhuishouden en ruimt zichzelf op (inclusief tijdelijke Picnic-testproducten in de gedeelde catalogus). Het bouwen van deze suite legde twee echte, losstaande productiebugs bloot die nu ook zijn gefixt: (1) `ensureMealPlan`/`ensureShoppingList` (`src/lib/mealPlan.ts`, `src/lib/shoppingList.ts`) waren niet veilig bij bijna-gelijktijdige aanvragen voor dezelfde week — de "bestaat al?"-check en de `create()` vormden een race, waarbij de verliezer op een unique-constraint-fout crashte in plaats van gewoon de winnaar terug te geven; nu gevangen (Prisma-foutcode `P2002`) en afgehandeld door de net aangemaakte rij opnieuw op te halen. (2) `/gerechten?direction=day` (de richting die de "Vervang [dag]"-link vanaf de hoofdpagina gebruikt) filterde hard op geleerde daggewoontes — een vers huishouden zonder geleerde patronen zag daardoor een lege lijst zonder enige "Kies"-knop; valt nu terug op alle gerechten wanneer die filter niets oplevert (zelfde fix toegepast op de vergelijkbare "Favorieten"-richting). `PICNIC_BASE_URL` (server-only env-override in `src/lib/picnic/client.ts`, alleen actief in e2e) ontbrak nog echt in de codebase — zonder deze override liep de suite altijd tegen de live Picnic-API, wat de bovenstaande bugs aanvankelijk maskeerde als "mock wordt niet bereikt". Geverifieerd: `tsc`/`eslint`/`npm test` (155/155) groen, `npm run test:e2e` twee keer op rij groen (10/10 subtests). Geen nieuwe migratie nodig. |
| WP60 | Provider-neutraliteit voor productreferenties | DATAMODEL_AUDIT.md punt 7: `Product.externalRef` werd stilzwijgend als "het Picnic-product-ID" behandeld. Nieuw veld `Product.provider` (enum `ProductProvider`, nu alleen `PICNIC`, default `PICNIC`) maakt dat expliciet; de unieke sleutel is nu `(ingredientId, provider, externalRef)` i.p.v. `(ingredientId, externalRef)`, zodat een toekomstige tweede provider nooit per ongeluk op dezelfde combinatie kan botsen. Bewust géén `ProductExternalRef`-tabel of provider-abstractielaag — dat zou overengineering zijn zolang er maar één provider is; alleen het schema-veld dat de aanname expliciet maakt. Vijf `prisma.product.upsert`-aanroepen (`src/app/actions.ts`, `src/app/recepten/actions.ts`, `src/app/boodschappen/fixedGroceriesActions.ts`, `src/app/controle/actions.ts`, `src/app/controle/page.tsx`) bijgewerkt naar de nieuwe samengestelde sleutel. Migratie nodig: `20260729160000_product_provider`. |
| WP61 | Voorraadcheck voelt als wekelijkse check, niet als administratie | DATAMODEL_AUDIT.md punt 8: `InventoryItem.status` bleef voor altijd bewaard (terecht, geen reset), maar de UI op `/boodschappen` toonde daardoor élke week opnieuw alle ~16 basisproducten met 3 knoppen — ook producten die allang als "genoeg" bevestigd waren. Nieuwe pure functie `needsInventoryAttention(status, updatedAt)` (`src/lib/inventory.ts`) — geen nieuw model nodig, hergebruikt het bestaande `updatedAt`-veld: "bijna op"/"op"/nooit ingevuld vraagt altijd om een blik, maar een bevestigd "genoeg" blijft 21 dagen geldig voordat het weer kort om een herbevestiging vraagt. De sectie toont nu alleen wat écht aandacht verdient ("Voorraadcheck: 4 basisproducten" i.p.v. "voor 16 basisproducten", of "Voorraad ziet er goed uit" als niets hoeft); recent bevestigde producten staan ingeklapt onder "X recent bevestigd" en blijven aanpasbaar. Unit- en integratietests toegevoegd (`src/lib/inventory.test.ts`). Geen nieuwe migratie nodig. |
| WP62 | Loginscherm lekt geen huishoudennamen meer | Kritische UX/security-review (op verzoek van de gebruiker) tegen AGENTS.md/PRODUCT_VISION.md wees uit dat `/login` de namen van *alle* huishoudens met een toegangscode publiek toonde aan iedere bezoeker — bij meerdere gezinnen op dezelfde app zag een vreemde bezoeker dus welke gezinsnamen de app gebruiken, nog vóórdat iemand had ingelogd. `hashHouseholdAccessCode` neemt het huishouden-ID al mee in de hash, dus een toegangscode hoort ondubbelzinnig bij precies één huishouden — nieuwe `signInByAccessCode(accessCode)` (`src/lib/auth.ts`) vindt het juiste huishouden door de code tegen elk huishouden te proberen, zonder dat de gebruiker eerst een naam uit een lijst hoeft te kiezen. `/login` toont nu alleen nog één toegangscode-veld, met een nette inline foutmelding bij een verkeerde code (`?status=wrong-code`, was voorheen een onafgevangen fout die op de generieke foutpagina belandde). Geverifieerd: handmatige Playwright-controle bevestigt dat een net aangemaakt testhuishouden én het bestaande "Familie Van der Lecq" nergens in de loginpagina-HTML voorkomen, dat een verkeerde code de foutmelding toont, en dat de juiste code correct bij het eigen huishouden inlogt. `tsc`/`eslint`/`npm test` (161/161)/`npm run test:e2e` (10/10) groen. Geen nieuwe migratie nodig. |
| Bugfix | Picnic-mandje vullen kon stoppen op één transiënte verbindingsfout | Tijdens het herhaald draaien van de e2e-suite (los van WP60/61) bleek "Mandje vullen" af en toe 0 producten toe te voegen terwijl de bevestigingstekst wél "toegevoegd" toonde. Oorzaak: `addShoppingListToPicnicCart` (Fase 7, bewust) stopt de hele batch meteen bij een netwerkfout i.p.v. door te blijven proberen op de rest — maar `PicnicClient.request()` had geen enkele retry, dus één kortstondige verbindingshapering op het eerste product blokkeerde alle 23 regels tegelijk. Eén stille automatische retry toegevoegd in `src/lib/picnic/client.ts` bij een falende `fetch` (niet bij een foutcode ván Picnic zelf, alleen bij het uitblijven van een respons) — een reële Fase 7-verbetering voor echte gebruikers met een korte wifi-hapering, niet alleen een testfix. Geverifieerd: nieuwe unit test bevestigt dat één mislukte poging gevolgd door een geslaagde poging alsnog data teruggeeft; `npm run test:e2e` zes keer achter elkaar groen (was 2 van de 3 keer rood vóór deze fix). Geen nieuwe migratie nodig. |
| WP63 | Picnic koppelen via de app (i.p.v. terminal) | Kritische UX-review wees uit dat een Picnic-account koppelen alléén kon via `npm run picnic:login` in de terminal — geen enkel gezinslid buiten de ontwikkelaar kon dit zelf doen, en de letterlijke tekst "Draai eenmalig `npm run picnic:login` in de terminal" stond zichtbaar op `/boodschappen` (P0-bevinding). Nieuwe sectie "Picnic" op `/ons-gezin` (`PicnicConnection.tsx`) laat een huishouden zelf e-mailadres en wachtwoord invullen; die komen alleen binnen om rechtstreeks bij Picnic in te loggen en worden daarna nooit opgeslagen — alleen het sessie-token. Ondersteunt ook tweestapsverificatie (sms): bij `second_factor_authentication_required` wordt het gedeeltelijke Picnic-token apart opgeslagen in een nieuw veld `Household.picnicPendingAuthToken` (losstaand van het echte, werkende `picnicAuthToken` — voorkomt dat een halverwege gestaakte koppelpoging ooit voor een werkend token wordt aangezien), en toont de pagina een code-invoerstap. "Ontkoppelen" heeft een expliciete bevestigingsstap (matcht het bestaande "mandje legen"-patroon). De kernlogica staat in nieuwe `src/lib/picnic/accountConnection.ts`, los van de "use server"-actie (`src/app/ons-gezin/picnicActions.ts`) die alleen autorisatie en de redirect toevoegt — dezelfde scheiding als de rest van de Picnic-integratie, en nodig omdat de actie zelf (met `cookies()`/`redirect()`) niet los van een echte Next.js-requestcontext getest kan worden. `scripts/picnic-login.ts` blijft als terugvaloptie werken. Migratie nodig: `20260729173000_picnic_pending_auth_token`. Geverifieerd: 7 nieuwe integratietests (`accountConnection.test.ts`, gefakete `fetch` — geen live Picnic-account nodig) dekken succes, verkeerde combinatie, netwerkfout, lege velden, de volledige 2FA-heen-en-terug-flow en een verlopen 2FA-poging; daarnaast handmatige Playwright-controle tegen een lokale mock-Picnic-server bevestigt via zowel database- als ruwe-HTML-inspectie dat koppelen/ontkoppelen het huishouden correct bijwerkt en dat "Toevoegen aan Picnic-mandje" pas verschijnt na een geslaagde koppeling. `tsc`/`eslint`/`npm test` (168/168)/`npm run test:e2e` (10/10) groen. |
| WP64 | `/gerechten`: schijnfunctionaliteit weghalen | Kritische UX-review vond twee elementen op `/gerechten` die eruitzagen als knoppen maar niets deden bij een tik — verboden door AGENTS.md ("Geen schijnfunctionaliteit"). Het schuifregelaar-icoon (`SlidersHorizontal`) rechtsboven in de header oogde als een filterknop; vervangen door `UtensilsCrossed`, hetzelfde icoon dat de onderste navigatie al gebruikt als identiteitsicoon voor "Gerechten" (consistent met hoe elke andere pagina — `/boodschappen`, `/controle`, `/recepten` — een puur decoratief thema-icoon rechts in de header toont, geen knop). De pijl (`ChevronRight`) naast elke gerecht-titel suggereerde "tik voor meer info", terwijl alleen de dropdown en de "Kies"-knop daadwerkelijk iets deden; verwijderd in plaats van nagebouwd, omdat er geen detailweergave bestaat om naartoe te tikken. Geverifieerd: Playwright-controle bevestigt dat beide iconen weg zijn; `tsc`/`eslint`/`npm test` (168/168)/`npm run test:e2e` (10/10) groen. Geen nieuwe migratie nodig. |
| WP65 | `/ons-gezin` opgeschoond met progressive disclosure | Kritische UX-review wees `/ons-gezin` aan als de meest overladen pagina van de app: "Maaltijdsoorten" (8 categorieën × 4 knoppen), "Jullie weekritme" (7 dagen × 2 knoppen), "Onthouden Picnic-producten", "Productkeuze" en "Toegangscode" stonden allemaal permanent volledig uitgeklapt — in schril contrast met `/boodschappen` en `/controle`, die al met `<details>`-secties zijn opgeschoond, en met de productvisie ("Beheer hoort achter 'Meer'..."). Alle vijf secties zijn nu ingeklapt achter een `<summary>`, zelfde patroon als het bestaande "Wat ik heb geleerd"-blok; "Onthouden Picnic-producten" toont het aantal rechtstreeks in de samenvatting. Bewust wél zichtbaar gehouden: "Gezinsleden" (primaire, veelgebruikte data) en de nieuwe "Picnic"-koppelsectie uit WP63 (belangrijke status, moet gemakkelijk vindbaar blijven). Geen inhoudelijke wijziging — alleen de standaard open/dicht-staat; alle bestaande acties/formulieren werken ongewijzigd. Geverifieerd: Playwright-controle bevestigt zowel de ingeklapte als volledig opengeklapte staat tonen de juiste content; `tsc`/`eslint`/`npm test` (168/168)/`npm run test:e2e` (10/10) groen. Geen nieuwe migratie nodig. |
| WP66 | `/recepten`: zoeken in de receptenlijst | Kritische UX-review wees uit dat `/recepten` de volledige receptenbibliotheek (39 recepten, ±8000px) zonder enige zoek- of filterfunctie toonde — zelfs als bewust secundaire beheerpagina onbruikbaar lang om één specifiek recept te vinden. Nieuw zoekveld direct boven de lijst (`?q=...`, hergebruikt hetzelfde formulierpatroon als `/gerechten`'s wensveld) filtert op receptnaam én ingrediëntnaam, met een teller ("10 van 39 recepten voor 'kip'") en een expliciete "Wis zoekopdracht"-link; een onzinzoekopdracht toont een eerlijke lege staat in plaats van niets. Server-side filtering op de al opgehaalde receptenlijst — geen extra databasequery. Geverifieerd: Playwright-controle bevestigt filteren, de lege staat en het wissen van de zoekopdracht; `tsc`/`eslint`/`npm test` (168/168)/`npm run test:e2e` (10/10) groen. Geen nieuwe migratie nodig. |
| WP67 | Polish-ronde uit de kritische UX-review (P2/P3) | Drie kleine, losstaande inconsistenties uit dezelfde review afgerond. (1) De "Volgende"-knop in `OnboardingWizard.tsx` gebruikte `bg-ink`/`text-bg` (zwart) terwijl elke andere primaire actie in de app (inclusief de laatste onboardingstap zelf, "Maak mijn eerste week") `bg-accent`/`text-accent-ink` (oranje) gebruikt — nu gelijkgetrokken. (2) De "Meer opties"-sectie op `/gerechten` was de enige `RecipeSection` zonder icoon naast de titel, terwijl "Nieuwe suggesties" (Sparkles) en "Jullie favorieten" (Heart) dat wel hebben — nu een `UtensilsCrossed`-icoon toegevoegd, consistent met het paginaheader-icoon. (3) De "Deze dag"-richting op `/gerechten` valt stilzwijgend terug op alle gerechten wanneer er nog geen geleerde daggewoonte is (bestaand, bewust gedrag om een lege lijst te voorkomen bij een vers huishouden) — maar liet de gebruiker niet weten dát dit een fallback was; nu een korte toelichting ("Ik heb nog geen voorkeur geleerd voor maandag. Dit zijn alle suggesties — kies er een en ik leer wat bij deze dag past.") zodat het verschil met een echte, geleerde "Deze dag"-selectie duidelijk is. Geverifieerd met Playwright tegen een vers testhuishouden (knopkleur, fallback-tekst, icoon-aanwezigheid), daarna het testhuishouden weer verwijderd; `tsc`/`eslint`/`npm test` (168/168)/`npm run test:e2e` (10/10) groen. Geen migratie. Dit sluit de WP62–67-roadmap uit de kritische UX-review af. |
| WP68 | Gerecht verbergen na herhaalde negatieve feedback + herstellen (Fase 11) | AGENTS.md Fase 11 vraagt expliciet: "één negatieve beoordeling verlaagt de score; meerdere negatieve beoordelingen kunnen een gerecht verbergen; gebruiker kan een verborgen gerecht herstellen." Dat laatste deel bestond nog niet — `recalculateVariantConfidence` verlaagde bij herhaalde negatieve feedback alleen de score/stance (`RATHER_NOT`), maar een gerecht bleef altijd gewoon tussen de suggesties staan. Nieuwe pure module `src/domain/learning/hiddenRecipes.ts` (`deriveHiddenState`, 8 unit tests) bepaalt of een gerecht verborgen moet zijn: pas bij twéé of meer echte smaak-afkeursignalen sinds de laatste keer herstellen (een expliciete duim-omlaag, of een vervanging met reden NOT_TASTY/NEVER_USE — dezelfde twee zwaarst gewogen redenen als de bestaande `replacementPenaltyForReason`; contextuele redenen als "verkeerde dag" of "te veel werk vandaag" tellen bewust niet mee, want die zeggen niets over het gerecht zelf). Nieuw `Preference.hiddenAt`-veld (losstaand van `stance`/`confidence`, die blijven ongewijzigd de zachte scorebijstelling doen) en een nieuwe `FeedbackEventType.RESTORED` voor het herstellen. `recalculateVariantConfidence` (`src/lib/scoring.ts`) zet/wist dit veld nu bij elke herberekening. Verborgen gerechten worden uitgesloten van zowel `/gerechten` als de automatische weekplanner (`ensureMealPlan` in `src/lib/mealPlan.ts`, met voorrang boven zelfs een vaste daggewoonte — "hard is hard", dezelfde regel als bij harde dieetbeperkingen) — beide hergebruiken een preference-query die toch al werd opgehaald, dus geen extra databasequery. Nieuwe sectie "Verborgen gerechten" op `/gerechten` (ingeklapt, met teller) toont waarom een gerecht minder vaak wordt voorgesteld en heeft een "Toch weer tonen"-knop per gerecht (nieuwe server action `restoreHiddenRecipeVariant`). Geverifieerd: nieuwe integratietest in `mealPlan.test.ts` bevestigt dat een verborgen gerecht zelfs als vaste daggewoonte wordt genegeerd; Playwright-verificatie tegen een productiebuild bevestigt het hele pad (twee negatieve beoordelingen → verdwijnt uit de gewone lijst → verschijnt onder "Verborgen gerechten" → "Toch weer tonen" → status-bevestiging → weer gewoon zichtbaar); `tsc`/`eslint`/`npm test` (177/177)/`npm run test:e2e` (10/10) groen. Migratie nodig: `20260729215155_recipe_hiding` (nieuwe kolom `preferences.hidden_at` + nieuwe enum-waarde `RESTORED`, beide additief, geen bestaande data geraakt). |
| WP69 | Attention-laag: één bron van waarheid voor "wat staat er nog open" (voorbereiding pushmeldingen) | Eerste stap van een groter plan (gebruiker vroeg om rustige push-/appmeldingen voor vier situaties: weekmenu klaar zonder boodschappenlijst, langdurig openstaande productcontrole, gecontroleerde lijst nog niet naar Picnic, Picnic-mandje gevuld maar niet bevestigd besteld). Analyse wees uit dat er nog geen centrale attention-laag bestond — de homepage had alleen een lokale, niet-herbruikbare `nextStepCopy()`-functie — en dat er geen betrouwbare "sinds wanneer"-tijdstempels bestonden om "langer dan 2 dagen open" te bepalen, en geen manier om te weten of een Picnic-bestelling daadwerkelijk is afgerond (die laatste kán ook niet, Fase 7/8: geen bestel-API). Dit WP bouwt bewust **alleen** de attention-laag zelf, nog geen push/PWA/cron (die volgen in een later, apart werkpakket zodra deze laag zich bewijst). Nieuwe pure module `src/domain/attention/attentionItems.ts` (`getAttentionItems`, 10 unit tests) berekent vier `AttentionItem`-types (`WEEK_MENU_READY_NO_GROCERIES`, `PRODUCT_REVIEW_OPEN`, `GROCERIES_READY_NOT_SENT_TO_PICNIC`, `PICNIC_CART_FILLED_NOT_CONFIRMED`) inclusief `relevantSince` en `dedupeKey`, zonder zelf een wachttijd-drempel toe te passen — dat is bewust beleid van de latere pushlaag, niet van deze laag: de homepage toont een item altijd meteen (Fase 12: één primaire actie per scherm), terwijl push straks dezelfde items aanvult met een stiltedrempel (2 dagen voor productcontrole, zoals expliciet gevraagd) zonder de voorwaarden zelf te dupliceren. Nieuwe `src/lib/attention.ts` haalt dit in één query op (verving de twee losse `shoppingList`/`reviewCount`-queries die de homepage eerst deed — per saldo een query minder, niet meer). Drie nieuwe, puur additieve `ShoppingList`-velden nodig omdat die "sinds wanneer" nergens al werd bijgehouden: `reviewFlaggedAt` (gezet bij `ensureShoppingList` zodra er voor het eerst regels met `needsReview` ontstaan, en ook bijgewerkt in het zeldzame geval dat `syncShoppingListForInventoryChange` ná het aanmaken van de lijst alsnog een controle-behoevende regel toevoegt), `reviewedAt` (gezet in `confirmShoppingList`), en `orderConfirmedAt` (nieuw: een kleine, eerlijke "Ik heb besteld"-knop op `/boodschappen`, alleen zichtbaar zodra er echt regels naar Picnic zijn overgedragen — nooit een claim dat de bestelling geplaatst is, wél een optionele bevestiging van de gebruiker zelf; wordt teruggezet bij het legen van het mandje). De homepage-tegel (`nextStepCopy` in `src/app/page.tsx`) leest nu uit dezelfde attention-items in plaats van een eigen kopie van de logica; leervragen blijven wel hogere prioriteit houden (bestaand, geen onderdeel van deze push-scope). Geverifieerd: Playwright tegen een productiebuild bevestigt alle vier tegelteksten inclusief de nieuwe "Rond je bestelling af in Picnic" en de "Ik heb besteld"-knop die na bevestigen weer verdwijnt (zowel op `/boodschappen` als op de homepage); `tsc`/`eslint`/`npm test` (187/187)/`npm run test:e2e` (10/10) groen. Migratie nodig: `20260729221535_shopping_list_attention_timestamps` (drie nieuwe nullable kolommen op `shopping_lists`, geen bestaande data geraakt). Vervolg-WP (nog niet gestart): PWA-manifest, service worker, `PushSubscription`/`NotificationPreference`/`NotificationDeliveryLog`-modellen, VAPID-env-vars, cron-endpoint met tijdvenster/dedupe/caps. |
| WP70 | Echte pushmeldingen: PWA, service worker, cron-endpoint bovenop de attention-laag | Vervolg op WP69 — de attention-laag bestond al, dit WP maakt er daadwerkelijk een pushbericht op de telefoon van. Nieuwe pure module `src/domain/attention/notificationPolicy.ts` (`selectNotificationToSend`, `isWithinNotificationWindow`, `notificationDayKey`, 14 unit tests incl. zomer-/wintertijdgrenzen) bevat al het pushbeleid (tijdvenster 08:00–21:00 Europe/Amsterdam, wachttijd per meldingstype — 2 dagen voor productcontrole zoals expliciet gevraagd, 24u/2u eigen invulling voor de andere drie —, voorkeuren per type, max 1 per type per dag, max 1 per huishouden per dag) los van `attentionItems.ts` zelf: push dupliceert geen businesslogica, hij legt er alleen beleid bovenop. Drie nieuwe modellen (`PushSubscription`, `NotificationPreference`, `NotificationDeliveryLog`, plus Prisma-enum `NotificationType` — noodgedwongen een losse enum omdat `AttentionItemType` een TS-uniontype is, geen Prisma-enum, waarden moeten identiek blijven). PWA: `src/app/manifest.ts` (Next.js file-conventie, automatisch gelinkt), gegenereerde iconen (`public/icons/`, via `sharp` uit een simpele SVG), `appleWebApp`-metadata in `layout.tsx` (inclusief de verplichte iOS-uitleg: pushmeldingen werken op iPhone alléén als de app als PWA op het beginscherm staat — een gewoon Safari-tabblad ondersteunt dit niet), en `public/sw.js` (bewust minimaal: alleen `push`/`notificationclick`, geen offline-cachingstrategie, dat zou onnodige complexiteit zijn voor deze app). Nieuwe sectie "Meldingen" op `/ons-gezin` (`PushNotificationSettings.tsx`) detecteert eerlijk of push ondersteund wordt: toont nooit een "meldingen staan aan"-knop als het platform het niet aankan, legt op iOS expliciet uit dat de app eerst op het beginscherm moet staan, en toont pas voorkeur-checkboxes per type nádat er écht een werkende subscriptie is (geverifieerd via de echte browser-Push-API-status, niet aangenomen). Nieuwe server-side `src/lib/webPush.ts` (dunne `web-push`-wrapper, vertaalt HTTP-statuscodes naar `sent`/`gone`/`error`) en `src/lib/notifications.ts` (`runReminderSweepForHousehold`/`runReminderSweep`: haalt voor elk huishouden dezelfde attention-items op als de homepage, past het beleid toe, verstuurt, schakelt een verlopen subscription uit bij 410/404 in plaats van hem te verwijderen — een spoor blijft bewaard voor foutopsporing). Nieuw geauthenticeerd cron-endpoint `src/app/api/cron/reminders` (Bearer-token-check tegen `CRON_SECRET`, retourneert alleen een samenvatting, nooit gevoelige data) plus `vercel.json` met een bewust conservatief dagelijks schema (`0 12 * * *`, UTC) — Vercel Hobby-abonnementen staan geen vaker-dan-dagelijkse cron toe; bij een Pro-abonnement kan dit handmatig verkort worden naar bijvoorbeeld elk kwartier, het endpoint zelf is daar sowieso al veilig voor (idempotent, zelf-beperkend via de dedupe-log). Env vars nodig (lokaal al gezet in `.env`, `.env*` is gitignored — voor productie moet de gebruiker zelf een eigen VAPID-sleutelpaar genereren en samen met `CRON_SECRET` in Vercel instellen): `WEB_PUSH_PUBLIC_KEY`, `WEB_PUSH_PRIVATE_KEY`, `WEB_PUSH_SUBJECT`, `CRON_SECRET`. Geverifieerd: 14 nieuwe unit tests voor het beleid + 6 nieuwe integratietests voor `runReminderSweepForHousehold` (verzenden, dagelijkse dedupe, uitgeschakelde voorkeur, 410-uitschakeling, geen subscriptions, verzendfout-afhandeling — de laatste twee via een geïnjecteerde `sendFn`, hetzelfde `now`-parameterpatroon als elders in de codebase, omdat `web-push` altijd over HTTPS praat en een lokale test-pushserver dat onnodig ingewikkeld zou maken); tegen een echte productiebuild bevestigd met curl/Playwright: manifest en `<link rel="manifest">`/`appleWebApp`-tags correct, `sw.js` correct geserveerd, cron-endpoint geeft 401 zonder geldige `Authorization`-header en een correcte `{withinWindow, checked, sent}`-samenvatting mét geldige header, `/ons-gezin` toont de nieuwe sectie en start de echte subscribe-flow (toestemming vragen, service worker registreren) correct. **Bekende grens van deze sandbox:** de daadwerkelijke `pushManager.subscribe()`-aanroep faalt in deze headless Chromium-sandbox (`AbortError: Registration failed - permission denied`) omdat er geen echte verbinding met een pushdienst beschikbaar is — dit is een omgevingsbeperking van de sandbox, geen codefout: de UI toont in dat geval terecht een eerlijke foutmelding in plaats van iets te doen alsof. Een echte end-to-end-test (browser op een telefoon, na installatie als PWA) kan alleen door de gebruiker zelf gedaan worden. Migratie nodig: `20260729223219_push_notifications` (drie nieuwe tabellen + nieuwe enum, puur additief). |
| WP71 | Picnic-bezorgmoment: voorkeur instellen + eerlijke statuscontrole (geen automatisch reserveren/bestellen) | Gebruikersverzoek: een standaard voorkeursbezorgdag/-tijd instellen (bv. vrijdag 18:00) en rustig gewaarschuwd worden als dat moment mogelijk vol raakt — nooit automatisch reserveren of bestellen, en nooit "gegarandeerd" claimen (de Picnic-API is ongedocumenteerd/onofficieel, R1). Nieuwe methode `PicnicClient.getDeliverySlots()` (`src/lib/picnic/client.ts`) probeert eerst het `delivery_slots`-veld dat al in de bestaande `/cart`-respons zit (zoals de python-picnic-api2-referentie waar deze client op gebaseerd is), en valt pas terug op een los `/cart/delivery_slots`-endpoint als dat veld ontbreekt — dezelfde voorzichtige aanpak als de rest van de client bij een onofficiële API. Nieuwe pure domeinmodule `src/lib/picnic/deliverySlots.ts` (16 unit tests) zet de ruwe respons om naar een stabiel `PicnicDeliverySlot`-type (nooit een crash bij een onverwachte vorm, logt en laat de regel dan gewoon weg) en berekent met `findPreferredDeliverySlotStatus()` een van vijf statussen (`AVAILABLE`/`EXACT_TIME_UNAVAILABLE`/`NO_NEARBY_SLOTS`/`NO_SLOTS_FOR_DAY`/`UNKNOWN`) inclusief een voorzichtig geformuleerde Dutch-language toelichting ("Volgens Picnic is er nu een bezorgmoment beschikbaar" / "Je voorkeurstijd lijkt niet meer beschikbaar" — nooit "gegarandeerd"). Dag/tijd-vergelijkingen gebeuren bewust met `Intl.DateTimeFormat`/`Europe/Amsterdam` (zelfde patroon als WP70's `notificationPolicy.ts`) — niet met servertijd (UTC op Vercel), anders zou de dag-van-de-week soms fout worden berekend. Nieuw Prisma-model `PicnicDeliveryPreference` (dag, tijd, marge in minuten, dagen-van-tevoren voor een herinnering, meldingen aan/uit) — bewust een nieuw model en niet het bestaande `Household.deliveryPreference`-veld, want dat gaat over productkeuze (goedkoopst/gebalanceerd/premium), niet over bezorgmomenten. Nieuwe sectie "Gewenst bezorgmoment" op `/ons-gezin` (ingeklapt, zelfde patroon als WP65) laat het huishouden dag/tijd/marge instellen; nieuwe `PicnicDeliveryStatusCard` op `/boodschappen` toont bij elk paginabezoek een **live** Picnic-controle (bewust geen opgeslagen "laatste bekende status" — dat zou schijnzekerheid geven). Bij een Picnic-fout (verlopen token, netwerkfout, onbekende respons) toont de kaart eerlijk "Picnic kon nu niet worden gecontroleerd" in plaats van te doen alsof alles in orde is — gedekt door 4 nieuwe integratietests (`deliveryStatus.test.ts`, gefakete `fetch`, geen live Picnic-account). Dit WP bouwt bewust **alleen** de voorkeur + live statuskaart (stappen 1-4 van het overeengekomen plan); het koppelen aan echte pushmeldingen/cron (stappen 5-6) is een apart, nog niet gestart vervolg-WP — zie hieronder. Geverifieerd: `tsc`/`eslint`/`npm test` (231/231)/`npm run build`/`npm run test:e2e` (10/10) groen; handmatige Playwright-controle tegen een productiebuild bevestigt de instelsectie, de opslagbevestiging, de voorkeur in de database en de statuskaart op `/boodschappen` met de juiste dag/tijd. Migratie nodig: `20260729232013_picnic_delivery_preference` (nieuwe tabel + nieuwe enum-waarde `PICNIC_DELIVERY_SLOT_AT_RISK` op `NotificationType`, puur additief, nog niet aan de cron gekoppeld). |
| WP72 | Bezorgmoment-risico wordt een echte pushmelding | Vervolg op WP71 — de live statuskaart bestond al, dit WP koppelt hem aan de bestaande WP70-pushinfrastructuur zodat het huishouden een melding krijgt zonder de app zelf te hoeven openen. Bewust **geen** wijziging aan `src/domain/attention/attentionItems.ts`: die laag is puur DB-afgeleid (geen `now`, geen netwerkaanroep), terwijl een bezorgmoment-risico juist de dag-van-de-week én een live Picnic-check nodig heeft — dat hoort dus in `src/lib/notifications.ts` zelf, niet in de attention-laag. Nieuwe `getDeliverySlotNotificationCandidate()` (`src/lib/notifications.ts`) is de tweede en enige andere bron voor een pushmelding naast de vier bestaande attention-items: alleen actief als het huishouden Picnic gekoppeld heeft, een bezorgvoorkeur met `notificationsEnabled` heeft ingesteld, en vandaag precies `reminderDaysBefore` dagen (standaard 2, zoals expliciet gevraagd) vóór de voorkeursdag valt (`shouldCheckDeliverySlotToday()`, bestond al sinds WP71 maar werd nog nergens aangeroepen). Geeft bewust `null` terug bij status `UNKNOWN` (Picnic niet bereikbaar) — dat zou een foutmelding pushen in plaats van een echt signaal; de statuskaart op `/boodschappen` toont zoiets al eerlijk bij een bezoek. `runReminderSweepForHousehold` probeert eerst de bestaande vier attention-items (ongewijzigde prioriteit — de kernweekflow gaat voor); alleen als dat niets oplevert én het huishouden vandaag nog niets kreeg, volgt de (mogelijk trage, live) Picnic-check — zo blijft de bestaande "max 1 melding per huishouden per dag"-regel vanzelf ook voor dit type gelden, zonder aparte dedupe-logica. Kleine, noodzakelijke bijvangst: `runReminderSweepForHousehold` verwachtte voorheen altijd een bestaand weekmenu (`if (!mealPlan) return { sent: false }`), wat een bezorgmoment-melding blokkeerde voor een huishouden zonder actief weekmenu; dat gaf nu gewoon een lege attention-itemlijst in plaats van meteen te stoppen. Geverifieerd: 7 nieuwe integratietests in `notifications.test.ts` (melding bij risico, geen melding bij een nog beschikbaar moment, geen Picnic-aanroep op de verkeerde dag/bij een uitgeschakelde voorkeur/zonder Picnic-koppeling — elk bevestigd doordat een expres falende `fetch`-fake nooit wordt aangeroepen —, voorrang voor een bestaand attention-item, en dat een al verstuurde melding vandaag een tweede bezorgmoment-check blokkeert); `tsc`/`eslint`/`npm test` (238/238)/`npm run build`/`npm run test:e2e` (10/10) groen. Geen nieuwe migratie nodig — het cron-endpoint (`src/app/api/cron/reminders`) en `vercel.json` blijven ongewijzigd, `runReminderSweep()` roept nu vanzelf ook deze nieuwe kandidaat aan. |
| WP73 | Fase 16 afronden: client-side fouten en generieke serverfouten alsnog gestructureerd loggen | WP58 dekte Fase 16 al voor de bekende gebieden (Picnic, productmatching, weekplangeneratie, enz.), maar liet twee gaten open: een fout die alleen in de browser optreedt (React-renderfout) belandde nergens in de gestructureerde server-logging, en een niet-al-geïnstrumenteerde serverfout (bijv. een losse Prisma-aanroep buiten de bekende flows) werd door `error.tsx` wel netjes aan de gebruiker getoond maar ook niet gelogd. Beide lopen in de praktijk door dezelfde twee bestaande error-boundaries (`src/app/error.tsx`, `src/app/recepten/error.tsx`) — dus geen generieke Prisma-middleware nodig (dat was al bij WP58 bewust afgewezen als overengineering), alleen die twee boundaries zelf hoefden iets te melden. Nieuw gedeeld `src/components/ErrorBoundaryScreen.tsx` (vervangt de verder identieke JSX in beide `error.tsx`-bestanden) meldt bij het tonen van het foutscherm éénmalig (`fetch` met `keepalive: true`, fire-and-forget) aan een nieuwe route `src/app/api/log/client-error` — die route bepaalt zelf het huidige huishouden (voor correlatie, zonder dat de client dat hoeft mee te sturen) en schrijft er via de bestaande `logEvent()` een regel van (`area: "client_render"`), met een harde lengtebeperking op de tekstvelden i.p.v. een aparte validatielaag. Geverifieerd: handmatig tegen een productiebuild bevestigd dat een POST met een geldige payload een 204 teruggeeft én de verwachte JSON-logregel op de server verschijnt, en dat een niet-JSON payload netjes een 400 teruggeeft zonder te crashen; `tsc`/`eslint`/`npm test` (238/238)/`npm run build`/`npm run test:e2e` (10/10) groen. Geen nieuwe migratie nodig. |
| WP74 | Fase 10-check: planningsstijl na onboarding aanpasbaar maken | Doorgelicht welke Fase 10-punten (persoonlijke gezinslogica) al bestonden vóór nieuwe code: wie per dag mee-eet, allergieën/harde dieetbeperkingen (hard uitgesloten, nooit als gewone voorkeur — `dietaryRestrictions.ts`), afkeuren/favorieten (`Stance`-enum), portiegrootte (`portionMultiplier`), kind/volwassene (`PersonRole`) en drukke/rustige dagen (`Household.weeklyRhythm`, al verwerkt in de weekplanscore) bleken allemaal al aanwezig en functioneel. Eén concreet gat gevonden: "voorkeur voor herhaalbare gerechten" bestond ook al (`Household.planningStyle`: SAFE/BALANCED/ADVENTUROUS, met echte scoring-impact in `scoreMealPlanCandidate.ts` — SAFE geeft een bonus aan bewezen/eerder-gekozen gerechten, ADVENTUROUS juist aan nieuwe), maar kon alleen bij onboarding gezet worden en was daarna nergens meer aan te passen — terwijl een huishoudens smaak voor variatie wel degelijk verandert. Nieuwe sectie "Planningsstijl" op `/ons-gezin` (ingeklapt, zelfde patroon als de andere secties) met `PlanningStyleEditor.tsx` (client component, zelfde onmiddellijke-opslaan-per-klik-patroon als `WeeklyRhythmEditor.tsx`) en server action `updatePlanningStyle` (`src/app/ons-gezin/actions.ts`), hergebruikt dezelfde labels/omschrijvingen als de onboarding-wizard. Bewust **niet** opgepakt in dit WP: "maximale kooktijd per dag" (geen enkel recept heeft een echte minutenwaarde in het schema of de seed-data — dit zou fictieve tijden vereisen, wat tegen "geen schijnfunctionaliteit" ingaat; het dichtstbijzijnde bestaande signaal is de grovere `VariantType`/`weeklyRhythm`-combinatie die drukke dagen al naar snellere varianten stuurt) en "optionele sport-/agenda-informatie" (expliciet optioneel in de blueprint, zou een volledig nieuwe kalenderintegratie vereisen — te groot en te speculatief voor dit werkpakket). Geverifieerd: `tsc`/`eslint`/`npm test` (238/238)/`npm run build`/`npm run test:e2e` (10/10) groen; handmatige Playwright-controle bevestigt dat de keuze na opslaan ook na een verse paginalading (zoals een echte gebruiker het zou ervaren) correct blijft staan — een eerste ad-hoc verificatiepoging met een aparte, racende databaseverbinding gaf incidenteel een verouderde lezing terug (zelfde soort omgevingsartefact als eerdere sessies al tegenkwamen bij ad-hoc scripts), maar dat bleek geen echt gedrag voor een gebruiker, alleen voor een tweede proces dat sneller leest dan de server zelf. Geen nieuwe migratie nodig — hergebruikt het bestaande `Household.planningStyle`-veld. |
| WP75 | Fase 13: gemeten databasequeries + een echte N+1 verholpen | Eerste stap volgens de blueprint ("meet vóór en na") — tijdelijke, opt-in querylogging toegevoegd aan `src/lib/prisma.ts` (alleen actief met `DEBUG_PRISMA_QUERIES=true`, standaard uit, geen productierisico) en daarmee de homepage/`/gerechten`/`/boodschappen`/`/ons-gezin` gemeten tegen een realistisch gevuld testhuishouden. De meeste queryaantallen (21–32 per pagina) bleken Prisma's normale "één query per relatie"-gedrag te zijn (bijv. losse SELECTs voor `meal_plan_entries`/`recipe_variants`/`recipes`/`recipe_ingredients`), niet echt N+1: dat aantal blijft constant, ongeacht hoeveel data een huishouden heeft. Wél een echte N+1 gevonden: `ensureMealPlanInner` (bij het genereren van een nieuwe weekplanning) haalde de personendata van het huishouden **8 keer** afzonderlijk op — 7x via `getHouseholdHardRestrictions(dayKey)` (één keer per dag) plus 1x via `getHouseholdMealParticipantsByDay()` — terwijl het exact dezelfde onderliggende query is, alleen anders gefilterd in het geheugen. Hetzelfde patroon (2x dezelfde query) zat ook op `/gerechten` en in beide server actions die daar een gerecht kiezen/vervangen. Nieuwe gecombineerde `getHouseholdHardRestrictionsAndParticipantsByDay()` (`src/lib/household.ts`) haalt de personen nu één keer op en berekent beide resultaten daarna puur in het geheugen — geverifieerd met querylogging dat dit de personenquery van 8x naar 1x terugbrengt bij het genereren van een weekplanning, en van 2x naar 1x op `/gerechten`. Bewust geen risicovollere ingreep zoals overschakelen op Prisma's `relationLoadStrategy: "join"` (zou tientallen losse `include`-aanroepen door de hele app raken voor een moeilijk hard te bewijzen winst in deze sandbox, die toch al lage lokale netwerklatency heeft) — dat blijft een mogelijke vervolgstap als de productieomgeving (Vercel → Supabase, wél echte netwerklatency) daar aanleiding toe geeft. Geverifieerd: `tsc`/`eslint`/`npm test` (238/238)/`npm run build`/`npm run test:e2e` (10/10) groen. Geen nieuwe migratie nodig. |
| WP76 | Afvinklijst voor zelf boodschappen doen | Gebruikersverzoek na een productbrainstorm ("kijk als ervaren app-ontwikkelaar mee, wat helpt een gezin dagelijks echt verder"): de bestaande `PicnicTransfer`-tekstexport ("Naar Picnic") is puur een kopieerbare lijst om handmatig in de Picnic-app te plakken — geen echte optie voor een gezin dat gewoon zelf naar de Aldi/Jumbo loopt. Nieuwe sectie "Zelf boodschappen doen" op `/boodschappen` (ingeklapt, altijd zichtbaar — ook mét Picnic-koppeling, want ook Picnic-huishoudens halen weleens iets zelf) met een interactieve afvinklijst van alle regels (maaltijd- én vaste boodschappen), met productfoto, naam, hoeveelheid en een voortgangsteller ("X van Y afgevinkt"). Nieuw veld `ShoppingListLine.pickedUpAt` (nullable timestamp, zelfde patroon als `transferredToPicnicAt`/`reviewedAt` — losstaand van de Picnic-overdrachtsstatus, een huishouden kan beide tegelijk gebruiken). Reset vanzelf elke week: een nieuwe week krijgt nieuwe `ShoppingListLine`-rijen via `ensureShoppingList`, dus geen aparte "wis alle vinkjes"-logica nodig. Nieuwe server action `toggleShoppingListLinePickedUp` doet bewust geen `redirect()` zoals de andere regelacties (die zijn ontworpen voor één wijziging per keer, met een statusmelding) — hier moet je snel door 20 producten heen kunnen tikken, dus optimistische lokale state in `ShoppingChecklist.tsx` (`"use client"`, `useTransition`) met de server als achtergrondsynchronisatie. Geverifieerd: 6-staps Playwright-controle tegen een productiebuild bevestigt aanvinken/uitvinken, dat de voortgangsteller klopt, dat de staat bewaard blijft na een verse paginalading (zoals een echte gebruiker het zou zien) en dat uitvinken `pickedUpAt` weer op `null` zet; `tsc`/`eslint`/`npm test` (238/238)/`npm run build`/`npm run test:e2e` (10/10) groen. Migratie nodig: `20260730084359_shopping_list_line_picked_up` (één nieuwe nullable kolom, puur additief). |
| Bugfix | Nep-hamburgermenu linksboven op het beginscherm | Gebruikersrapport: het driestrepen-icoon linksboven op `/` deed niets bij een tik. Uitgezocht: het was een puur decoratief `<Menu>`-icoon (geen link, geen `onClick`) — vermoedelijk restant van een vroege opzet met een uitklapmenu, dat overbodig werd zodra de vaste onderste navigatiebalk die rol overnam. Op alle andere schermen (`/boodschappen`, `/gerechten`, `/controle`, `/recepten`) staat links altijd een écht werkende terugknop en rechts een puur decoratief thema-icoon — het beginscherm had dat precies andersom (links schijnfunctionaliteit, rechts pas de echte link naar "Ons gezin"). Overwogen om er een echt uitklapmenu van te maken, maar alle mogelijke inhoud daarvan (instellingen, Picnic, meldingen, uitloggen) zit al één tik verderop onder "Ons gezin" — een menu zou alleen bestaande navigatie dupliceren. In plaats daarvan `Menu` vervangen door `CalendarDays`, consistent met het decoratieve-icoon-patroon van de andere schermen. Geverifieerd met Playwright tegen een productiebuild dat het icoon geen `<a>`/`<button>` meer is; `tsc`/`eslint`/`npm test` (238/238)/`npm run build`/`npm run test:e2e` (10/10) groen. Geen nieuwe migratie nodig. |
| WP77 | Inloggen met gebruikersnaam + wachtwoord (i.p.v. losse toegangscode) | Gebruikersverzoek: de bestaande `/login` werkte met één gedeelde toegangscode per huishouden (`Household.accessCodeHash`), met de zorg "stel dat twee mensen hetzelfde wachtwoord kiezen, gaat het dan fout?". Gekozen aanpak (huishoudens optie 2, niet individuele accounts per gezinslid): gebruikersnaam + wachtwoord, nog steeds gedeeld per huishouden. **Eerste implementatie bevatte een echte fout die het exact gevreesde scenario niet voorkwam**: de gebruikersnaam werd samen met het wachtwoord gehasht en gezouten met het huishouden-ID (`hash(householdId + username + password)`), en inloggen probeerde die hash tegen elk huishouden. Met een Playwright-script dat twee huishoudens bewust dezelfde gebruikersnaam+wachtwoord liet kiezen, bleek de tweede meldingsloos in te loggen op het **eerste** huishouden (willekeurige iteratievolgorde besliste, niet de bedoeling van de gebruiker) — het huishouden-ID voorkwam alleen dat een fóúte gok op een ander huishouden zou passen, niet dat twee bewust identieke combinaties elkaar zouden verwarren. Herontworpen met een echte oplossing: `username` is nu een aparte, **unieke** kolom in het schema (`@unique`, genormaliseerd — kleine letters, getrimd) in plaats van onderdeel van de hash; `passwordHash` blijft per huishouden gezouten. Inloggen is daardoor een directe opzoeking op gebruikersnaam (nooit meer dan één kandidaat, dus geen giswerk over huishoudens heen) gevolgd door één wachtwoordcontrole, met nog altijd één generieke foutmelding voor zowel een onbekende gebruikersnaam als een fout wachtwoord (voorkomt dat een bezoeker kan aftasten welke gebruikersnamen bestaan, zelfde WP62-principe als voorheen). Een bezette gebruikersnaam wordt nu al bij het **kiezen** geweigerd (bij onboarding en bij wijzigen via `/ons-gezin`), niet pas bij een verwarrende login achteraf. **Tweede bevinding tijdens verificatie**: een geworpen `Error` uit een server-actie wordt door Next.js in productiebuilds gereduceerd tot een generiek "er is iets misgegaan"-bericht (redactie van foutdetails, standaardgedrag — zie `node_modules/next/dist/docs/.../10-error-handling.md`), waardoor de nieuwe "deze gebruikersnaam is al in gebruik"-melding de gebruiker nooit zou bereiken. `completeOnboarding` (`src/app/onboarding/actions.ts`) geeft verwachte fouten nu terug als `{ error }` in plaats van te throwen (Next's eigen aanbevolen patroon voor server-acties); `updateCredentials` (`src/app/ons-gezin/actions.ts`, aangeroepen via een gewone `<form action>`) redirect bij een botsing naar een statuscode (`?status=username-taken`) die `/ons-gezin` als rode melding toont — zelfde patroon als de bestaande groene statusmeldingen op die pagina. Nieuwe domeinmodule `src/domain/household/credentials.ts` (wachtwoordhashing + gebruikersnaam-normalisatie/validatie, bewust gescheiden van `src/lib/auth.ts` omdat dat bestand `server-only` importeert en dus niet los te unittesten is — zelfde patroon als eerdere domeinmodules). Geverifieerd met een dedicated Playwright-scenario tegen een productiebuild: (1) twee huishoudens met bewust identieke inloggegevens — de tweede wordt nu geweigerd mét zichtbare melding, geen half aangemaakt huishouden blijft achter; (2) inloggen met de gedeelde combinatie gaat naar het juiste (enige) huishouden; (3) een fout wachtwoord toont nog steeds de generieke, niet-lekkende melding; (4) wijzigen naar een bezette gebruikersnaam via `/ons-gezin` toont dezelfde duidelijke fout. `tsc`/`eslint`/`npm test` (245/245)/`npm run build`/`npm run test:e2e` (10/10) groen. **Migratie nodig** (nog niet op productie gedraaid, gebruiker moet toestemming geven): `20260730110756_household_username_password` — verwijdert `access_code_hash`, voegt `username` (uniek) en `password_hash` toe. **Let op voor na de migratie**: de oude toegangscode kan niet worden omgezet (de kolom verdwijnt) — elk huishouden moet na de migratie eenmalig via `/ons-gezin` een nieuwe gebruikersnaam + wachtwoord instellen voordat een nieuwe login-sessie nodig is; de huidige sessiecookie blijft intussen gewoon werken (sessies staan los van de inloggegevens), dus geen acute uitsluiting. |
| WP78 | Productiemigraties automatisch bij elke deploy | Vervolg op WP77: omdat Vercel bij elke push naar `main` automatisch een nieuwe productiedeploy uitrolt, maar de bijbehorende databasemigratie tot nu toe altijd een **losse, door de gebruiker zelf uit te voeren stap** was, kon appcode die een nieuwe kolom verwacht al live staan vóórdat die kolom in productie bestond (precies dit gebeurde meteen na WP77's merge). Gebruiker vroeg expliciet om dit voortaan automatisch te laten gebeuren. Nieuw: `package.json`'s `build`-script is nu `prisma migrate deploy && next build` — elke Vercel-deploy past dus eerst alle openstaande migraties toe, en faalt de deploy hard (fail-closed) als dat niet lukt, in plaats van appcode live te zetten die niet bij het schema past. Migraties hebben een niet-gepoolde databaseverbinding nodig (Supabase's transaction-mode pooler op poort 6543, waar de app zelf `DATABASE_URL` voor gebruikt, ondersteunt geen Prisma-migratielocking) — `prisma.config.ts` gebruikt daarom een nieuwe, optionele `DIRECT_URL` (valt terug op `DATABASE_URL` als die ontbreekt, zoals lokaal zonder pooler). Eenmalige actie die alleen de gebruiker zelf kan doen (buiten bereik van deze werkomgeving): in Vercel's projectinstellingen een environment variable `DIRECT_URL` toevoegen met Supabase's session-mode-pooler-connectiestring (poort 5432, "Connect to your project" → Session pooler in het Supabase-dashboard). Geverifieerd: lokale `npm run build` bevestigt dat `prisma migrate deploy` zonder `DIRECT_URL` gewoon terugvalt op `DATABASE_URL` en "No pending migrations to apply" meldt vóórdat `next build` start; e2e-tests bouwen via een eigen `next build`-aanroep (niet via `npm run build`) en zijn dus niet geraakt. `tsc`/`eslint`/`npm test` (245/245) groen. Geen nieuwe Prisma-migratie in dit WP zelf. **Update na productiegebruik**: de eerste échte poging op Vercel faalde met `P1013: the scheme is not recognized` — de gebruiker had bij het invullen van `DIRECT_URL` per ongeluk de variabelenaam en de aanhalingstekens (`DIRECT_URL="...”`) mee in de waarde gezet in plaats van alleen de kale `postgresql://...`-string. Na correctie in Vercel's environment variables slaagde de productiedeploy en is bevestigd dat de WP77-migratie (`username`/`password_hash`) automatisch is toegepast: de gebruiker heeft op de live site succesvol een gebruikersnaam+wachtwoord ingesteld, is uitgelogd en daarmee weer ingelogd. Deze automatische migratiepijplijn is dus in de praktijk end-to-end bevestigd, niet alleen lokaal. |
| WP79 | Agent-workflow-fundamenten (analyse + Fase 1) | Op gebruikersverzoek: een volledige analyse van hoe goed dit project is ingericht voor zelfstandig werken door Claude Code (zie het analyserapport in de chatgeschiedenis). Belangrijkste bevinding: de daadwerkelijke werkregels stonden in `PROGRESS.md`, dat — anders dan `CLAUDE.md`/`AGENTS.md` — niet gegarandeerd geladen wordt in een verse sessie; en er was geen `README.md` met echte projectinhoud, geen samengevoegd verificatiecommando en geen losstaande Definition of Done. Dit WP pakt Fase 1 van het prioriteitenplan op. Nieuw `WORKFLOW.md` (bevat nu de "Manier van werken"-regels uit `PROGRESS.md` plus een expliciete Definition-of-Done-checklist en een sectie "wanneer geen aannames doen") wordt automatisch geladen via `CLAUDE.md` (`@AGENTS.md` + `@WORKFLOW.md`). Nieuw `OPERATIONS.md` bevat de verplaatste "Niet-voor-de-hand-liggende operationele kennis" uit `PROGRESS.md` (Prisma-migraties in de sandbox, productie-migraties/poolerpoorten, testconventies, architectuuroverzicht, beveiligingsincidenten) — gericht te raadplegen, niet elke sessie automatisch geladen, om `CLAUDE.md`'s automatische context klein te houden. `PROGRESS.md` verwijst nu naar beide in plaats van de inhoud te dupliceren. `package.json` heeft nieuwe scripts `typecheck` (`tsc --noEmit`) en `verify` (lint + typecheck + test + build in één commando). `README.md` volledig herschreven van de ongewijzigde `create-next-app`-boilerplate naar projectspecifieke inhoud (wat de app doet, leesvolgorde van de documentatie, lokale setup, commando-overzicht). Nieuw `.env.example` met alle bekende environment-variabelen (verplicht: `DATABASE_URL`; optioneel: `DIRECT_URL`, `WEB_PUSH_*`, `CRON_SECRET`) en toelichting. Geverifieerd: `npm run verify` zelf gedraaid en groen (lint, typecheck, 245/245 tests, migratie+build) — dogfooding van het nieuwe commando. Geen nieuwe Prisma-migratie. Bewust **niet** in dit WP: de CI-workflow (`.github/workflows/`) uit Fase 2 van het prioriteitenplan — dat is een aparte, iets risicovollere stap (introduceert een nieuwe automatische gate) en volgt als eigen WP. |
| WP80 | CI-workflow (Fase 2) | Vervolg op WP79: het belangrijkste gat uit de agent-workflow-analyse was dat er **geen enkele onafhankelijke controle** bestond — tests/lint/typecheck werden alleen gedraaid als de uitvoerende sessie zelf daaraan dacht. Nieuw `.github/workflows/ci.yml`: draait bij elke pull request naar en push op `main` (plus handmatig via `workflow_dispatch`) een Postgres 16-servicecontainer op, migreert die (`npx prisma migrate deploy`, nodig omdat de integratietests een al-gemigreerde database verwachten — zie `OPERATIONS.md`), en draait daarna `npm run verify` (het nieuwe verzamelcommando uit WP79: lint + typecheck + tests + build). Bewust **geen** `npm run test:e2e` in CI opgenomen — dat vereist een los geïnstalleerde Playwright-browser plus een langere looptijd, en is voor de huidige schaal van dit project een bewuste latere stap in plaats van meteen meegenomen (zie ook het prioriteitenplan uit de analyse). Losstaand opgeruimd: `scripts/smoke-test.ts` verwijderd — een nooit in `package.json` geregistreerd, verweesd script dat hetzelfde (household aanmaken/opruimen tegen lokale Postgres) minder grondig deed dan de bestaande integratietests. Geverifieerd: lokaal `tsc`/`eslint` groen. **De eerste proefdraai faalde daadwerkelijk** (24/245 tests rood, bijv. `prisma.ingredient.findUniqueOrThrow` op "kipfilet" in `src/domain/product-matching/repository.test.ts`) — de workflow migreerde de CI-database wel, maar seedde 'm nooit, terwijl meerdere integratietests aannemen dat de gedeelde ingrediënten-/receptcatalogus al bestaat (die komt uit `npm run db:seed`, niet uit de migraties). Toegevoegd: een `npm run db:seed`-stap tussen migreren en `npm run verify`. Geverifieerd door de exacte CI-volgorde lokaal tegen een gloednieuwe, niet eerder gebruikte database te herhalen (migreren → seeden → `npm test`): 245/245 groen. Dit is precies waarom deze workflow als eigen WP is behandeld in plaats van blind vertrouwd: de eerste proefdraai op echte CI-infrastructuur vond een reëel gat dat lokaal (met een allang gevulde ontwikkeldatabase) nooit zichtbaar was geworden. Geen nieuwe Prisma-migratie. |
| WP81 | Code-reviewer-subagent + expliciete stopvoorwaarden (Fase 3) | Laatste stap van het prioriteitenplan uit de agent-workflow-analyse — bewust het minst ingrijpende deel van Fase 3: geen losse product-/UX-/frontend-/backend-/test-engineer-agents (voor de huidige schaal van dit project — één ontwikkelaar, sequentieel werk — zou dat overengineering zijn, expliciet zo beoordeeld in het analyserapport), en git worktrees blijven bewust achterwege zolang er geen echt parallel werk is. Wel toegevoegd: nieuwe `.claude/agents/code-reviewer.md` — een alleen-lezende, puur adviserende subagent (tools: Read/Grep/Glob/Bash, geen schrijftoegang) die vóór het mergen van een gevoelige wijziging (authenticatie/sessiebeheer, household-isolatie, Picnic-integratie, databaseschema) een onafhankelijke, kritische blik geeft — expliciet gemodelleerd naar de WP77-geschiedenis, waar een eerste implementatie zelf overtuigd "opgelost" leek totdat een gericht scenario het tegendeel bewees. `WORKFLOW.md` verwijst er nu naar in de Definition-of-Done-checklist (punt 2), zodat het geen los, nooit aangeroepen bestand blijft. Ook een nieuwe sectie "Stopvoorwaarden" in `WORKFLOW.md`: stop en leg voor bij (a) `npm run verify` dat na 2-3 gerichte pogingen nog steeds faalt, (b) een wijziging die duidelijk groter wordt dan gevraagd, (c) een productiemigratie/destructieve git-actie (herhaling van een al bestaande regel, nu ook expliciet als stopvoorwaarde benoemd), (d) een conflict tussen wat gevraagd wordt en een vastgelegde architecturale aanname uit `AGENTS.md`/`PRODUCT_VISION.md`. Geverifieerd: `tsc`/`eslint` groen (puur documentatie/configuratie, geen applicatiecode geraakt). Geen nieuwe Prisma-migratie. Met dit WP is het volledige prioriteitenplan uit de agent-workflow-analyse (Fase 1 t/m 3) afgerond. |
| WP82 | Eenmalig product toevoegen ("Product toevoegen") | Gebruikersverzoek: kinderen moeten makkelijk iets aan de boodschappenlijst kunnen toevoegen, voorlopig via het gedeelde huishoud-account (geen individuele accounts — bestaande, bewuste keuze). Onderzoek wees uit dat de enige bestaande manier om een los Picnic-product toe te voegen ("vaste boodschappen") het altijd meteen als **permanente wekelijkse gewoonte** registreert — voor een eenmalig extraatje (bijv. chips voor een verjaardag) zou dat ongewenst een blijvende standaard zetten. Nieuwe, losse `MANUAL`-regelbron (naast bestaande `MEAL`/`FIXED`/`INVENTORY`) die expliciet **geen** `FixedGrocery`-rij aanmaakt. Nieuwe sectie "Product toevoegen" bovenaan `/boodschappen` (niet weggestopt achter een `<details>`, in tegenstelling tot "vaste boodschappen" dat meer als instelling aanvoelt) — zoekt bij Picnic (hergebruikt de bestaande zoekfunctie) en voegt direct een regel toe aan de lijst van déze week. Gedeelde logica tussen FIXED en MANUAL (ingrediënt/product resolven uit een Picnic-zoekresultaat) is uit `saveFixedPicnicProduct` geëxtraheerd naar een nieuwe `src/lib/picnicProductChoice.ts` (`resolvePicnicProductChoice`), zodat er geen duplicatie ontstaat tussen de twee paden — `fixedGroceriesActions.ts`'s bestaande gedrag blijft ongewijzigd, alleen intern hergebruikt. Nieuwe `src/app/boodschappen/manualProductActions.ts` (`addManualProduct`) maakt bewust altijd een **nieuwe** regel aan (geen upsert-per-ingrediënt zoals bij FIXED) — twee keer hetzelfde toevoegen geeft dus twee regels, simpeler dan hoeveelheden samenvoegen en net zo makkelijk met de bestaande verwijderknop op te lossen. Tijdens het bouwen bleek de bestaande "verwijderen"-actie (`removeBoodschappenLineThisWeek`) alleen in de dagweergave ("Bekijk per dag") gerenderd te worden, gekoppeld aan een receptregel — een MANUAL-regel heeft geen recept, en zou dus **onverwijderbaar** zijn geweest zonder aanpassing; opgelost met een nieuwe verwijderknop direct in de hoofdlijst "Jullie boodschappenlijst", alleen zichtbaar voor MANUAL-regels. Geverifieerd met een dedicated Playwright-scenario tegen een productiebuild + mock-Picnic-server: een gezocht product toevoegen maakt een MANUAL-regel aan, maakt **geen** `FixedGrocery`-rij aan (het kernverschil met vaste boodschappen), verschijnt in de hoofdlijst, en is met de nieuwe knop weer te verwijderen. `tsc`/`eslint`/`npm test` (245/245)/`npm run build` groen. Vóór het mergen is de `code-reviewer`-subagent ingeschakeld (raakt zowel het databaseschema als de Picnic-integratie, zoals `WORKFLOW.md` sinds WP81 voorschrijft) — zie de vervolgregel hieronder voor de uitkomst. Migratie: `20260730153000_manual_shopping_line_source` (additieve enum-waarde, geen databreuk). |
| WP82-vervolg | Code-review van WP82: twee P1-bevindingen gefixt | Eerste echte inzet van de in WP81 gebouwde `code-reviewer`-subagent — en die vond meteen twee reële, bevestigde P1-bugs in de WP82-implementatie, precies waarvoor die subagent bedoeld is. **(1) Household-isolatie-gat**: `addManualProduct`/`addFixedPicnicProduct`/`addBulkFixedPicnicProducts` riepen alleen `assertCurrentHousehold(householdId)` aan op een los `householdId`-formulierveld — dat bewijst dat dát veld bij de eigen sessie hoort, maar niet dat de apart meegestuurde `shoppingListId` ook echt bij hetzelfde huishouden hoort. Een sessie van huishouden A zou dus een `shoppingListId` van huishouden B kunnen meesturen en daar een regel in kunnen schrijven. Gefixt met een nieuwe gedeelde `src/lib/shoppingListAccess.ts` (`assertShoppingListAccess`) die het huishouden-ID juist *afleidt* uit de boodschappenlijst zelf (`shoppingList.mealPlan.householdId`) — hetzelfde patroon dat elders in de codebase al correct werd gebruikt. `src/app/boodschappen/actions.ts`'s eigen, inhoudelijk identieke private functie is vervangen door deze gedeelde versie. Bewust **niet** meegenomen: `addFixedGrocery` (zelfde bestand) heeft hetzelfde onbeveiligde patroon maar viel buiten wat de reviewer aanmerkte — geen ongevraagde scope-uitbreiding. Bewezen met een nieuwe e2e-test `e2e/shoppingListAccess.e2e.ts`: huishouden A probeert via een gemanipuleerd verborgen formulierveld een regel in huishouden B's lijst te schrijven; vóór de fix slaagde die aanval echt (24 regels i.p.v. 23), na de fix wordt hij geweigerd. **(2) Verkeerde verpakkingsberekening voor MANUAL+stuks**: de bestaande `line.source === "FIXED" && line.unit === "PIECE"`-kortsluiting (die een letterlijk gekozen aantal verpakkingen laat doorlopen i.p.v. door de receptgedreven verpakkingsengine) gold niet voor MANUAL-regels — een MANUAL-regel met `unit: PIECE, quantity: 3` op een ml/gram-product gaf via de engine ten onrechte maar 1 verpakking terug in plaats van 3. Gefixt met een nieuwe gedeelde `isUserChosenPackageCount()` in `src/lib/shoppingList.ts`, toegepast op alle 5 plekken die voorheen los `FIXED && PIECE` checkten (`page.tsx` ×3, `controle/page.tsx`, `cartService.ts`). Bewezen met een nieuwe integratietest in `cartService.test.ts`: vóór de fix stuurde het mandje-vullen `count: 1` naar Picnic i.p.v. het gekozen `count: 3`. Beide fixes zijn expliciet geverifieerd met revert-en-bevestig: fix tijdelijk teruggedraaid → nieuwe test faalt aantoonbaar → fix hersteld → test slaagt weer. **Bijkomende testinfrastructuur-fixes, ontdekt doordat dit de eerste keer was dat twee e2e-bestanden samen draaiden** (`test:e2e` ging van één hardgecodeerd bestand naar `tsx --test $(find e2e -name '*.e2e.ts')`): (a) `criticalFlow.e2e.ts`'s zoekknop-selector werd dubbelzinnig doordat de nieuwe "Product toevoegen"-sectie een identiek gelabelde knop kreeg — gefixt met scoping op `#add-fixed-grocery`; (b) Node's testrunner draait meerdere testbestanden **standaard gelijktijdig**, waardoor twee `next build`-aanroepen tegelijk naar dezelfde gedeelde `.next/`-map schreven — dit veroorzaakte een echte, reproduceerbare hang (een testrun bleef **36+ minuten** letterlijk niets doen, geen enkel subproces meer actief) in plaats van een nette fout; opgelost door `test:e2e` te forceren tot seriële uitvoering (`--test-concurrency=1`), simpeler en veiliger dan elk e2e-bestand een eigen build-map te geven; (c) tijdens het uitzoeken van die hang bleek `startMockPicnicServer`'s `server.listen(...)`-promise geen `'error'`-listener te hebben — bij een bezette poort (bijv. een restant van een eerder afgebroken run) vuurt `'listening'` nooit en bleef de promise voor altijd hangen zonder ooit te falen; gefixt met een expliciete `'error'`-listener die de promise laat rejecten. Volledige `npm run test:e2e` (beide bestanden, serieel) opnieuw gedraaid ná alle fixes: 13/13 groen, inclusief de nieuwe household-isolatie-test. `tsc`/`eslint`/`npm test` (250/250, +5 t.o.v. WP82) groen. Geen nieuwe migratie in dit vervolg-WP. |
| WP83 | SYSTEM_AUDIT.md-vervolg: A1–A5 (IDOR's, ingrediëntcatalogus, wachtwoordhashing, rate limiting), legacy-fallback begrensd, brede IDOR-hercontrole | Vervolgopdracht op `SYSTEM_AUDIT.md` (de volledige, read-only audit uit een eerdere sessie): elke bevestigde bevinding geverifieerd tegen de huidige code en waar nodig gefixt, telkens met een revert-en-bevestig-regressietest. **A1/A2 — IDOR in vaste boodschappen** (`src/app/boodschappen/fixedGroceriesActions.ts`): `addFixedGrocery` riep nergens `assertShoppingListAccess` aan op de meegestuurde `shoppingListId` (bleek dode code, geen enkele UI verwijst ernaar, dus zonder praktisch exploitpad — toch gefixt volgens hetzelfde bewezen patroon); `removeFixedGroceryPermanently` verifieerde de eigenaarschap van de meegestuurde `lineId` niet vóór het verwijderen — nu via `loadFixedLine(lineId)` (dezelfde helper als elders in dit bestand), de twee gekoppelde deletes (`fixedGrocery` + `shoppingListLine`) samengevoegd in één `prisma.$transaction([...])` voor atomiciteit. **A3 — ingrediëntcatalogus-autorisatie**: `updateIngredient` (`src/app/recepten/actions.ts`) liet elk huishouden `category`/`restrictionTags` van een gedeeld, globaal ingrediënt wijzigen — veiligheidskritiek, want `restrictionTags`/`category` bepalen mede of een recept een allergie/hard beperking passeert (`dietaryRestrictions.ts`). Kleinste veilige fix (geen schema-wijziging, geen aparte catalogus per huishouden): die twee velden uit `updateIngredient` gehaald, alleen `name`/`likelyInStock` blijven aanpasbaar; `/recepten` toont categorie/restricties nu read-only. `createIngredient`/`createIngredientProduct` bewust ongewijzigd gelaten (`Ingredient.name` is al `@unique`, kan een bestaand ingrediënt dus niet overschaduwen; `createIngredientProduct` raakt alleen `Product`, geen veiligheidskritieke velden). **A4 — wachtwoordhashing**: `src/domain/household/credentials.ts` volledig herbouwd van één ongesalte, snelle sha256-hash (met `householdId` als saltvervanger) naar Node's ingebouwde `scrypt` (bewust géén bcrypt/argon2 — dat zijn meestal native dependencies, een risico op Vercel serverless) met een echte, willekeurige salt per wachtwoord, zelfbeschrijvend opslagformaat (`scrypt$N$r$p$keylen$saltHex$hashHex`, zodat toekomstige parameteraanpassingen bestaande hashes niet breken) en timing-safe vergelijking. Volledig achterwaarts-compatibele migratie: een oude sha256-hash wordt nog herkend (vaste lengte/vorm) en gecontroleerd, en bij een geslaagde legacy-login meteen herhasht naar het nieuwe formaat (`src/lib/auth.ts`, `signInByCredentials`) — geen enkel huishouden wordt uitgesloten, niemand hoeft zelf iets te doen. **A5 — rate limiting op `/login`**: nieuw `LoginAttempt`-model + migratie (`20260731145811_login_attempts`) en nieuwe `src/lib/loginRateLimit.ts` — bewust database-gebaseerd (Postgres via Prisma) in plaats van een in-memory `Map`, omdat de app serverless draait (Vercel) en dit project geen Redis/KV heeft; bewust alleen op genormaliseerde gebruikersnaam (geen IP), om het dreigingsmodel (herhaald gokken tegen één bekende gebruikersnaam) af te dekken zonder extra persoonsgegevens te bewaren. Na 8 mislukte pogingen binnen 15 minuten wordt zelfs een correct wachtwoord tijdelijk geblokkeerd (geen bypass), met dezelfde generieke foutmelding als een gewoon fout wachtwoord (geen aparte "geblokkeerd"-tekst die zelf al iets zou prijsgeven); een geslaagde login wist de teller. **Deel B — legacy single-household fallback**: `getLegacySingleHousehold` (`src/lib/auth.ts`) gaf tot nu toe *voor altijd* sessieloze toegang aan het enige huishouden zonder `username`, zonder tijdsgrens — kon in deze sandbox niet worden geverifieerd of de productie-installatie van de gebruiker inmiddels een gebruikersnaam heeft (geen netwerktoegang tot productie), dus bewust niet stilzwijgend verwijderd. In plaats daarvan begrensd met een vaste datumgrens (nieuwe, pure `src/domain/household/legacyAccess.ts`, `selectLegacySingleHousehold`): alleen een huishouden dat al vóór dit werkpakket bestond komt nog in aanmerking — een nieuw aangemaakt huishouden (waarvoor onboarding altijd meteen een gebruikersnaam zet) kan hier nooit meer via toegang krijgen, ook niet in het randgeval van een halverwege gecrashte onboarding. **Deel C — brede IDOR-hercontrole**: alle ~45 exported server actions die een `shoppingListId`/`lineId`/`ingredientId`/`recipeId`/`recipeVariantId`/`personId`/`productId` uit formData ontvangen, systematisch nagelopen op exact hetzelfde patroon (een los `householdId`-veld geverifieerd, maar de eigenlijke foreign id niet) — eerst via een onderzoeksagent, daarna een aantal kritieke claims zelf handmatig geverifieerd in de broncode. Geen nieuwe bevestigde instanties gevonden: alle overige plekken scopen de foreign id al correct (via `assertShoppingListAccess`/`loadFixedLine`/`loadEditableShoppingLine`/`loadLineForCurrentHousehold`/`accessibleRecipeWhere`/`editableRecipeWhere`, of een directe `where:{id, householdId}`), of betreffen de bewust gedeelde, niet-huishoudgescoopte `Ingredient`/`Product`-catalogus. Eén laag-risico datapunt bevestigd maar bewust niet gefixt (al bekend en als zodanig gedocumenteerd in `SYSTEM_AUDIT.md` sectie 9): `setDayRoutine` slaat een `recipeVariantId` op zonder eigen verificatie, maar `ensureMealPlan` dereferentie 'm alleen via een al huishoud-gescoopte varianten-lijst — een vervalste waarde wordt dus genegeerd, geen leesbaar/schrijfbaar lek. Nieuwe/uitgebreide tests: `credentials.test.ts` herschreven (12 tests, nieuwe salt-per-aanroep-eigenschap incompatibel met het oude, deterministische testontwerp), nieuwe `legacyAccess.test.ts` (6 tests), nieuwe `loginRateLimit.test.ts` (9 tests, met een injecteerbare `now`-parameter voor deterministisch testen zonder echte wachttijden), en `e2e/shoppingListAccess.e2e.ts` uitgebreid met scenario's voor elk van A1/A2 (vervalste `lineId`), A3 (vervalste `restrictionTags`/`category`), A4 (legacy-login-en-migratie) en A5 (blokkering na te veel pogingen + herstel na wissen). Alle nieuwe/gewijzigde tests expliciet met revert-en-bevestig geverifieerd (fix tijdelijk teruggedraaid → test faalt aantoonbaar → fix hersteld → test slaagt weer). Onafhankelijke `code-reviewer`-subagent ingeschakeld op de volledige diff vóór afronding (authenticatie/household-isolatie/schema, precies wanneer `WORKFLOW.md` dat voorschrijft) — zie de eigen vervolgregel hieronder voor de uitkomst. **Nieuw `MEAL_PLANNING_GAP_PLAN.md`** (Deel F, puur analyse/planning, geen code): 13 productwensen uit een aparte opdracht geïnventariseerd op bestaande bouwstenen vs. ontbrekende functionaliteit, met een voorgestelde minimale uitbreiding per wens en een aanbevolen (niet-genummerde) uitvoeringsvolgorde — nog niets van geïmplementeerd. `tsc`/`eslint`/`npm test` (270/270)/`npm run build`/`npm run test:e2e` (21/21, over beide e2e-bestanden) groen. Migratie: `20260731145811_login_attempts` (nieuwe, op zichzelf staande tabel, geen bestaande kolommen geraakt). |
| WP83-vervolg | Code-review van WP83: één P1-bevinding gefixt (timing-lek), twee bewust openstaand | De onafhankelijke `code-reviewer`-subagent (ingeschakeld vóór het mergen van WP83, zoals `WORKFLOW.md` voorschrijft voor authenticatiewijzigingen) vond drie bevindingen. **(1) Timing-side-channel voor username-enumeratie (gefixt)**: de overstap naar scrypt (A4) introduceerde zelf een nieuw lek — een onbekende gebruikersnaam deed voorheen geen enkele hashberekening (want geen hash om tegen te controleren), terwijl een bestaande gebruikersnaam met een fout wachtwoord nu een volle scrypt-berekening kost (~45ms, tegen sha256's ~0.04ms voorheen). Dat verschil is ruim meetbaar en zou precies het username-enumeratielek blootleggen dat de generieke foutmelding (WP62) juist moet voorkomen. Gefixt met een vaste `DUMMY_PASSWORD_HASH_FOR_TIMING` (`src/domain/household/credentials.ts`) waartegen `signInByCredentials` (`src/lib/auth.ts`) bij een onbekende gebruikersnaam of ontbrekende hash alsnog een even dure, genegeerde scrypt-verificatie uitvoert. Empirisch bevestigd met een handmatige timingmeting (niet als geautomatiseerde test — timing is inherent te ruisgevoelig voor een betrouwbare CI-assert): het verschil ging van ~42ms vóór de fix naar ~0.1ms erna. Nieuwe unit test in `credentials.test.ts` dekt de functionele correctheid van de dummy-hash (geldig scrypt-formaat, accepteert nooit een echt wachtwoord). **(2) Rate limiter is check-then-act, niet atomair (bewust niet gefixt)**: bij veel gelijktijdige, parallelle aanvragen tegen dezelfde gebruikersnaam kunnen meerdere requests de teller lezen vóórdat een eerdere poging gecommit is, en zo allemaal langs de limiet komen — sequentiële aanvragen (het geteste scenario) blijven wel correct geblokkeerd. Zou een atomaire aanpak vereisen (advisory lock of een gecombineerde insert-met-voorwaarde); expliciet als bekende, geaccepteerde beperking vastgelegd in `SYSTEM_AUDIT.md` in plaats van in deze cyclus mee te nemen. **(3) `createIngredient`/`upsertParsedRecipeIngredients` staan nog vrij `category`/`restrictionTags` toe op een NIEUW ingrediënt (bewust niet gefixt)**: A3 dekte alleen het bewerkpad van een bestaand ingrediënt; het aanmaakpad (inclusief automatische recepttekst-parsing) viel buiten de scope van die bevinding. `SYSTEM_AUDIT.md` bevinding 3 is bijgewerkt om dit expliciet als openstaand deelprobleem te vermelden in plaats van als volledig opgelost te tellen. `tsc`/`eslint`/`npm test` (271/271, +1 voor de nieuwe dummy-hash-test) groen. Geen nieuwe migratie. |
| WP84 | Dag overslaan in het weekmenu ("we eten uit") | Gebruikersverzoek: als het gezin een avond uit eet, moet die dag uit de weekplanning te halen zijn zonder de rest van de logica te verstoren. Nieuw veld `MealPlanEntry.skipped` (boolean, standaard `false`, additief) — de geplande maaltijd zelf blijft bestaan (voor geschiedenis/afwisseling), maar telt niet meer mee. Twee plekken moesten daadwerkelijk rekening houden met een overgeslagen dag: (1) de boodschappenlijst-opbouw (`aggregateMealNeeds` in `src/lib/shoppingList.ts`, gebruikt door zowel `ensureShoppingList` als `findShoppingListShortfalls`, plus een los tweede aggregatiepunt in `syncShoppingListForInventoryChange`) — een overgeslagen dag levert nu geen ingrediënten meer op voor de lijst; (2) de "stille bevestiging"-logica (`entriesForSilentAcceptance`, `src/domain/meal-planning/silentAcceptance.ts`), aangeroepen door `acceptProposedMealPlanEntries` zodra de boodschappenlijst bevestigd wordt — zonder deze aanpassing zou een overgeslagen dag alsnog een positieve `CHOSEN`-feedback en een geleerd patroon opleveren voor een maaltijd die nooit gekookt is, precies het risico dat de gebruiker expliciet benoemde ("het slimme gedeelte mag er geen last van hebben"). Nieuwe server action `toggleMealPlanEntrySkipped` (`src/app/actions.ts`) — bewust geen los `entryId` van de client vertrouwd; de regel wordt afgeleid via `householdId` + `weekStart` + `dayKey` (zelfde veilige patroon als `replaceMealPlanEntry`), dus geen nieuwe IDOR-oppervlakte. UI: simpele aan/uit-knop ("Uit eten" / "Toch wel") per dag op `/` naast de bestaande "Wissel"-knop, met een amber "Uit eten"-tag en een gedimde kaart wanneer actief — bewust geen apart notitieveld (expliciete gebruikerskeuze voor eenvoud). Geverifieerd: nieuwe unit tests in `shoppingList.test.ts` en `silentAcceptance.test.ts` (pure functies), plus een nieuwe integratietest in `inventory.test.ts` die een echte, via Prisma opgebouwde boodschappenlijst controleert — alle drie met revert-en-bevestig geverifieerd (fix teruggedraaid → test faalt aantoonbaar → hersteld → test slaagt weer). `tsc`/`eslint`/`npm test` (274/274)/`npm run build`/`npm run test:e2e` (21/21) groen. Migratie: `20260731160544_meal_plan_entry_skipped` (één nieuwe kolom met `DEFAULT false`, puur additief, geen bestaande data geraakt). |

## Nog te doen (roadmap, nog niet gestart)

- **Nieuwe hoofdflow is leidend:** `Week kiezen → Aanvullen → Per dag controleren → Totaalbestelling controleren → Naar Picnic`.
- De expliciete prioriteitenlijst uit `DATAMODEL_AUDIT.md` (punten 1 t/m 6: receptscope, typed tags, waarom-signalen, afgeleide patronen, onboardingprofiel, entry-context) is nu volledig afgerond (WP20–WP23, WP52).
- De recente "bevestiging bij elke opslagactie"-sweep is afgerond voor de bekende losse acties op `/`, `/gerechten`, `/recepten`, `/ons-gezin` en `/boodschappen`. Bij nieuwe server actions dezelfde regel blijven volgen: na succesvolle mutatie redirecten naar een verse render met `?status=...` en, op lange pagina's, terug naar dezelfde sectie/regel.
- WP56 maakt geleerde weekpatronen zichtbaar en beheerbaar op `/ons-gezin`; WP57 voegt daar daggerichte gerechtvoorkeuren aan toe (los van de automatisch geleerde patronen — dit zijn expliciete, door de gebruiker gezette voorkeuren).
- WP58 dekt Fase 16 (monitoring/logging) voor de belangrijkste gebieden; WP73 sluit de resterende twee gaten (client-side renderfouten, generieke serverfouten via de error-boundaries).
- Fase 15 (tests): afgerond met WP59 — `npm run test:e2e` dekt nu de kritieke flow end-to-end tegen een mock-Picnic-server.
- `DATAMODEL_AUDIT.md` punt 7 (provider-neutraliteit) en punt 8 (voorraad als wekelijkse check i.p.v. administratie) zijn afgerond met WP60/WP61.
- WP72 rondt de WP69–WP72-reeks (attention-laag → pushmeldingen → bezorgmoment-voorkeur → bezorgmoment-pushmelding) af. Verder geen vastgelegde "volgende WP" — kies in overleg met de gebruiker.
- **Let op voor de volgende sessie:** werk vanaf hier verder op `main`/een nieuwe branch vanaf `main` — zowel Claude- als Codex-sessies hebben recent rechtstreeks tegen dit project gewerkt (zie de WP53–WP57-commits), dus neem niet aan dat de laatst bekende branch-staat uit een eerdere sessie nog actueel is. Controleer altijd eerst `git log origin/main` en dit bestand voordat je verder bouwt.

## Operationele kennis

Verplaatst naar `OPERATIONS.md`: Prisma-migraties in een niet-interactieve
sandbox, productie-migraties tegen Supabase (inclusief de poolerpoorten en
de `DIRECT_URL`-valkuil), testconventies, architectuuroverzicht en
beveiligingsincident-historie. Raadpleeg dat bestand gericht wanneer je
tegen een van die onderwerpen aanloopt.
