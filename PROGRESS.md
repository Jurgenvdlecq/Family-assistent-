# Voortgang & overdrachtsdocument

Dit bestand is bedoeld voor een AI-coding-agent (Claude Code, Codex, of wie
dan ook) die dit project overneemt in een nieuwe sessie zonder chatgeschiedenis.
Lees eerst `AGENTS.md` (de actieve kernregels; het oorspronkelijke,
gefaseerde bouwplan staat — niet meer actief sturend — in
`PROJECT_BLUEPRINT.md`), `PRODUCT_VISION.md` (het actuele productkompas),
`DATAMODEL_AUDIT.md`
(toetsing van schema/architectuur tegen de visie), `WORKFLOW.md` (de
werkregels en Definition of Done — wordt automatisch geladen via
`CLAUDE.md`, dus vermoedelijk al bekend) en dan dit bestand voor de actuele
status. Loop je tegen een migratie-, test- of architectuurvraag aan, raadpleeg
dan ook `OPERATIONS.md`. De volledige beschrijving van WP1 t/m WP75 (hieronder
alleen nog titel-only, zie `WORKFLOW.md` sectie "context-/tokenefficiëntie")
staat in `PROGRESS_ARCHIVE.md` — alleen raadplegen als de details van een
ouder werkpakket echt nodig zijn.

## Manier van werken

Zie `WORKFLOW.md` — die werkregels (branchstrategie, mergebeleid, wanneer
expliciet toestemming vragen, commit-taal) staan daar nu vast, zodat ze
automatisch geladen worden in elke sessie in plaats van dat je dit bestand
apart moet lezen.

## Status: work packages

Alle work packages hieronder zijn **gemerged in `main`** en staan live in
productie (Vercel + Supabase), tenzij anders vermeld.

*(WP1 t/m WP75 gecomprimeerd tot titel-only — volledige beschrijving per werkpakket in `PROGRESS_ARCHIVE.md`. Vanaf WP76 hieronder nog volledig, net als in het origineel.)*

| WP | Titel |
|----|-------|
| WP1 | Dieetbeperkingen |
| WP2 | Vaste boodschappen |
| WP3 | Quantity-engine |
| WP4 | Eenvoudig voorraadmodel |
| WP5 | Productmatching als domein |
| WP6 | Controlepagina herbouwd |
| WP7 | Picnic-mandje professionaliseren |
| WP8 | Uitlegbare weekplanning-scoring |
| WP9 | Multi-household / authenticatie |
| WP10 | Persoonlijke gezinslogica |
| WP11 | Persoonsvoorkeuren per gerecht |
| WP12 | Persoonsvoorkeuren per categorie/ingrediënt |
| WP13 | Voorkeurenbeheer op Ons gezin |
| WP14 | Weekplanning opnieuw genereren |
| WP15 | Recepten/varianten beheren |
| WP16 | Ingrediëntenbeheer en receptingrediënten bewerken |
| WP17 | Productkeuzes per ingrediënt beheren |
| WP18 | Productvisie vastleggen |
| WP19 | Datamodel toetsen aan productvisie |
| WP20 | Receptscope en basisdata scheiden |
| WP21 | Typed meal tags en slimme wens-invoer |
| WP22 | Typed waarom-signalen bij vervangen |
| WP23 | Afgeleide patronen en leervragen |
| WP24 | Onboardingprofiel met snel/beter afstemmen |
| WP25 | Hoofdflow als assistent |
| WP26 | Aanvullen en controle versimpelen |
| WP27 | Directe knopfeedback op controle |
| WP28 | Echte product- en gerechtfoto's |
| WP29 | Controle blijft op dezelfde plek |
| WP30 | Vaste boodschappen zoeken en productkeuze sturen |
| WP31 | Navigatie en controle sneller maken |
| WP32 | Vaste boodschappen wijzigen en klikfeedback |
| WP33 | Performance quick wins zonder migratie |
| WP34 | Performance-indexen en product-upserts |
| WP35 | Jouw week als assistent-flow |
| WP36 | Favorieten en productvoorkeuren aanpassen |
| WP37 | Receptenpagina versimpeld |
| WP38 | Productkeuze en concrete maaltijdwens |
| WP39 | Maaltijdwens-parser aangescherpt |
| WP40 | Bulk vaste boodschappen invoeren |
| WP41 | Bulk boodschappenlijst blijft staan |
| WP42 | Bestellen in verpakkingen tonen |
| WP43 | Per-dag boodschappencontrole |
| WP44 | Gekozen bulkregel verdwijnt |
| WP45 | Dagcontrole met acties en kosten |
| WP46 | Recept snel toevoegen |
| WP47 | Dagcontrole sluit beantwoorde keuzes |
| WP48 | Zoeken overrulet gerecht-suggesties |
| WP49 | Losse maaltijd per dag |
| WP50 | Maaltijdhoeveelheid versus weektotaal |
| WP51 | Vaste daggewoontes bovenop losse maaltijden |
| WP52 | MealPlanEntry-context |
| UX WP1 | `/boodschappen` naar 1 samengevatte lijst |
| UX WP2 | Navigatie, titels, controle-teksten, startscherm compacter |
| Bugfix | Halve stuks op `/boodschappen` |
| Feature | Vangnet tegen stilzwijgend onder-bestellen |
| Bugfix | `/recepten`-wijzigingen leken te "reverten" |
| Feature | Bevestiging bij elke opslagactie (homepage + `/ons-gezin`) |
| Feature | Recept verwijderen |
| Fix | Laatste opslagfeedback en algemene foutboundary |
| WP53 | Leren van stil geaccepteerde weekplanning |
| WP54 | Leervragen uit herhaald stil accepteren |
| WP55 | Bevestigde acceptatiepatronen sturen planning zacht |
| WP56 | Geleerde weekpatronen beheren |
| WP57 | Daggerichte gerechtvoorkeuren |
| Bugfix | Vaste boodschappen: geen productfoto's + onzinnige verpakkingsinfo |
| Bugfix | Onderste navigatie verdween tijdens scrollen op mobiel |
| Feature | Gerechtfoto's verwijderd + ingrediënten inzichtelijk op de week |
| Bugfix | Vers/Houdbaar-badge negeerde het echte gekozen product + wenszoekbox stond op onlogische plek |
| Bugfix | Vers/Diepvries-badge alsnog weggehaald |
| WP58 | Gestructureerde logging (Fase 16) |
| WP59 | End-to-end testsuite (Fase 15) |
| WP60 | Provider-neutraliteit voor productreferenties |
| WP61 | Voorraadcheck voelt als wekelijkse check, niet als administratie |
| WP62 | Loginscherm lekt geen huishoudennamen meer |
| Bugfix | Picnic-mandje vullen kon stoppen op één transiënte verbindingsfout |
| WP63 | Picnic koppelen via de app (i.p.v. terminal) |
| WP64 | `/gerechten`: schijnfunctionaliteit weghalen |
| WP65 | `/ons-gezin` opgeschoond met progressive disclosure |
| WP66 | `/recepten`: zoeken in de receptenlijst |
| WP67 | Polish-ronde uit de kritische UX-review (P2/P3) |
| WP68 | Gerecht verbergen na herhaalde negatieve feedback + herstellen (Fase 11) |
| WP69 | Attention-laag: één bron van waarheid voor "wat staat er nog open" (voorbereiding pushmeldingen) |
| WP70 | Echte pushmeldingen: PWA, service worker, cron-endpoint bovenop de attention-laag |
| WP71 | Picnic-bezorgmoment: voorkeur instellen + eerlijke statuscontrole (geen automatisch reserveren/bestellen) |
| WP72 | Bezorgmoment-risico wordt een echte pushmelding |
| WP73 | Fase 16 afronden: client-side fouten en generieke serverfouten alsnog gestructureerd loggen |
| WP74 | Fase 10-check: planningsstijl na onboarding aanpasbaar maken |
| WP75 | Fase 13: gemeten databasequeries + een echte N+1 verholpen |

| WP | Titel | Kern |
|----|-------|------|
| WP76 | Afvinklijst voor zelf boodschappen doen | Gebruikersverzoek na een productbrainstorm ("kijk als ervaren app-ontwikkelaar mee, wat helpt een gezin dagelijks echt verder"): de bestaande `PicnicTransfer`-tekstexport ("Naar Picnic") is puur een kopieerbare lijst om handmatig in de Picnic-app te plakken — geen echte optie voor een gezin dat gewoon zelf naar de Aldi/Jumbo loopt. Nieuwe sectie "Zelf boodschappen doen" op `/boodschappen` (ingeklapt, altijd zichtbaar — ook mét Picnic-koppeling, want ook Picnic-huishoudens halen weleens iets zelf) met een interactieve afvinklijst van alle regels (maaltijd- én vaste boodschappen), met productfoto, naam, hoeveelheid en een voortgangsteller ("X van Y afgevinkt"). Nieuw veld `ShoppingListLine.pickedUpAt` (nullable timestamp, zelfde patroon als `transferredToPicnicAt`/`reviewedAt` — losstaand van de Picnic-overdrachtsstatus, een huishouden kan beide tegelijk gebruiken). Reset vanzelf elke week: een nieuwe week krijgt nieuwe `ShoppingListLine`-rijen via `ensureShoppingList`, dus geen aparte "wis alle vinkjes"-logica nodig. Nieuwe server action `toggleShoppingListLinePickedUp` doet bewust geen `redirect()` zoals de andere regelacties (die zijn ontworpen voor één wijziging per keer, met een statusmelding) — hier moet je snel door 20 producten heen kunnen tikken, dus optimistische lokale state in `ShoppingChecklist.tsx` (`"use client"`, `useTransition`) met de server als achtergrondsynchronisatie. Geverifieerd: 6-staps Playwright-controle tegen een productiebuild bevestigt aanvinken/uitvinken, dat de voortgangsteller klopt, dat de staat bewaard blijft na een verse paginalading (zoals een echte gebruiker het zou zien) en dat uitvinken `pickedUpAt` weer op `null` zet; `tsc`/`eslint`/`npm test` (238/238)/`npm run build`/`npm run test:e2e` (10/10) groen. Migratie nodig: `20260730084359_shopping_list_line_picked_up` (één nieuwe nullable kolom, puur additief). |
| Bugfix | Nep-hamburgermenu linksboven op het beginscherm | Gebruikersrapport: het driestrepen-icoon linksboven op `/` deed niets bij een tik. Uitgezocht: het was een puur decoratief `<Menu>`-icoon (geen link, geen `onClick`) — vermoedelijk restant van een vroege opzet met een uitklapmenu, dat overbodig werd zodra de vaste onderste navigatiebalk die rol overnam. Op alle andere schermen (`/boodschappen`, `/gerechten`, `/controle`, `/recepten`) staat links altijd een écht werkende terugknop en rechts een puur decoratief thema-icoon — het beginscherm had dat precies andersom (links schijnfunctionaliteit, rechts pas de echte link naar "Ons gezin"). Overwogen om er een echt uitklapmenu van te maken, maar alle mogelijke inhoud daarvan (instellingen, Picnic, meldingen, uitloggen) zit al één tik verderop onder "Ons gezin" — een menu zou alleen bestaande navigatie dupliceren. In plaats daarvan `Menu` vervangen door `CalendarDays`, consistent met het decoratieve-icoon-patroon van de andere schermen. Geverifieerd met Playwright tegen een productiebuild dat het icoon geen `<a>`/`<button>` meer is; `tsc`/`eslint`/`npm test` (238/238)/`npm run build`/`npm run test:e2e` (10/10) groen. Geen nieuwe migratie nodig. |
| WP77 | Inloggen met gebruikersnaam + wachtwoord (i.p.v. losse toegangscode) | Gebruikersverzoek: de bestaande `/login` werkte met één gedeelde toegangscode per huishouden (`Household.accessCodeHash`), met de zorg "stel dat twee mensen hetzelfde wachtwoord kiezen, gaat het dan fout?". Gekozen aanpak (huishoudens optie 2, niet individuele accounts per gezinslid): gebruikersnaam + wachtwoord, nog steeds gedeeld per huishouden. **Eerste implementatie bevatte een echte fout die het exact gevreesde scenario niet voorkwam**: de gebruikersnaam werd samen met het wachtwoord gehasht en gezouten met het huishouden-ID (`hash(householdId + username + password)`), en inloggen probeerde die hash tegen elk huishouden. Met een Playwright-script dat twee huishoudens bewust dezelfde gebruikersnaam+wachtwoord liet kiezen, bleek de tweede meldingsloos in te loggen op het **eerste** huishouden (willekeurige iteratievolgorde besliste, niet de bedoeling van de gebruiker) — het huishouden-ID voorkwam alleen dat een fóúte gok op een ander huishouden zou passen, niet dat twee bewust identieke combinaties elkaar zouden verwarren. Herontworpen met een echte oplossing: `username` is nu een aparte, **unieke** kolom in het schema (`@unique`, genormaliseerd — kleine letters, getrimd) in plaats van onderdeel van de hash; `passwordHash` blijft per huishouden gezouten. Inloggen is daardoor een directe opzoeking op gebruikersnaam (nooit meer dan één kandidaat, dus geen giswerk over huishoudens heen) gevolgd door één wachtwoordcontrole, met nog altijd één generieke foutmelding voor zowel een onbekende gebruikersnaam als een fout wachtwoord (voorkomt dat een bezoeker kan aftasten welke gebruikersnamen bestaan, zelfde WP62-principe als voorheen). Een bezette gebruikersnaam wordt nu al bij het **kiezen** geweigerd (bij onboarding en bij wijzigen via `/ons-gezin`), niet pas bij een verwarrende login achteraf. **Tweede bevinding tijdens verificatie**: een geworpen `Error` uit een server-actie wordt door Next.js in productiebuilds gereduceerd tot een generiek "er is iets misgegaan"-bericht (redactie van foutdetails, standaardgedrag — zie `node_modules/next/dist/docs/.../10-error-handling.md`), waardoor de nieuwe "deze gebruikersnaam is al in gebruik"-melding de gebruiker nooit zou bereiken. `completeOnboarding` (`src/app/onboarding/actions.ts`) geeft verwachte fouten nu terug als `{ error }` in plaats van te throwen (Next's eigen aanbevolen patroon voor server-acties); `updateCredentials` (`src/app/ons-gezin/actions.ts`, aangeroepen via een gewone `<form action>`) redirect bij een botsing naar een statuscode (`?status=username-taken`) die `/ons-gezin` als rode melding toont — zelfde patroon als de bestaande groene statusmeldingen op die pagina. Nieuwe domeinmodule `src/domain/household/credentials.ts` (wachtwoordhashing + gebruikersnaam-normalisatie/validatie, bewust gescheiden van `src/lib/auth.ts` omdat dat bestand `server-only` importeert en dus niet los te unittesten is — zelfde patroon als eerdere domeinmodules). Geverifieerd met een dedicated Playwright-scenario tegen een productiebuild: (1) twee huishoudens met bewust identieke inloggegevens — de tweede wordt nu geweigerd mét zichtbare melding, geen half aangemaakt huishouden blijft achter; (2) inloggen met de gedeelde combinatie gaat naar het juiste (enige) huishouden; (3) een fout wachtwoord toont nog steeds de generieke, niet-lekkende melding; (4) wijzigen naar een bezette gebruikersnaam via `/ons-gezin` toont dezelfde duidelijke fout. `tsc`/`eslint`/`npm test` (245/245)/`npm run build`/`npm run test:e2e` (10/10) groen. **Migratie nodig** (nog niet op productie gedraaid, gebruiker moet toestemming geven): `20260730110756_household_username_password` — verwijdert `access_code_hash`, voegt `username` (uniek) en `password_hash` toe. **Let op voor na de migratie**: de oude toegangscode kan niet worden omgezet (de kolom verdwijnt) — elk huishouden moet na de migratie eenmalig via `/ons-gezin` een nieuwe gebruikersnaam + wachtwoord instellen voordat een nieuwe login-sessie nodig is; de huidige sessiecookie blijft intussen gewoon werken (sessies staan los van de inloggegevens), dus geen acute uitsluiting. |
| WP78 | Productiemigraties automatisch bij elke deploy | Vervolg op WP77: omdat Vercel bij elke push naar `main` automatisch een nieuwe productiedeploy uitrolt, maar de bijbehorende databasemigratie tot nu toe altijd een **losse, door de gebruiker zelf uit te voeren stap** was, kon appcode die een nieuwe kolom verwacht al live staan vóórdat die kolom in productie bestond (precies dit gebeurde meteen na WP77's merge). Gebruiker vroeg expliciet om dit voortaan automatisch te laten gebeuren. Nieuw: `package.json`'s `build`-script is nu `prisma migrate deploy && next build` — elke Vercel-deploy past dus eerst alle openstaande migraties toe, en faalt de deploy hard (fail-closed) als dat niet lukt, in plaats van appcode live te zetten die niet bij het schema past. Migraties hebben een niet-gepoolde databaseverbinding nodig (Supabase's transaction-mode pooler op poort 6543, waar de app zelf `DATABASE_URL` voor gebruikt, ondersteunt geen Prisma-migratielocking) — `prisma.config.ts` gebruikt daarom een nieuwe, optionele `DIRECT_URL` (valt terug op `DATABASE_URL` als die ontbreekt, zoals lokaal zonder pooler). Eenmalige actie die alleen de gebruiker zelf kan doen (buiten bereik van deze werkomgeving): in Vercel's projectinstellingen een environment variable `DIRECT_URL` toevoegen met Supabase's session-mode-pooler-connectiestring (poort 5432, "Connect to your project" → Session pooler in het Supabase-dashboard). Geverifieerd: lokale `npm run build` bevestigt dat `prisma migrate deploy` zonder `DIRECT_URL` gewoon terugvalt op `DATABASE_URL` en "No pending migrations to apply" meldt vóórdat `next build` start; e2e-tests bouwen via een eigen `next build`-aanroep (niet via `npm run build`) en zijn dus niet geraakt. `tsc`/`eslint`/`npm test` (245/245) groen. Geen nieuwe Prisma-migratie in dit WP zelf. **Update na productiegebruik**: de eerste échte poging op Vercel faalde met `P1013: the scheme is not recognized` — de gebruiker had bij het invullen van `DIRECT_URL` per ongeluk de variabelenaam en de aanhalingstekens (`DIRECT_URL="...”`) mee in de waarde gezet in plaats van alleen de kale `postgresql://...`-string. Na correctie in Vercel's environment variables slaagde de productiedeploy en is bevestigd dat de WP77-migratie (`username`/`password_hash`) automatisch is toegepast: de gebruiker heeft op de live site succesvol een gebruikersnaam+wachtwoord ingesteld, is uitgelogd en daarmee weer ingelogd. Deze automatische migratiepijplijn is dus in de praktijk end-to-end bevestigd, niet alleen lokaal. |
| WP79 | Agent-workflow-fundamenten (analyse + Fase 1) | Op gebruikersverzoek: een volledige analyse van hoe goed dit project is ingericht voor zelfstandig werken door Claude Code (zie het analyserapport in de chatgeschiedenis). Belangrijkste bevinding: de daadwerkelijke werkregels stonden in `PROGRESS.md`, dat — anders dan `CLAUDE.md`/`AGENTS.md` — niet gegarandeerd geladen wordt in een verse sessie; en er was geen `README.md` met echte projectinhoud, geen samengevoegd verificatiecommando en geen losstaande Definition of Done. Dit WP pakt Fase 1 van het prioriteitenplan op. Nieuw `WORKFLOW.md` (bevat nu de "Manier van werken"-regels uit `PROGRESS.md` plus een expliciete Definition-of-Done-checklist en een sectie "wanneer geen aannames doen") wordt automatisch geladen via `CLAUDE.md` (`@AGENTS.md` + `@WORKFLOW.md`). Nieuw `OPERATIONS.md` bevat de verplaatste "Niet-voor-de-hand-liggende operationele kennis" uit `PROGRESS.md` (Prisma-migraties in de sandbox, productie-migraties/poolerpoorten, testconventies, architectuuroverzicht, beveiligingsincidenten) — gericht te raadplegen, niet elke sessie automatisch geladen, om `CLAUDE.md`'s automatische context klein te houden. `PROGRESS.md` verwijst nu naar beide in plaats van de inhoud te dupliceren. `package.json` heeft nieuwe scripts `typecheck` (`tsc --noEmit`) en `verify` (lint + typecheck + test + build in één commando). `README.md` volledig herschreven van de ongewijzigde `create-next-app`-boilerplate naar projectspecifieke inhoud (wat de app doet, leesvolgorde van de documentatie, lokale setup, commando-overzicht). Nieuw `.env.example` met alle bekende environment-variabelen (verplicht: `DATABASE_URL`; optioneel: `DIRECT_URL`, `WEB_PUSH_*`, `CRON_SECRET`) en toelichting. Geverifieerd: `npm run verify` zelf gedraaid en groen (lint, typecheck, 245/245 tests, migratie+build) — dogfooding van het nieuwe commando. Geen nieuwe Prisma-migratie. Bewust **niet** in dit WP: de CI-workflow (`.github/workflows/`) uit Fase 2 van het prioriteitenplan — dat is een aparte, iets risicovollere stap (introduceert een nieuwe automatische gate) en volgt als eigen WP. |
| WP80 | CI-workflow (Fase 2) | Vervolg op WP79: het belangrijkste gat uit de agent-workflow-analyse was dat er **geen enkele onafhankelijke controle** bestond — tests/lint/typecheck werden alleen gedraaid als de uitvoerende sessie zelf daaraan dacht. Nieuw `.github/workflows/ci.yml`: draait bij elke pull request naar en push op `main` (plus handmatig via `workflow_dispatch`) een Postgres 16-servicecontainer op, migreert die (`npx prisma migrate deploy`, nodig omdat de integratietests een al-gemigreerde database verwachten — zie `OPERATIONS.md`), en draait daarna `npm run verify` (het nieuwe verzamelcommando uit WP79: lint + typecheck + tests + build). Bewust **geen** `npm run test:e2e` in CI opgenomen — dat vereist een los geïnstalleerde Playwright-browser plus een langere looptijd, en is voor de huidige schaal van dit project een bewuste latere stap in plaats van meteen meegenomen (zie ook het prioriteitenplan uit de analyse). Losstaand opgeruimd: `scripts/smoke-test.ts` verwijderd — een nooit in `package.json` geregistreerd, verweesd script dat hetzelfde (household aanmaken/opruimen tegen lokale Postgres) minder grondig deed dan de bestaande integratietests. Geverifieerd: lokaal `tsc`/`eslint` groen. **De eerste proefdraai faalde daadwerkelijk** (24/245 tests rood, bijv. `prisma.ingredient.findUniqueOrThrow` op "kipfilet" in `src/domain/product-matching/repository.test.ts`) — de workflow migreerde de CI-database wel, maar seedde 'm nooit, terwijl meerdere integratietests aannemen dat de gedeelde ingrediënten-/receptcatalogus al bestaat (die komt uit `npm run db:seed`, niet uit de migraties). Toegevoegd: een `npm run db:seed`-stap tussen migreren en `npm run verify`. Geverifieerd door de exacte CI-volgorde lokaal tegen een gloednieuwe, niet eerder gebruikte database te herhalen (migreren → seeden → `npm test`): 245/245 groen. Dit is precies waarom deze workflow als eigen WP is behandeld in plaats van blind vertrouwd: de eerste proefdraai op echte CI-infrastructuur vond een reëel gat dat lokaal (met een allang gevulde ontwikkeldatabase) nooit zichtbaar was geworden. Geen nieuwe Prisma-migratie. |
| WP81 | Code-reviewer-subagent + expliciete stopvoorwaarden (Fase 3) | Laatste stap van het prioriteitenplan uit de agent-workflow-analyse — bewust het minst ingrijpende deel van Fase 3: geen losse product-/UX-/frontend-/backend-/test-engineer-agents (voor de huidige schaal van dit project — één ontwikkelaar, sequentieel werk — zou dat overengineering zijn, expliciet zo beoordeeld in het analyserapport), en git worktrees blijven bewust achterwege zolang er geen echt parallel werk is. Wel toegevoegd: nieuwe `.claude/agents/code-reviewer.md` — een alleen-lezende, puur adviserende subagent (tools: Read/Grep/Glob/Bash, geen schrijftoegang) die vóór het mergen van een gevoelige wijziging (authenticatie/sessiebeheer, household-isolatie, Picnic-integratie, databaseschema) een onafhankelijke, kritische blik geeft — expliciet gemodelleerd naar de WP77-geschiedenis, waar een eerste implementatie zelf overtuigd "opgelost" leek totdat een gericht scenario het tegendeel bewees. `WORKFLOW.md` verwijst er nu naar in de Definition-of-Done-checklist (punt 2), zodat het geen los, nooit aangeroepen bestand blijft. Ook een nieuwe sectie "Stopvoorwaarden" in `WORKFLOW.md`: stop en leg voor bij (a) `npm run verify` dat na 2-3 gerichte pogingen nog steeds faalt, (b) een wijziging die duidelijk groter wordt dan gevraagd, (c) een productiemigratie/destructieve git-actie (herhaling van een al bestaande regel, nu ook expliciet als stopvoorwaarde benoemd), (d) een conflict tussen wat gevraagd wordt en een vastgelegde architecturale aanname uit `AGENTS.md`/`PRODUCT_VISION.md`. Geverifieerd: `tsc`/`eslint` groen (puur documentatie/configuratie, geen applicatiecode geraakt). Geen nieuwe Prisma-migratie. Met dit WP is het volledige prioriteitenplan uit de agent-workflow-analyse (Fase 1 t/m 3) afgerond. |
| WP82 | Eenmalig product toevoegen ("Product toevoegen") | Gebruikersverzoek: kinderen moeten makkelijk iets aan de boodschappenlijst kunnen toevoegen, voorlopig via het gedeelde huishoud-account (geen individuele accounts — bestaande, bewuste keuze). Onderzoek wees uit dat de enige bestaande manier om een los Picnic-product toe te voegen ("vaste boodschappen") het altijd meteen als **permanente wekelijkse gewoonte** registreert — voor een eenmalig extraatje (bijv. chips voor een verjaardag) zou dat ongewenst een blijvende standaard zetten. Nieuwe, losse `MANUAL`-regelbron (naast bestaande `MEAL`/`FIXED`/`INVENTORY`) die expliciet **geen** `FixedGrocery`-rij aanmaakt. Nieuwe sectie "Product toevoegen" bovenaan `/boodschappen` (niet weggestopt achter een `<details>`, in tegenstelling tot "vaste boodschappen" dat meer als instelling aanvoelt) — zoekt bij Picnic (hergebruikt de bestaande zoekfunctie) en voegt direct een regel toe aan de lijst van déze week. Gedeelde logica tussen FIXED en MANUAL (ingrediënt/product resolven uit een Picnic-zoekresultaat) is uit `saveFixedPicnicProduct` geëxtraheerd naar een nieuwe `src/lib/picnicProductChoice.ts` (`resolvePicnicProductChoice`), zodat er geen duplicatie ontstaat tussen de twee paden — `fixedGroceriesActions.ts`'s bestaande gedrag blijft ongewijzigd, alleen intern hergebruikt. Nieuwe `src/app/boodschappen/manualProductActions.ts` (`addManualProduct`) maakt bewust altijd een **nieuwe** regel aan (geen upsert-per-ingrediënt zoals bij FIXED) — twee keer hetzelfde toevoegen geeft dus twee regels, simpeler dan hoeveelheden samenvoegen en net zo makkelijk met de bestaande verwijderknop op te lossen. Tijdens het bouwen bleek de bestaande "verwijderen"-actie (`removeBoodschappenLineThisWeek`) alleen in de dagweergave ("Bekijk per dag") gerenderd te worden, gekoppeld aan een receptregel — een MANUAL-regel heeft geen recept, en zou dus **onverwijderbaar** zijn geweest zonder aanpassing; opgelost met een nieuwe verwijderknop direct in de hoofdlijst "Jullie boodschappenlijst", alleen zichtbaar voor MANUAL-regels. Geverifieerd met een dedicated Playwright-scenario tegen een productiebuild + mock-Picnic-server: een gezocht product toevoegen maakt een MANUAL-regel aan, maakt **geen** `FixedGrocery`-rij aan (het kernverschil met vaste boodschappen), verschijnt in de hoofdlijst, en is met de nieuwe knop weer te verwijderen. `tsc`/`eslint`/`npm test` (245/245)/`npm run build` groen. Vóór het mergen is de `code-reviewer`-subagent ingeschakeld (raakt zowel het databaseschema als de Picnic-integratie, zoals `WORKFLOW.md` sinds WP81 voorschrijft) — zie de vervolgregel hieronder voor de uitkomst. Migratie: `20260730153000_manual_shopping_line_source` (additieve enum-waarde, geen databreuk). |
| WP82-vervolg | Code-review van WP82: twee P1-bevindingen gefixt | Eerste echte inzet van de in WP81 gebouwde `code-reviewer`-subagent — en die vond meteen twee reële, bevestigde P1-bugs in de WP82-implementatie, precies waarvoor die subagent bedoeld is. **(1) Household-isolatie-gat**: `addManualProduct`/`addFixedPicnicProduct`/`addBulkFixedPicnicProducts` riepen alleen `assertCurrentHousehold(householdId)` aan op een los `householdId`-formulierveld — dat bewijst dat dát veld bij de eigen sessie hoort, maar niet dat de apart meegestuurde `shoppingListId` ook echt bij hetzelfde huishouden hoort. Een sessie van huishouden A zou dus een `shoppingListId` van huishouden B kunnen meesturen en daar een regel in kunnen schrijven. Gefixt met een nieuwe gedeelde `src/lib/shoppingListAccess.ts` (`assertShoppingListAccess`) die het huishouden-ID juist *afleidt* uit de boodschappenlijst zelf (`shoppingList.mealPlan.householdId`) — hetzelfde patroon dat elders in de codebase al correct werd gebruikt. `src/app/boodschappen/actions.ts`'s eigen, inhoudelijk identieke private functie is vervangen door deze gedeelde versie. Bewust **niet** meegenomen: `addFixedGrocery` (zelfde bestand) heeft hetzelfde onbeveiligde patroon maar viel buiten wat de reviewer aanmerkte — geen ongevraagde scope-uitbreiding. Bewezen met een nieuwe e2e-test `e2e/shoppingListAccess.e2e.ts`: huishouden A probeert via een gemanipuleerd verborgen formulierveld een regel in huishouden B's lijst te schrijven; vóór de fix slaagde die aanval echt (24 regels i.p.v. 23), na de fix wordt hij geweigerd. **(2) Verkeerde verpakkingsberekening voor MANUAL+stuks**: de bestaande `line.source === "FIXED" && line.unit === "PIECE"`-kortsluiting (die een letterlijk gekozen aantal verpakkingen laat doorlopen i.p.v. door de receptgedreven verpakkingsengine) gold niet voor MANUAL-regels — een MANUAL-regel met `unit: PIECE, quantity: 3` op een ml/gram-product gaf via de engine ten onrechte maar 1 verpakking terug in plaats van 3. Gefixt met een nieuwe gedeelde `isUserChosenPackageCount()` in `src/lib/shoppingList.ts`, toegepast op alle 5 plekken die voorheen los `FIXED && PIECE` checkten (`page.tsx` ×3, `controle/page.tsx`, `cartService.ts`). Bewezen met een nieuwe integratietest in `cartService.test.ts`: vóór de fix stuurde het mandje-vullen `count: 1` naar Picnic i.p.v. het gekozen `count: 3`. Beide fixes zijn expliciet geverifieerd met revert-en-bevestig: fix tijdelijk teruggedraaid → nieuwe test faalt aantoonbaar → fix hersteld → test slaagt weer. **Bijkomende testinfrastructuur-fixes, ontdekt doordat dit de eerste keer was dat twee e2e-bestanden samen draaiden** (`test:e2e` ging van één hardgecodeerd bestand naar `tsx --test $(find e2e -name '*.e2e.ts')`): (a) `criticalFlow.e2e.ts`'s zoekknop-selector werd dubbelzinnig doordat de nieuwe "Product toevoegen"-sectie een identiek gelabelde knop kreeg — gefixt met scoping op `#add-fixed-grocery`; (b) Node's testrunner draait meerdere testbestanden **standaard gelijktijdig**, waardoor twee `next build`-aanroepen tegelijk naar dezelfde gedeelde `.next/`-map schreven — dit veroorzaakte een echte, reproduceerbare hang (een testrun bleef **36+ minuten** letterlijk niets doen, geen enkel subproces meer actief) in plaats van een nette fout; opgelost door `test:e2e` te forceren tot seriële uitvoering (`--test-concurrency=1`), simpeler en veiliger dan elk e2e-bestand een eigen build-map te geven; (c) tijdens het uitzoeken van die hang bleek `startMockPicnicServer`'s `server.listen(...)`-promise geen `'error'`-listener te hebben — bij een bezette poort (bijv. een restant van een eerder afgebroken run) vuurt `'listening'` nooit en bleef de promise voor altijd hangen zonder ooit te falen; gefixt met een expliciete `'error'`-listener die de promise laat rejecten. Volledige `npm run test:e2e` (beide bestanden, serieel) opnieuw gedraaid ná alle fixes: 13/13 groen, inclusief de nieuwe household-isolatie-test. `tsc`/`eslint`/`npm test` (250/250, +5 t.o.v. WP82) groen. Geen nieuwe migratie in dit vervolg-WP. |
| WP83 | SYSTEM_AUDIT.md-vervolg: A1–A5 (IDOR's, ingrediëntcatalogus, wachtwoordhashing, rate limiting), legacy-fallback begrensd, brede IDOR-hercontrole | Vervolgopdracht op `SYSTEM_AUDIT.md` (de volledige, read-only audit uit een eerdere sessie): elke bevestigde bevinding geverifieerd tegen de huidige code en waar nodig gefixt, telkens met een revert-en-bevestig-regressietest. **A1/A2 — IDOR in vaste boodschappen** (`src/app/boodschappen/fixedGroceriesActions.ts`): `addFixedGrocery` riep nergens `assertShoppingListAccess` aan op de meegestuurde `shoppingListId` (bleek dode code, geen enkele UI verwijst ernaar, dus zonder praktisch exploitpad — toch gefixt volgens hetzelfde bewezen patroon); `removeFixedGroceryPermanently` verifieerde de eigenaarschap van de meegestuurde `lineId` niet vóór het verwijderen — nu via `loadFixedLine(lineId)` (dezelfde helper als elders in dit bestand), de twee gekoppelde deletes (`fixedGrocery` + `shoppingListLine`) samengevoegd in één `prisma.$transaction([...])` voor atomiciteit. **A3 — ingrediëntcatalogus-autorisatie**: `updateIngredient` (`src/app/recepten/actions.ts`) liet elk huishouden `category`/`restrictionTags` van een gedeeld, globaal ingrediënt wijzigen — veiligheidskritiek, want `restrictionTags`/`category` bepalen mede of een recept een allergie/hard beperking passeert (`dietaryRestrictions.ts`). Kleinste veilige fix (geen schema-wijziging, geen aparte catalogus per huishouden): die twee velden uit `updateIngredient` gehaald, alleen `name`/`likelyInStock` blijven aanpasbaar; `/recepten` toont categorie/restricties nu read-only. `createIngredient`/`createIngredientProduct` bewust ongewijzigd gelaten (`Ingredient.name` is al `@unique`, kan een bestaand ingrediënt dus niet overschaduwen; `createIngredientProduct` raakt alleen `Product`, geen veiligheidskritieke velden). **A4 — wachtwoordhashing**: `src/domain/household/credentials.ts` volledig herbouwd van één ongesalte, snelle sha256-hash (met `householdId` als saltvervanger) naar Node's ingebouwde `scrypt` (bewust géén bcrypt/argon2 — dat zijn meestal native dependencies, een risico op Vercel serverless) met een echte, willekeurige salt per wachtwoord, zelfbeschrijvend opslagformaat (`scrypt$N$r$p$keylen$saltHex$hashHex`, zodat toekomstige parameteraanpassingen bestaande hashes niet breken) en timing-safe vergelijking. Volledig achterwaarts-compatibele migratie: een oude sha256-hash wordt nog herkend (vaste lengte/vorm) en gecontroleerd, en bij een geslaagde legacy-login meteen herhasht naar het nieuwe formaat (`src/lib/auth.ts`, `signInByCredentials`) — geen enkel huishouden wordt uitgesloten, niemand hoeft zelf iets te doen. **A5 — rate limiting op `/login`**: nieuw `LoginAttempt`-model + migratie (`20260731145811_login_attempts`) en nieuwe `src/lib/loginRateLimit.ts` — bewust database-gebaseerd (Postgres via Prisma) in plaats van een in-memory `Map`, omdat de app serverless draait (Vercel) en dit project geen Redis/KV heeft; bewust alleen op genormaliseerde gebruikersnaam (geen IP), om het dreigingsmodel (herhaald gokken tegen één bekende gebruikersnaam) af te dekken zonder extra persoonsgegevens te bewaren. Na 8 mislukte pogingen binnen 15 minuten wordt zelfs een correct wachtwoord tijdelijk geblokkeerd (geen bypass), met dezelfde generieke foutmelding als een gewoon fout wachtwoord (geen aparte "geblokkeerd"-tekst die zelf al iets zou prijsgeven); een geslaagde login wist de teller. **Deel B — legacy single-household fallback**: `getLegacySingleHousehold` (`src/lib/auth.ts`) gaf tot nu toe *voor altijd* sessieloze toegang aan het enige huishouden zonder `username`, zonder tijdsgrens — kon in deze sandbox niet worden geverifieerd of de productie-installatie van de gebruiker inmiddels een gebruikersnaam heeft (geen netwerktoegang tot productie), dus bewust niet stilzwijgend verwijderd. In plaats daarvan begrensd met een vaste datumgrens (nieuwe, pure `src/domain/household/legacyAccess.ts`, `selectLegacySingleHousehold`): alleen een huishouden dat al vóór dit werkpakket bestond komt nog in aanmerking — een nieuw aangemaakt huishouden (waarvoor onboarding altijd meteen een gebruikersnaam zet) kan hier nooit meer via toegang krijgen, ook niet in het randgeval van een halverwege gecrashte onboarding. **Deel C — brede IDOR-hercontrole**: alle ~45 exported server actions die een `shoppingListId`/`lineId`/`ingredientId`/`recipeId`/`recipeVariantId`/`personId`/`productId` uit formData ontvangen, systematisch nagelopen op exact hetzelfde patroon (een los `householdId`-veld geverifieerd, maar de eigenlijke foreign id niet) — eerst via een onderzoeksagent, daarna een aantal kritieke claims zelf handmatig geverifieerd in de broncode. Geen nieuwe bevestigde instanties gevonden: alle overige plekken scopen de foreign id al correct (via `assertShoppingListAccess`/`loadFixedLine`/`loadEditableShoppingLine`/`loadLineForCurrentHousehold`/`accessibleRecipeWhere`/`editableRecipeWhere`, of een directe `where:{id, householdId}`), of betreffen de bewust gedeelde, niet-huishoudgescoopte `Ingredient`/`Product`-catalogus. Eén laag-risico datapunt bevestigd maar bewust niet gefixt (al bekend en als zodanig gedocumenteerd in `SYSTEM_AUDIT.md` sectie 9): `setDayRoutine` slaat een `recipeVariantId` op zonder eigen verificatie, maar `ensureMealPlan` dereferentie 'm alleen via een al huishoud-gescoopte varianten-lijst — een vervalste waarde wordt dus genegeerd, geen leesbaar/schrijfbaar lek. Nieuwe/uitgebreide tests: `credentials.test.ts` herschreven (12 tests, nieuwe salt-per-aanroep-eigenschap incompatibel met het oude, deterministische testontwerp), nieuwe `legacyAccess.test.ts` (6 tests), nieuwe `loginRateLimit.test.ts` (9 tests, met een injecteerbare `now`-parameter voor deterministisch testen zonder echte wachttijden), en `e2e/shoppingListAccess.e2e.ts` uitgebreid met scenario's voor elk van A1/A2 (vervalste `lineId`), A3 (vervalste `restrictionTags`/`category`), A4 (legacy-login-en-migratie) en A5 (blokkering na te veel pogingen + herstel na wissen). Alle nieuwe/gewijzigde tests expliciet met revert-en-bevestig geverifieerd (fix tijdelijk teruggedraaid → test faalt aantoonbaar → fix hersteld → test slaagt weer). Onafhankelijke `code-reviewer`-subagent ingeschakeld op de volledige diff vóór afronding (authenticatie/household-isolatie/schema, precies wanneer `WORKFLOW.md` dat voorschrijft) — zie de eigen vervolgregel hieronder voor de uitkomst. **Nieuw `MEAL_PLANNING_GAP_PLAN.md`** (Deel F, puur analyse/planning, geen code): 13 productwensen uit een aparte opdracht geïnventariseerd op bestaande bouwstenen vs. ontbrekende functionaliteit, met een voorgestelde minimale uitbreiding per wens en een aanbevolen (niet-genummerde) uitvoeringsvolgorde — nog niets van geïmplementeerd. `tsc`/`eslint`/`npm test` (270/270)/`npm run build`/`npm run test:e2e` (21/21, over beide e2e-bestanden) groen. Migratie: `20260731145811_login_attempts` (nieuwe, op zichzelf staande tabel, geen bestaande kolommen geraakt). |
| WP83-vervolg | Code-review van WP83: één P1-bevinding gefixt (timing-lek), twee bewust openstaand | De onafhankelijke `code-reviewer`-subagent (ingeschakeld vóór het mergen van WP83, zoals `WORKFLOW.md` voorschrijft voor authenticatiewijzigingen) vond drie bevindingen. **(1) Timing-side-channel voor username-enumeratie (gefixt)**: de overstap naar scrypt (A4) introduceerde zelf een nieuw lek — een onbekende gebruikersnaam deed voorheen geen enkele hashberekening (want geen hash om tegen te controleren), terwijl een bestaande gebruikersnaam met een fout wachtwoord nu een volle scrypt-berekening kost (~45ms, tegen sha256's ~0.04ms voorheen). Dat verschil is ruim meetbaar en zou precies het username-enumeratielek blootleggen dat de generieke foutmelding (WP62) juist moet voorkomen. Gefixt met een vaste `DUMMY_PASSWORD_HASH_FOR_TIMING` (`src/domain/household/credentials.ts`) waartegen `signInByCredentials` (`src/lib/auth.ts`) bij een onbekende gebruikersnaam of ontbrekende hash alsnog een even dure, genegeerde scrypt-verificatie uitvoert. Empirisch bevestigd met een handmatige timingmeting (niet als geautomatiseerde test — timing is inherent te ruisgevoelig voor een betrouwbare CI-assert): het verschil ging van ~42ms vóór de fix naar ~0.1ms erna. Nieuwe unit test in `credentials.test.ts` dekt de functionele correctheid van de dummy-hash (geldig scrypt-formaat, accepteert nooit een echt wachtwoord). **(2) Rate limiter is check-then-act, niet atomair (bewust niet gefixt)**: bij veel gelijktijdige, parallelle aanvragen tegen dezelfde gebruikersnaam kunnen meerdere requests de teller lezen vóórdat een eerdere poging gecommit is, en zo allemaal langs de limiet komen — sequentiële aanvragen (het geteste scenario) blijven wel correct geblokkeerd. Zou een atomaire aanpak vereisen (advisory lock of een gecombineerde insert-met-voorwaarde); expliciet als bekende, geaccepteerde beperking vastgelegd in `SYSTEM_AUDIT.md` in plaats van in deze cyclus mee te nemen. **(3) `createIngredient`/`upsertParsedRecipeIngredients` staan nog vrij `category`/`restrictionTags` toe op een NIEUW ingrediënt (bewust niet gefixt)**: A3 dekte alleen het bewerkpad van een bestaand ingrediënt; het aanmaakpad (inclusief automatische recepttekst-parsing) viel buiten de scope van die bevinding. `SYSTEM_AUDIT.md` bevinding 3 is bijgewerkt om dit expliciet als openstaand deelprobleem te vermelden in plaats van als volledig opgelost te tellen. `tsc`/`eslint`/`npm test` (271/271, +1 voor de nieuwe dummy-hash-test) groen. Geen nieuwe migratie. |
| WP84 | Dag overslaan in het weekmenu ("we eten uit") | Gebruikersverzoek: als het gezin een avond uit eet, moet die dag uit de weekplanning te halen zijn zonder de rest van de logica te verstoren. Nieuw veld `MealPlanEntry.skipped` (boolean, standaard `false`, additief) — de geplande maaltijd zelf blijft bestaan (voor geschiedenis/afwisseling), maar telt niet meer mee. Twee plekken moesten daadwerkelijk rekening houden met een overgeslagen dag: (1) de boodschappenlijst-opbouw (`aggregateMealNeeds` in `src/lib/shoppingList.ts`, gebruikt door zowel `ensureShoppingList` als `findShoppingListShortfalls`, plus een los tweede aggregatiepunt in `syncShoppingListForInventoryChange`) — een overgeslagen dag levert nu geen ingrediënten meer op voor de lijst; (2) de "stille bevestiging"-logica (`entriesForSilentAcceptance`, `src/domain/meal-planning/silentAcceptance.ts`), aangeroepen door `acceptProposedMealPlanEntries` zodra de boodschappenlijst bevestigd wordt — zonder deze aanpassing zou een overgeslagen dag alsnog een positieve `CHOSEN`-feedback en een geleerd patroon opleveren voor een maaltijd die nooit gekookt is, precies het risico dat de gebruiker expliciet benoemde ("het slimme gedeelte mag er geen last van hebben"). Nieuwe server action `toggleMealPlanEntrySkipped` (`src/app/actions.ts`) — bewust geen los `entryId` van de client vertrouwd; de regel wordt afgeleid via `householdId` + `weekStart` + `dayKey` (zelfde veilige patroon als `replaceMealPlanEntry`), dus geen nieuwe IDOR-oppervlakte. UI: simpele aan/uit-knop ("Uit eten" / "Toch wel") per dag op `/` naast de bestaande "Wissel"-knop, met een amber "Uit eten"-tag en een gedimde kaart wanneer actief — bewust geen apart notitieveld (expliciete gebruikerskeuze voor eenvoud). Geverifieerd: nieuwe unit tests in `shoppingList.test.ts` en `silentAcceptance.test.ts` (pure functies), plus een nieuwe integratietest in `inventory.test.ts` die een echte, via Prisma opgebouwde boodschappenlijst controleert — alle drie met revert-en-bevestig geverifieerd (fix teruggedraaid → test faalt aantoonbaar → hersteld → test slaagt weer). `tsc`/`eslint`/`npm test` (274/274)/`npm run build`/`npm run test:e2e` (21/21) groen. Migratie: `20260731160544_meal_plan_entry_skipped` (één nieuwe kolom met `DEFAULT false`, puur additief, geen bestaande data geraakt). |
| WP85 | Huishoudbrede harde regel ("geen vis") — eerste punt uit `MEAL_PLANNING_GAP_PLAN.md` | Vervolg op de gap-analyse: wens 2 ("geen vis als harde huishoudregel") gebouwd volgens het daar al uitgewerkte, kleinste-veilige-uitbreiding-voorstel. Nieuw, additief veld `Household.hardRestrictions` (JSON, standaard `[]`, zelfde patroon als het bestaande `Person.hardRestrictions`) — een gezinsbrede regel, los van een individuele allergie: geldt voor iedereen, ongeacht wie er die dag mee-eet, in tegenstelling tot een persoonsbeperking die alleen telt als die persoon aanwezig is. `collectHardRestrictions` (`src/lib/household.ts`, gebruikt door zowel `getHouseholdHardRestrictions` als `getHouseholdHardRestrictionsAndParticipantsByDay`) neemt dit veld nu mee naast de per-persoon-lijst — geen wijziging nodig in `dietaryRestrictions.ts` of `ensureMealPlan` zelf, die zien toch alleen de al-samengevoegde tag-set. Nieuwe server action `updateHouseholdHardRestrictions` (`src/app/ons-gezin/actions.ts`, hergebruikt de bestaande `parseHardRestrictions`-parser) en een nieuwe sectie "Huishoudbrede beperkingen" op `/ons-gezin`, met dezelfde tekstinvoer (komma-gescheiden) als bij een gezinslid. Geverifieerd: nieuwe `src/lib/household.test.ts` (3 integratietests tegen een echte Postgres — huishoudregel telt ook zonder persoonsbeperking, wordt correct samengevoegd zonder duplicaten, en geldt voor elke dag ongeacht aanwezigheid), revert-en-bevestig geverifieerd (fix teruggedraaid → 2 van de 3 tests falen aantoonbaar → hersteld → alle 3 slagen weer). `tsc`/`eslint`/`npm test` (277/277)/`npm run build` groen. `npm run test:e2e`: eerste run toonde één fout in "8. Mandje vullen" (`criticalFlow.e2e.ts`) — bleek bij nader onderzoek een transiënte flake, niet gerelateerd aan deze wijziging (raakt geen enkel bestand in het Picnic-mandje-pad): een geïsoleerde herhaling van hetzelfde testbestand slaagde meteen 10/10, en de volledige suite (beide e2e-bestanden) daarna nog eens volledig 21/21. Migratie: `20260801063024_household_hard_restrictions` (één nieuwe kolom met `DEFAULT '[]'`, puur additief). |
| WP86 | Context-/tokenefficiëntie: `AGENTS.md` en `PROGRESS.md` ingekort | Op gebruikersverzoek (naar aanleiding van een vraag over efficiënter werken): het automatisch geladen `AGENTS.md` groeide uit tot een 3225 woorden blueprint dat elke beurt opnieuw werd meegestuurd. Origineel bouwplan verplaatst naar nieuw, alleen-op-aanvraag `PROJECT_BLUEPRINT.md`; `AGENTS.md` teruggebracht tot 469 woorden actieve kernregels + een verwijzing naar dat archief. Nieuwe sectie "Context- en tokenefficiëntie" in `WORKFLOW.md` legt de regel zelf vast (richtlijn: `AGENTS.md`+`WORKFLOW.md` samen onder ~2.000 tokens). `PROGRESS.md` WP1–WP75 gecomprimeerd tot titel-only (volledige tekst ongewijzigd verplaatst naar nieuw `PROGRESS_ARCHIVE.md`, zero information loss — geverifieerd met een extractiescript in plaats van handmatig overtypen). Alle losse verwijzingen naar `AGENTS.md`-Fase-inhoud in andere bestanden (`README.md`, `OPERATIONS.md`, `.claude/agents/code-reviewer.md`, `SYSTEM_AUDIT.md`, `MEAL_PLANNING_GAP_PLAN.md`, `WORKFLOW.md`, twee codecommentaren) bijgewerkt naar `PROJECT_BLUEPRINT.md`. Resultaat: automatisch geladen content (`AGENTS.md`+`WORKFLOW.md`) van ~5545 naar ~2001 tokens (~64% minder per beurt); `PROGRESS.md` van 14309 naar 5717 woorden. Puur documentatie, geen applicatiecode of schema geraakt — `tsc`/`eslint` blijven ongewijzigd groen. |
| WP87 | Recept importeren via link | Op gebruikersverzoek: een knop "Recept importeren via link" op `/recepten`, naast het bestaande "snel toevoegen". Nieuwe server action `importRecipeFromUrl` (`src/app/recepten/actions.ts`) hergebruikt het bestaande huishoud-check-patroon en dezelfde ingrediënttekstparser als "snel toevoegen" — geen schemawijziging (het bestaande vrije-tekst `source`-veld bevat nu de bron-URL). Nieuw `src/lib/recipeImport.ts`: `extractRecipeFromHtml` leest alleen de machineleesbare schema.org/Recipe-data (JSON-LD) die de meeste receptensites al standaard meeleveren voor Google — geen HTML-scraping van vrije tekst, en bij ontbrekende data een duidelijke foutmelding i.p.v. giswerk. Dit is de eerste plek waar de server zelf een door de gebruiker aangewezen externe link ophaalt (nieuw aanvalsoppervlak); daarom `assertPubliclyReachableUrl` + `fetchRecipePageHtml` met SSRF-bescherming: alleen http(s), hostname-resolutie via `dns.lookup` met blokkade van private/interne IP-reeksen (10.x/172.16-31.x/192.168.x/169.254.x inclusief het cloud-metadata-adres, loopback, IPv6-equivalenten), en de check wordt herhaald bij elke doorverwijzing (max 5 hops) in plaats van `fetch`'s ingebouwde redirect te vertrouwen — bewust geaccepteerde restbeperking: het opgeloste IP wordt niet vastgepind voor de eigenlijke fetch (geen volledige DNS-rebinding-bescherming), passend bij een kleinschalige huishoud-app. Na import wordt de gebruiker expliciet naar "controleer de ingrediënten" gestuurd (nieuwe statustekst `recipe-imported`), want machinaal geparste hoeveelheden uit vrije, vaak Engelstalige tekst zijn per definitie een twijfelgeval. Geverifieerd: unit tests (`recipeImport.test.ts`, pure functies + injecteerbare `lookup`/`fetchImpl` zodat er geen echte netwerktoegang nodig is), met revert-en-bevestig op de kritieke SSRF-doorverwijzingscheck (bescherming tijdelijk uitgeschakeld → test faalt aantoonbaar → hersteld → test slaagt weer). Handmatige Playwright-smoketest tegen een lokale build bevestigde dat het formulier verschijnt en dat een verboden link (privé-IP) serverzijdig de verwachte foutmelding logt; de daadwerkelijke happy-path tegen een live receptensite kon in deze sandbox niet getest worden (uitgaand netwerkverkeer naar willekeurige hosts is hier op organisatieniveau geblokkeerd — geen beperking van de app zelf, die krijgt op Vercel gewoon normale internettoegang). **Code-review** (`code-reviewer`-subagent, proactief ingeschakeld vanwege het nieuwe externe aanvalsoppervlak, niet letterlijk verplicht volgens `WORKFLOW.md`'s trigger-lijst): eindoordeel "veilig om te mergen", geen bevindingen op household-isolatie of Picnic-integratie; drie MEDIUM-bevindingen (geen actief exploiteerbaar gat, wel goedkope robuustheidsfixes) meteen verwerkt vóór het mergen — (1) `fetchRecipePageHtml` valideerde alleen doorverwijzingen, niet zijn eigen startpunt (leunde stilzwijgend op aanroeper-discipline); nu valideert de functie zichzelf ook, met een eigen regressietest + revert-en-bevestig; (2) IPv4-mapped IPv6-adressen werden alleen in dotted-decimalvorm herkend (`::ffff:127.0.0.1`), niet in de equivalente hex-vorm (`::ffff:7f00:1`) — `isPrivateIPv6` zet nu elk IPv6-adres eerst om naar 8 hextets vóór de vergelijking; (3) geen limiet op de responsgrootte (`response.text()` zonder cap) — nu een streaming lezer met een limiet van 5MB (een receptpagina is nooit groter). 21 unit tests in totaal (298/298 in de hele suite). `npm run verify` groen (lint, typecheck, build). Geen nieuwe migratie. |
| WP88 | Bugfix: importknop leek niets te doen + geen tijdslimiet op recept-import | Gebruikersmelding: "ik druk op Importeren, maar er lijkt niks te gebeuren". Twee dingen tegelijk gefixt. **(1) Geen visuele feedback**: `/recepten` gebruikte overal nog een kale `<button type="submit">` in plaats van de al bestaande, op `/boodschappen` en `/controle` gebruikte `PendingSubmitButton` (`useFormStatus`) — daardoor gaf geen enkele knop op deze pagina een "bezig..."-signaal tijdens een server-actie. Alle 15 submit-knoppen op `/recepten` (inclusief de nieuwe importknop) omgezet naar `PendingSubmitButton`, consistent met de rest van de app. **(2) Onbegrensde wachttijd**: `assertPubliclyReachableUrl`'s `dns.lookup` had geen timeout (kon bij een trage/niet-reagerende naamserver onbeperkt hangen), en `fetchRecipePageHtml` gaf elke doorverwijzing opnieuw een volle 10s (tot 5 hops = tot 50s) — ruim boven de tijdslimiet die een Vercel-serverless-functie zelf al heeft (10s op de standaard/Hobby-laag), die ook de rest van de actie (ingrediënten opslaan, Picnic-kandidaten zoeken) nog moet halen binnen dezelfde aanroep. Bij een trage/hangende bron werd de functie daardoor hard afgebroken door het platform, zonder dat onze eigen nette foutmelding ooit aan bod kwam — precies het "er gebeurt niks"-gevoel. Nu: een DNS-timeout op de lookup zelf, en één totaalbudget (6s) over de hele operatie heen (DNS + alle doorverwijzingen + download samen), met een duidelijke "duurde te lang"-foutmelding als het budget op is. Geverifieerd: 2 nieuwe unit tests specifiek voor deze fix (DNS-timeout, totaalbudget over meerdere doorverwijzingen heen — `totalBudgetMs` injecteerbaar gemaakt zodat de test niet zelf hoeft te wachten), met revert-en-bevestig (budget-mechanisme tijdelijk teruggedraaid → test faalt aantoonbaar → hersteld → test slaagt weer). Losstaand een Playwright-smoketest tegen een lokale build die met een niet-bestaande hostnaam bevestigt dat de knop tijdens de aanroep daadwerkelijk "Importeren..." toont en disabled is — niet alleen aangenomen dat `useFormStatus` het zou doen. 300/300 tests in de hele suite, `npm run verify` groen (lint, typecheck, build). Geen nieuwe migratie. |
| WP89 | Bugfix: import toonde de generieke Next.js-foutpagina i.p.v. de eigen foutmelding | Vervolg op WP88: ook ná die fix zag de gebruiker bij een mislukte import nog steeds "Dat lukte niet — An error occurred in the Server Components render..." i.p.v. de bedoelde Nederlandse tekst. Oorzaak: Next.js verbergt in productiebuilds bewust de exacte boodschap van een `throw` uit een server-actie (om te voorkomen dat interne details lekken) — dit raakt in principe elke `throw` in de hele app, bevestigd door ditzelfde gedrag ook bij een bestaande, allang werkende actie te reproduceren. De door Next.js zelf aanbevolen aanpak voor dit soort "verwachte" fouten is: niet gooien, maar een normale statusmelding teruggeven — precies het patroon dat dit project al gebruikt voor succesmeldingen (het groene balkje) en voor twee foutmeldingen op `/ons-gezin`. Alleen voor `importRecipeFromUrl` toegepast (niet de hele app — dat zou een veel grotere, aparte operatie zijn): nieuwe `tryImportRecipeFromUrl`-helper vangt de bekende faalredenen op en geeft een resultaat terug in plaats van te gooien; `classifyImportError` vertaalt dat naar een van acht vaste statuscodes (`import-url-missing/-invalid/-blocked/-unreachable`, `import-no-recipe-found`, `import-no-ingredients`, `import-duplicate-title`, `import-failed`); nieuwe `ERROR_STATUS_MESSAGES` in `page.tsx` toont een rood balkje (zelfde stijl als `/ons-gezin`). Bewuste architectuurkeuze, ook expliciet door Next.js's eigen documentatie voorgeschreven (`redirect should be called outside the try block`): `redirect()` wordt nu alleen op het hoogste niveau van `importRecipeFromUrl` aangeroepen, nooit vanuit een catch-blok — een eerdere tussenversie deed dat wel, en hoewel de server toen wel al correct de juiste `x-action-redirect`-header teruggaf, bleek de client de navigatie daarna niet af te maken (knop bleef vastzitten op "Importeren..."). Geverifieerd met een Playwright-smoketest tegen een lokale build (poll op de uiteindelijke URL i.p.v. `networkidle`, dat bleek geen betrouwbaar signaal voor wanneer Next's client-side redirect-afhandeling klaar is): een geblokkeerde link toont nu binnen enkele seconden het rode balkje "Deze link wijst niet naar een normale website." met de knop terug in normale staat, in plaats van de generieke foutpagina. `npm run verify` groen (lint, typecheck, 300/300 tests, build) — geen wijziging aan `recipeImport.ts` zelf, dus geen nieuwe unit tests nodig. Geen nieuwe migratie. |
| WP90 | Geïmporteerde recepten: Engelse ingrediënten + Amerikaanse maten omzetten naar Nederlands/metrisch | Gebruikersverzoek: importeer een niet-Nederlands recept en vertaal het altijd naar het Nederlands (ingrediënten inclusief), met alleen gram/kg/ml/liter, geen buitenlandse eenheden. Voorgelegd via `AskUserQuestion` of dit met de Claude API zou mogen (nieuwe kosten/afhankelijkheid/API-sleutel) — gebruiker koos expliciet **niet** voor AI: "alleen Engels ondersteunen met een woordenboek". Dit WP implementeert precies die kleinere, kosteloze scope. **(1) Woordenboek**: nieuwe `src/lib/ingredientTranslation.ts` met `translateIngredientTextToDutch()` — een vaste lijst veelvoorkomende Engelse ingrediëntwoorden/-frases (eerst meerwoordsfrases als "chicken breast" → "kipfilet", dan losse woorden als "onion" → "ui"); onherkende woorden (bijv. "diced", merknamen) blijven bewust ongewijzigd staan, geen giswerk. **(2) Eenheden**: `UNIT_ALIASES` in `src/lib/recipeIngredientText.ts` uitgebreid met Amerikaanse maten (cup=240ml, tbsp=15ml, tsp=5ml, pint=473ml, quart=946ml, gallon=3785ml, oz=28g, lb=454g) — dezelfde tabel die ook de bestaande Nederlandse "snel toevoegen"-flow gebruikt, dus risicoloos daar. **(3) Bewuste volgorde**: vertaling gebeurt vóór het parsen (`tryImportRecipeFromUrl` in `actions.ts` roept nu `translateIngredientTextToDutch` aan op elke ingrediëntregel, daarna pas `parseRecipeIngredientText`) — noodzakelijk omdat de categorie-herkenning (`inferRecipeIngredientCategory`, bijv. kip→MEAT) alleen Nederlandse trefwoorden kent en in de parser zelf zit; zonder deze volgorde zouden geïmporteerde Engelse ingrediënten altijd in categorie "OTHER" belanden. **(4) Bewuste, expliciete beperking**: titel en bereidingswijze worden niet vertaald — woord-voor-woord vertalen van lopende zinnen geeft kromme/grammaticaal onjuiste tekst, wat volgens `AGENTS.md` ("geen schijnfunctionaliteit") erger is dan gewoon Engels laten staan; dit staat nu ook expliciet in de hulptekst op `/recepten` bij het importformulier. Geverifieerd: nieuwe `src/lib/ingredientTranslation.test.ts` (frase- en woordvertaling, hoofdletterongevoeligheid, onbekende woorden blijven staan, en twee end-to-end-achtige tests die bewijzen dat de volgorde-fix werkt: "chicken breast" → "kipfilet" → categorie MEAT, "1 cup coconut milk" → "kokosmelk" + 240 ML → categorie DAIRY); nieuwe test in `recipeIngredientText.test.ts` voor de Amerikaanse eenheden. 307/307 tests, `npm run verify` groen (lint, typecheck, build). Geen nieuwe migratie, geen nieuwe environment-variable, geen actie van de gebruiker nodig — puur een woordenboek, geen externe afhankelijkheid. |
| WP91 | Snelle bestelling: alleen vaste boodschappen (+ losse toevoegingen) naar Picnic | Gebruikersverhaal: onderweg in de auto snel even standaard boodschappen bestellen, zonder de hele (vaak nog niet gecontroleerde) weekmenu-boodschappenlijst mee te sturen. Eerste van drie work packages voor deze "snelle bestelling"-flow (WP92: batch-toevoegen op naam, WP93: bezorgmoment morgen). **Kern**: `addShoppingListToPicnicCart` (`src/lib/picnic/cartService.ts`) kreeg een optioneel `options.onlySources`-filter dat de te versturen regels beperkt vóór de hoofdlus. `getPicnicConfirmationSummary` en `addToPicnicCart` (`src/app/boodschappen/actions.ts`) kregen een `scope: "all" | "fixed"`-parameter; bij `"fixed"` gaan alleen `FIXED`- en `MANUAL`-regels mee (`QUICK_ORDER_SOURCES`) — bewust ook `MANUAL` (losse, deze-week-toevoegingen), niet alleen de terugkerende vaste boodschappen, omdat die net zo min uit het weekmenu komen. Bij `scope: "fixed"` wordt `markTransferred` (dat de hele `ShoppingList.status` op `TRANSFERRED` zet) bewust **niet** aangeroepen — de lijst is dan maar gedeeltelijk overgedragen; de bestaande "rond je bestelling af"/"klaar om te versturen"-meldingen blijven correct werken omdat die al op per-regel `transferredToPicnicAt` draaien, niet op de lijststatus (expliciet gecontroleerd, geen aanname). Nieuwe knop "Vaste boodschappen + losse toevoegingen (N)" naast de bestaande "Toevoegen aan Picnic-mandje" in `AddToPicnicCart.tsx`, alleen zichtbaar als er nog niet-overgedragen regels van dat type zijn; het bevestigingsscherm zegt expliciet dat weekmenu-producten niet meegaan. Onafhankelijke `code-reviewer`-review uitgevoerd (verplicht: raakt de Picnic-integratie) — geen kritieke bevindingen; twee kleine verbeteringen direct verwerkt: (1) knop-/bevestigingstekst noemde aanvankelijk alleen "vaste boodschappen" terwijl `MANUAL`-regels ook meegaan, tekst aangepast naar "vaste boodschappen + losse toevoegingen"; (2) `scope`-parameter wordt nu server-side genormaliseerd (`normalizeScope`) zodat een vervalste Server-Action-aanroep met een onverwachte waarde nooit tussen "fixed" en "all" in kan vallen (dat zou anders alles versturen zonder de lijststatus bij te werken). Geverifieerd: nieuwe integratietest in `cartService.test.ts` die aantoont dat bij `onlySources: ["FIXED"]` de MEAL-regel in de database ongemoeid blijft (`transferredToPicnicAt` blijft `null`) terwijl de FIXED-regel wél wordt overgedragen — bewijst het scenario, niet alleen "het compileert". 308/308 tests, `npm run verify` groen (lint, typecheck, build). Geen nieuwe migratie. |
| WP92 | Snel meerdere producten toevoegen, automatisch gematcht op eerdere keuzes | Vervolg op WP91: onderweg in de auto snel een lijstje kunnen intypen (bv. "Rijst, Sperziebonen") en die producten automatisch laten matchen, zonder telkens handmatig door Picnic-zoekresultaten te bladeren. **Kern**: nieuw `src/app/boodschappen/quickOrderActions.ts` met `addQuickOrderProduct` (één regel kiezen uit zoekresultaten, draagt de rest van de batch door via hidden fields, hergebruikt `removeBulkFixedGroceryLine`) en `addQuickOrderTrustedProducts` (bulk toevoegen van regels met een al bekende, door dit huishouden zelf eerder gekozen Picnic-keuze — zonder te hoeven bladeren). Nieuwe `searchQuickOrderPreview()` in `page.tsx` hergebruikt `parseBulkFixedGroceryInput` (bestond al voor vaste boodschappen) en zoekt per regel eerst of er een eigen eerdere keuze is vóór een live Picnic-zoekopdracht; nieuwe `#quick-order`-sectie met tekstvak, een bulkformulier voor automatisch herkende regels en losse keuzeformulieren voor de rest. Onafhankelijke `code-reviewer`-review uitgevoerd (verplicht: raakt de Picnic-integratie) — één inhoudelijk belangrijke bevinding, vóór het mergen opgelost: de eerste versie gebruikte `matchProductForIngredient` (dezelfde matcher als de weekmenu-lijst) om "automatisch toevoegen zonder klikken" te bepalen, maar die geeft ook `MATCHED_TRUSTED` terug wanneer er wereldwijd precies één Product-kandidaat voor een ingrediënt bestaat — óók als dít huishouden dat product nooit zelf gekozen heeft (Product is een gedeelde catalogus, geen per-huishouden data). Voor de weekmenu-lijst is dat acceptabel (blijft een concept-regel die nog gecontroleerd wordt), maar de nieuwe knop belooft expliciet "jullie eerdere keuze" en voegt toe zónder enige preview — dat mocht niet op een toevallige wereldwijde coïncidentie berusten (AGENTS.md: "nooit stilzwijgend of ongecontroleerd"). Gefixt door rechtstreeks `getTrustedPreferences` (huishouden-gescoped) te gebruiken in plaats van de volledige matcher-cascade. Drie kleinere bevindingen ook verwerkt: ontbrekende `Number.isFinite`-validatie op `quantity`/`price` in de bulk-JSON-payload (nu gevalideerd vóór opslaan, zelfde patroon als `saveFixedPicnicProduct`); een ontbrekende `try/finally` rond de Picnic-tokenverversing in de zoekfunctie (nu net als de bestaande `searchBulkFixedProductResults` beveiligd tegen een verloren ververst token bij een mislukte zoekopdracht halverwege de batch); dubbele React-keys bij twee identieke regels in één batch (nu `key={raw}-{index}`). Geverifieerd: nieuwe e2e-stap in `criticalFlow.e2e.ts` die beide paden dekt (een al vertrouwd ingrediënt automatisch, een nieuw ingrediënt via handmatige keuze, en dat de nog-openstaande regel na de bulk-actie in beeld blijft); nieuwe household-isolatietest in `shoppingListAccess.e2e.ts` (spiegelt de bestaande `addManualProduct`-test) die bevestigt dat `addQuickOrderProduct` een vervalste `shoppingListId` van een ander huishouden weigert. 308/308 unit tests, volledige e2e-suite (23 stappen) groen, `npm run verify` groen. Geen nieuwe migratie. |
| Bugfix | "Mandje legen" toonde de generieke Next.js-foutpagina i.p.v. de eigen melding | Gebruikersmelding op productie: "ik kan mn boodschappenlijst niet legen", met alleen de generieke tekst "An error occurred in the Server Components render...". Oorzaak, hetzelfde patroon als WP89 (recept-import) maar op een ander soort aanroep: `clearPicnicCartForShoppingList`/`addShoppingListToPicnicCart` (`src/lib/picnic/cartService.ts`) gooiden een kale `Error` zodra Picnic geen `picnicAuthToken` had of een auth-/netwerkfout teruggaf. Bij WP89 ging het om een `<form>`-actie met `redirect()`; hier wordt `clearPicnicCart`/`addToPicnicCart` rechtstreeks als async functie vanuit `AddToPicnicCart.tsx` (client component) aangeroepen — een `throw` gaat sowieso over de Server-Actions-grens, en Next.js redact in productiebuilds altijd de exacte boodschap daarvan, ongeacht of de aanroeper een `try/catch` heeft (dat catch-blok ontvangt zelf ook alleen de geredigeerde melding). **Fix**: `clearPicnicCartForShoppingList` geeft nu een `{ok:true} | {ok:false, message}`-resultaat terug in plaats van te gooien (nieuwe `describePicnicError()`-helper vertaalt `PicnicAuthError`/`PicnicNetworkError`/overige fouten naar een uitlegbare Nederlandse melding); `addShoppingListToPicnicCart`'s eerdere kale throw bij een ontbrekend Picnic-token is nu ook een gewone `PicnicCartResult`-regel (`errors: [...]`) i.p.v. een throw. `clearPicnicCart` (`actions.ts`) geeft dat resultaat door; `AddToPicnicCart.tsx` toont nu `result.message`/`result.errors[].message` in plaats van (voorheen) alleen de geredigeerde `e.message` uit een catch-blok. Bijvangst: de "Fout bij: …"-tekst bij mandje vullen toonde al langer alleen de ingrediëntnaam, nooit de eigenlijke reden — nu wordt de reden ook getoond; en de groene "N verpakking(en) toegevoegd"-tekst verscheen voorheen ook bij 0 toevoegingen (bv. bij deze fout) — nu alleen als er ook echt iets is toegevoegd. Onafhankelijke `code-reviewer`-review uitgevoerd (verplicht: raakt de Picnic-integratie) — geen kritieke bevindingen; wel de suggestie dat de kernclaim (gebruiker ziet in de browser de échte melding, niet de generieke Next.js-pagina) alleen op TypeScript's typedwang leunde en niet op een uitgevoerde test. Daarom `e2e/fixtures/mockPicnicServer.ts` uitgebreid met een instelbare `failClear`-vlag (laat `/cart/clear` een Picnic `AUTH_ERROR` teruggeven) en een nieuwe e2e-stap "8b" in `criticalFlow.e2e.ts` die dit reproduceert: mandje legen met een gesimuleerde Picnic-fout, en bevestigt zowel dat de eigen Nederlandse melding verschijnt als dat de generieke "An error occurred in the Server Components render"-tekst nergens op de pagina staat — de exacte situatie uit de gebruikersmelding, nu ook echt in de browser getest. Geverifieerd: twee nieuwe integratietests in `cartService.test.ts` (ontbrekend Picnic-account en een gesimuleerde Picnic-AUTH_ERROR-respons geven allebei een `{ok:false, message}`-resultaat terug i.p.v. te gooien, met de verwachte Nederlandse tekst), plus de bestaande e2e-stap "Fout herstellen: mandje legen" bevestigt dat het happy path (`{ok:true}`) nog steeds werkt ná de gesimuleerde fout. 311/311 unit tests, volledige e2e-suite (24 stappen) groen, `npm run verify` groen. Geen nieuwe migratie. |
| Bugfix | Picnic-mandje-aanroepen faalden met "Client version is required..." — nu op elk verzoek de juiste headers | Vervolg op de vorige bugfix: nu de echte foutmelding zichtbaar was (i.p.v. de generieke Next.js-pagina), meldde de gebruiker letterlijk "Client version is required to preview the cart page" bij het legen van het mandje. Oorzaak gevonden in `src/lib/picnic/client.ts`: de `x-picnic-agent`/`x-picnic-did`-headers (identificeren de "app-versie" richting Picnic, onderdeel van deze niet-officiële, reverse-engineered integratie) werden alleen meegestuurd bij inloggen, 2FA en zoeken — niet bij `addProduct`/`removeProduct`/`getCart`/`clearCart`/`getDeliverySlots`. Picnic accepteert die mandje-aanroepen kennelijk niet meer zonder die headers. **Fix**: de headers zijn nu standaard onderdeel van élk verzoek (verplaatst naar de gedeelde `request()`-methode), in plaats van per aanroep te moeten worden meegegeven — voorkomt dat dit opnieuw per ongeluk op een nieuw endpoint vergeten wordt. De losse `PICNIC_HEADERS`-argumenten bij login/2FA/zoeken zijn als overbodige duplicatie opgeruimd. Geverifieerd: nieuwe test in `client.test.ts` die de daadwerkelijk verstuurde headers opvangt bij `addProduct`/`clearCart`/`search` en bevestigt dat beide headers er altijd bij zitten; volledige e2e-suite (24 stappen, incl. de mock-Picnic-server) blijft groen — de mock controleert headers niet, dus dit bewijst vooral dat de bestaande flows niet stuklopen, niet de fix zelf (die zit in de nieuwe unit test). 312/312 unit tests, `npm run verify` groen. Geen nieuwe migratie. Dit is en blijft een niet-officiële koppeling (`AGENTS.md`/technisch ontwerp, risico R1): Picnic kan zonder aankondiging opnieuw iets aan het protocol wijzigen. |
| WP93 | Losse boodschappenlijst starten — los van het weekmenu van vanavond | Vervolg op het autoscenario uit WP91/92: soms wil je gewoon standaard boodschappen bestellen zonder het hele weekmenu erbij, en zelf een nieuwe lijst samenstellen. Via een mockup-artifact (met de exacte kleuren/typografie uit `globals.css`) heen-en-weer ontworpen met de gebruiker — eerste versie heette "Boodschappenlijst opnieuw beginnen" met een losse checkbox "ook geen weekmenu"; op feedback herschreven naar een positievere framing: **"Losse boodschappenlijst starten"**, waarbij loskoppelen van het weekmenu geen aparte keuze meer is maar gewoon wat de knop doet. **Kern**: nieuwe server action `startLooseShoppingList` (`src/app/boodschappen/looseListActions.ts`) zet alle `MealPlanEntry`-rijen van de huidige week op `skipped: true` (dezelfde vlag als de bestaande, per-dag "Uit eten"-knop op de startpagina — hier alleen in bulk voor de hele week) en roept de al bestaande `invalidateShoppingList()` aan (verwijdert de hele `ShoppingList`-rij; bestond al, werd tot nu toe alleen intern gebruikt door de per-dag skip-toggle). De eerstvolgende `ensureShoppingList`-aanroep bouwt daardoor een verse lijst zonder weekmenu-regels, met vaste boodschappen automatisch terug (`FixedGrocery` is een los, huishouden-breed model, wordt niet aangeraakt) — de rest voegt de gebruiker zelf toe (bv. via de bestaande "snel meerdere producten toevoegen"-sectie uit WP92). Nieuwe client component `LooseListCard.tsx` (idle/bevestig-patroon, zelfde stijl als "Picnic-mandje legen") bovenaan `/boodschappen`. **Ingangspunt op de startpagina**: bewust géén verplichte vraag bij het openen van de app (zou tegen de kernvisie ingaan — "de app stelt een passende week voor", niet "vraag de gebruiker eerst wat die wil") — in plaats daarvan een duidelijke, optionele link "Alleen boodschappen nodig? Start een losse lijst" naast de bestaande weekmenu-samenvatting, die rechtstreeks naar de nieuwe kaart springt. Rondom de gebruikersvraag ("leegt dit ook echt mijn Picnic-mandje?") is expliciet uitgelegd en in de UI-tekst herhaald: dit raakt nooit het echte Picnic-mandje, alleen de lijst in Family Assistant. Onafhankelijke `code-reviewer`-review uitgevoerd (verplicht: raakt household-scoped mutaties) — household-isolatie zelf was in orde (geen los meegestuurd resource-ID om te vervalsen, `assertCurrentHousehold` volstaat), maar twee bevindingen vóór het mergen opgelost: (1) de action berekende `weekStart` zelf opnieuw via `getCurrentWeekStart()` in plaats van de week te gebruiken die de pagina daadwerkelijk toonde — bij een aanvraag die net na middernacht zondag/maandag binnenkomt zou dat een `MealPlan` van de nieuwe (nog niet bestaande) week opzoeken en met `findUniqueOrThrow` een kale, door Next.js geredigeerde foutpagina tonen (hetzelfde patroon als de eerder gefixte Picnic-bugs); nu krijgt de actie `weekStart` mee via een hidden formuliervveld (zelfde patroon als de bestaande `toggleMealPlanEntrySkipped`) en valt bij een ontbrekende meal plan netjes terug op een amber statusmelding i.p.v. een throw. (2) De bulk-reset verwijdert ook `transferredToPicnicAt`/`orderConfirmedAt` van de bestaande lijst — als er al producten van deze week naar het echte Picnic-mandje waren overgedragen, "vergeet" de app dat, met een reëel risico op dubbel bestellen bij een latere "Toevoegen aan Picnic-mandje"; de bevestigingstekst waarschuwt hier nu expliciet voor wanneer dat van toepassing is (`hasTransferredLines`-prop, hergebruik van de al bestaande berekening uit WP91). Geverifieerd: nieuwe e2e-stap 10 in `criticalFlow.e2e.ts` (klik vanaf de startpagina → bevestigen → alle dagen van de week staan op `skipped` → de opnieuw opgebouwde lijst bevat geen `MEAL`-regels meer maar wél de eerder toegevoegde vaste boodschap) plus nieuwe stap 10b die het vervalste-`weekStart`-scenario uit de codereview reproduceert en bevestigt dat de nette amber melding verschijnt, nooit de generieke Next.js-foutpagina. 312/312 unit tests, volledige e2e-suite (26 stappen) groen, `npm run verify` groen. Geen nieuwe migratie (bestaande `skipped`-kolom en `invalidateShoppingList` hergebruikt). |
| Bugfix | "Snel meerdere producten toevoegen" (WP92): één product per klik was te traag — nu selecteren en in één keer toevoegen | Gebruikersmelding: bij meerdere gezochte producten (bv. "cola, appelsap") moest je per zoekresultaat apart op "Toevoegen" klikken, en elke klik deed een volle server-actie-aanroep + paginaherlaad ("dat duurt steeds 5 seconden, dat gaat tegen het idee van de app in"). **Fix**: de "nog te kiezen"-sectie in `src/app/boodschappen/page.tsx` toont nu per gezochte term een radiogroep (`choice-<regelindex>`, het beste/eerste resultaat al aangevinkt) in plaats van een los formulier-met-knop per zoekresultaat, met daaronder één gedeelde "Voeg toe"-knop. Nieuwe server action `addQuickOrderPickedProducts` (`src/app/boodschappen/quickOrderActions.ts`) verwerkt alle geselecteerde keuzes in één aanroep — dezelfde beveiligde structuur als de al bestaande `addQuickOrderTrustedProducts` (elke unieke `shoppingListId` apart geverifieerd via `assertShoppingListAccess` vóórdat er iets geschreven wordt, per-regel `raw`-tekst gebruikt om alleen de zojuist toegevoegde regels uit de resterende batchtekst te strepen zodat nog-niet-gevonden regels in beeld blijven). De oude, nu overbodige `addQuickOrderProduct` (één product per server-actie-aanroep) is verwijderd, inclusief de toen-ongebruikt geworden `parseQuantity`/`parseOptionalPrice`-helpers — quantity/eenheid per product zijn niet langer los instelbaar vóór het toevoegen (was toch al niet mogelijk bij de al bestaande "Automatisch toevoegen"-knop voor herkende producten; een verkeerde hoeveelheid is achteraf op de hoofdlijst aan te passen). Geverifieerd: bestaande e2e-stap "5b" in `criticalFlow.e2e.ts` aangepast naar de nieuwe radioknop-plus-knop-flow (i.p.v. losse "Toevoegen"-klik); de household-isolatietest in `shoppingListAccess.e2e.ts` (WP92) aangepast om het `shoppingListId`-veld nu in het JSON-value van de radioknop te vervalsen in plaats van een los formulierveld — bevestigt dat de beveiliging identiek blijft. 312/312 unit tests, volledige e2e-suite (26 stappen) groen, `npm run verify` groen. Geen nieuwe migratie. |
| Bugfix | "Snel meerdere producten toevoegen": geen van de 3 voorgestelde Picnic-producten goed? Nu zelf zoeken zonder je lijstje kwijt te raken | Vervolg op de vorige bugfix: "Het systeem geeft nu 3 opties, maar kan je ook nog een 4e erbij zetten die ik zelf kan zoeken?". In plaats van een 4e radio-optie te bouwen (heeft geen `externalRef` totdat er echt gezocht is, past niet in dezelfde "Voeg toe"-indiening) is de al bestaande, losstaande "Product toevoegen"-zoekbox (`#quick-add-product`, `addManualProduct` in `manualProductActions.ts`) hergebruikt. **Kern**: elke regel in de `#quick-order`-picklijst (`src/app/boodschappen/page.tsx`) heeft nu een link "Niet het goede product? Zelf zoeken" die naar `#quick-add-product` springt met de zoekterm alvast ingevuld (`?manualQ=...`), plus de resterende batchtekst en de ruwe regeltekst van precies díe regel (`quickOrder`/`quickOrderRaw`) als extra queryparameters. Die twee extra waarden gaan als hidden inputs mee in zowel de zoek-form als elke per-resultaat "Toevoegen"-form binnen `#quick-add-product`. `addManualProduct` streept, wanneer een `raw`-veld aanwezig is, die regel via de al bestaande `removeBulkFixedGroceryLine`-helper uit de meegegeven batchtekst en redirect terug naar `#quick-order` in plaats van het gebruikelijke `#quick-add-product` — zonder dat veld (het normale, niet-vanuit-`#quick-order`-gebruik van de zoekbox) blijft het gedrag exact zoals voorheen. Geen nieuwe server action, geen schemawijziging — puur hergebruik van bestaande, al beveiligde infrastructuur (`assertShoppingListAccess` in `addManualProduct` is ongewijzigd; de nieuwe velden bepalen alleen tekst/redirect-doel, nooit welk huishouden/lijst geschreven wordt). Onafhankelijke `code-reviewer`-review uitgevoerd (verplicht: raakt de Picnic-integratie-aangrenzende flow) — geen kritieke bevindingen (household-isolatie intact, geen open-redirect/injectierisico, `removeBulkFixedGroceryLine`-logica handmatig met een multi-regel-batch geverifieerd, bestaande pad zonder `raw`-veld ongewijzigd); wel de terechte observatie dat de eerste e2e-test alleen het triviale één-regel-geval dekte (waarbij de "rest van je lijstje blijft in beeld"-belofte na afhandeling toevallig leeg was) en niet bewees dat een échte resterende regel zichtbaar blijft. Vóór het mergen verwerkt: e2e-stap "5c" in `criticalFlow.e2e.ts` uitgebreid naar een batch van twee regels — "Zelf zoeken" voor de eerste regel, waarna de tweede regel ("spruitjes") aantoonbaar nog in `#quick-order` staat (`quickOrder`-parameter na de redirect bevat exact de resterende regel, de opgeloste regel is uit de DOM verdwenen, en de nog-niet-gekozen regel staat nog niet als `ShoppingListLine` in de database). 312/312 unit tests, volledige e2e-suite (27 stappen) groen, `npm run verify` groen. Geen nieuwe migratie. |

## Nog te doen (roadmap, nog niet gestart)

- **Nieuwe hoofdflow is leidend:** `Week kiezen → Aanvullen → Per dag controleren → Totaalbestelling controleren → Naar Picnic`.
- De expliciete prioriteitenlijst uit `DATAMODEL_AUDIT.md` (punten 1 t/m 6: receptscope, typed tags, waarom-signalen, afgeleide patronen, onboardingprofiel, entry-context) is nu volledig afgerond (WP20–WP23, WP52).
- De recente "bevestiging bij elke opslagactie"-sweep is afgerond voor de bekende losse acties op `/`, `/gerechten`, `/recepten`, `/ons-gezin` en `/boodschappen`. Bij nieuwe server actions dezelfde regel blijven volgen: na succesvolle mutatie redirecten naar een verse render met `?status=...` en, op lange pagina's, terug naar dezelfde sectie/regel.
- WP56 maakt geleerde weekpatronen zichtbaar en beheerbaar op `/ons-gezin`; WP57 voegt daar daggerichte gerechtvoorkeuren aan toe (los van de automatisch geleerde patronen — dit zijn expliciete, door de gebruiker gezette voorkeuren).
- WP58 dekt Fase 16 (monitoring/logging) voor de belangrijkste gebieden; WP73 sluit de resterende twee gaten (client-side renderfouten, generieke serverfouten via de error-boundaries).
- Fase 15 (tests): afgerond met WP59 — `npm run test:e2e` dekt nu de kritieke flow end-to-end tegen een mock-Picnic-server.
- `DATAMODEL_AUDIT.md` punt 7 (provider-neutraliteit) en punt 8 (voorraad als wekelijkse check i.p.v. administratie) zijn afgerond met WP60/WP61.
- WP72 rondt de WP69–WP72-reeks (attention-laag → pushmeldingen → bezorgmoment-voorkeur → bezorgmoment-pushmelding) af. Verder geen vastgelegde "volgende WP" — kies in overleg met de gebruiker.
- **Let op voor de volgende sessie:** werk vanaf hier verder op `main`/een nieuwe branch vanaf `main` — zowel Claude- als Codex-sessies hebben recent rechtstreeks tegen dit project gewerkt (zie de WP53–WP57-commits), dus neem niet aan dat de laatst bekende branch-staat uit een eerdere sessie nog actueel is. Controleer altijd eerst `git log origin/main` en dit bestand voordat je verder bouwt.

| UX-verbetering | Kritische UX-review van de hele app (op verzoek) — top 8 verbeterpunten, eerste twee gebouwd | Gebruikersverzoek: "kijk kritisch naar het gebruikersgemak... minimaal 5 goede verbeteringen." Drie parallelle onderzoeksagents doorgenomen (startpagina/onboarding/inloggen, `/controle`, `/gerechten`+`/recepten`+`/ons-gezin`) plus zelf `/boodschappen` en de navigatie geanalyseerd. Acht concrete frictiepunten teruggekoppeld, gerangschikt op impact: (1) de belangrijkste knop ("Toevoegen aan Picnic-mandje") stond onderaan `/boodschappen`, ná acht andere secties; (2) `/controle` kost meer klikken dan nodig per twijfelgeval (geen "volgende", geen bulkactie, tegenstrijdige knoppen op een al-bevestigde kaart); (3) een typefout in het wachtwoord bij onboarding kon het hele gezin buitensluiten (geen bevestigingsveld, geen hersteloptie); (4) "Goedemorgen" stond hardcoded ongeacht tijdstip; (5) de app vroeg feedback over maaltijden die nog moesten komen; (6) kleine acties herladen de hele pagina en scrollen terug naar boven; (7) "Ons gezin" is een onoverzichtelijke stapel van 13+ losse secties; (8) elke gerechtkeuze op `/gerechten` vereist een overbodige "Waarom wil je wisselen?"-stap. Gebruiker koos #1 en #3 om nu te bouwen; #2/#4–8 staan genoteerd voor een volgende sessie. |
| UX-verbetering | #1: belangrijkste knop op `/boodschappen` naar boven — direct na je lijst i.p.v. onderaan de pagina | Fix voor bovenstaand punt 1. Pure JSX-herordening in `src/app/boodschappen/page.tsx` (uitgevoerd met een klein Python-script dat exacte regelblokken verplaatste, dus geen handmatige overtypfouten; nadien met `git diff` geverifieerd dat het écht alleen een verplaatsing was — geen inhoud/props/ids gewijzigd). De twee blokken "Jullie boodschappenlijst" en de Picnic-actiecluster (`PicnicDeliveryStatusCard`/`PicnicTransfer`/`AddToPicnicCart`) staan nu direct na de header/twijfelgevallen-banner; alle overige, minder frequent gebruikte secties (losse lijst starten, product zoeken, snel meerdere producten, per-dag-overzicht, vaste boodschappen, voorraadcheck, "zelf boodschappen doen"-checklist) staan nu daaronder, in dezelfde relatieve volgorde als voorheen. Geen nieuwe server actions, geen schemawijziging, alle scroll-anchors (`#quick-order`, `#fixed-groceries`, `#inventory-check`, …) ongewijzigd. Onafhankelijke `code-reviewer`-review uitgevoerd (samen met wijziging hieronder, omdat die de accountaanmaak-flow raakt) — bevestigt dat het een zuivere verplaatsing is zonder verwijzingen elders in de app die op de oude volgorde leunden. 312/312 unit tests, volledige e2e-suite groen, `npm run verify` groen. Geen nieuwe migratie. |
| UX-verbetering | #3: wachtwoordbevestiging bij onboarding — voorkomt dat een typefout je gezin buitensluit | Fix voor bovenstaand punt 3. Bewust beperkt in scope: geen "wachtwoord vergeten"-herstelfunctie gebouwd (aparte, grotere feature — vereist een manier om identiteit te verifiëren zonder het bestaande wachtwoord, bv. e-mail of herstelcode; expliciet als vervolgstap genoteerd, niet stilzwijgend meegenomen). In plaats daarvan: `OnboardingWizard.tsx` kreeg een tweede wachtwoordveld ("Bevestig wachtwoord"); `handleSubmit()` controleert vóór de server-aanroep of beide velden overeenkomen en toont anders een duidelijke foutmelding — zonder dat `completeOnboarding()` ooit wordt aangeroepen bij een mismatch. Geen wijziging aan `src/lib/auth.ts`, het wachtwoord-hashing-pad of het databaseschema. Ook een korte, eerlijke waarschuwing toegevoegd ("bewaar dit wachtwoord goed — er is nog geen manier om het te herstellen") zodat de app niet doet alsof er al een hersteloptie bestaat (AGENTS.md: "geen schijnfunctionaliteit"). Onafhankelijke `code-reviewer`-review uitgevoerd (verplicht: raakt de accountaanmaak-flow, samen met de `/boodschappen`-herordening hierboven) — bevestigt dat de check zuiver client-side is (het `confirmPassword`-veld gaat nooit naar `completeOnboarding()`, er is dus niets te omzeilen) en niets aan de server-side validatie/beveiliging verandert; concludeert "veilig om te mergen". Twee niet-blokkerende suggesties uit die review vóór het mergen verwerkt: (1) de submitknop ("Maak mijn eerste week") staat nu uit zolang de twee velden niet overeenkomen, i.p.v. de fout pas ná een klik te tonen; (2) deze regel expliciet vermeldt de tijdens de review waargenomen `test:e2e`-uitkomst hieronder, in plaats van kortweg "groen" te beweren. Geverifieerd: gedeelde e2e-onboardinghelper (`e2e/fixtures/testHousehold.ts`) bijgewerkt om het nieuwe veld in te vullen; e2e-stap "1b" in `criticalFlow.e2e.ts` bewijst zowel de disabled-knop als het opnieuw inschakelen bij een correctie, en dat er geen huishouden wordt aangemaakt vóór een echte verzending. 312/312 unit tests, `npm run verify` groen. `npm run test:e2e`: bij herhaald draaien tijdens de review faalde stap "8. Mandje vullen" 2 van de 4 keer (`mockPicnic.addedProducts.length > 0`) — dit raakt geen enkel bestand uit deze wijziging (alleen DOM-positie/onboarding-UI aangepast, niets in het Picnic-mandje-pad) en is dezelfde reeds bekende, transiënte flake als bij WP85; een losse herhaling na deze laatste aanpassing gaf 28/28 groen. Geen nieuwe migratie. |
| UX-verbetering | #2: `/controle` kost minder klikken per twijfelgeval — doorstroom, gesplitste secties, twee bugfixes | Fix voor punt 2 uit de kritische UX-review. Vier gerichte wijzigingen, geen bulkactie/matchReasons-herschrijving (bewust uit scope gehouden — grotere, risicovollere ingrepen op de matching-engine, apart te overwegen). (1) **Doorstroom**: nieuwe helper `redirectToNextReviewLine` (`src/app/controle/actions.ts`) — na een oplossende actie (kiezen, alleen deze week, zonder product doorgaan, verwijderen, of een afwijzing die toevallig meteen een vertrouwde match oplevert) redirect je nu naar de eerstvolgende regel die nog aandacht vraagt (alfabetisch, zelfde sortering als de pagina) i.p.v. terug naar dezelfde (nu opgeloste) regel. Blijft de regel zelf nog een twijfelgeval, dan blijf je er gewoon op staan. `adjustLineQuantity`/`searchPicnicProductsForLine` gebruiken bewust de oude `refreshControle` (blijf op dezelfde regel — daar loopt nog een bewerking). (2) **Gesplitste secties**: de twijfelgevallen-lijst toont nu apart "Vragen aandacht" en "Niet gevonden" (voorheen door elkaar, ondanks dat de tellertjes bovenaan al gescheiden waren). (3) **Bugfix**: zoeken vanuit "vertrouwde keuzes bekijken" zette een vertrouwde regel voorheen altijd terug op `needsReview: true`, ook als je alleen even rondkeek zonder iets te kiezen — nu alleen bij een regel die al een twijfelgeval was. (4) **Tegenstrijdige knoppen**: op een al-bevestigde productkaart stonden "Alleen deze week"/"Nooit" nog steeds naast de "Opgeslagen"-badge; "Alleen deze week" is nu weg (slaat nergens meer op voor het actieve product), "Nooit" is behouden als kleinere knop naast de badge (code-review-bevinding: je moet het huidige product ook kunnen afwijzen zonder eerst een alternatief te zoeken). Nieuwe algemene statusmelding bovenaan (`generalStatusMessage`, naar het patroon van `/boodschappen`) voor als je doorspringt naar een andere regel dan de per-regel-melding zou raken. Onafhankelijke `code-reviewer`-review uitgevoerd (raakt Picnic-zoekintegratie en een nieuwe cross-regel query) — bevestigt dat `redirectToNextReviewLine` alleen queryt op een `shoppingListId` die al via `assertCurrentHousehold` bewezen bij het eigen huishouden hoort (geen nieuw IDOR-pad), en dat de zoek-bugfix niets anders in de codebase stilzwijgend breekt; concludeert "veilig om te mergen". Geverifieerd: twee nieuwe e2e-stappen in `criticalFlow.e2e.ts` — "7b" forceert via directe Prisma-writes een deterministisch scenario met 2 twijfelgevallen en bewijst dat bevestigen op de eerste regel doorspringt naar de tweede (exacte `#line-<id>`-hash-check, niet alleen "geen crash"), "7c" bewijst dat zoeken vanuit vertrouwde keuzes de `needsReview`-vlag niet meer omzet. 312/312 unit tests, volledige e2e-suite (30 stappen) groen, `npm run verify` groen. `npm run test:e2e` flakete tijdens dit werk twee keer op de reeds bekende "8. Mandje vullen"-flake (ongerelateerd aan deze wijziging, raakt geen van de aangepaste bestanden) — beide keren groen bij één gerichte herhaling. Geen nieuwe migratie. |
| UX-verbetering | #4 + #5: tijdstip-bewuste groet + geen feedbackvraag over nog te eten maaltijden | Twee kleine, losstaande fixes uit de UX-review, samengevoegd omdat ze allebei puur om "hoe laat/welke dag is het écht" gaan. (4) De startpagina zei altijd "Goedemorgen", ook 's avonds — nieuwe pure functie `timeOfDayGreeting()` (`src/lib/week.ts`, Europe/Amsterdam-tijdzone, zelfde patroon als het bestaande pushbeleid in `notificationPolicy.ts`) geeft nu "Goedemorgen"/"Goedemiddag"/"Goedenavond" op basis van het actuele uur. (5) De "Nieuw gerecht — hoe was dit?"-feedbackvraag verscheen bij een verse week voor alle 7 dagen tegelijk, ook dagen die nog moesten komen — nieuwe pure functie `isDayStartedOrPast()` (zelfde bestand/tijdzone) is toegevoegd aan de bestaande `isNew`-voorwaarde in `src/app/page.tsx`, zodat de vraag pas verschijnt zodra de dag zelf ook echt begonnen is. Beide functies zijn puur en context-vrij (nemen `now`/`date` als parameter, geen impliciete `new Date()` diep in de functie) — makkelijk te testen zonder tijd te hoeven mocken. Geen wijziging aan server actions, geen auth/household-isolatie/Picnic/schema geraakt, dus geen code-reviewer-stap nodig (WORKFLOW.md-trigger niet van toepassing). Geverifieerd: nieuw `src/lib/week.test.ts` (5 tests — ochtend/middag/avond, de exacte 12:00/18:00-grenzen, en het WP-scenario zelf: een dag verderop deze week telt nog niet als begonnen). 317/317 unit tests, volledige e2e-suite (30 stappen) groen (de reeds bekende "8. Mandje vullen"-flake kwam tijdens dit werk een paar keer voorbij, telkens groen bij herhaling — ongerelateerd, raakt geen van de aangepaste bestanden), `npm run verify` groen. Geen nieuwe migratie. |
| UX-verbetering | #8: `/gerechten` — juiste standaarddag, geen overbodige stap bij een lege dag, blijf in context na kiezen | Drie gerichte fixes. (1) **Verkeerde standaarddag**: zonder `?day=` viel `/gerechten` altijd terug op "monday" — nieuwe, gedeelde pure functie `currentDayKey()` (`src/lib/week.ts`, Europe/Amsterdam-tijdzone) vervangt dat én de losse, bijna-identieke berekening die al op de startpagina stond (dat was voorheen serverlokale/UTC-tijd — code-review-bevinding, gefixed vóór het mergen: gebruikt nu `Intl.DateTimeFormat` net als de andere tijdzone-bewuste functies in hetzelfde bestand, anders gaf dit rond middernacht lokale tijd nog de vorige dag terug). (2) **Overbodige "waarom wil je wisselen?"-stap** bij een dag die nog leeg is (niets om een reden voor te geven) — `RecipeSection` (`src/app/gerechten/page.tsx`) toont de reden-dropdown nu alleen als er al een gerecht gepland stond. (3) **Blijf in context na kiezen**: koos je een gerecht, dan sprong je altijd terug naar de startpagina — vervelend bij meerdere dagen achter elkaar aanpassen (filters kwijt, telkens opnieuw "Ander gerecht" nodig). Gebruiker expliciet gevraagd via `AskUserQuestion` welke van drie opties (terug naar startpagina/terug naar `/gerechten` zelfde context/automatisch doorspringen naar volgende dag) het beste voelde — koos voor "terug naar `/gerechten`, zelfde dag/filter" op eigen advies. Nieuwe helper `redirectToGerechten()` (`src/app/gerechten/actions.ts`) vervangt `redirectToHome()` in `replaceMealPlanEntry`; `direction` wordt server-side gevalideerd tegen een vaste set vóór het in de redirect-URL komt. Het "maak precies deze maaltijd"-pad (`chooseLiteralMealPlanEntry`) bewust ongewijzigd gelaten (blijft naar de startpagina gaan — na een letterlijke wens is er niets meer om doorheen te bladeren). Onafhankelijke `code-reviewer`-review uitgevoerd (raakt server actions in de gerechten-flow) — concludeert "veilig om te mergen", geen household-isolatie- of injectieprobleem; twee niet-blokkerende bevindingen vóór het mergen verwerkt: (a) de tijdzone-bug in `currentDayKey()` hierboven al genoemd, (b) een ontbrekende directe unit test voor die functie toegevoegd (incl. een gerichte regressietest voor precies het middernacht-scenario dat de review aankaartte). Geverifieerd: e2e-stap "3" uitgebreid (bevestigt dat je op `/gerechten` blijft, met behouden dag-context, en meteen een tweede keer kunt kiezen zonder opnieuw te navigeren), nieuwe stappen "3b" (juiste standaarddag zonder `?day=`) en "3c" (geen reden-stap bij een lege dag, geforceerd via een directe DB-manipulatie — bewijst ook dat de dag alsnog gewoon wordt ingevuld). 319/319 unit tests, volledige e2e-suite (32 stappen) groen (de reeds bekende "8. Mandje vullen"-flake kwam tijdens dit werk een paar keer voorbij, telkens groen bij herhaling — ongerelateerd), `npm run verify` groen. Geen nieuwe migratie. |
| UX-verbetering | #6: kleine acties op de startpagina herladen niet meer terug naar boven | Fix voor punt 6 uit de UX-review: "elke kleine actie (voorkeur zetten, dag overslaan, leervraag beantwoorden) herlaadt de hele pagina en scrollt terug naar boven — vooral onder 'Meer voor deze dag' → 'Voorkeuren aanpassen' moet je na elke klik terugscrollen en opnieuw openklappen." Bewust beperkt tot de drie acties die bij één specifieke dag horen en zich in/vlak bij hetzelfde dagblok bevinden (voorkeur zetten, losse maaltijd invullen, dag overslaan) — leervragen zijn bewust ongemoeid gelaten: die zitten niet in een `<details>` (het kernprobleem — steeds opnieuw openklappen — is daar niet van toepassing) en zijn door het bestaande productmaximum van 2 per sessie sowieso beperkt in impact; expliciet genoemd als bewuste scope-keuze, niet stilzwijgend gemist. `redirectToHome()` (`src/app/actions.ts`) kreeg twee nieuwe optionele parameters: `focusDayKey` (welke dag) en `openDayDetails` (of "Meer voor deze dag" ook meteen openklapt). `setPersonMealPreference` en `setLooseMealForDay` geven allebei hun dayKey mee (details blijft open, precies waar je was); `toggleMealPlanEntrySkipped` ("Uit eten") geeft de dayKey óók mee voor de scroll-positie, maar met `openDayDetails: false` — code-review-bevinding: die knop zit niet in de sectie, dus zou anders ongevraagd een leeg paneel openklappen. Elke dag kreeg een `id={`day-<dayKey>`}` + `scroll-mt-6` (zelfde `#anchor`+`scroll-mt`-patroon als /boodschappen en /controle). Onafhankelijke `code-reviewer`-review uitgevoerd (raakt meerdere server actions op de startpagina) — concludeert "veilig om te mergen", geen household-isolatie-probleem (`dayKey`/`focusDayKey` sturen alleen redirect-URL/UI, nooit een databasequery); de "Uit eten klapt ongevraagd open"-bevinding vóór het mergen verwerkt. Geverifieerd: nieuwe e2e-stappen "2b" (voorkeur zetten op dinsdag → blijft op `#day-tuesday`, `<details>` blijft aantoonbaar open via `HTMLDetailsElement.open`) en "2c" (Uit eten op woensdag → scrollt terug maar laat het paneel dicht, daarna weer teruggezet zodat latere stappen een normale week aantreffen). 319/319 unit tests, volledige e2e-suite (34 stappen) groen (de reeds bekende "8. Mandje vullen"-flake — nu ook onafhankelijk door de reviewer gereproduceerd op de ongewijzigde basisbranch, dus bevestigd een bestaand, van deze wijziging los issue — kwam een paar keer voorbij, telkens groen bij herhaling), `npm run verify` groen. Geen nieuwe migratie. |
| Feature | "Bestel nu" rechtstreeks vanaf de startpagina, zonder omweg via `/boodschappen` | Op gebruikersverzoek na een role-play UX/PM-critique (frictionless-design-perspectief): de belangrijkste vervolgstap richting Picnic (mandje vullen / bestelling bevestigen) is nu inline zichtbaar in de startpagina's volgende-stap-kaart zodra `attentionItems` aangeeft dat dit relevant is (`GROCERIES_READY_NOT_SENT_TO_PICNIC`/`PICNIC_CART_FILLED_NOT_CONFIRMED`) — geen klik naar `/boodschappen` meer nodig om verder te komen. Hergebruikt bewust het bestaande, al geteste `AddToPicnicCart`-component (geen dubbele implementatie); `src/app/page.tsx` haalt er zelf alleen een lichte, gerichte `shoppingList`-query bij (gescoped via `mealPlan.id`, geen nieuw IDOR-pad). **Harde eis, expliciet zo gevraagd door de gebruiker**: de app plaatst zelf nooit de echte Picnic-bestelling — "Toevoegen aan Picnic-mandje" vult alleen het mandje via de Picnic-API, "Ik heb besteld" zet uitsluitend lokaal een `orderConfirmedAt`-vlag; de daadwerkelijke bestelling/betaling blijft altijd een bewuste, handmatige actie van de gebruiker in de echte Picnic-app — ongewijzigd, structureel geverifieerd (geen nieuwe aanroep naar een Picnic-checkout-endpoint toegevoegd). Onafhankelijke `code-reviewer`-review uitgevoerd (verplicht: raakt de Picnic-integratie) — vond een echte, reproduceerbare bug (~50% van de kliks): de "Ik heb besteld"-knop bleef vastzitten op "Bezig…" wanneer aangeroepen vanaf de startpagina, terwijl de database-update zelf altijd wél binnen ~1s lukte. Oorzaak: `confirmPicnicOrder` revalideert zowel `/boodschappen` als `/`, en `/` is nu voor het eerst zowel de aanroepende als de gerevalideerde pagina — dat botste met Next.js' transition-gebonden "patch de huidige route direct bij"-gedrag (alleen actief bij een `useTransition`-gewikkelde server-actie-aanroep). Vóór het mergen gefixt door de pending-state van die knop los te koppelen van `useTransition` en lokaal bij te houden (`AddToPicnicCart.tsx`) — omzeilt de hele bugklasse, ongeacht de exacte Next-interne oorzaak. Bewust **niet** de reviewer's kleinere suggestie overgenomen om ook `revalidatePath("/")` aan `addToPicnicCart`/`clearPicnicCart` toe te voegen: dat zou dezelfde botsing kunnen introduceren voor de "Toevoegen aan Picnic-mandje"-knop, die nu ook vanaf `/` aanroepbaar is, zonder dat er een concrete noodzaak voor was. Ook een dubbelzinnige testlocator gefixt in e2e-stap "8. Mandje vullen" (en de nieuwe, ervan gekopieerde stap "10c"): de test wachtte op een tekstfragment dat al vóór de bevestigingsklik zichtbaar was, wat de al langer bekende, als "omgevingsflake" bestempelde intermitterende faal van stap 8 (sinds WP85) grotendeels lijkt te verklaren als een race in de test zelf. Geverifieerd: twee nieuwe e2e-stappen "10c"/"10d" (bestellen vanaf de startpagina bewijst een echte mock-Picnic-add én blijft op `/`; "Ik heb besteld" zet `orderConfirmedAt` en de bevestigingstegel verschijnt niet opnieuw bij een vers bezoek). `npm run verify` groen (319/319 unit tests). `npm run test:e2e`: 3x volledig groen op rij (36/36) ná de fix — vóór de fix faalde stap 10d in meerdere runs precies op de nu-verklaarde "Bezig…"-hang. Geen nieuwe migratie. |
| UX-verbetering | #7a: "Ons gezin" — de twee losse "wat ik heb geleerd"-panelen samengevoegd tot één sectie | Eerste, kleine stap van punt #7 uit de UX-review ("Ons gezin is een onoverzichtelijke stapel van 13+ losse secties"). Concreet: `LearnedPatternsPanel` (bovenaan, toonde weekpatronen uit het `LearnedPattern`-model onder de titel "Wat ik over jullie week heb geleerd") en een losse `<details>` onderaan `page.tsx` (toonde gerechtvoorkeuren uit `Preference`/`INFERRED` onder bijna dezelfde titel "Wat ik heb geleerd") waren twee verschillende databronnen achter vrijwel identieke, verwarrende koppen. Samengevoegd tot één `<details>` "Wat ik heb geleerd" met twee duidelijke subkopjes (Weekpatronen / Gerechtvoorkeuren); `LearnedPatternsPanel` kreeg een nieuwe prop `learnedRecipes` (al berekend in `page.tsx`, dus geen extra query) i.p.v. zelf twee keer dezelfde info te tonen. Zuiver presentationeel — geen server actions, schema of auth/Picnic geraakt, dus geen code-reviewer-stap nodig; `/ons-gezin` zit niet in `criticalFlow.e2e.ts`, dus geen `test:e2e` nodig. `npm run verify` groen (319/319 unit tests, lint/typecheck/build). Geen nieuwe migratie. Resterend deel van punt #7 (de overige 11 losse `<details>`-blokken herindelen in samenhangende groepen: Voorkeuren & smaak / Planning / Picnic / Account) staat genoteerd als vervolg-WP. |
| UX-verbetering | #7b: "Ons gezin" — de overige 11 losse secties herindeeld in 4 samenhangende groepen | Tweede en laatste stap van punt #7. De platte lijst van 13+ losse `<details>`/koppen op `/ons-gezin` is teruggebracht tot 6 duidelijke groepskoppen: **Gezinsleden** (ongewijzigd), **Voorkeuren & smaak** (nieuw — persoonlijke voorkeuren, favorieten/eetstijl, vaste gerechten per dag, huishoudbrede beperkingen), **Wat ik heb geleerd** (blijft na WP7a op zijn plek, direct na de voorkeuren-groep), **Planning** (nieuw — weekritme, planningsstijl), **Picnic** (bestond al, uitgebreid met bezorgmoment/productkeuze/onthouden producten die voorheen los tussen andere onderwerpen stonden), **Meldingen** (ongewijzigd), **Account** (nieuw — inloggegevens). Zuivere JSX-herordening in `src/app/ons-gezin/page.tsx`: elk bestaand blok (summary-tekst, formulieren, hidden inputs, server action-namen, `<details>`-inhoud) is ongewijzigd verplaatst — geen enkele actie, prop of databasequery aangepast, alleen nieuwe `<h2>`-groepskoppen toegevoegd volgens hetzelfde stijlpatroon als de bestaande "Gezinsleden"/"Meldingen"-koppen. Na de wijziging: 6 `<h2>`-groepen (was 3) en 9 `<details>`-blokken (ongewijzigd aantal na WP7a), geverifieerd met een gerichte `grep`-telling vóór het committen. Geen server actions, schema of auth/Picnic-client zelf gewijzigd, dus geen code-reviewer-stap nodig; `/ons-gezin` zit niet in `criticalFlow.e2e.ts`, dus geen `test:e2e` nodig. `npm run verify` groen (319/319 unit tests, lint/typecheck/build) — tussendoor twee keer gefaald op "Can't reach database server", beide keren de lokale Postgres-cluster (stond down, waarschijnlijk door een sandbox-herstart) herstart en daarna meteen groen; geen van beide faalpatronen had iets met deze wijziging te maken. Geen nieuwe migratie. |
| Feature | Picnic koppelen kan al tijdens onboarding, direct na accountaanmaak | Eerste, grootste punt van een nieuwe kritische UX-review (paginalengte, navigatietegels, volledige flow van account aanmaken tot bestellen — via drie parallelle onderzoeksagents). Belangrijkste frictiepunt uit de flow-analyse: een nieuwe gebruiker doorliep de hele onboarding-wizard, zag een weekmenu en een boodschappenlijst, en stuitte pas bij het echte mandje-vullen op een kleine tekstlink naar een heel ander scherm (Ons gezin) om alsnog bij Picnic in te loggen — precies het moment waarop iemand kan afhaken. Oplossing: `OnboardingWizard.tsx` stuurt na een geslaagde `completeOnboarding()` niet meer direct naar "/", maar naar een nieuwe, **altijd overslaanbare** tussenpagina `src/app/onboarding/picnic/page.tsx` die het al bestaande, geteste `PicnicConnection`-component hergebruikt (geen dubbele implementatie). Bij een geslaagde koppeling stuurt die pagina zelf meteen door naar "/" (geen apart "gekoppeld"-tussenscherm nodig); de "Overslaan, dit doe ik later →"-link staat buiten het component en blijft in elke stand (idle, foutmelding, 2FA-pending) zichtbaar — nooit een verplichte stap, conform AGENTS.md ("geen schijnfunctionaliteit"/nooit dwingen). Om na een koppelactie op de juiste pagina terug te komen (i.p.v. altijd naar Ons gezin) kreeg `PicnicConnection` een optionele `returnTo`-prop die als hidden veld meegaat in alle vier de formulieren (koppelen/2FA bevestigen/2FA annuleren/ontkoppelen); `src/app/ons-gezin/picnicActions.ts` leest dat veld en gebruikt een vaste whitelist (`["/ons-gezin", "/onboarding/picnic"]`, exacte match) om open-redirect via een gemanipuleerd formulierveld uit te sluiten. Onafhankelijke `code-reviewer`-review uitgevoerd (verplicht: raakt onboarding/sessiebeheer-aanverwante acties en Picnic-koppeling) — bevestigt op alle vijf gestelde risicovragen "veilig": de whitelist gebruikt array-exact-match (geen substring-kwetsbaarheid), de bestaande `assertCurrentHousehold`-autorisatie in de vier acties is ongewijzigd en blijft vóór elke mutatie de enige bron van waarheid, de sessie is na `completeOnboarding()` al correct het nieuwe huishouden vóórdat de tussenpagina laadt (geen sessie-vermenging tussen huishoudens), en de "Overslaan"-link blijft in elke weergavestatus werkend — geen blokkerende bevindingen. Geverifieerd: gedeelde e2e-fixture `completeOnboardingViaUi` (`e2e/fixtures/testHousehold.ts`) bijgewerkt om de nieuwe tussenstap te verwachten en over te slaan; nieuwe e2e-substap "1c" in `criticalFlow.e2e.ts` doorloopt de échte koppel-tijdens-onboarding-flow tegen de mock-Picnic-server in een eigen, volledig geïsoleerde browserpagina (bewust niet de gedeelde `page`/sessie van de hoofdflow — een eerdere versie deelde die sessie wel en veroorzaakte een cascade van 21 mislukte vervolgstappen door sessie-interferentie na het opruimen van het tijdelijke testhuishouden; met een eigen browsercontext volledig opgelost). `npm run verify` groen (319/319 unit tests, lint/typecheck/build — inclusief twee keer een Postgres-herstart tussendoor, ongerelateerd aan deze wijziging). `npm run test:e2e`: 37/37 groen. Geen nieuwe migratie. Resterende twee punten uit de nieuwe UX-review (navigatietegels van 6 naar 3, tekst inkorten op meerdere pagina's) staan genoteerd als vervolg-WP's. |
| UX-verbetering | Navigatiebalk van 6 naar 3 tegels (Jouw week / Boodschappen / Ons gezin) | Tweede punt van de nieuwe UX-review. De onderzoeksagent liet zien dat "Gerechten" geen bestemming is maar een contextuele actie (vervang dít gerecht op déze dag, vereist een dag om te kloppen — het enige bestaande gebruik is al vanaf "/" en "/boodschappen"), "Controle" gewoon stap 2 is van de boodschappenflow (al bereikbaar via een link vanaf "/boodschappen", geen aparte behoefte), en "Recepten" laagfrequent technisch receptbeheer is dat zichzelf al omschrijft als zodanig. Bovendien overlapten "Gerechten" en "Recepten" qua naam voor een niet-technische gebruiker. `src/components/NavBar.tsx` teruggebracht tot precies de drie tegels die een gebruiker structureel nodig heeft; de drie verwijderde routes blijven allemaal bereikbaar (Gerechten/Controle via hun bestaande links, Recepten via een nieuwe link "Receptenbeheer" onderaan de "Voorkeuren & smaak"-groep op `/ons-gezin`) — geen enkele pagina is onbereikbaar geworden, alleen de permanente plek in de hoofdnavigatie is weg. Geen server actions, schema of auth/Picnic geraakt, dus geen code-reviewer-stap nodig. Geverifieerd: geen bestaande e2e-test navigeert via de tegel-namen (allemaal directe `page.goto`/inhoudelijke links), dus geen testaanpassing nodig; `npm run verify` groen (319/319 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (37/37) omdat deze wijziging op elke pagina van de kritieke flow zichtbaar is. Geen nieuwe migratie. Resterend punt uit de UX-review (tekst inkorten op meerdere pagina's) staat genoteerd als vervolg-WP. |
| UX-verbetering | Tekst ingekort op meerdere pagina's (Boodschappen, Controle, Recepten, Ons gezin, Login) | Derde en laatste punt van de nieuwe UX-review. Gericht geschrapt/ingekort, niet blind alles wat de eerdere tekstdichtheid-analyse noemde — twee kandidaten uit die analyse bleken bij nader inzien géén overbodige tekst (de "Ik zoek op…"-zinnen op `/gerechten` bevestigen hoe een vrije-tekstwens is geïnterpreteerd, en de "losse lijst starten"-link op de startpagina is functioneel iets anders dan de CTA "Naar boodschappenlijst" verderop — beide bewust ongewijzigd gelaten). Wel doorgevoerd: (1) **Boodschappen** — twee bijna-identieke formaat-uitlegzinnen ("Typ/Zet producten onder elkaar of gescheiden door komma's…") ingekort, want ze herhaalden alleen wat de placeholder al toonde; de niet-overbodige scope-zin ("Voor deze week alleen") en de "ik zoek dit per regel op"-boodschap zijn behouden. (2) **Controle** — de rode banner "Controleer de gemarkeerde regels…" verwijderd; zei letterlijk hetzelfde als de subtitel + de al aanwezige aandacht-badges. (3) **Recepten** — de subtitel (metacommentaar over de paginastructuur zelf) verwijderd, en de 4-zinnige importuitleg teruggebracht tot één zin die alle drie kernpunten behoudt (werkt op de meeste sites, automatische maatomzetting, fallback naar "Snel toevoegen"). (4) **Ons gezin** — de meldingenzin gehalveerd zonder de belofte te verliezen (max 1 per dag, tussen 08:00-21:00). (5) **Login** — bij een leeg systeem stonden er twee identieke "Nieuw huishouden"-knoppen op één scherm; de onderste (algemene) link verschijnt nu alleen nog als er al een huishouden is om in te loggen. Zuiver tekst-/JSX-wijzigingen, geen server actions, schema of auth-logica aangepast (de Login-fix verandert alleen wanneer een bestaande link zichtbaar is, niet de onderliggende `hasAnyHousehold`-berekening of de inlogflow zelf), dus geen code-reviewer-stap nodig. Vooraf gecontroleerd dat geen enkele e2e-test op de verwijderde/gewijzigde tekst leunt (op één na: de "losse lijst"-link, die bewust ongewijzigd is gelaten). `npm run verify` groen (319/319 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (37/37). Geen nieuwe migratie. Hiermee is de volledige nieuwe UX-review (paginalengte, navigatietegels, onboarding-flow) afgerond. |
| UX-verbetering | Weekmenu-regels kunnen nu ook direct van de boodschappenlijst verwijderd worden | Gebruikersverzoek naar aanleiding van een screenshot: producten uit het weekmenu (bv. bijgerechten als frites/kaastengels) waren niet te verwijderen op `/boodschappen` — de verwijderknop stond alleen bij handmatig toegevoegde producten (`source === "MANUAL"`), terwijl de onderliggende actie (`removeBoodschappenLineThisWeek`) weekmenu- en voorraadregels altijd al veilig kon verwijderen (blokkeert alleen vaste boodschappen, die hun eigen beheer hebben via een dedicated foutmelding). Bleek zuiver een UI-omissie: de sectie waarin de knop staat (`mealLines`) bevat sowieso nooit vaste boodschappen — die staan al in een aparte sectie — dus de knop kon zonder voorwaarde altijd getoond worden. `ensureShoppingList` is idempotent per weekmenu (bouwt de lijst maar één keer op), dus een verwijderde regel komt niet stilzwijgend terug bij een volgend bezoek — geen schijnfunctionaliteit. Geen server actions, schema of auth/Picnic-client zelf gewijzigd, dus geen code-reviewer-stap nodig. Geverifieerd: nieuwe e2e-stap "4b" verwijdert een echte weekmenu-regel via de UI (bewust de alfabetisch laatste, zodat latere teststappen die zelf nog specifieke producten toevoegen via zoeken — Aardappelen, Bloemkool, Spruitjes — er nooit mee kunnen botsen) en bewijst zowel dat het DOM-element verdwijnt als dat de regel echt uit de database weg is. `npm run verify` groen (319/319 unit tests, lint/typecheck/build — tussendoor een Postgres-herstart, ongerelateerd), `npm run test:e2e` volledig groen (38/38). Geen nieuwe migratie. |
| UX-verbetering | Volledige boodschappenlijst (incl. vaste boodschappen) zichtbaar en verwijderbaar op één plek | Vervolg op bovenstaand punt, naar aanleiding van een tweede screenshot: "Jullie boodschappenlijst" toonde alleen weekmenu-/voorraad-/handmatige regels (4 in het voorbeeld), terwijl de bevestiging vóór het Picnic-mandje vullen 19 producten meldde — de 15 vaste boodschappen stonden in een aparte, ver weg ingeklapte sectie, zonder duidelijke koppeling. Plan eerst uitgelegd en door de gebruiker bevestigd vóór het bouwen. Oplossing (bewust "niet standaard", zoals gevraagd): direct onder de weekmenu-regels staat nu, alleen als er actieve vaste boodschappen zijn, een uitklapbare regel "+ N vaste boodschap(pen) worden ook besteld — toon volledige lijst"; opengeklapt tonen de vaste boodschappen zich in exact dezelfde rij-opmaak en met dezelfde eenvoudige verwijderknop als de weekmenu-regels. Onderliggend: de rij-JSX is uit `/boodschappen/page.tsx` naar een gedeelde `renderShoppingLineRow()`-functie getrokken (weekmenu- en vaste-boodschappenlijst renderen nu identiek, geen gedupliceerde opmaak), en de eerdere bron-blokkade in `removeBoodschappenLineThisWeek` (`src/app/boodschappen/actions.ts`) is weggehaald — die deed al exact hetzelfde (`shoppingListLine.delete`) als de bestaande, aparte `removeFixedLineThisWeek`, dus vaste boodschappen kunnen nu ook via deze route "voor deze week" verwijderd worden zonder de onderliggende vaste-boodschap-sjabloon te raken (blijft, net als voorheen, terugzetbaar via "Toch toevoegen" in de bestaande "Vaste boodschappen"-sectie, die ongewijzigd blijft bestaan als beheerscherm voor hoeveelheden/permanent verwijderen). Geen server-actie-autorisatie gewijzigd (`assertCurrentHousehold` blijft ongemoeid de enige poortwachter), geen schema, dus geen code-reviewer-stap nodig. Geverifieerd: nieuwe e2e-stap "11" (bewust als allerlaatste stap, ná alle andere die op "Aardappelen" als vaste boodschap leunen) opent de nieuwe uitklaplijst, verwijdert de vaste boodschap eruit, en bewijst zowel dat de regel van deze week weg is als dat de vaste-boodschap-sjabloon zelf blijft bestaan. `npm run verify` groen (319/319 unit tests, lint/typecheck/build). `npm run test:e2e`: eerste run 37/38 (nieuwe stap "4b" faalde op een timeout); één gerichte herhaling gaf 37/38 met een compleet andere, ongerelateerde faal (stap "2b", niet door deze wijziging geraakt) terwijl "4b" en de nieuwe stap "11" die keer wél slaagden — dat wisselende patroon (steeds een andere stap, nooit dezelfde) bevestigt de reeds bekende omgevingsflakiness uit deze sessie, geen regressie. Geen nieuwe migratie. |
| UX-verbetering | Picnic-bevestigingskaart: duidelijk aantal + link naar de volledige lijst | Vervolg op de twee bovenstaande punten, naar aanleiding van een derde screenshot: de knop "Vaste boodschappen + losse toevoegingen (15)" was dubbelzinnig (leek op 15 losse toevoegingen, was eigenlijk een opgeteld totaal van twee verschillende dingen), en nergens op de kaart kon je zien wát er precies wordt toegevoegd vóór je bevestigt. Plan eerst uitgelegd en bevestigd. `AddToPicnicCart.tsx` (gedeeld component, gebruikt op zowel `/` als `/boodschappen` — één fix werkt op beide plekken) kreeg twee losse props `fixedCount`/`manualCount` i.p.v. één samengevoegde `quickOrderCount`, zodat de knoptekst nu expliciet "Vaste boodschappen (X) + losse toevoegingen (Y)" toont (of alleen het relevante deel als er maar één soort is) — geen enkel opgeteld getal meer zonder uitleg. Beide aanroepende pagina's (`src/app/page.tsx`, `src/app/boodschappen/page.tsx`) splitsen hun bestaande gecombineerde telling in twee losse filters (`source === "FIXED"` resp. `"MANUAL"`) — geen nieuwe databasequery nodig, de data was al opgehaald. Daarnaast een link "Bekijk de volledige lijst" toegevoegd op twee plekken: direct onder de knoppen (idle-status) én in het bevestigingsscherm zelf, vlak naast "Ja, voeg toe aan mandje"/"Annuleren" — beide linken naar `/boodschappen#jullie-boodschappenlijst`, waar je sinds de vorige twee wijzigingen alles (incl. vaste boodschappen) al kunt zien en verwijderen. Zo kun je vlak vóór het definitieve bevestigen nog naar de volledige lijst springen i.p.v. blind te moeten vertrouwen op een aantal. Geen server actions, schema of `src/lib/picnic/*` zelf gewijzigd (alleen het app-component en de twee aanroepende pagina's), dus geen code-reviewer-stap nodig. Geverifieerd: geen bestaande e2e-test leunde op de oude, nu gewijzigde knoptekst. `npm run verify` groen (319/319 unit tests, lint/typecheck/build). `npm run test:e2e` volledig groen (39/39). Geen nieuwe migratie. |
| UX-verbetering | Na een gerechtkeuze automatisch terug naar het weekmenu, op de juiste dag | Gebruikersverzoek dat direct botste met een eerdere, bewuste WP8-keuze ("blijf op /gerechten na kiezen", zelf gekozen via `AskUserQuestion` om filters te behouden bij meerdere dagen achter elkaar aanpassen) — conflict expliciet benoemd en voorgelegd i.p.v. stilzwijgend overschreven. Gebruiker koos voor een derde, "slimmere" optie; onderzoek wees uit dat /gerechten geen eigen manier heeft om tussen dagen te wisselen (elke dag-context komt altijd van een verse link vanaf "/" of "/boodschappen"), dus een betrouwbaar onderscheid tussen "snel één dag fixen" en "meerdere dagen doorlopen" is server-side niet te maken zonder een nieuwe dag-wisselaar te bouwen — buiten scope. Gekozen, voorgelegde en geaccepteerde interpretatie: altijd terug naar het weekmenu na een echte wijziging, maar dan wél precies gescrold naar de aangepaste dag (zelfde `focusDay`+`#day-<dag>`-patroon als andere dag-acties op de startpagina) i.p.v. bovenaan de pagina — nog een dag aanpassen kost nog steeds maar één klik op "Wissel". `redirectToHome()` (voorheen een lokale kopie in zowel `src/app/actions.ts` als `src/app/gerechten/actions.ts`) is samengevoegd tot één gedeelde helper in het nieuwe `src/lib/homeRedirect.ts` — bewust een apart, niet-`"use server"`-bestand, want een `"use server"`-bestand mag alleen async functies exporteren en deze helper is puur synchroon. `replaceMealPlanEntry` en `chooseLiteralMealPlanEntry` gebruiken 'm nu allebei met `dayKey`; de "hetzelfde gerecht opnieuw gekozen" (geen echte wijziging) blijft bewust wél op /gerechten staan via de bestaande `redirectToGerechten`, want daar is niets afgerond om naar terug te keren. Geen server-actie-autorisatie of schema gewijzigd, dus geen code-reviewer-stap nodig. Geverifieerd: e2e-stap "3. Gerecht vervangen" herschreven — bewijst nu dat je op `/` landt, exact op `#day-monday` (niet bovenaan), met de bevestigingsmelding, en dat de hele heen-en-terugflow ook een tweede keer (dinsdag) gewoon werkt; stap "3c" (geen reden-stap bij een lege dag) bleek toevallig al source-agnostisch qua assertie (checkt alleen `status=meal-replaced`, niet het pad) en werkte ongewijzigd door. `npm run verify` groen (319/319 unit tests, lint/typecheck/build). `npm run test:e2e` volledig groen (39/39). Geen nieuwe migratie. |
| Bugfix | Dubbel bestellen voorkomen: overgedragen regels overleven een weekmenuwijziging | Grootste punt uit een nieuwe, brede kritische analyse (drie parallelle onderzoeksagents: techniek, UX, productgaten). **Het probleem**: `transferredToPicnicAt` op een `ShoppingListLine` is de énige bron van waarheid voor "dit product ligt al in het echte Picnic-mandje" — `addShoppingListToPicnicCart` slaat zo'n regel over. Maar de lijst werd bij élke weekmenuwijziging in zijn geheel weggegooid, waarna een volgende "Toevoegen aan Picnic-mandje" alles nog een keer bestelde. Echt geldverlies; het risico was deels bekend (er stond een waarschuwing bij "losse lijst starten") maar de dagelijkse paden deden het stilzwijgend. **De fix**: `invalidateShoppingList` (`src/lib/shoppingList.ts`) gooit een al overgedragen regel nooit meer weg — die blijft staan (hij ligt immers echt in het mandje) en de rest van de lijst wordt opnieuw opgebouwd; zolang er niets is overgedragen blijft het oude, goedkope "weggooien en lui herbouwen"-gedrag. De regelopbouw is daarvoor geëxtraheerd naar een herbruikbare `buildShoppingListLines` (bewezen zuivere verplaatsing, door de reviewer met `diff` geverifieerd). **Onafhankelijke `code-reviewer`-review uitgevoerd (verplicht: Picnic-bestelveiligheid) — verdict was "niet mergen in deze vorm", en terecht**: (a) de eerste fix dekte maar 5 van de 21 paden die de lijst weggooien — de twee gedeelde `invalidateCurrentShoppingList`-helpers (`recepten/actions.ts`, `ons-gezin/actions.ts`, samen 15 call sites, o.a. "kies dit product voortaan als standaard" en portiegrootte aanpassen) liepen nog via een rauwe `deleteMany`, en "Week opnieuw plannen" (`app/actions.ts`) had een eigen delete; (b) de deduplicatie op alléén `ingredientId` introduceerde een nieuwe, stille onder-bestel-bug: een overgedragen vaste boodschap liet de receptbehoefte voor hetzelfde ingrediënt verdwijnen (precies de staat na de knop "Vaste boodschappen (n)", die alleen FIXED/MANUAL overdraagt) — de reviewer reproduceerde dat tegen de database, en de meegeleverde test legde het foute gedrag zelfs vast. Beide blokkerende punten verwerkt: de sleutel is nu (ingrediënt + bron + eenheid); de twee helpers lopen via `invalidateShoppingList`; "Week opnieuw plannen" kan de markeringen niet bewaren (de `MealPlan` zelf verdwijnt via cascade) en blokkeert daarom eerlijk met een amberen uitleg zolang er iets in het mandje ligt. Verder uit de review overgenomen: `orderConfirmedAt` vervalt bij nieuwe regels (anders verdween de herinnering "rond je bestelling af in Picnic" na een tweede overdracht), `shortfallAcknowledged` wordt gereset (een groeiend tekort mag niet verborgen blijven), een regel die al in het mandje ligt kan niet meer van de lijst gehaald worden op `/boodschappen` én `/controle` (dat zou liegen én de markering wissen), en de tegenstrijdige tekst in `LooseListCard` is gelijkgetrokken. Meegenomen uit de UX-analyse omdat het dezelfde bestanden raakte: de statuscodes `shopping-reviewed` (na de grote "Bevestigen"-knop op /controle verscheen géén melding — de code ontbrak in de meldingenlijst) en `removed` zijn toegevoegd, plus een amber-variant voor waarschuwende meldingen op `/` en `/boodschappen`. Geverifieerd: nieuw `src/lib/shoppingListInvalidate.test.ts` (4 integratietests, incl. één die het door de reviewer gereproduceerde FIXED/MEAL-scenario afdwingt — expliciet gecontroleerd dat die test faalt tegen de oude, foute deduplicatie); e2e-stap "11" uitgebreid zodat hij beide kanten dekt (verwijderen geblokkeerd zolang het product in het mandje ligt, daarna wél mogelijk zonder de vaste-boodschap-sjabloon te raken). `npm run verify` groen (323/323 unit tests), `npm run test:e2e` volledig groen (39/39). Geen schemawijziging, geen migratie. |
| UX-verbetering | Bevestigings-cluster: eerlijke kop, echte eindtoestand, juiste primaire knop | Tweede punt uit de brede kritische analyse — drie samenhangende fixes rond "de week voelt nooit af". (1) **Eerlijke kop op `/boodschappen`**: de h1 zei altijd "Jullie boodschappen staan klaar" mét groen vinkje, ook pal boven een amberen banner "X producten vragen aandacht". Nu drie stadia: "staan bijna klaar" (zonder vinkje) zolang er twijfelgevallen open staan, "zijn besteld ✓" na "ik heb besteld", anders het bestaande "staan klaar ✓". (2) **Echte eindtoestand op de startpagina**: was álles afgerond, dan viel de volgende-stap-tegel terug op "Boodschappen staan klaar — loop de lijst nog even na" — opnieuw een taak, terwijl er niets meer te doen was. `nextStepCopy()` (`src/app/page.tsx`) kent nu een orderConfirmed-tak: "Alles is geregeld voor deze week", bewust zónder knop (geen taak, geen call-to-action; `href`/`cta` zijn daarvoor optioneel gemaakt). (3) **Juiste primaire knop in de afrondfase** (`AddToPicnicCart.tsx`): zodra het mandje gevuld was maar de bestelling nog niet afgerond, toonde het component nog steeds de grote accentknop "Toevoegen aan Picnic-mandje" bovenaan — een haastige tik riskeerde dubbel toevoegen — terwijl de eigenlijke vervolgstap ("Ik heb besteld") een klein randknopje onderaan was. Het "Rond je bestelling af"-blok staat nu bovenaan met "Ik heb besteld" als accentknop, en "Toevoegen aan Picnic-mandje" wordt in die toestand visueel secundair (randknop); buiten die toestand blijft alles zoals het was. Zuiver JSX/copy in drie bestanden, geen server actions, schema of `src/lib/picnic/*` gewijzigd — geen code-reviewer-stap nodig. Geverifieerd: bestaande e2e-stappen 10c/10d dekken precies deze flow (mandje vullen vanaf de startpagina, daarna "Ik heb besteld") en bleven groen zonder aanpassing — de teksten waarop ze wachten zijn bewust ongewijzigd gelaten. `npm run verify` groen (323/323 unit tests; één run faalde eerst op de bekende gestopte lokale Postgres, na herstart meteen groen — ongerelateerd), `npm run test:e2e` volledig groen (39/39). Geen nieuwe migratie. |
| UX-verbetering | Foutafhandeling: nette meldingen i.p.v. crashpagina's, en herstel bij een verlopen Picnic-sessie | Derde punt uit de brede kritische analyse. **Probleem**: veel server actions gooiden bij gebruikersbereikbare fouten een kale `throw new Error(...)` — in productie redigeert Next.js die boodschap weg, dus de gebruiker zag een generieke Engelse foutpagina. En als het opgeslagen Picnic-token verlopen was, crashte `/boodschappen` al bij het renderen (vier render-time Picnic-zoekopdrachten) zonder herstelroute. **De fix, in vier delen**: (1) **Gebruikersbereikbare fouten → status-redirects**: `gerechten/actions.ts` (conflicten met nooit-voorkeuren/beperkingen, onherkenbare vrije-tekstwens — de wens-formulieren kregen daarvoor hidden `direction`/`q`-velden zodat de context bij een redirect behouden blijft), `app/actions.ts` (los gerecht zonder titel/regels/onherkenbaar, dag zonder gerecht), `controle/actions.ts` en `boodschappen/actions.ts` (ongeldige hoeveelheid) redirecten nu met een statuscode naar de eigen pagina; alle vier de pagina's kregen bijpassende meldingen met amber-styling voor waarschuwingen (`ERROR_STATUS_MESSAGES`/`WARNING_STATUSES`-patroon, `LineControlCard` kreeg een `statusIsWarning`-prop). Bewust NIET omgezet: tamper-guards zoals "Onbekende dag/eenheid" (alleen bereikbaar met een gemanipuleerd formulier — defense-in-depth mag hard falen) en de gedeelde `parseQuantity`-helpers (browser-`min`/`required` dekt het normale pad al af). (2) **Verlopen Picnic-sessie op `/boodschappen`**: de vier render-time Picnic-zoekopdrachten zijn in een `safePicnicSearch`-wrapper gezet die `{ value, error: "auth" | "other" | null }` teruggeeft (geen closure-mutatie — de `react-hooks/immutability`-lintregel verbiedt dat in een async server component) — de pagina rendert nu altijd, met een amberen herstelbanner: bij een verlopen sessie een directe link naar `/ons-gezin` om opnieuw te koppelen, bij een storing een "probeer later opnieuw"-melding. (3) **Picnic-fouten in de controle-zoekactie**: `searchPicnicProductsForLine` vangt `PicnicAuthError` (→ `picnic-session-expired`) en overige fouten (→ `picnic-unreachable`) en meldt ook eerlijk `picnic-not-connected` als er nog geen koppeling is; `/controle` toont bovendien proactief een amberen banner ("nog geen Picnic gekoppeld") zodra er twijfelgevallen zijn maar geen token — met de tip dat "Zonder product doorgaan" ook kan. (4) **`global-error.tsx`** (nieuw): Nederlandstalig vangnet voor fouten in de root-layout zelf, bewust minimaal en zonder app-componenten. Geen server-actie-autorisatie, schema of `src/lib/picnic/*` zelf gewijzigd (alleen aanroepende pagina's/actions vangen fouten netter af), dus geen code-reviewer-stap nodig. Geverifieerd: `npm run verify` groen (323/323 unit tests, lint/typecheck/build). `npm run test:e2e`: eerste run 37/39 (stap "11" faalde op een timeout die in de twee direct voorafgaande WP-runs wél slaagde); één gerichte herhaling volledig groen (39/39, incl. stap 11) — bekende omgevingsflakiness, geen regressie. Geen nieuwe migratie. |
| Feature | WP A — Bezorgmomenten-overzicht: alle dagen die Picnic teruggeeft, live, met tijdstempel | Eerste werkpakket van de door de gebruiker goedgekeurde koerswijziging "boodschappen eerst" (zie de mockup-sessie: de app was weekmenu-first gebouwd terwijl het gezin meestal alleen vaste boodschappen + wat extra's wil bestellen, en vooraf wil zien wanneer er nog bezorgd kan worden). **Probleem**: `getDeliverySlots()` haalde de complete slotenlijst bij Picnic op, maar de app gebruikte daar één ding van — past het ingestelde voorkeursmoment (bv. vrijdag 18:00) er nog in, ja of nee — en gooide de rest weg. Wie geen voorkeur had ingesteld zag zelfs helemaal niets (`PicnicDeliveryStatusCard` gaf `null` terug zonder `PicnicDeliveryPreference`). **De fix**: nieuwe pure functie `groupDeliverySlotsByDay()` in `src/lib/picnic/deliverySlots.ts` bundelt de sloten per bezorgdag (sorteert dagen én tijdvakken zelf, groepeert op Nederlandse kalenderdag i.p.v. UTC-dag, en houdt een dag waarop álles vol zit expliciet zichtbaar met `unavailableCount` — anders lijkt zo'n dag simpelweg niet te bestaan); `formatSlotWindow()` is geëxporteerd voor de UI. Nieuwe `getDeliveryOverviewForHousehold()` in `deliveryStatus.ts` haalt in **één** Picnic-aanroep zowel het dagoverzicht als de voorkeursstatus op (expliciet getest: `callCount === 1`), en faalt nooit hard — een verlopen sessie komt terug als `error: "auth"`, een storing als `"other"`, zodat de pagina gewoon rendert met een herstelbanner (zelfde patroon als de zoekopdrachten op /boodschappen). Het bestaande `getPreferredDeliverySlotStatusForHousehold` is ongemoeid gelaten, want `src/lib/notifications.ts` gebruikt het. **Bewust geen vast aantal dagen ingebouwd**: hoever Picnic vooruit kijkt is hun rollende venster en staat nergens in onze code of testmock — de kaart toont de eerste 3 dagen direct en klapt de rest in achter "Verder vooruit kijken (N dagen)", dus het werkt of Picnic er nu 5 of 15 teruggeeft. `PicnicDeliveryStatusCard.tsx` vervangen door `DeliverySlotsCard.tsx` (enige gebruiker was `/boodschappen`), met tijdstempel "opgehaald om HH:MM" en een nieuwe `DeliverySlotsRefreshButton` (client component, `router.refresh()` in een `useTransition`) — geverifieerd tegen `node_modules/next/dist/docs/.../use-router.md`: dat doet een nieuwe serveraanvraag en rendert server components opnieuw, en omdat elke pagina `force-dynamic` is en de Picnic-fetches nergens gecached worden is dat écht een verse vraag aan Picnic, geen opgeslagen kopie. Zonder Picnic-koppeling toont de kaart nu een nette link naar /ons-gezin i.p.v. te verdwijnen. Eerlijkheidsregel in de UI: "Picnic legt een tijdvak pas vast als je het in de Picnic-app zelf kiest" — "beschikbaar" mag niet als reservering gelezen worden. Geen schemawijziging, geen schrijfactie richting Picnic (uitsluitend de bestaande GET), geen server action en geen door de client aangeleverde id in een databaselookup — de wijziging raakt de Picnic-client/cartService zelf niet, dus conform `WORKFLOW.md` geen aparte code-reviewer-stap. Geverifieerd: 9 nieuwe tests (5 pure groepeertests incl. de UTC-vs-Nederlandse-dag-grens en de "alles vol"-dag, 4 integratietests voor het overzicht incl. beide foutpaden en de één-aanroep-eis). `npm run verify` groen (332/332 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (39/39) in één run. Onderweg bleek de sandbox herstart te zijn: `node_modules`, de lokale Postgres-rol/-database en `.env` waren weg — opnieuw opgezet zoals `.github/workflows/ci.yml` het doet (`postgres:postgres@localhost:5432/family_assistant`, `migrate deploy` + `db:seed`); niets daarvan houdt verband met deze wijziging. Geen nieuwe migratie. |
| UX-verbetering | WP B — Boodschappen is de startpagina, het weekmenu verhuist naar /week | Tweede werkpakket van de koerswijziging "boodschappen eerst". De app opende altijd op het weekmenu van zeven avonden, terwijl het huishouden in de praktijk vooral de vaste boodschappen + wat extra's wil bestellen. **Gekozen aanpak, bewust de kleinste van twee**: niet beide pagina's fysiek verhuizen (dat zou ~86 routeverwijzingen raken, inclusief alle server-actie-redirects op /boodschappen), maar alleen de weekpagina verplaatsen — `src/app/page.tsx` → `src/app/week/page.tsx` (`git mv`, alleen twee relatieve imports aangepast) — en `/` behouden als vaste voordeur die naar `/boodschappen` doorstuurt. Zo blijft precies één plek bepalen wat "thuis" is (bladwijzers, telefoonstartscherm, en alle `redirect("/")` na inloggen/onboarding komen automatisch goed uit), terwijl de navigatiebalk rechtstreeks naar `/boodschappen` linkt zodat dagelijks gebruik geen extra doorverwijzing kost. **Belangrijkste valkuil, vooraf gevonden en opgelost**: `/boodschappen` en `/controle` stuurden bij een ontbrekend weekplan door naar `/` — dat was destijds de weekpagina, die `ensureMealPlan` aanriep. Met `/` als voordeur naar `/boodschappen` zou dat een doorverwijslus worden. `/boodschappen` roept daarom nu zelf `ensureMealPlan` aan (idempotent, zelfde aanroep als de weekpagina) met hetzelfde harde vangnet bij `null` als de weekpagina heeft; `/controle` mag gewoon naar de voordeur blijven sturen, want die lus bestaat niet meer. Verder: alle `revalidatePath("/")` (8 bestanden) betekenden "de weekdata is veranderd" en wijzen nu naar `/week`; `src/lib/homeRedirect.ts` (de gedeelde terug-naar-het-weekmenu-helper met `focusDay`) stuurt naar `/week?...#day-<dag>`; de vier terugpijlen "Terug naar Jouw week" (ons-gezin, recepten, gerechten, controle) wijzen naar `/week`. Op `/boodschappen` zelf is de terugpijl vervangen door een agenda-icoon dat naar `/week` linkt — een "terug"-pijl op de startpagina klopt niet meer. `NavBar.tsx`: volgorde nu Boodschappen · Week · Gezin, met een agenda-icoon voor de week (het huis-icoon paste niet meer nu de week niet de startpagina is). De foutschermteksten "Terug naar Jouw week" (`error.tsx`, `global-error.tsx`) zijn "Terug naar de app" geworden, want die knop leidt nu naar de boodschappen. Geen schemawijziging, geen server-actie-autorisatie gewijzigd, geen `src/lib/picnic/*` geraakt — dus geen code-reviewer-stap. Geverifieerd: e2e-suite bijgewerkt op de nieuwe routes (10 navigaties gesplitst naar `/week` resp. `/boodschappen`, plus de `waitForURL`-controles na een gerechtkeuze en de "blijft op de startpagina"-assertie in stap 10c, en de gedeelde onboarding-fixture die nu op `/boodschappen` landt). `npm run verify` groen (332/332 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (39/39) in één run. Geen nieuwe migratie. Volgende stap (WP B2): de volgorde ván de boodschappenpagina zelf — bezorgmomenten en het bestelblok naar boven, zoals in de goedgekeurde mockup. |
| UX-verbetering | WP B2 — Volgorde van de boodschappenpagina: bezorgmomenten en bestellen bovenaan | Afronding van WP B. De pagina was nu wel de startpagina, maar opende nog steeds met de productlijst; de bezorgmomenten stonden halverwege en de bestelknop daaronder — precies andersom van de goedgekeurde mockup. Nieuwe volgorde direct onder de kop: (1) `DeliverySlotsCard` (wanneer kan er bezorgd worden), (2) `PicnicTransfer`/`AddToPicnicCart` (wat staat klaar + de knop naar het mandje), (3) de lijst zelf (`#jullie-boodschappenlijst` met de uitklapbare vaste boodschappen), daarna ongewijzigd: product toevoegen, snel meerdere producten, dagelijkse controle, vaste boodschappen, voorraad en de checklist. `LooseListCard` is naar de onderkant verplaatst (vlak boven de navigatiebalk): een zelden gebruikte uitwijkmogelijkheid hoort niet tussen de dagelijkse acties — en die wordt bij WP C sowieso vervangen door de dagkeuze. **Zuivere JSX-verplaatsing**: de blokken zijn per regelbereik verplaatst zonder één teken aan hun inhoud te wijzigen (props, ids, teksten, voorwaarden allemaal identiek) — geverifieerd met een gerichte controle op de blokgrenzen vóór het verplaatsen. Alle ankers blijven werken: de link "Bekijk de volledige lijst" in `AddToPicnicCart` wijst naar `#jullie-boodschappenlijst` (staat nu ónder de kaart, scrollt gewoon omlaag) en de weekpagina linkt nog steeds naar `#loose-list`. Geen server actions, schema, auth of `src/lib/picnic/*` geraakt — geen code-reviewer-stap. Geverifieerd: `npm run verify` groen (332/332 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (39/39) in één run — inclusief stap 10, die via het `#loose-list`-anker de nu verplaatste kaart bereikt. Geen nieuwe migratie. |
| Feature | WP C — Dagkeuze: avondeten is opt-in per avond, ook over de weekgrens heen | Derde en grootste werkpakket van de koerswijziging "boodschappen eerst", rechtstreeks uit de goedgekeurde mockup. **Uitgangspunt**: vaste boodschappen staan altijd klaar; koken is een keuze per avond. **Schemawijziging (nieuw veld, migratie `..._meal_plan_entry_included_in_groceries`)**: `MealPlanEntry.includedInGroceries` (`@default(false)`). Bewust een ánder veld dan `skipped` — dit was expliciet aan de gebruiker voorgelegd via `AskUserQuestion` nadat bleek dat mijn eerdere uitspraak "geen schemawijziging nodig" onvolledig was: `skipped` zit al achter de knop "Uit eten" op de weekpagina, dus hergebruik zou een niet-aangevinkte avond daar tonen als "uit eten" terwijl de gebruiker dat nooit gezegd heeft (schijnfunctionaliteit). Gebruiker koos voor twee losse velden. De migratie zet bestaande rijen op `true` zodat al geplande weken zich blijven gedragen zoals ze achtergelaten zijn; de kolomstandaard `false` maakt nieuwe weken opt-in. **Weekgrens**: nieuwe gedeelde helper `getGroceryMealEntries(mealPlanId)` in `shoppingList.ts` bundelt de maaltijden van déze én de volgende week — een boodschappenlijst hangt aan één weekplan, maar de behoefte mag uit twee plannen komen (bezorging zaterdag, koken dinsdag). Gedeeld door de lijstopbouw, `findShoppingListShortfalls` (beide aanroepers omgezet) en `syncShoppingListForInventoryChange`, want die moeten per definitie dezelfde maaltijden zien — anders meldt de één een tekort op een gerecht dat de ander niet in de lijst heeft gezet. `aggregateMealNeeds` slaat een avond over bij `skipped || !includedInGroceries` (één plek waar die regel staat). **Nieuwe pure module `src/lib/orderDays.ts`**: het venster met avonden begint bij het eerste bezorgmoment dat Picnic nog aanbiedt (koken vóór de bezorging kan niet met deze boodschappen) en stopt bij het einde van de volgende week — verder aanbieden zou een belofte zijn die de lijstopbouw niet nakomt. `isSelectableOrderDate` staat daar bewust los van: het venster bepaalt wat de app *toont*, deze controle wat de server *accepteert* (een tabblad dat sinds gisteren openstaat). **Nieuwe server-actie** `setMealIncludedInGroceries` werkt op een **datum** uit het formulier, niet op een `mealPlanEntryId` — er komt dus geen door de client aangeleverde id in een databaselookup; het weekplan wordt opgezocht via het huishouden uit de sessie. Valt de datum in de volgende week en bestaat dat plan nog niet, dan maakt `ensureMealPlan` het alsnog aan: daarmee is "volgende week plannen vóór de bezorgdag" (los punt uit de eerdere analyse) hier vanzelf ingevuld. **UI**: `MealDayPicker` (server component, elke dag een eigen formulier — geen client-JS nodig) staat tussen de bezorgmomenten en het bestelblok; per aangevinkte avond het voorgestelde gerecht met de reden erbij. De "Wissel"-knop wordt **alleen** getoond voor dagen in de huidige week: het gerechtenscherm werkt op de huidige weekplanning, dus voor een avond in de volgende week bieden we die knop niet aan in plaats van een link die iets anders doet dan hij zegt (bekende, bewuste beperking — vervolgpunt). `DeliverySlotsCard` haalt zijn gegevens niet meer zelf op maar krijgt ze als prop, zodat de bezorgkaart én het dagvenster samen één Picnic-aanroep kosten. De weekpagina kreeg één regel uitleg met link naar de dagkeuze, omdat een gepland gerecht daar niet langer vanzelf boodschappen oplevert — dat werkte voorheen wél zo. Nieuwe statusmeldingen (`meal-day-added`/`-removed`, `order-day-out-of-range`, `order-day-plan-missing`), de laatste twee amber. Geverifieerd: 14 nieuwe tests — 11 pure tests voor het dagvenster (weekgrens-markering, afkappen bij een laat bezorgmoment, bezorgmoment in het verleden, elke dag van de week nagelopen) en 3 integratietests voor de weekgrens tegen de echte database (een aangevinkte avond in de vólgende week levert echt regels op de lijst van deze week; de bundeling pakt nooit een week verder vooruit). Bestaande fixtures aangepast waar ze uitgingen van "alle avonden tellen mee". Nieuwe e2e-stap "3d" bewijst de hele keten door de UI: verse week heeft géén weekmenu-regels, na het aantikken wél, na uitzetten weer weg terwijl het geplande gerecht blijft staan. Die stap faalde eerst wisselend doordat er op `?status=` in de URL werd gewacht — die staat er na de eerste klik al, waardoor de volgende wachtactie meteen terugkeerde op een nog niet herladen pagina; nu wordt op `aria-pressed` van de knop zelf gewacht, waarna twee volledige e2e-runs op rij groen waren (40/40). `npm run verify` groen (348/348 unit tests, lint/typecheck/build). Migratie lokaal toegepast en drift gecontroleerd (alleen de bekende, onschadelijke `RENAME INDEX`-drift uit `OPERATIONS.md` blijft staan, bewust niet meegenomen); in productie draait `prisma migrate deploy` automatisch mee met de Vercel-deploy (WP78), dus geen handmatige actie voor de gebruiker. **Niet gedaan, bewust gemeld**: de `code-reviewer`-subagent is deze sessie niet ingeschakeld ondanks de schemawijziging — de sessie-instructies verbieden het starten van subagents zonder expliciet verzoek van de gebruiker; dit is aan de gebruiker gemeld zodat die er alsnog om kan vragen. `LooseListCard` is door de dagkeuze feitelijk overbodig geworden (alle avonden uitzetten doet hetzelfde, zonder iets weg te gooien) — staat als opruimpunt genoteerd. |
| Feature + Bugfix | WP D — Verse bezorgcontrole bij het bevestigen, uitsplitsing per herkomst, en de dagkeuze merkbaar sneller | **Deel 1 (WP D uit het plan)**: het bevestigingsscherm vóór het vullen van het Picnic-mandje controleert nu op dát moment opnieuw de bezorgmomenten — het laatste punt waarop de gebruiker nog kan besluiten te wachten. `getPicnicConfirmationSummary` geeft daarvoor een `delivery`-blok terug (`checkedAt`, de eerstvolgende twee dagen met vrije tijdvakken, de minimale bestelwaarde, en een `error`-veld). Blokkeert nooit: mislukt die controle, dan zegt de kaart dat eerlijk en kun je gewoon doorgaan — het mandje vullen is iets anders dan een bezorgmoment vastleggen, en dat laatste doet de app sowieso niet. Over de **minimale bestelwaarde** wordt alleen een oordeel gegeven wanneer we het echt weten: bij onbekende prijzen (het totaal is dan een ondergrens) of wanneer er al producten in het mandje liggen (die tellen wél voor Picnic maar niet in dit bedrag) noemt de kaart het bedrag zonder verdict. Verder de uit de mockup overgenomen eis "geen getal zonder herkomst": `buildConfirmationSummary` geeft nu `toTransferBySource` terug en het bevestigingsscherm splitst uit in vaste boodschappen / zelf toegevoegd / voor het avondeten / voorraad. Het al bestaande, eerlijke prijstotaal (uit onze eigen productdata, met "minstens" bij onbekende prijzen) bleef ongewijzigd — geen tweede implementatie ernaast gebouwd. **Deel 2 (prestatiefix, gemeld door de gebruiker tijdens dit werkpakket: "een dag aanklikken duurt 5 seconden")**: gemeten met een tijdelijk meetscript dat het databasewerk isoleerde — een dagklik kost daar ~100 ms (weekplan opzoeken 14 ms, lijst weggooien 12 ms, herbouwen 55 ms), dus daar zat het niet. De oorzaak was de bezorgmomenten-aanroep die sinds WP C **blokkerend vóór de rest van de pagina** stond: elke render van /boodschappen — dus ook die ná elke dagklik — wachtte op een Picnic-netwerkaanroep (die intern zelfs twee keer kan uitgaan, `/cart` met terugval op `/cart/delivery_slots`). De vier zoekopdrachten op deze pagina bleken al voorwaardelijk én al in één `Promise.all`, dus die waren niet de boosdoener. Opgelost door (a) de bezorgkaart achter een `<Suspense>`-grens te zetten in een eigen `DeliverySlotsSection`, zodat de pagina meteen rendert en alleen die kaart later inschuift (met een nette "Bezorgmomenten ophalen…"-plaatshouder), en (b) het dagvenster niet meer op de live slotenlijst te baseren maar op de **opgeslagen bezorgvoorkeur** (nieuwe pure functie `nextDateForWeekday`) — het venster begint dus nog steeds bij het verwachte bezorgmoment, maar zonder netwerkaanroep in het kritieke pad; de kaart toont onverminderd de echte, live beschikbaarheid. Daarnaast doet `setMealIncludedInGroceries` niet langer twee keer `ensureMealPlan` wanneer de aangetikte dag gewoon in deze week valt. Geen schemawijziging, geen migratie. Geverifieerd: 2 nieuwe tests (uitsplitsing per herkomst incl. de eis dat de uitsplitsing optelt tot het getal op de knop; `nextDateForWeekday` rond de weekgrens), bestaande dagvenster-tests meeverhuisd naar het nieuwe, netwerkvrije signaal. `npm run verify` groen (350/350 unit tests, lint/typecheck/build). `npm run test:e2e`: eerste run 38/40 met een faal in stap "2c" — die zit op de weekpagina en raakt niets van deze wijziging (navigatie-timeout, bekende omgevingsflakiness); één gerichte herhaling volledig groen (40/40). |
| Feature + Bugfix | Wisselen ook voor avonden in de volgende week, losse lijst opgeruimd, en twee blokkerende reviewbevindingen opgelost | Drie punten die na WP D nog openstonden, plus de uitkomst van de alsnog uitgevoerde `code-reviewer`-controle op de schemawijziging van WP C. **(1) Wisselen over de weekgrens**: `/gerechten` accepteert nu `?week=next` (bewust geen vrije datum in de URL — alleen deze en de volgende week zijn geldig, precies zover als de boodschappenlijst reikt); de filterlinks houden die parameter vast, en de dagkeuze toont de "Wissel"-knop nu voor élke aangevinkte avond. Na een echte keuze keert de gebruiker terug naar wáár hij vandaan kwam: het weekmenu bij een dag in deze week, de dagkeuze op `/boodschappen#avondeten` bij een dag in de volgende week (`redirectAfterMealChosen`). **(2) Losse boodschappenlijst verwijderd**: `LooseListCard`, `looseListActions.ts`, de bijbehorende statusmeldingen, de link op de weekpagina en de twee e2e-stappen (10/10b) zijn weg. De dagkeuze doet hetzelfde beter: alle avonden uitgevinkt laten geeft exact "alleen vaste boodschappen", zonder iets weg te gooien en zonder de rode, onomkeerbare waarschuwing. **(3) Reviewbevindingen.** De review vond twee blokkerende problemen, allebei terecht. **Blokkerend A (opgelost)**: een gerecht wisselen op een avond in de volgende week riep `invalidateShoppingList(planVanDeVolgendeWeek)` aan — maar dát plan heeft zelf geen boodschappenlijst, dus er gebeurde niets en de lijst van de huidige week bleef stilzwijgend op het óude gerecht staan (verkeerde producten besteld, en de tekortcontrole ziet het niet omdat er voor het nieuwe gerecht helemaal geen regel bestaat). Sinds `getGroceryMealEntries` is de aanname "invalideer het plan dat je wijzigt" niet meer geldig. Opgelost met een nieuwe gedeelde ingang `invalidateShoppingListForPlanChange(householdId, changedMealPlanId)` die altijd óók de lijst van de huidige week herbouwt; beide aanroepen in `gerechten/actions.ts` lopen daar nu doorheen. Vastgelegd met een integratietest die eerst het foute gedrag reproduceert (via de oude aanroep) en daarna bewijst dat de nieuwe ingang de lijst wél bijwerkt naar het nieuwe gerecht. **Blokkerend B (nog niet opgelost, expliciet aan de gebruiker voorgelegd)**: `includedInGroceries` wordt nergens teruggezet, dus een avond in de volgende week die al besteld én bezorgd is, komt na de weekwissel opnieuw als verse regel op de lijst. Dat vraagt een keuze over wanneer die vlag vervalt (bij overdracht naar Picnic, of bij het opbouwen van een nieuwe week) en raakt `src/lib/picnic/cartService.ts` — daarom voorgelegd in plaats van zelf gekozen. **Niet-blokkerende bevinding over de migratie meegenomen**: die zet bestaande rijen op `true`, óók dagen eerder deze week die buiten het keuzevenster vallen — die stonden dan aan zonder dat de gebruiker ze ergens kon uitzetten. De dagkeuze toont nu ook aangevinkte avonden buiten het venster (`buildOrderDay`), en `isSelectableOrderDate` kreeg een `intent`-parameter: toevoegen kan alleen vanaf vandaag, weghalen mag altijd binnen deze en de volgende week. Ook de dode, zware `entries`-include in `syncShoppingListForInventoryChange` opgeruimd (bevinding 5) — die hield precies de "alleen deze week"-verzameling in scope waar bevinding A over ging. Bevinding 4 (losse lijst negeert de volgende week) verviel doordat die functie hierboven verwijderd is. **Correctie op eerdere rapportage**: in de PROGRESS-regel en commit-boodschap van WP C stond dat de "Wissel"-knop bewust niet wordt aangeboden voor avonden in de volgende week. Dat klopte bij het mergen van WP C, maar geldt sinds dit werkpakket niet meer — de knop werkt nu voor beide weken. Geverifieerd: 5 nieuwe tests (2 voor de `intent`-grens van `isSelectableOrderDate`, 1 integratietest voor het wisselen over de weekgrens incl. reproductie van de bug, plus de bestaande suite). `npm run verify` groen (353/353 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (38/38 — twee stappen minder door het verwijderen van de losse lijst) in één run. Geen schemawijziging, geen migratie. |
| Bugfix | Avondeten staat weer echt uit: correctie op de backfill van de dagkeuze-migratie | Gebruikersmelding: "ik heb geen enkele avond geselecteerd, maar toch staat er al een hele lijst aan producten in die besteld kunnen worden." **Eerst gereproduceerd, niet gegokt** — met een tijdelijk diagnosescript tegen de lokale database: een vers huishouden zonder aangevinkte avonden levert **0 regels** op (dus de opt-in-logica zelf klopt), maar zodra alle avonden op `included_in_groceries = true` staan — exact wat de migratie van WP C deed met bestaande rijen — levert hetzelfde huishouden **27 MEAL-regels** uit 7 avonden op. **De oorzaak was dus mijn eigen backfill.** Die zette bestaande weken op "telt mee" met de redenering "laat bestaande weken doen wat ze deden", maar dat botst frontaal met de reden van de dagkeuze: de gebruiker opent de app, heeft niets aangevinkt, en ziet toch een volle lijst. Erger nog: die avonden waren vaak niet eens zíchtbaar in de dagkeuze, want het dagvenster begint bij het verwachte bezorgmoment — staat dat bezorgmoment in de volgende week, dan toont de picker uitsluitend dagen van die volgende week terwijl de aangevinkte avonden van déze week er buiten vallen. Er viel dus niets uit te vinken. (De `buildOrderDay`-uitbreiding uit het vorige werkpakket verzachtte dat al, maar loste de verkeerde begintoestand niet op.) **Nieuwe migratie** `..._meal_days_opt_in_by_default`: zet `included_in_groceries` overal op `false`, en verwijdert de weekmenu-regels die daardoor ten onrechte op bestaande lijsten stonden — die worden namelijk niet vanzelf opnieuw opgebouwd, want `ensureShoppingList` geeft een bestaande lijst gewoon terug. Die opruiming is bewust nauw begrensd: **alleen** regels met `source = 'MEAL'`, **alleen** wanneer `transferred_to_picnic_at IS NULL` (een overgedragen regel is de enige bron van waarheid voor "dit ligt al in het echte Picnic-mandje" — die markering weggooien zou een volgende overdracht het product nog een keer laten bestellen), en **alleen** voor weekplannen vanaf de huidige week (`week_start >= date_trunc('week', CURRENT_DATE)`), zodat afgeronde weken hun geschiedenis houden. Vaste boodschappen, zelf toegevoegde producten en voorraadaanvullingen blijven staan — die hebben niets met de dagkeuze te maken. **Bijwerking, expliciet gemeld aan de gebruiker**: had iemand tussen de twee deploys al bewust avonden aangevinkt, dan staan die na deze migratie weer uit en moeten ze opnieuw aangetikt worden; onderscheid maken tussen "door de backfill aangezet" en "door de gebruiker aangezet" is in de data niet mogelijk. Geverifieerd: migratie lokaal toegepast, daarna 0 aangevinkte avonden over en alleen de bekende, onschadelijke `RENAME INDEX`-drift. `npm run verify` groen (353/353 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (38/38) in één run — inclusief e2e-stap "3d", die expliciet vastlegt dat een verse week géén weekmenu-regels heeft. In productie draait de migratie automatisch mee met de Vercel-deploy. |
| Bugfix | Een besteld avondeten in de volgende week wordt niet opnieuw voorgesteld | Laatste openstaande blokkerende bevinding uit de code-review op WP C: `includedInGroceries` werd nergens teruggezet. Scenario: je vinkt in week W een avond van W+1 aan, bestelt en krijgt bezorgd; zodra W+1 de huidige week wordt, bouwt de app daarvoor een verse lijst — zónder de `transferredToPicnicAt`-markeringen van de vorige bestelling — en stelt hetzelfde gerecht doodleuk opnieuw voor. Geen gebroken belofte (de gebruiker bevestigt nog steeds), maar wel een stilzwijgend voorstel tot dubbel bestellen, precies in het scenario waarvoor de weekgrens gebouwd is. **Aanpak, preciezer dan het aan de gebruiker voorgelegde voorstel**: bij het uitwerken bleek "alle aangevinkte avonden terugzetten bij overdracht" onnodig breed. Binnen de huidige week kan er niets dubbel besteld worden — `invalidateShoppingList` bewaart overgedragen regels en `cartService` slaat ze over — dus daar mag de dagkeuze gewoon blijven staan zoals de gebruiker 'm koos. Het risico zit uitsluitend één week verderop. Nieuwe `releaseNextWeekMealDays(householdId, weekStart)` in `shoppingList.ts` zet daarom alleen de avonden van het **volgende** weekplan terug; `addShoppingListToPicnicCart` roept die aan, maar alleen wanneer er in díe aanroep daadwerkelijk MEAL-regels zijn overgedragen (`transferredMealLines > 0`) — een "snelle bestelling" (alleen vaste boodschappen en losse toevoegingen) gaat niet over het avondeten en mag de gekozen avonden dus niet stilzwijgend uitzetten. Voordeel van deze smallere scope: de dagkeuze van de huidige week blijft na het bestellen tonen wat je gekozen hebt, in plaats van plotseling helemaal leeg te staan. **Bekende beperking, in de code gedocumenteerd**: leeg je daarna je Picnic-mandje, dan komen de overdrachtsmarkeringen terug maar deze keuze niet — welke avond bij welke regel hoorde is nergens vastgelegd. De regels blijven wel gewoon op de lijst staan; alleen een herbouw zou ze laten vervallen. Geverifieerd: 3 nieuwe tests — een integratietest die de volledige weekwissel simuleert (avond van W+1 aangevinkt en "besteld", daarna bewijzen dat de lijst van W+1 géén maaltijdregels bevat, én dat de avond van de huidige week wél aangevinkt blijft), plus twee tests op `cartService` die de wiring en de scope vastleggen (een maaltijdoverdracht zet de volgende week terug; een snelle bestelling zonder maaltijdregels laat de dagkeuze met rust). `npm run verify` groen (356/356 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (38/38) in één run. Geen schemawijziging, geen migratie. **Raakt `src/lib/picnic/cartService.ts`**, waarvoor `WORKFLOW.md` een aparte `code-reviewer`-controle voorschrijft; die is niet gedraaid omdat de sessie-instructies het starten van subagents zonder expliciet verzoek van de gebruiker verbieden — expliciet aan de gebruiker gemeld zodat die er alsnog om kan vragen. |
| Bugfix | Mandje legen maakt de lijst weer kloppend, en handmatige producten overleven een herbouw | Gebruikersmelding met screenshot: "nog steeds staan al deze boodschappen er, terwijl ik niks geselecteerd heb". **Diagnose**: dit was iets anders dan de vorige melding. Bij elke regel op de screenshot stond groen "Staat al in je Picnic-mandje" — die regels waren dus al naar het echte Picnic-mandje overgedragen, en de correctiemigratie laat overgedragen regels bewust staan (die markering is de enige bescherming tegen dubbel bestellen). Op zichzelf correct gedrag, maar de weg terug ontbrak: `clearPicnicCartForShoppingList` zette wel `transferredToPicnicAt` terug, maar bouwde de lijst niet opnieuw op. Na het legen van het mandje bleven de producten dus gewoon staan zonder dat er nog één avond aangevinkt was — alleen nog stuk voor stuk met het kruisje weg te halen. **Fix 1**: `clearPicnicCartForShoppingList` roept nu `invalidateShoppingList` aan, zodat de lijst weer precies weerspiegelt wat er nú gekozen is. **Fix 2 (voorwaarde om fix 1 veilig te maken)**: een herbouw gooide handmatig toegevoegde producten weg — `buildShoppingListLines` bouwt alleen MEAL/FIXED/INVENTORY, dus een MANUAL-regel ("bananen") verdween stilzwijgend bij élke invalidatie, ook bij een simpele gerechtwissel. Dat was een bestaande fout die door fix 1 veel vaker geraakt zou worden. `invalidateShoppingList` bewaart nu naast overgedragen regels ook `source = "MANUAL"`. **Fix 3 (gevonden doordat een bestaande test omviel)**: de eerste versie van fix 1 verwijderde de hele `ShoppingList`-rij wanneer er niets te bewaren viel. Dat is prima voor een luie herbouw bij een volgend paginabezoek, maar niet wanneer de aanroeper uit een scherm komt dat dat lijst-id al vasthoudt (de knoppen op /boodschappen): de eerstvolgende klik zou dan naar een niet meer bestaande lijst wijzen. `invalidateShoppingList` kreeg daarom een `keepListRow`-optie die de rij laat staan en alleen de regels herbouwt; `clearPicnicCartForShoppingList` gebruikt die. Geverifieerd: 3 nieuwe tests — een handmatig product overleeft een herbouw; mandje legen laat geen maaltijdregel achter zonder aangevinkte avond; plus de bestaande test die bewijst dat het lijst-id na het legen bruikbaar blijft (die viel om bij de eerste versie en dekt nu fix 3). `npm run verify` groen (358/358 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (38/38) in één run. Geen schemawijziging, geen migratie. **Raakt opnieuw `src/lib/picnic/cartService.ts`** — de door `WORKFLOW.md` voorgeschreven `code-reviewer`-controle is niet gedraaid (sessie-instructies verbieden subagents zonder expliciet verzoek), expliciet aan de gebruiker gemeld. |
| Bugfix | Lijst zonder aangevinkte avonden toont wél de vaste boodschappen en een kloppende prijs | Gebruikersmelding met screenshot: "nu heb ik de standaard boodschappen erin gedaan en nu zegt ie dat het nog leeg is en geen prijs" — terwijl er 15 vaste boodschappen klaarstonden. Twee samenhangende oorzaken, allebei een gevolg van het feit dat het weekmenu-deel sinds de opt-in-dagkeuze meestal leeg is. **(1) Prijs**: de prijs boven "Jullie boodschappenlijst" werd berekend over alleen `mealLines` (weekmenu/voorraad/handmatig). Zonder aangevinkte avonden is dat 0, dus stond er "Prijs onbekend" boven een lijst met vijftien producten. Nu wordt de prijs over álle regels van de lijst berekend — dat is immers wat er besteld gaat worden. **(2) Lege indruk**: bij nul weekmenu-regels rendeerde er een leeg omkaderd blok, met de vaste boodschappen weggestopt achter een ingeklapt "toon volledige lijst". De lijst zag er daardoor uit alsof er niets besteld werd. Nu: het lege blok verdwijnt, en zijn de vaste boodschappen de hele lijst dan staan ze meteen open met de kop "N vaste boodschappen staan klaar" in plaats van "+ N worden ook besteld — toon volledige lijst" (die formulering klopt alleen als er iets boven staat). Is er werkelijk niets — geen avond aangevinkt én geen vaste boodschappen — dan staat er nu een expliciete lege-toestand met de twee manieren om iets toe te voegen, in plaats van een leeg kader. Zuiver presentatiewerk op `src/app/boodschappen/page.tsx`: geen server actions, schema, auth of `src/lib/picnic/*` geraakt, dus geen code-reviewer-stap. Geverifieerd: `npm run verify` groen (358/358 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (38/38) in één run — inclusief de stappen die via `#alle-te-bestellen-producten` een vaste boodschap uit de uitklaplijst verwijderen. Geen nieuwe migratie. |
| UX-verbetering | Doorlichting punt 1 en 5: één veld voor extra's, en de meldingen ontdubbeld | Eerste twee punten uit de kritische doorlichting (zie de mockup "De kortere weg"). **Punt 1 — extra's toevoegen**: dit is de meest gebruikte actie van dit huishouden ("standaard boodschappen + wat extra's"), maar stond in twee losse kaders ónder de lijst: "Product toevoegen" (zoek één product) en "Snel meerdere producten toevoegen" (rijtje met komma's). Bij het uitzoeken bleek het geen twee functies maar één: het zoekkader is de **kies-zelf-stap** van het rijtje-kader (een niet-herkend product linkt ernaartoe met `quickOrderRaw`). Nu één ingang "Nog iets nodig?" direct onder de dagkeuze, met een placeholder die beide gevallen dekt; het zoekkader staat er nog steeds direct onder maar rendert alleen wanneer het daadwerkelijk in gebruik is (`quickOrderRawFocus || manualSearchQuery`) in plaats van permanent. Geen functionaliteit verdwenen — alleen het tweede, altijd zichtbare kader. **Punt 5 — meldingen**: het plan was "verplaats de aandachtstegel van de weekpagina naar de boodschappenpagina", maar bij het nalopen van `getAttentionItems` bleek verplaatsen niet nodig: alle vier de situaties staan op `/boodschappen` al op hun eigen plek — `PRODUCT_REVIEW_OPEN` als de amberen "N producten vragen jullie aandacht"-balk met link naar /controle, en de twee Picnic-situaties in `AddToPicnicCart` zelf mét de echte knop erbij in plaats van een verwijzing; `WEEK_MENU_READY_NO_GROCERIES` kan daar niet meer voorkomen omdat de pagina zelf `ensureShoppingList` aanroept. De tegel op de weekpagina was dus een tweede plek die hetzelfde beweerde en uit elkaar kon lopen. Daarom **ontdubbeld in plaats van verplaatst**: `nextStepCopy` is vervangen door `learningPromptCard()` en de weekpagina toont die kaart alleen nog als er leervragen open staan; de inline `AddToPicnicCart` en de bijbehorende gerichte `shoppingList`-query zijn daar weg (bestellen hoort bij de pagina waar de lijst staat). `getAttentionItems`/`getAttentionItemsForMealPlan` blijven ongemoeid — de pushlaag (`src/lib/notifications.ts`) leest daar nog uit. Geen server actions, schema, auth of `src/lib/picnic/*` gewijzigd, dus geen code-reviewer-stap. Geverifieerd: twee e2e-stappen aangepast omdat ze op het oude gedrag leunden — 10c/10d draaiden tegen de weekpagina toen die de startpagina was en wachtten op de tussentekst "Klaar om naar Picnic te gaan" (die er niet meer is; de knop staat er nu meteen), en de IDOR-test in `shoppingListAccess.e2e.ts` ging ervan uit dat het zoekveld permanent zichtbaar was — die navigeert nu rechtstreeks naar `?manualQ=...`, want de test gaat over het formulier dat er dán staat, niet over de weg ernaartoe. `npm run verify` groen (358/358 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (38/38). Geen nieuwe migratie. |
| UX-verbetering | Doorlichting punt 3: aantal en keuze op de productregel zelf | Derde punt uit de kritische doorlichting. **Probleem**: "weg" betekende drie verschillende dingen bij een vaste boodschap, verspreid over drie plekken — het kruisje in de lijst (alleen deze week), "uit" in het beheerblok (deze week, met een aparte "toch toevoegen"-knop), en "verwijder voorgoed" (het sjabloon zelf). Nergens stond welke je gebruikte. Daarbij kon het aantal alleen in het beheerblok worden aangepast, terwijl "deze week twee pakken melk" juist de meest voor de hand liggende wijziging is. **Nu**: elke regel in de boodschappenlijst heeft − / + rond het aantal, en een optiemenu (`<details>`, geen client-JS) waarin per optie staat wat hij doet: bij een vaste boodschap "Deze week niet — blijft je vaste boodschap; volgende week staat hij er weer" en "Nooit meer — haalt hem uit jullie vaste boodschappen"; bij overige regels "Van de lijst halen — alleen deze bestelling". Een MEAL-regel krijgt er de uitleg bij dat het bij een aangevinkte avond hoort en dat je die avond kunt uitvinken om alles ervan in één keer weg te halen. Alle onderliggende acties bestonden al en zijn hergebruikt (`removeBoodschappenLineThisWeek`, `removeFixedGroceryPermanently` — die laatste inclusief de al aanwezige IDOR-controle via `loadFixedLine`), dus er is geen nieuwe server-actie bijgekomen. Eén gerichte gedragswijziging: `adjustBoodschappenLineQuantity` weigerde FIXED-regels ("gebruik de vaste-boodschappenregel") — die blokkade is weg, want dat is precies wat de nieuwe knopjes doen. De actie past alleen de regel van déze week aan en raakt de vaste-boodschap-standaard niet; "onthouden als standaard" blijft in het beheerscherm. De vergelijkbare blokkades in `setBoodschappenLinePackageCount` en `chooseBoodschappenProduct` blijven staan — die horen bij andere flows. Een regel die al in het Picnic-mandje ligt krijgt geen − / + (die hoeveelheid ligt er al); het optiemenu blijft daar wel staan, zodat de bestaande, uitleggende blokkademelding bereikbaar blijft. Geen schemawijziging; geen `src/lib/picnic/*`, auth of nieuwe server-actie geraakt, dus geen code-reviewer-stap. Geverifieerd: drie e2e-stappen aangepast omdat de verwijderknop nu in het optiemenu zit (eerst het menu openen), en de selector voor de uitklaplijst met vaste boodschappen aangescherpt tot `#alle-te-bestellen-producten > summary` — die matchte sinds deze wijziging ook de optiemenu's van de regels erbinnen. `npm run verify` groen (358/358 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (38/38). Geen nieuwe migratie. |
| UX-verbetering | Doorlichting punt 2: alle beheerblokken achter één ingang | Vierde punt uit de kritische doorlichting: de bestelpagina bestond uit twaalf blokken, waarvan er vier nodig zijn om te bestellen. De overige acht (per dag bekijken, vaste boodschappen beheren, nieuwe vaste boodschap, voorraadcheck, winkelchecklist) zijn beheer dat je tijdens het bestellen bijna nooit nodig hebt, maar dat permanent tussen de bestelling en de onderkant van het scherm stond. **Gekozen aanpak, bewust afwijkend van de mockup**: die tekende een apart scherm "Instellen & beheren". Bij het uitwerken bleek dat een aparte route betekende dat de hele dataophaal én de in de pagina gedefinieerde renderhelpers (`renderShoppingLineRow`, `ProductThumb`, de formatters) gedupliceerd of geëxtraheerd moesten worden uit een bestand van ~2.000 regels — een echte refactor met navenant risico, voor exact dezelfde zichtbare winst. In plaats daarvan staan de vijf beheersecties nu ongewijzigd binnen één ingeklapte `<details id="beheer">` met de kop "Instellen & beheren"; de bestelpagina leest daardoor als vier blokken plus één regel onderaan, precies zoals in de mockup, en elke bestaande redirect/anker blijft werken zonder aanpassing. Als het straks alsnog een eigen scherm moet worden, is het een mechanische extractie. Klapt vanzelf open (`beheerFocused`) wanneer een melding of focusparameter naar iets binnenin wijst — anders zou een gebruiker na een beheeractie op een pagina landen waar zijn regel nergens te zien is. **Bijkomend gefixt**: "Nooit meer" in het nieuwe optiemenu op de lijst (punt 3) riep `removeFixedGroceryPermanently` aan, die altijd terugstuurde naar `#fixed-groceries` — dat zou de gebruiker nu ongevraagd in het beheerblok laten belanden. Die actie kent nu een `returnTo=list`-veld (vaste, gesloten keuze uit twee bekende bestemmingen, geen pad uit het formulier) met een eigen status `fixed-removed-from-list`, zodat je gewoon bij je lijst terugkomt én het beheerblok dicht blijft. Link naar Receptenbeheer onderaan het blok toegevoegd. Geen schemawijziging, geen nieuwe server-actie, `src/lib/picnic/*` en auth niet geraakt — geen code-reviewer-stap. Geverifieerd: e2e-helper `openBeheer()` toegevoegd in beide e2e-bestanden (zet `<details open>` via de DOM in plaats van op een summary te klikken — idempotent, dus veilig als een eerdere stap het blok al opende) en gebruikt in de stappen die vaste boodschappen, voorraad en de IDOR-controle raken. Daarnaast één unit-test gecorrigeerd die een verouderde fixture-aanname vastlegde: sinds "mandje legen" de lijst herbouwt, bevat die lijst alle regels die het weekmenu nodig heeft en niet meer alleen de ene handgemaakte fixtureregel — de test rekende op `added.length === 1`. Met een diagnosescript geverifieerd dat de app het juiste doet (8 regels voor het gekozen recept); de assertie is niet slapper gemaakt maar gerichter: hij controleert nu dat exact hetzelfde product na het legen opnieuw naar Picnic is gegaan, op ingrediënt in plaats van op een regel-id dat de herbouw niet overleeft. `npm run verify` groen (358/358 unit tests, lint/typecheck/build), `npm run test:e2e` volledig groen (38/38). Geen nieuwe migratie. |
| Feature | Doorlichting punt 4: de app vraagt zelf of er besteld is, en toont een bonnetje | Laatste punt uit de kritische doorlichting. **Probleem**: "Ik heb besteld" was een handmatige stap die je vergeet — op de screenshots van de gebruiker stond de oranje "rond je bestelling af"-kaart nog bovenaan, en die blijft daar tot je er zelf aan denkt. **Wat de app wél kan zien**: hoeveel producten er in het echte Picnic-mandje liggen. Is dat mandje leeg terwijl wij er producten in hebben gelegd, dan is afrekenen de meest voor de hand liggende verklaring. **Bewust een vraag, geen conclusie** — je kunt dat mandje ook elders geleegd hebben, en de app mag nooit doen alsof ze weet wat ze niet weet. Nieuwe pure module `src/lib/picnic/cartState.ts` met `parseCartItemCount()`: leest `total_count` als die betrouwbaar is, valt terug op het aantal regels, en geeft **`null` bij alles wat niet met zekerheid te lezen is** — de UI vraagt strikt alleen bij `=== 0`, dus een verkeerd gelezen respons kan de vraag nooit uitlokken. Om dit zonder extra netwerkverkeer te doen is `PicnicClient.getCartAndDeliverySlots()` toegevoegd: die haalde `/cart` al op voor de bezorgmomenten en gooide de rest weg; nu komen mandjetelling én tijdvakken uit dezelfde aanroep (`getDeliverySlots()` delegeert ernaartoe, zodat de pushlaag ongewijzigd blijft). `DeliverySlotsSection` toont bovenaan `OrderPlacedQuestion` wanneer er overgedragen regels zijn, de bestelling nog niet bevestigd is en het mandje leeg blijkt. "Ja, besteld" roept het bestaande `confirmPicnicOrder` aan; **"Nee, nog niet" slaat bewust alleen deze weergave over** (`?bestelvraag=later`) en raakt de overdrachtsmarkeringen níet — die weghalen op grond van één leesactie zou betekenen dat een volgende overdracht alles opnieuw bestelt, precies de dubbel-bestel-klasse die we eerder hebben dichtgezet; wie echt opnieuw wil beginnen gebruikt "Picnic-mandje legen". Daarnaast een **bonnetje** na bevestigen: datum, aantal producten en het geschatte bedrag, berekend over de daadwerkelijk overgedragen regels. Bewust **zonder bezorgmoment**, hoewel de mockup dat toonde: welk tijdvak de gebruiker in de Picnic-app kiest weten we niet, dus dat zou verzonnen zijn. Geen schemawijziging. Geverifieerd: 6 nieuwe unit-tests voor `parseCartItemCount` (expliciete telling, terugval op regels, en vier vormen die `null` moeten geven i.p.v. een gok); de mock-Picnic-server geeft `/cart` nu een echte inhoud terug (`total_count` uit zijn eigen mandje) in plaats van `{}`, waardoor dit überhaupt te testen is; nieuwe e2e-stap "10e" doorloopt het volledige scenario — mandje buiten de app om leegmaken, de vraag zien verschijnen, "Ja, besteld" klikken, `orderConfirmedAt` gezet zien worden, en daarna het bonnetje zien in plaats van opnieuw de vraag. `npm run verify` groen (364/364 unit tests, lint/typecheck/build). `npm run test:e2e`: eerste run 37/39 met een faal in stap "4b" (verwijderen van een weekmenu-regel — raakt niets van deze wijziging, en dezelfde stap faalde eerder deze sessie al eens op dezelfde manier); één gerichte herhaling volledig groen (39/39). **Raakt `src/lib/picnic/*`** (client + nieuwe module), waarvoor `WORKFLOW.md` een `code-reviewer`-controle voorschrijft — niet gedraaid omdat de sessie-instructies subagents zonder expliciet verzoek verbieden; aan de gebruiker gemeld. |
| Feature + Bugfix | Vorige bestelling herhalen, plus drie bevindingen uit de Picnic-review | Twee dingen tegelijk: het laatste kleine punt uit de doorlichting, en de uitkomst van de alsnog gedraaide `code-reviewer`-controle op de Picnic-wijzigingen sinds PR #147. **(1) Herhalen.** Aan de gebruiker voorgelegd wát "herhalen" moet overnemen; gekozen: de losse toevoegingen én dezelfde kookavonden, maar met verse gerechtvoorstellen (twee weken achter elkaar hetzelfde eten is meestal niet de bedoeling). Nieuwe `src/lib/repeatOrder.ts` met `getPreviousOrderSummary()` — de knop verschijnt alleen als er in de vórige week écht naar Picnic is overgedragen, anders belooft hij iets wat er niet is. Nieuwe server-actie `repeatPreviousOrder` (in `repeatOrderActions.ts`) vinkt dezelfde weekdagen weer aan — alleen dagen die nog niet voorbij zijn, via `isSelectableOrderDate` — en kopieert de MANUAL-regels, met overslaan van producten die al op de lijst staan. Werkt zonder enige id uit het formulier: alles komt uit het huishouden in de sessie. De melding achteraf noemt de echte aantallen ("2 losse producten en 1 kookavond overgenomen") of zegt eerlijk dat er niets nieuws over te nemen viel. De query is bewust in `lib` gezet en niet in het actiebestand, want dat laatste trekt via `auth.ts` `server-only` mee en is daardoor niet testbaar. **(2) Reviewbevindingen.** De review vond géén dubbel-bestel-lek (de idempotentie op `transferredToPicnicAt` houdt overal stand), maar wel drie echte fouten in de andere richting. **Opgelost — bevinding 1 (belangrijkst)**: `releaseNextWeekMealDays` draaide na élke geslaagde maaltijdregel, óók als de overdracht daarna afbrak. Bij een halve overdracht werd de gekozen avond van de volgende week dan vrijgegeven terwijl niet alle boodschappen ervoor in het mandje lagen — bij een volgende herbouw vielen de resterende regels geruisloos van de lijst en kreeg het huishouden niets bezorgd voor een avond waarvan het dacht besteld te hebben. Nu alleen bij `!stoppedEarly && errors.length === 0`, net zo streng als `markTransferred`. **Opgelost — bevinding 2**: producten die ná een "ik heb besteld" alsnog werden overgedragen, telden mee in het bonnetje als "besteld" én lieten de herinnering "rond je bestelling af" weg. `addShoppingListToPicnicCart` wist nu `orderConfirmedAt` zodra er echt iets is toegevoegd. **Opgelost — bevinding 3 (deels)**: na "Nee, nog niet" zat de gebruiker klem — de app slaat die producten voortaan over ("liggen al in het mandje") en de enige uitweg stond alleen in codecommentaar. Die uitweg staat nu in de kaart zelf. Het onthouden van die keuze buiten de URL is niet gedaan (zou een cookie of schemaveld vragen) — genoteerd. **Bevindingen 4 en 5 niet opgelost, wel genoteerd**: "Picnic-mandje legen" ná een bevestigde bestelling behandelt die bestelling weer als niet-besteld zonder dat de bevestigingstekst dat zegt; en `clearPicnicCartForShoppingList` kan sinds #148 tóch gooien terwijl de docstring "gooit bewust nooit" belooft (de herbouw zit niet in een try/catch). **Ook uit de review overgenomen**: de test van #148 bewees `keepListRow` niet — die assertie is toegevoegd. Geverifieerd: 5 nieuwe tests (3 voor `getPreviousOrderSummary` incl. "een lijst die nooit naar Picnic ging telt niet", 1 voor de halve overdracht, 1 voor het vervallen van de bevestiging) en een nieuwe e2e-stap "12" die het herhalen door de UI bewijst. Onderweg vond mijn eigen e2e een echte bug die de reviewer ook signaleerde: `OrderPlacedQuestion` leunde op revalidatie om te verdwijnen, waardoor de knop op "Bezig…" bleef hangen — opgelost met een eigen eindtoestand, zelfde patroon als `AddToPicnicCart`. Daarnaast de al langer wisselende e2e-falen op stappen 4b/11 verklaard en verholpen: die klikken op de boodschappenpagina terwijl de bezorgkaart na-streamt (Suspense), waardoor de lay-out onder de klik verschoof; nieuwe helper `waitForBoodschappenSettled()` wacht daarop. `npm run verify` groen (369/369 unit tests, lint/typecheck/build), `npm run test:e2e` twee keer op rij volledig groen (40/40). Geen schemawijziging, geen migratie. |
| UX | Dagtegels reageren meteen op een tik | Melding van de gebruiker: "alleen de knoppen van de dagen duren nog erg lang voordat er iets gebeurt, nog steeds meerdere seconden". **Eerst gemeten in plaats van gegokt** (tijdelijk meetscript tegen de lokale database): de server-actie kost 18 ms (`ensureMealPlan` 9 ms, `invalidateShoppingList` 6 ms) en het opbouwen van de paginagegevens ~50 ms (`ensureShoppingList`-herbouw 37 ms, `getGroceryMealEntries` 6 ms). De database is dus niet de oorzaak; de resterende tijd zijn de twee netwerkrondes naar Vercel (actie + redirect, elk met mogelijke koude start). **De echte vondst**: de dagtegels waren gewone `<button type="submit">` zónder enige terugkoppeling — tussen de tik en het herladen gebeurde er zichtbaar niets, en dat leest als "de app doet niks". Nieuw `DayToggleButton.tsx` (client, `useFormStatus`): de tegel houdt zijn dagletter en -nummer, maar springt tijdens het versturen alvast naar de toestand die je hebt aangetikt, met `aria-busy` en een lichte doffing. Bewust niet `PendingSubmitButton` hergebruikt: die vervangt zijn label door "Bezig…", wat niet in een tegel van twee tekens past. **Wat níet gelukt is, eerlijk gemeld**: de redirect na de actie weghalen om één netwerkronde te besparen. Uitgeprobeerd én getest — deze `force-dynamic`-pagina werkt zich met alleen `revalidatePath` niet bij. Ik concludeerde dat eerst te snel, bedacht daarna dat de zojuist gevonden streaming-race dezelfde faalhandtekening geeft, en heb het na toevoegen van `waitForBoodschappenSettled()` opnieuw getest: de variant zonder redirect faalt echt. Redirect dus teruggezet, met een commentaar dat klopt met wat er gemeten is. Onderweg ook de laatste e2e-wankelheid opgelost: de klikken op een `<summary>` in de regelopties zijn vervangen door een DOM-helper `openRowOptions()` die `details.open` zet — een klik op een samenvatting die net verschoven is, opende soms niets. `npm run verify` groen (369/369), `npm run test:e2e` groen (40/40). Geen schemawijziging, geen migratie. |
| Feature | Weekritme A — ISO-weken, aanwezigheid per datum, porties per datum | Eerste werkpakket van het weekritme (plan: zie de zes werkpakketten A t/m F). **Doel van A**: aanwezigheid kan een oneven/even-ritme volgen én een uitzondering voor één datum hebben, en alle portieberekeningen gaan van "per weekdag" naar "per datum". **Nieuwe pure module `src/domain/week/isoWeek.ts`**: `isoWeekNumber`, `isoWeekYear`, `weekParityForDate`, `parityAppliesToDate`, `calendarDateKey`. Bewust ISO-8601 (week 1 = de week met de eerste donderdag) en niet "de hoeveelste maandag": alleen zo klopt de jaargrens — 29 december 2025 hoort bij week 1 van 2026, 1 januari 2027 nog bij week 53 van 2026. De rekenkern zet de lokale kalenderdatum om naar UTC vóór het optellen, zodat zomertijd geen dag kan verschuiven. Onafhankelijk geverifieerd tegen `date +%V/%G` in plaats van alleen tegen mijn eigen verwachting. **Schemawijziging (migratie `..._person_presence_week_parity_and_date_overrides`)**: `PersonPresenceOverride.weekParity` (`EVERY`/`ODD`/`EVEN`, standaard `EVERY`) met de unieke sleutel van (persoon, dag) naar (persoon, dag, pariteit), plus nieuwe tabel `PersonPresenceDateOverride(personId, date, present)`. Pariteit gaat bewust in de bestáánde aanwezigheidstabel: die heeft al een UI en tests, en een tweede bron van waarheid ernaast gaat gegarandeerd afwijken. De datum-uitzondering wél in een eigen tabel, omdat die het patroon nóóit mag veranderen — één afwijkende vrijdag betekent niet dat vrijdag voortaan anders is. Bestaande rijen krijgen `EVERY` en gedragen zich exact als voorheen. **Oplossingsvolgorde** (één plek, `isPersonPresentOnDate`): datum-uitzondering → pariteitsregel → elke-week-regel → `defaultPresent`. `isPersonPresentOnDay` blijft bestaan voor schermen die het ritme tónen zonder concrete week (de aanwezigheidsknoppen op /ons-gezin) en kijkt daar bewust alleen naar de elke-week-regels. **Porties per datum**: `getHouseholdPortionScaleForDate` geeft een fúnctie terug in plaats van een tabel per weekdag — de personen worden één keer geladen, daarna raakt het de database niet meer. `aggregateMealNeeds` en `findShoppingListShortfalls` nemen die functie aan; `getGroceryMealEntries` hangt nu een `date` aan elke avond, want juist dáár is bekend uit welk weekplan een avond komt. `getHouseholdHardRestrictionsAndParticipantsByDay` → `...ForWeek(householdId, weekStart)` (mealPlan.ts, /gerechten, /week geven de week nu expliciet mee). **Hiermee is een bestaande fout opgelost die nog niet zichtbaar was**: de lijst neemt sinds de dagkeuze avonden uit de vólgende week mee, maar schaalde ze met de aanwezigheid van déze week — onzichtbaar zolang aanwezigheid puur per weekdag was, maar fout zodra vrijdag afwisselend 4 en 2 eters heeft. Datum-uitzonderingen worden begrensd meegeladen (vanaf 14 dagen terug); de planner en de lijst kijken alleen vooruit. **Geverifieerd**: 20 nieuwe tests (8 ISO-week incl. jaargrens beide kanten op en zomertijd, 9 aanwezigheid/pariteit/uitzondering, 1 lijstschaling per datum, 2 databaseniveau: leest het pariteitspatroon én de uitzondering écht uit de kolommen, en de uitzondering laat het patroon ongemoeid). De databasetest gebruikt bewust datums ten opzichte van vandaag, anders zou hij op termijn stil gaan falen op het laadvenster. `npm run verify` groen (389/389), `npm run test:e2e` twee keer op rij 40/40. **Onderweg**: e2e-stap 3d faalde twee keer op rij — bleek geen regressie maar dezelfde streaming-race als eerder (elke tik herlaadt, de bezorgkaart streamt na, de tegels verschuiven onder de volgende klik); de kliklus wacht nu per ronde op `waitForBoodschappenSettled`. Om dat te kunnen vaststellen is `E2E_SERVER_LOG=1` toegevoegd aan de testserver: zonder live serveruitvoer is een falende server action in de e2e onzichtbaar. |

| Feature + Bugfix | Weekritme B — dagregels, dagprofielen, en de echte oorzaak van "ik tik en er gebeurt niets" | **Dagregels.** Nieuwe tabel `MealDayRule(householdId, dayOfWeek, weekParity, profileKey, fixedRecipeVariantId?, preferredCategory?)` (migratie `..._meal_day_rules`). Wie er mee-eet staat er bewust níét in — dat is al vastgelegd in de aanwezigheidstabel, en herhalen zou een tweede bron van waarheid zijn. Oplossing per datum via `resolveMealDayRule`: eerst de regel voor de weeksoort, dan de regel voor elke week, anders niets. **Dagprofielen** als register in code (`src/domain/meal-planning/dayProfiles.ts`), niet als enum (elk nieuw profiel zou een migratie zijn) en niet als tabel (er is geen scherm om er zelf een te bouwen; dat zou een lege belofte zijn). Acht profielen uit de opdracht, elk met gewenste/te vermijden eigenschappen plus vlaggen (`adultOnly`, `fixed`, `allowSplitMeal`, `allowExternalIngredients`, `preferPicnicComplete`). De scoring krijgt er `dayProfile` bij: bonus per gewenste eigenschap, straf per te vermijden eigenschap, nooit een uitsluiting — en de reden komt in gewone taal in de "waarom dit?"-uitleg. **Volgorde in de planner**: vast gerecht uit de dagregel → daggewoonte (`DayRoutine`, ongewijzigd) → bij een `FIXED`-profiel zonder vast gerecht hetzelfde als vorige week → gewone scoring. Alle vier blijven achter de harde-beperkingenfilter: "hard is hard" geldt ook voor iets dat als vast is ingesteld. **Drie nieuwe tags** (`CHICKEN`, `POTATO`, `WORLD_FOOD`) worden uit ingrediënten/titel afgeleid; bewust géén zoektermen, want dan zou dezelfde wens ("met kip") twee keer punten opleveren en verandert een functie die prima werkt. **Bijvangst-bugfix**: `EXTENSIVE` bestond wel als zoekterm maar werd nergens uit een gerecht afgeleid — wie "uitgebreid" typte kreeg dus nooit een treffer. Nu symmetrisch. **De belangrijkste vondst van dit pakket zit niet in het weekritme.** e2e-stappen 3d, 4b en 11 faalden wisselend; met een diagnostische dump bleek: de actie slaagde (`?status=meal-day-added` stond in de URL) maar de dagtegel bleef "uit" staan. Oorzaak: al die acties redirecten naar een URL die identiek is aan de pagina waar de gebruiker al staat. De router slaat zo'n navigatie over of serveert zijn eigen, inmiddels verouderde kopie — de wijziging staat wél in de database, maar niet op het scherm. Dat is precies de klacht "ik tik een dag aan en er gebeurt niets" uit het vorige werkpakket: geen traagheid, maar een tik die zichtbaar verdampt. Opgelost door de redirect uniek te maken per actie: de datum bij de dagkeuze, het regel-id bij het verwijderen van een boodschappenregel en bij het uitzetten van een vaste boodschap (plus een bevestigingstekst, die er bij verwijderen helemaal niet was). Vóór de fix faalde de e2e ongeveer om de andere run, erna drie keer op rij groen. **Geverifieerd**: 18 nieuwe tests (dagregel-oplossing, profielregister, profielscoring inclusief "zonder profiel verandert er niets", vijf integratietests op databaseniveau: vast gerecht, vast gerecht versus allergie, profiel stuurt de keuze, pariteit raakt alleen die weken, en twee identieke huishoudens zonder regels krijgen dezelfde week). `npm run verify` groen (407/407), `npm run test:e2e` drie keer op rij 40/40. |

| Feature | Weekritme C — samengestelde maaltijden | "AVG" is geen gerecht maar een vorm: aardappel + vlees + groente, elk met een paar opties. **Schemawijziging (migratie `..._composite_meals`)**: `MealTemplate` → `MealComponentGroup` (rol + volgorde) → `MealComponentOption` (naam, ingrediënt, hoeveelheid per persoon), plus `MealPlanEntryComponent` voor de gekozen opties, `MealDayRule.mealTemplateId` en `MealPlanEntry.mealTemplateId`. **`MealPlanEntry.recipeVariantId` is nullable geworden**: een avond is óf een recept, óf een samenstelling. Het alternatief — voor elke combinatie stilletjes een echt `Recipe` aanmaken — is verworpen: dat vervuilt /recepten met tientallen gegenereerde gerechten en maakt de leerlaag blind. Het risico van een optioneel veld bleek beheersbaar precies zoals voorspeld: de typecheck wees elke lezer aan, tien bestanden, geen stille. **Nieuwe domeinmodule `mealEntry.ts`** is de énige plek die het verschil recept/samenstelling kent: `mealEntryTitle` ("Schnitzel met aardappelblokjes en broccoli", genoemd naar het eiwitcomponent) en `mealEntryNeeds`. Die laatste rekent bewust verschillend voor de twee vormen: een recept staat opgeschreven voor het hele gezin (dus × schaal), een component per persoon (dus × aanwezige porties). Hetzelfde ingrediënt uit twee componenten wordt één regel; hetzelfde ingrediënt in een andere eenheid juist niet, want gram en stuks kun je niet optellen zonder te gokken. **Nieuwe domeinmodule `mealComposition.ts`**: deterministische componentkeuze (geen `Math.random()`), met de zwaarste straf op "deze week al gekozen" (dat is de eis "dinsdag en vrijdag niet dezelfde combinatie"), een aflopende straf op recent gekozen, en een bonus voor wat al lang niet geweest is. Componenten gaan langs dezelfde harde-beperkingenfilter als recepten: valt een optie af door een allergie, dan verdwijnt alleen díé optie; valt de hele maaltijd weg, dan valt de dag terug op een gewoon recept in plaats van een halve maaltijd te tonen. **Bewust niet gedaan**: een samengestelde avond levert geen `MealSuggestion` en geen `FeedbackEvent` op — daar is geen receptvariant voor, en er een nepvariant voor verzinnen zou de geschiedenis vervuilen. Leren van componentkeuzes komt in werkpakket F; de keuzes zelf staan al vast in `MealPlanEntryComponent`. **Geverifieerd**: 23 nieuwe tests (10 voor de titel-/hoeveelheidsberekening inclusief ontdubbelen, 9 voor de componentkeuze inclusief determinisme en variatie, 4 integratietests op databaseniveau: dinsdag/vrijdag verschillen, hoeveelheden per persoon, ontdubbelen over twee avonden, en een allergie die alleen die ene optie weghaalt). `npm run verify` groen (430/430), `npm run test:e2e` 40/40. |

| Feature | Weekritme D — verdeelde avonden en boodschappen die je zelf haalt | **Schemawijziging (migratie `..._fulfillment_and_split_meals`)**: nieuwe enum `FulfillmentSource` (`PICNIC`/`OTHER_STORE`/`SELF_PROVIDED`), `ShoppingListLine.fulfillment` (standaard PICNIC, dus bestaande regels veranderen niet), `HouseholdIngredientFulfillment` ("biefstuk halen wij zelf" — één keer per huishouden vastgelegd in plaats van bij elk recept opnieuw), en `MealAssignment` + `MealAssignmentPerson` + `MealAssignmentItem` voor avonden waarop niet iedereen hetzelfde eet. **Herkomst end-to-end**: de lijstopbouw zet de herkomst per regel, `addShoppingListToPicnicCart` draagt alleen PICNIC-regels over, en /boodschappen toont een apart blok "Zelf halen (n)" met per regel of je hem elders koopt of zelf regelt. Het producttotaal dat naar Picnic gaat telt die regels niet meer mee. Zo'n regel blijft dus wél op de lijst staan (je moet hem nog steeds hebben) maar belandt nooit ongevraagd in het mandje — allebei nodig, want één van beide weglaten geeft ofwel een vergeten boodschap ofwel iets in je mandje dat je daar nooit wilde. **Verdeelde avonden** in `mealEntryNeeds`: staat er minstens één deel bij een avond, dan komt de behoefte daaruit. Porties per deel zijn de som van de porties van de aan dát deel toegewezen personen die op díé datum ook echt mee-eten (`DayPortionScale` geeft daarvoor nu `personPortions`). Een deel met `SELF_PROVIDED` levert niets op maar sluit de rest van de avond niet uit — dat is precies waarom verdeelde avonden bestaan. De titel wordt "Bolletjes met knakworst · Zelf iets regelen": er één naam van maken zou verbergen dat er twee dingen op tafel staan. **Scoring**: een gerecht met een extern ingrediënt scoort lager (en zwaarder bij een profiel met `preferPicnicComplete`, zoals de donderdagavond die bestellen moet vervangen), met een waarschuwing in de uitleg — maar wordt nooit uitgesloten. **Geverifieerd**: 12 nieuwe tests (7 voor verdeelde avonden incl. porties per deel, afwezige personen, ontdubbelen en het verschil OTHER_STORE/SELF_PROVIDED; 2 voor de scoring; 3 integratietests op databaseniveau voor de herkomst). Die laatste zijn bewust aangescherpt met een bevestigde productkeuze: zonder die keuze bestelt de app sowieso niets, en dan zou de test om de verkéérde reden slagen. `npm run verify` groen (442/442), `npm run test:e2e` 40/40. **Nog geen UI om zelf een verdeling of een herkomst in te stellen** — dat komt in werkpakket E; tot dan is dit gedrag alleen via de database te bereiken. |

| Feature | Weekritme E — de schermen: ritme instellen, sjablonen bouwen, snel corrigeren | Alles uit A t/m D was tot nu toe alleen via de database te bereiken. **Nieuw op /ons-gezin: "Weekritme per dag"** — per weekdag welk dagprofiel erbij hoort, welk samengesteld sjabloon (indien van toepassing), en wie er mee-eet. De oneven/even-varianten staan achter een eigen uitklap die alleen open staat als die dag ook echt wisselt: de meeste dagen zijn elke week hetzelfde, en drie identieke blokken per dag zou het scherm onleesbaar maken. Het formulier toont wat er daadwerkelijk gaat gelden (pariteit → elke week → standaard), niet wat er toevallig in de tabel staat. **Nieuw op /ons-gezin: "Samengestelde maaltijden"** — een sjabloon aanmaken en er per onderdeel (basis/vlees/groente/…) opties aan toevoegen, met de hoeveelheid per persoon. Bewust alleen bestaande ingrediënten kiesbaar (via een datalist): een nieuw ingrediënt aanmaken hoort bij het zoeken van een echt Picnic-product, anders ontstaat er een ingrediënt waar nooit een product bij gevonden wordt — de melding zegt dat ook zo. **Nieuw op /boodschappen**: per regel instellen waar het product vandaan komt (Picnic / andere winkel / zelf regelen). Dat wordt onthouden per ingrediënt voor het hele huishouden en meteen toegepast op de regels van deze week — behalve op regels die al in het mandje liggen, want die zijn al besteld en "zelf halen" tonen zou liegen. **Nieuw op /week**: "Anders met eten deze week" per dag — een uitzondering voor precies die datum, met een expliciete "terug naar het gewone ritme"-knop als er een uitzondering staat. Verandert bewust niets aan het weekritme zelf. **Na een ritmewijziging** wordt de weekplanning van déze week opnieuw gegenereerd, maar alleen als er nog niets van besteld is: liggen er al producten in het Picnic-mandje, dan blijft de week staan en gaat het nieuwe ritme vanzelf in vanaf volgende week. De melding zegt welke van de twee er gebeurd is. Alle nieuwe redirects hebben iets unieks in de URL (de dag, de weeksoort, het sjabloon-id) — anders slaat de router de navigatie over, zoals in werkpakket B is aangetoond. **Geverifieerd**: nieuwe e2e-stap "13" die het hele pad door de UI aflegt (profiel kiezen → dagregel in de database, iedereen uitvinken → aanwezigheidsafwijkingen in de database). `npm run verify` groen (442/442), `npm run test:e2e` 41/41. **Nog niet gebouwd**: een scherm om een avond te verdelen over gezinsleden (het datamodel en de logica staan er wél, getest in werkpakket D). |

| Feature + Bugfix | Weekritme F — leren van correcties, en de zelfreview | **Leren van aanwezigheid** (opdrachtpunt 13). Nieuwe module `presencePatterns.ts` plus twee enumwaarden (migratie `..._presence_learning`): een correctie voor één datum wordt geteld, maar verandert het weekritme **nooit** vanzelf. Pas bij de derde keer dezelfde correctie op dezelfde weekdag verschijnt er een leervraag ("De afgelopen 3 keer at Kai op woensdag niet mee. Zal ik dat zo in jullie weekritme zetten?"), en pas een uitdrukkelijk "ja" schrijft het patroon weg — naar de regel die élke week geldt, want oneven/even afleiden zou te veel geraden zijn. "Eet juist wél mee" en "eet niet mee" zijn bewust twee losse patronen: bij elkaar optellen zou drie keer heen en weer corrigeren tot een "gewoonte" maken. Een afgewezen patroon vraagt niet nog eens. De vraag krijgt op /week een echte ja-knop in plaats van de smaakredenen die bij maaltijdvragen horen. **Zelfreview (opdrachtpunt 20) — twee echte fouten gevonden en opgelost.** (1) **Datumopslag over tijdzones**: een datum-uitzondering werd als lokale middernacht opgeslagen, en Prisma bewaart in een `@db.Date`-kolom het UTC-datumdeel. Met `TZ=Europe/Amsterdam` tegen de echte database aangetoond: 11 september werd 10 september, waarna de uitzondering nooit meer werd teruggevonden. Onzichtbaar op Vercel (dat draait UTC), maar fout zodra iemand de app lokaal draait. Opgelost met `toCalendarDate` (schrijven) en `calendarDateKeyFromColumn` (lezen), met tests die de twee tegen elkaar controleren; geverifieerd in drie tijdzones. (2) **Dubbele `dayKeyForDate`**: identieke datumberekening in `orderDays.ts` én `presence.ts` — twee kopieën lopen vroeg of laat uiteen. Nu één definitie in `lib/week.ts`. **Verder gecontroleerd en in orde bevonden**: alle vier de routes naar een maaltijd (vast gerecht uit de dagregel, daggewoonte, "hetzelfde als vorige week", scoring) lopen door dezelfde harde-beperkingenfilter, net als de componentopties; geen N+1 in de planner (alles in één `Promise.all`, de dagenlus doet geen enkele query); elke nieuwe server-actie controleert het huishouden uit de sessie en zoekt sjablonen/opties/regels binnen dat huishouden op; de componentkeuze kan niet in een vaste uitkomst blijven hangen (deze week al gekozen kost 100 punten, recent gekozen loopt af); de Picnic-overdracht filtert alleen tóé en raakt de idempotentie niet. **Bewust blijven staan, expliciet gemeld**: er is nog geen scherm om een avond te verdelen, en de items van zo'n verdeling gaan (nog) niet door de harde-beperkingenfilter — dat moet gebeuren zodra dat scherm er komt, want dan pas kan er een onveilig deel ontstaan. `npm run verify` groen (450/450), `npm run test:e2e` 41/41. |

| Feature | Prijslaag P0 — prijs als waarneming in de tijd | Fundament voor de prijsvergelijking met Albert Heijn en Dirk. Geen zichtbare verandering, wel de enige stap die het schema raakt op een manier die later duur is om te herzien. **Schemawijziging (migratie `..._price_observations`)**: `ProductProvider` uitgebreid met `AH` en `DIRK` (de unieke sleutel op `Product` bevatte de provider al, dus dit is een uitbreiding en geen verbouwing), `Product.qualityTier` + `Product.gtin`, en de nieuwe tabel `PriceObservation` met van-prijs, eenheidsprijs, kortingsmechanisme, einddatum, waarnemingsmoment en bron. **Prijs is bewust een waarneming en geen producteigenschap**: dat maakt een vergelijking dateerbaar, maakt een nep-korting herkenbaar (was die van-prijs vorige maand ook al de prijs?) en voorkomt dat een verouderde prijs stilzwijgend als actueel op het scherm komt. `Product.price` blijft bestaan als laatst bekende prijs, zodat elk bestaand scherm ongewijzigd blijft werken. **Nieuw domein `src/domain/pricing/`**: `unitPrice.ts` normaliseert naar €/liter, €/kilo of €/stuk — nodig omdat AH melk per liter verkoopt en Dirk in dezelfde categorie vaak per halve liter, en twee prijzen naast elkaar dan niets zeggen; een multipack ("2 x 350 g") wordt correct als 700 gram gelezen, en wat niet ondubbelzinnig te lezen is geeft `null` in plaats van een gok. `qualityTier.ts` bepaalt de klasse (budget/standaard/premium/bio) uit merk, naam en keurmerken — met als kernregel dat twijfel `null` oplevert, want een klasse verzinnen om te kúnnen vergelijken is precies de fout die het equivalentiemodel moet voorkomen (het voorbeeld uit de opdracht: verse melk stilletjes vervangen door houdbare melk levert 34% "besparing" op die niemand gevraagd heeft). `types.ts` legt de provider-interface vast met een expliciet `capabilities`-blok (heeft barcode, heeft allergenen, heeft eenheidsprijs, kan bestellen, api of scrape) — werkende code, geen documentatie: de vergelijker gebruikt het om te weten wat hij mág concluderen. **Picnic achter dezelfde interface**, zodat de vergelijking alle winkels identiek behandelt; zoeken blijft bij `src/lib/picnic/*` omdat dat de huishoudsessie nodig heeft, en de provider gooit expliciet in plaats van stilzwijgend leeg terug te geven. **Direct effect**: elke keer dat de app al een Picnic-prijs ziet (bij het kiezen van een product) wordt dat nu ook een waarneming — de prijsgeschiedenis bouwt zich dus vanzelf op vanaf nu. **Geverifieerd**: 22 nieuwe tests (9 eenheidsprijs incl. het melkvoorbeeld uit de opdracht, 7 klassebepaling, 6 integratietests tegen de database: klasse en barcode opslaan, twee identieke prijzen leveren twee waarnemingen op, een oudere waarneming die later binnenkomt overschrijft de actuele prijs niet, actiegegevens blijven bewaard). `npm run verify` groen (472/472). **Niet vanuit de sandbox te verifiëren**: `api.ah.nl` en `www.dirk.nl` zijn hier geblokkeerd (403 op de proxy), net als de Picnic-API. De endpoints zijn door de gebruiker zelf geverifieerd op 28-08-2026; live gedrag is pas in productie te bevestigen. |

| Feature | Prijslaag P1 — Albert Heijn erbij | Eerste zichtbare resultaat van de prijslaag. **Netwerkkant** (`src/lib/pricing/ahClient.ts`): anoniem token (in-process gecached met een uur marge — bewust geen tabel voor iets wat vanzelf verloopt), zoeken met de verplichte `X-Application`-header, en het detailscherm voor barcode/allergenen/keurmerken. Gooit bij een storing in plaats van een lege lijst terug te geven: "geen resultaten" en "de winkel is onbereikbaar" mogen nooit hetzelfde betekenen, anders lijkt AH ineens gratis. **Leeskant** (`src/domain/pricing/providers/ahProvider.ts`) bewust apart, zodat die zonder internet te testen is. Twee dingen die daar echt fout kunnen gaan zijn expliciet afgevangen: een `priceBeforeBonus` gelijk aan de huidige prijs wordt géén van-prijs (AH vult dat veld ook zonder actie, en "van €1,29 voor €1,29" suggereert een korting die er niet is), en een resultaat zonder id/prijs/naam wordt overgeslagen in plaats van op nul gezet. **Matching** (`storeMatch.ts`): een voorzichtige, uitlegbare naamdekking met drempel — liever geen match (die wordt zichtbaar getoond) dan een verkeerde (die verdwijnt in een totaalbedrag). Onderweg door een test gevonden en verholpen: een bioproduct drong voor bij het gewone product omdat beide de ingrediëntnaam volledig dekten; de rangschikking kijkt nu ook naar hoeveel woorden een productnaam extra heeft. **Dagelijkse verversing** (`refresh.ts` + `/api/cron/prices`, 05:00 in `vercel.json`): alleen ingrediënten uit het eigen receptenboek en de vaste boodschappen, met 350 ms tussen de aanroepen, en stoppen na tien fouten op rij. Nul producten is een fóút met HTTP 500, geen lege uitslag — anders staat de cron op groen terwijl de prijzen bevriezen. Nooit tijdens het laden van een pagina. **Zichtbaar**: per regel op /boodschappen staat nu de AH-prijs mét verpakkingsgrootte (een bedrag zonder verpakking zegt niets als de ene winkel liters en de andere halve liters verkoopt), een eventueel actielabel, en een waarschuwing als de prijs ouder is dan een dag. **Extra opbrengst**: `Product.freeFromAllergens` (migratie `..._product_free_from_allergens`) met AH's gestructureerde allergeeninformatie — alleen `FREE_FROM` telt, want "kan sporen bevatten" is bij een allergie geen garantie. Wordt alleen als **bevestiging** getoond bij een harde regel van het huishouden ("Albert Heijn bevestigt: vrij van vis"), nooit als waarschuwing: het ontbreken van die garantie betekent niet dat er vis in zit. **Geverifieerd**: 21 nieuwe tests (8 AH-ontleding tegen de geverifieerde antwoordvormen, 7 matching, 6 integratietests voor de verversing: alleen gebruikte ingrediënten, waarnemingen opslaan, geen match wordt geteld, tien fouten op rij stopt de rest, één fout niet, en een ingrediënt zonder waarneming ontbreekt in plaats van nul te kosten). `npm run verify` groen (493/493), `npm run test:e2e` 41/41 (één run faalde op de bekende wisselende stap 2c, de herhaling was groen). **Geen nieuwe environment-variable nodig**: de cron gebruikt dezelfde `CRON_SECRET` als de herinneringen. **Nog niet live te verifiëren**: `api.ah.nl` is vanuit deze omgeving geblokkeerd; het echte gedrag blijkt pas bij de eerste cron-run in productie. |

| Feature | Prijslaag P2 — het mandje doorrekenen | Hiermee is de oorspronkelijke vraag beantwoord: wat kost deze lijst bij Albert Heijn? Bewust een **mandje-simulatie en geen prijsvergelijker** — je koopt verpakkingen, geen liters, dus 3 liter melk is bij de ene winkel 3 pakken en bij de andere 6 halve pakken, en dan is het lagere bedrag op het schap het duurdere mandje. **Nieuw scherm `/prijzen`**, bereikbaar via één regel onder de boodschappenlijst. **Drie bedragen, nooit één**: wat de lijst bij Picnic kost, wat dezelfde regels bij AH kosten (alleen identieke en gelijkwaardige producten), en wat het wordt als je ook alternatieven meerekent — met daarnaast altijd hoeveel regels *niet* meetellen. Een winkel die een product niet heeft krijgt geen nul maar een ontbrekende regel; zonder dat onderscheid lijkt de winkel met het kleinste assortiment de goedkoopste. **De belangrijkste correctie in dit werkpakket**: `hardTotal` mag alleen naast een eigen totaal over precies dezelfde regels staan (`referenceTotalForHardLines`). Anders zou "€3,87 bij AH tegenover €8,70 bij Picnic" als halvering op het scherm komen terwijl er in werkelijkheid gewoon een product ontbrak — met een test vastgelegd. Kennen we onze eigen prijs voor een regel niet, dan telt die regel aan geen van beide kanten mee (de prijs blijft wel op de regel staan), want alleen de winkelkant optellen scheeft dezelfde vergelijking de andere kant op. **Equivalentie in vier niveaus** (`equivalence.ts`): zelfde barcode, zelfde merk en verpakking, zelfde klasse en soort, of een alternatief. Twee echte fouten die de eigen tests aantoonden en die zijn opgelost: de vers/houdbaar-controle stond ná de merk-en-verpakking-regel, waardoor "AH Verse halfvolle melk 1 l" en "AH Houdbare halfvolle melk 1 l" als identiek golden — precies het paar waar de opdracht mee opent; en het zoeken op woorddelen liet "houdbare" niet meetellen terwijl "diverse" wel als "vers" gold (nu woordstammen). **Correctie per regel** (schemawijziging, migratie `..._household_store_product_choice`): je kunt zelf een ander winkelproduct aanwijzen, en dat wordt per huishouden per winkel onthouden — bewust een aparte tabel naast `HouseholdProductPreference`, want die gaat over wat er besteld wordt (altijd Picnic) en deze alleen over waarmee we vergelijken. Onderweg gevonden: de klasse van ons eigen product stond nooit in de database (alleen de prijsverversing vult dat veld), waardoor élke regel "niet te vergelijken" zou heten; die wordt nu bij het vergelijken afgeleid, en blijft `null` — dus eerlijk onvergelijkbaar — als ons product geen merk heeft. **Bestellen verandert niet**: dit scherm geeft alleen inzicht, het Picnic-mandje en de bevestiging blijven exact zoals ze waren. **Geverifieerd**: 12 nieuwe tests op de doorrekening (verpakkingen in plaats van liters, halve liters die duurder uitpakken, een alternatief dat alleen in het tweede bedrag telt, gelijkwaardig boven goedkoop-maar-anders, ontbrekend is geen nul, onbekende verpakking geeft geen bedrag, het overschot, ongelijke mandjes) plus e2e-stap 14 die het scherm en de correctie via de echte pagina doorloopt. **Uit de code-review gekomen en verwerkt vóór de merge**: een handmatige correctie werd als filter over de shortlist van de matcher toegepast, waardoor een gekozen product dat buiten de top 6 viel stilzwijgend verdween en de regel als "niet gevonden" toonde — de keuze wordt nu rechtstreeks opgehaald en overrulet de shortlist; het alternatieve bedrag stond naast het harde referentiebedrag dat over minder regels gaat (nu een eigen referentiebedrag over dezelfde regels); vaste en handmatige regels in stuks zijn een door de gebruiker gekozen aantal verpakkingen en geen hoeveelheid ("2x brood" werd één verpakking in plaats van twee); een verpakking in een andere eenheid dan de regel levert nu een reden op in plaats van een bedrag; en de e2e-stap controleert nu dat de correctie de doorrekening ook echt verandert in plaats van alleen dat er een rij in de database staat. `npm run verify` groen (518/518), `npm run test:e2e` 42/42. **Migratie draait vanzelf mee** met de Vercel-build; geen handmatige actie nodig. |


| Feature | Prijslaag P3 — Dirk erbij | De kwetsbaarste koppeling, bewust als laatste winkel: de vergelijking werkte al voordat hier iets aan gehangen werd. **Dirk heeft geen API en geen bruikbare zoekfunctie** (de zoekpagina laadt client-side), dus de aanpak is anders dan bij Albert Heijn: één keer per dag de categoriepagina's crawlen naar een eigen index, en daarna lokaal zoeken. Dat is meteen verkeersvriendelijker dan per ingrediënt een pagina ophalen. De categorieën komen uit de overzichtspagina zelf en niet uit een vaste lijst in de code — die zou stilzwijgend verouderen en dan minder producten opleveren zonder dat iemand het merkt. **De valkuil van deze site** is de prijs: die staat gesplitst in `price-large` + `price-small`, en de klasse `hasEuros` bepaalt of het eerste getal hele euro's zijn of dat de hele prijs in centen staat. Zonder die klasse mee te wegen leest "89 cent" als € 89,00 — met een unittest én een integratietest tot in de database vastgelegd. **Geen barcode en geen allergenen**: dat staat expliciet in het `capabilities`-blok, en de app gebruikt dat ook echt — een Dirk-match kan nooit "identiek op barcode" heten, en op /prijzen staat bij Dirk "van de website gelezen — kan een dag achterlopen als de pagina verandert". Het merk wordt alleen herkend als het een huismerk is (`1 de Beste`, `G'woon`); het eerste woord van een productnaam als merk nemen zou "Verse" tot merk maken, en een verzonnen merk werkt door in de klassebepaling. **Nul producten is een storing, geen lege winkel** (het uitdrukkelijke risico uit de opdracht): de crawl gooit dan, de eerder opgeslagen prijzen blijven staan, en de cron geeft HTTP 500 — ook wanneer de crawl al vóór de ingrediënten afbreekt, wat de bestaande controle nog niet ving. Op /prijzen krijgt een winkel waar we vandaag niets van weten geen kolom per regel maar één eerlijk bericht ("dat is geen € 0"). **Alleen wat bij onze eigen ingrediënten past wordt bewaard** — de rest van het assortiment zou de database vullen met producten die niemand ooit ziet. **Geverifieerd**: 15 nieuwe tests (10 op het lezen van de pagina, 5 integratietests tegen een lokaal nagebouwde Dirk-site: crawlen en filteren, de centenprijs, nul producten, geen categorieën, en dat een mislukte verversing niets weggooit). `npm run verify` groen (533/533), `npm run test:e2e` 42/42. **Geen migratie en geen nieuwe environment-variable nodig** (`DIRK_BASE_URL` bestaat alleen om in tests een nagebouwde site aan te wijzen). **Niet live te verifiëren vanuit deze omgeving**: `www.dirk.nl` is hier geblokkeerd, net als AH en Picnic. De regels uit de opdracht zijn correct toegepast, maar of de opmaak van de site nog steeds zo is blijkt pas bij de eerste cron-run in productie — precies waar de "nul producten is een fout"-bewaking voor bedoeld is. |


| Feature | Prijslaag P4 — acties en historie | **Een kortingslabel is geen prijs.** "1+1 gratis" bij drie stuks is 33% korting, niet 50%, en bij één stuk helemaal geen. Nieuw domein `promotions.ts` rekent het mechanisme uit voor het aantal verpakkingen dat je écht nodig hebt: `1+1`/`2+1 gratis`, `2e halve prijs`, `3 voor 2`, `2 voor € 3,00` en een percentage. Het bedrag op het scherm klopt daarmee met de kassabon in plaats van met het bordje. **Kernregel: een mechanisme dat we niet zeker herkennen wordt niet toegepast** — dan blijft de gewone prijs staan en zie je het label er los bij. Een verzonnen korting maakt het bedrag fout; een gemiste korting maakt het alleen voorzichtig. Een verlopen actie telt niet mee, en bij een aantal waarbij de actie niets oplevert staat dat er eerlijk bij ("je krijgt pas voordeel vanaf 2 stuks"). **Nep-kortingen herkennen** (`priceHistory.ts`): het patroon waarbij een winkel de prijs een week omhoog zet en de gewone prijs daarna een "actie" noemt. Met de prijsgeschiedenis is dat zichtbaar — een van-prijs die in de afgelopen 60 dagen nooit gerekend is, bij een prijs die gewoon de normale prijs is, levert de melding "die van-prijs is hier de afgelopen weken niet gerekend" op. Drie mogelijke uitkomsten, en **onbekend is er nadrukkelijk één van**: bij minder dan vier waarnemingen zegt de app niets, in plaats van de winkel op zijn woord te geloven óf 'm ten onrechte van bedrog te betichten. De "gewone prijs" is de mediaan en niet het gemiddelde — één actieweek trekt een gemiddelde scheef. **Attentie op vaste boodschappen**: op de boodschappenlijst staat nu welke producten deze week echt in de actie zijn, met wat het scheelt, en vaste boodschappen eerst — die koop je elke week, dus daar telt een actie zwaarder. Alleen acties die bij het benodigde aantal daadwerkelijk geld schelen komen erin, en een nepkorting nooit; anders wordt de attentie een reclamebalk. **Geverifieerd**: 20 nieuwe tests (8 op de mechanismen inclusief de "1+1 bij één stuk"-val en het verschil tussen "2 voor 3.00" als bedrag versus als aantal, 8 op de geschiedenis en het nepkorting-oordeel, 3 op de doorwerking in het mandje, 1 integratietest die het prijsverloop uit de database haalt en er het oordeel op loslaat). `npm run verify` groen (553/553), `npm run test:e2e` 42/42. **Geen migratie nodig** — `PriceObservation` had alle velden al; dit is precies de opbrengst van de keuze in P0 om prijs als waarneming in de tijd te modelleren. **Bewust niet gebouwd**: de aanbiedingenpagina van Dirk (`/aanbiedingen`) wordt nog niet gelezen. De opdracht beschrijft wél dát daar van-prijs en actieprijs staan, maar niet in welke opmaak — en anders dan bij de categoriepagina's zou ik die opmaak moeten verzinnen. Voor AH werkt de actie-informatie wel volledig (die komt gestructureerd mee sinds P1). |


| Feature | Prijslaag P5 — het weekmenu kijkt mee | Aanbiedingen wegen nu mee bij het kiezen van een gerecht, **met een plafond**. Dat plafond is de hele kern van dit werkpakket: 16 punten in totaal, tegenover 16 punten per passend dagprofiel-kenmerk (en dat zijn er vaak meerdere), 14 tot 35 voor een persoonlijke voorkeur en 120 voor "wil dit nooit eten". Een aanbieding kan dus wél de doorslag geven tussen twee gerechten die verder gelijk scoren, en kan nooit een gerecht opdringen dat niet bij de avond of bij het gezin past — met een test die aantoont dat drie aanbiedingen verliezen van één "liever niet". **De reden is zichtbaar**: "kipfilet is deze week in de bonus bij Albert Heijn" komt gewoon in de uitleg op de dag te staan, naast de andere redenen. **Alleen échte acties tellen mee** (`offers.ts`), met drie filters die er allemaal zijn om te voorkomen dat dit een reclamebalk wordt: de actie moet nog lopen, de van-prijs moet volgens de prijsgeschiedenis kloppen (de nepkorting-detectie uit P4), en er moet bij het benodigde aantal daadwerkelijk geld bespaard worden — "1+1 gratis" op iets waarvan je er één nodig hebt levert niets op. **Achterwaarts compatibel**: een huishouden zonder prijsgegevens krijgt exact dezelfde planning als voorheen, met een test die de scores vergelijkt. Gaat het ophalen van aanbiedingen mis, dan plannen we gewoon zonder — een aanbieding is een extraatje, geen voorwaarde. Leest alleen opgeslagen prijzen; de weekplanning wacht nooit op een winkel. **Geverifieerd**: 4 nieuwe tests (3 op de scoring: de doorslag geven, verliezen van een afkeur, en niets veranderen zonder aanbiedingen; 1 integratietest tegen de database die aantoont dat dezelfde actie mét een kloppende prijsgeschiedenis wél als aanbieding telt en met een verzonnen van-prijs niet). `npm run verify` groen (557/557), `npm run test:e2e` 42/42 (één run faalde op de bekende wisselende stap 4b — een klik die niet registreert, ongerelateerd aan prijzen; de herhaling was groen). **Geen migratie en geen actie van de gebruiker nodig.** |


| Feature | Prijslaag P6 — splitsen en inslaan | Het laatste werkpakket van de prijslaag, en het meest terughoudende: allebei de adviezen zwijgen standaard. **Splitsingsadvies** (`splitAdvice.ts`): "deze 6 producten bij Dirk halen scheelt € 8,40 — dat is dan wel een tweede winkel". Alleen boven een drempel van € 5, want splitsen kost een extra rit of een tweede bestelling; een advies dat elke week verschijnt met € 0,40 leert de gebruiker om het te negeren. Rekent uitsluitend met identieke en gelijkwaardige producten — een "besparing" die je krijgt door iets anders te kopen is er geen — en noemt precies wélke producten het betreft in plaats van één totaalbedrag te beloven. **Hamsteradvies** (`stockUpAdvice.ts`): alleen bij een echte korting ten opzichte van de normale prijs uit het prijsverloop, alleen bij iets wat lang goed blijft, nooit meer dan 3 extra verpakkingen, en niet als er al genoeg in huis is — dat laatste komt rechtstreeks uit het voorraadmodel. De faalwijze die dit moet voorkomen is concreet: een kast vol met zes potten pindakaas omdat ze in de aanbieding waren, waarvan de helft over de datum gaat. Weggegooid eten is de duurste besparing die er is. Vier redenen om te zwijgen, elk met een eigen test. **Open punt uit de opdracht, bewust niet zelf ingevuld**: of Dirk bij ons bezorgt of dat het zelf halen is bepaalt uiteindelijk waar de splitsingsdrempel hoort te liggen. Tot dat bekend is staat er een bewust conservatieve € 5, met die afweging in de code opgeschreven. **Geverifieerd**: 9 nieuwe tests (4 op het splitsingsadvies: zwijgen onder de drempel, een concreet advies erboven, een alternatief telt niet mee, en zonder eigen prijs valt er niets te adviseren; 5 op het hamsteradvies: geen normale prijs, niet vers, kast al vol, het maximum, en een paar cent is geen advies). `npm run verify` groen (566/566), `npm run test:e2e` 42/42 — twee runs op rij groen, nadat één run twee wisselende stappen liet vallen (2c en een stap in `shoppingListAccess`, allebei het bekende "klik registreert niet"-patroon, ongerelateerd aan prijzen). **Geen migratie en geen actie van de gebruiker nodig.** |


| UX | Prijslaag P7 — de drie winkels naast elkaar | Op verzoek van de gebruiker: Picnic was tot nu toe "de referentie" in een losse alinea onder de winkels, terwijl de vraag "wat kost dit hier, en daar?" vraagt om bedragen náást elkaar. **Nieuw domeinstuk `lineComparison.ts`** maakt van een doorgerekende regel een rij cellen — Picnic voorop, dan AH en Dirk — met de goedkoopste gemarkeerd. Twee regels die daarbij niet mogen sneuvelen, allebei met een test: een **alternatief wordt nooit gekroond** als goedkoopste (goedkoper door iets anders te kopen is geen besparing, ook al is het bedrag lager), en met **maar één bekend bedrag wordt er niets gemarkeerd** — "het goedkoopst" met één deelnemer is geen vergelijking. **Op `/prijzen`**: de totalen staan nu in één tabel met een kolom per winkel (hetzelfde-of-gelijkwaardig, over hoeveel regels, wat Picnic voor diezelfde regels kost, en met alternatieven erbij), en elke regel opent met drie prijsvakjes naast elkaar. De details en de correctie zijn naar één uitklapblok verhuisd — bewust één niveau, want wie "welke producten" opent wil de alternatieven meteen zien in plaats van er nog een keer voor te klikken. **Op `/boodschappen`**: onder elke productregel staat nu "Picnic € 1,27 · Albert Heijn € 1,19 · Dirk € 1,09" met de goedkoopste gemarkeerd, gerekend over wat je deze week nodig hebt (niet de losse verpakkingsprijs — dat zou halve liters tegen liters afzetten). Een winkel zonder prijs staat er niet tussen, nooit als € 0, en een ander soort product krijgt dat er in gewone taal bij. **Onderweg gevonden in de eigen e2e-test**: de assertie sloeg aan op het bedrag in de keuzelijst in plaats van op de vergelijkingscel, waardoor de test groen kon zijn zonder dat de correctie iets deed — de cellen hebben nu een eigen `data-store-cell`, en de test controleert die. **Geverifieerd**: 7 nieuwe tests op het naast-elkaar-zetten. `npm run verify` groen (573/573), `npm run test:e2e` 42/42, twee runs op rij. **Geen migratie en geen actie van de gebruiker nodig.** |


| Feature + Bugfix | Prijzen zelf verversen, met zichtbare uitslag | Op verzoek van de gebruiker, en het loste een echt gat op: om de prijzen één keer op te halen moest je een servergeheim uit het Vercel-dashboard vissen en met de terminal in de weer — terwijl de gebruiker het project niet eens lokaal heeft staan. **Nieuw op `/prijzen`: een knop "Prijzen nu verversen"**, alleen voor een ingelogd huishouden, met daarboven per winkel wanneer er voor het laatst is opgehaald en wat dat opleverde. **Schemawijziging** (migratie `..._price_refresh_runs`): tabel `PriceRefreshRun` met per verversing één regel per winkel. De regel wordt **vóór** het werk aangemaakt en daarna bijgewerkt — dat lost twee dingen tegelijk op: een verversing die door de hostingpartij wordt afgekapt laat een zichtbaar spoor achter ("de verversing van 14:12 is niet afgerond") in plaats van niets, en een lopende regel is meteen het slot tegen een tweede gelijktijdige verversing (twee tabbladen zouden anders de zorgvuldig ingebouwde pauze tussen de winkelaanvragen verdubbelen, en een blokkade valt dan op de nachtelijke taak). De nachtelijke cron schrijft dezelfde regels weg. **Afgemeten en eerlijk daarover**: de knop doet 15 ingrediënten en 6 Dirk-categorieën, met die beperking letterlijk op het scherm — een knop die "alles bijgewerkt" suggereert terwijl hij een deel doet, is precies de belofte die deze app niet hoort te maken. **Uit de code-review gekomen en vóór het mergen verwerkt**: (1) het tijdstip stond in servertijd, waardoor de cron van 05:00 UTC structureel "om 05:00" zou melden terwijl de klok 07:00 aanwijst — nu `Europe/Amsterdam` (de bestaande projectconventie), met een test die de tijdstring zelf controleert; (2) één mislukt ingrediënt van de vijftien leverde de melding "het ophalen ging mis" op terwijl er zeventig producten waren bijgewerkt — het aantal staat nu vóórop en de fout is de nuance, met een test voor "deels gelukt"; (3) een groene "klaar"-balk verscheen ook als beide winkels niets opleverden — nu een aparte, oranje melding; (4) de laatste run werd met één query en een geraden aantal rijen opgehaald, waardoor een los ververste winkel stilzwijgend uit beeld kon vallen — nu een gerichte query per winkel, met een test die dat aantoont; (5) knop en statusregels stonden in de tak voor een gevulde boodschappenlijst, terwijl juist een nieuw huishouden zonder lijst ze nodig heeft. **Ook opgelost: een wisselende e2e-stap.** Stap 14 viel er regelmatig uit doordat hij op een klik-en-navigatie wachtte; hij wacht nu tot de server de keuze daadwerkelijk heeft opgeslagen en laadt daarna opnieuw. Twee volledige runs op rij groen. **Geverifieerd**: 8 tests op het vastleggen en verwoorden van verversingen. `npm run verify` groen (581/581), `npm run test:e2e` 42/42. **De migratie draait vanzelf mee met de Vercel-build**; geen handmatige actie nodig. |


| Bugfix | Eerste echte productiegebruik van de prijslaag — drie fouten eruit | De gebruiker gebruikte de knop "Prijzen nu verversen" in productie en stuurde een schermafbeelding. **Goed nieuws**: de Albert Heijn-koppeling werkt écht (21 producten opgehaald), en de Dirk-crawler is niet stuk — hij las de site, maar vond niets dat bij de ingrediënten paste. **Drie fouten die alleen bij echt gebruik zichtbaar werden.** (1) **De afgemeten verversing pakte de verkeerde ingrediënten**: alfabetisch de eerste vijftien, terwijl de gebruiker naar zijn boodschappenlijst kijkt. Resultaat: één AH-prijs op vijftien regels. `getPricedIngredients` kan nu de ingrediënten van de boodschappenlijst van dít huishouden vooraan zetten, en de knop gebruikt dat — de nachtelijke taak doet toch alles, dus die verandert niet. (2) **Het scherm sprak zichzelf tegen**: "Van Albert Heijn hebben we nog geen prijzen" stond boven regels waar wél een AH-bedrag stond. De melding keek naar `linesCompared` (alleen identiek/gelijkwaardig) maar zei "geen prijzen". Nu onderscheidt hij "we hebben wel prijzen, maar niets gelijkwaardigs" van "we hebben niets". Meteen ook de ontbrekende spatie in "Albert Heijnhebben" opgelost door de zin als één geheel op te bouwen in plaats van uit losse tekstdelen. (3) **"Geen passend product" was niet te onderscheiden van "de koppeling is stuk"** — precies het onderscheid dat je nodig hebt om te weten wat er moet gebeuren. Nieuwe kolom `itemsSeen` (migratie `..._price_refresh_items_seen`) legt vast hoeveel producten een winkel überhaupt opleverde vóór het matchen, en de melding gebruikt dat: "342 producten van de winkel gelezen, maar geen daarvan paste bij 15 ingrediënten van je lijst" tegenover "niets van de winkel teruggekregen". **Wat géén fout bleek**: dat AH-melk van € 0,79 als "ander soort" naast Picnic-melk van € 1,19 staat. Dat is houdbare melk tegenover verse — letterlijk het voorbeeld waar de opdracht mee opent, en het equivalentiemodel dat doet wat het moet doen. **Geverifieerd**: 2 nieuwe tests op het onderscheid tussen "wel gelezen, niets gematcht" en "niets teruggekregen". `npm run verify` groen (581/581), `npm run test:e2e` 42/42. **De migratie draait vanzelf mee met de Vercel-build**; geen handmatige actie nodig. |


| Feature + Bugfix | Vergelijken op soort in plaats van op merk, en de matcher die "Picnic" negeert | Uit productiegebruik bleek dat de meeste regels bij Albert Heijn "niet gevonden" of "soort product niet vast te stellen" gaven. Twee oorzaken, allebei opgelost. **(1) Onze eigen ingrediëntnamen bevatten de winkelnaam.** Ze komen uit Picnic-producten en heten daardoor letterlijk "Picnic Appelmoes". De matcher eiste dat álle betekenisdragende woorden terugkwamen, dus "AH Appelmoes" dekte maar de helft en viel onder de drempel — terwijl het product er gewoon ligt. `picnic` staat nu bij de ruiswoorden (naast `ah`, `albert`, `heijn`, `dirk`, die er al stonden), en er wordt bij de winkel gezocht met de opgeschoonde naam via `storeSearchTerm`. **(2) Bijgestuurde productregel van de gebruiker: "als het qua product maar vergelijkbaar is — verse melk = verse melk, merk maakt dan niet uit".** Het equivalentiemodel wees een onbekende klasse eerder af als "niet vergelijkbaar", en omdat onze eigen producten zelden een merkveld hebben viel er daardoor nergens iets te vergelijken. Nu geldt: een bekend verschil in klasse (bio, voordeelmerk, premium) blijft een alternatief, de vers/houdbaar-controle blijft onverkort staan, maar een onbekend merk staat gelijkwaardigheid niet meer in de weg. "Niet vergelijkbaar" blijft over voor het enige geval waarin er echt niets te zeggen valt: we weten niet wat wíj zouden kopen. Aanvullend leest `deriveQualityTier` de winkelnaam vooraan in een productnaam als huismerk ("Picnic Hagelslag" ís de hagelslag van Picnic) — alleen vooraan, en alleen voor de winkel waar het product vandaan komt. **Uit de code-review verwerkt vóór het mergen**: (a) de prioritering van "verversen nu" keek naar álle weeklijsten ooit in plaats van naar deze week, waardoor de geschiedenis de lijst van nu alsnog verdrong — nu afgebakend op het huidige weekplan, met een integratietest die met vier oude lijsten aantoont dat de ingrediënten van deze week binnen de eerste vijftien vallen; handmatige en voorraadregels tellen nu ook mee als kandidaat; (b) de nieuwe schermmelding was zelf in twee gevallen onwaar (een "niet te vergelijken"-regel werd "ander soort" genoemd, en een gelijkwaardige regel zonder eigen prijs werd "geen gelijkwaardig product") — de reden wordt nu uit de regels zelf afgeleid in `describeUncomparableStore`, met vier tests; (c) `itemsSeen` was `NOT NULL DEFAULT 0`, waardoor bestaande rijen "de winkel gaf niets terug" zouden melden terwijl er nooit gemeten was — kolom is nu nullable en `null` betekent onbekend; (d) het aantal producten wordt niet meer genoemd (bij AH is het de som van zoekresultaten, bij Dirk de omvang van de catalogus — twee grootheden onder één noemer), alleen het ondérscheid; (e) "van je lijst" staat er niet meer bij een nachtelijke run, die gaat over de hele catalogus. **Geverifieerd**: 11 nieuwe/aangepaste tests. `npm run verify` groen (592/592), `npm run test:e2e` 42/42. **Migratie draait vanzelf mee met de Vercel-build.** |


| Bugfix | Aantallen omrekenen tussen verpakkingen, en samenstellingen met of zonder spatie | Twee fouten uit productiegebruik, allebei door de gebruiker gemeld met een schermafbeelding. **(1) "3x Alpro" werd drie vierpakken.** Picnic rekende € 8,97 voor 3 stuks, de app zette daar € 32,94 bij Albert Heijn naast: drie keer een vierpak van € 10,98. Dat koopt er twaalf terwijl je er drie wilde. Oorzaak: bij een vaste of handmatige regel in stuks is de hoeveelheid een aantal verpakkingen, en dat aantal gaat over ónze verpakking — het werd klakkeloos op de winkelverpakking losgelaten. Nu gaat het aantal eerst door onze eigen verpakkingsinhoud heen naar een echte hoeveelheid, en pas daarna door die van de winkel: 3 stuks tegenover een vierpak is één pak van € 10,98 met één stuk over. Kennen we onze eigen verpakkingsinhoud niet, dan zegt de app "verpakkingen niet te vergelijken" in plaats van een bedrag dat er twaalf koopt. Eén stuk nodig blijft gewoon één verpakking bij elke winkel — daar is "koop er daar ook één" precies wat je wilt, en de verpakkingsgrootte staat ernaast. **(2) "Allesreinigerdoekjes" vond "Allesreiniger doekjes" niet.** Winkels schrijven samenstellingen verschillend; op letterniveau vond de ene vorm de andere niet en meldde het scherm "niet gevonden" terwijl het product er gewoon ligt. De matcher vergelijkt nu ook zonder spaties, in beide richtingen. Dat voegt geen enkele overeenkomst toe die niet uit precies dezelfde letters bestaat — een test legt vast dat "Appelmoes" nog steeds niet op "Bananen" matcht. **Geverifieerd**: 5 nieuwe tests, waaronder de Alpro-casus met de echte bedragen uit de schermafbeelding. `npm run verify` groen (597/597), `npm run test:e2e` 42/42 (twee runs lieten elk een andere, ongerelateerde stap vallen — het bekende "klik registreert niet"-patroon; de derde run was groen). **Geen migratie en geen actie van de gebruiker nodig.** |


| Feature | Meer productinformatie, een link naar de winkel, en de aanbieding zichtbaar in het overzicht | Op verzoek van de gebruiker: *"ik zou van alle drie de winkels meer informatie of eventueel een linkje willen hebben naar de site zodat ik kan vergelijken of zeker kan weten of ze hetzelfde zijn. Ook wil ik kunnen zien of het een aanbieding is."* De app zegt "gelijkwaardig" — nu kun je dat zelf nakijken. **Drie dingen tegelijk.** (1) **Een link naar de productpagina van de winkel.** **Schemawijziging** (migratie `..._product_url`): nieuwe, lege kolom `Product.productUrl`. Bij Albert Heijn is de link uit het webshop-id te vormen, bij Dirk lezen we hem van de categoriepagina die we toch al crawlen. Voor Picnic bestaat er geen publieke productpagina — daar blijft de link dus leeg in plaats van een gokje dat op een foutmelding uitkomt. De link staat op drie plekken: bij de productnaam in de compacte drie-kolomsweergave, als "Bekijk bij Albert Heijn" in het uitklapblok, en bij elk alternatief in de keuzelijst — juist bij het kiezen wil je kunnen nakijken wát je kiest. (2) **Meer productinformatie, voor alle drie de kolommen gelijk.** Naam, merk, verpakkingsgrootte en **prijs per liter/kilo** staan nu ook bij Picnic, zodat de kolommen dezelfde velden tonen. Die eenheidsprijs is het enige getal dat over verpakkingsgroottes heen vergelijkt: € 1,90 voor een halve liter is duurder dan € 1,29 voor een hele, en dat zie je pas zo. Voor Picnic wordt hij afgeleid uit de opgeslagen verpakkingsinhoud, met de verpakkingstekst als vangnet; is geen van beide bekend, dan staat er niets in plaats van een geraden getal. (3) **De aanbieding zichtbaar in het overzicht zelf**, niet pas na uitklappen: een "Actie"-markering in de cel op `/prijzen` en "· actie" op `/boodschappen`, met de einddatum erbij waar de winkel die meegeeft. **Bewust géén markering bij een van-prijs die de prijsgeschiedenis tegenspreekt** — die stond al als nep-korting in de gegevens, en een groene "Actie" zou daar een reclame van maken in plaats van een hulpmiddel. **Twee dingen die pas door het zichtbaar maken zelf naar boven kwamen, en meteen meegenomen.** (a) **Een verlopen actie stond nog als actie op de regel.** De korting werd al niet meer gerekend, maar het label bleef staan — zolang dat alleen in het uitklapblok stond was het hooguit rommelig; als "Actie" in het overzicht zou het een aanbieding beloven die er niet is. Het label verdwijnt nu zodra de einddatum voorbij is. Geeft de winkel geen einddatum, dan telt de actie gewoon als lopend: niets zeggen is geen bewijs van het tegendeel. (b) **Een link is externe data die als `href` op ons eigen scherm komt.** Bij Dirk wordt hij uit gescrapete HTML gelezen; een gewijzigde pagina zou daar een adres van een andere site — of een `javascript:`-adres — in kunnen zetten. Dirk-links worden nu alleen gevormd als het ondubbelzinnig een adres op dirk.nl is, en er staat een tweede horde vlak vóór het scherm (`displayableProductUrl`) die alles doorlaat behalve `http`/`https`. Die tweede horde is er omdat een rij in de database ook uit een oudere versie of een handmatige ingreep kan komen. **Uit de code-review gekomen en vóór het mergen verwerkt.** (1) **Een actie die bij dít aantal niets oplevert kreeg toch een markering.** "1+1 gratis" bij één pak melk is geen voordeel — de doorrekening rekende de korting terecht al niet mee, maar het overzicht zette er wel "Actie" boven. De attentielijst hanteerde die grens allang ("anders wordt het een reclamebalk"); de markering volgt nu dezelfde regel, en die regel staat op één plek: de doorrekening bepaalt of een actie meetelt, de weergave heeft er geen tweede, net iets andere versie van. Het label zelf blijft wél in het uitklapblok staan, mét de uitleg "je krijgt pas voordeel vanaf 2 stuks" — dát is informatie. Een gewone bonusprijs (korting zit al in de prijs) telt wél mee, op grond van de van-prijs. (2) **De nieuwe eenheidsprijs van de Picnic-kolom was bij multipacks een factor N te hoog.** De opgeslagen verpakkingsinhoud kent geen "6 x 1 liter" en bewaart daar 1000 — prima om verpakkingen mee te tellen, maar als €/liter leverde het € 6,00 op pal naast een correcte € 1,00 van Albert Heijn, en juist dat getal is op het scherm neergezet als hét getal dat over verpakkingsgroottes heen vergelijkt. Alle drie de kolommen lezen de verpakking nu met dezelfde functie. (3) **Twee eenheidsprijzen in verschillende eenheden staan niet meer naast elkaar** in de smalle cel ("per stuk" tegenover "per kilo" scheelt daar één woordje); in het uitklapblok, met ruimte eromheen, blijven ze wel staan. (4) **Het actielabel is uit de productkiezer gehaald**: die lijst weet niet of de van-prijs klopt, waardoor hetzelfde product bovenaan "let op: die van-prijs is niet gerekend" kon krijgen en twee regels lager alsnog "van 4,99 voor 3,87" als verkoopargument. (5) De AH-link is in de code eerlijk beschreven als een **patroon** (`wi<id>`) en niet als gelezen winkeldata — bij Dirk lezen we hem echt van de pagina, bij AH leiden we hem af uit het id. **Geverifieerd**: 14 nieuwe tests, waaronder de link die van waarneming tot scherm overleeft, de nep-korting en de actie-zonder-voordeel die géén markering krijgen, de verlopen actie, het multipack, en de geweigerde adressen. `npm run verify` groen (611/611), `npm run test:e2e` criticalFlow 30/30 (inclusief stap 29, de doorrekening bij een andere winkel). **Openstaand, niet door deze wijziging veroorzaakt**: `e2e/shoppingListAccess.e2e.ts` stap 8 ("de naam van een gedeeld ingrediënt aanpassen werkt gewoon") valt om — ook op de ongewijzigde `main`, geverifieerd door de wijziging tijdelijk weg te zetten. De veiligheidsassertie in datzelfde bestand (een ander huishouden mag `restrictionTags`/`category` niet wijzigen) blijft groen; wat faalt is de positieve tegenproef. **De migratie draait vanzelf mee met de Vercel-build**; geen handmatige actie nodig. |


| Bugfix | De CI stond een maand rood — en dat verborg een echte fout in de productmatching | **Aanleiding**: ik meldde bij elk work package "`npm run verify` groen", maar keek nooit naar de CI die GitHub na elke merge draait. Die faalde sinds 28 augustus 16:45 (PR #148), 26 merges lang. Het verschil: de CI draait tegen een **verse** database, mijn ontwikkelomgeving tegen een database die nooit was leeggemaakt en vol stond met restjes van eerdere testruns. **Wat er onder bleek te zitten.** (1) **Een echte fout in de app, geen testfout**: sinds de prijslaag (PR #162) staan de producten van Albert Heijn en Dirk in dezelfde `Product`-tabel als die van Picnic, en de matcher van de boodschappenlijst filterde niet op winkel. Een AH-product kon dus op een boodschappenregel belanden — die regel heeft geen Picnic-id, gaat stilzwijgend niet mee naar het mandje, en heeft bovendien het juiste Picnic-product verdrongen. De kandidatenquery's in `buildShoppingListLines` en `getShoppingListCandidatesByIngredient` kijken nu alleen naar `provider: PICNIC`. **Uit de code-review kwam dat die twee niet genoeg waren**: `matchProductForIngredient` — het pad voor álles wat niet de weekmenu-lijstopbouw is — miste het filter ook, en dat raakt precies de plekken waar de gebruiker zelf ingrijpt: een nieuwe vaste boodschap, een voorraadwijziging, en het opnieuw matchen nadat je op /controle een product afwijst. De reviewer mat bovendien dat een AH-product met bekende verpakkingsgrootte (wat de regel is, niet de uitzondering — AH levert de inhoud gewoon mee) een Picnic-product zonder verpakkingsgrootte verslaat bij álle drie de productvoorkeuren. Verder: de snel-bestellen-preview kon een vertrouwde voorkeur naar een winkelproduct tonen en dat webshop-id daarna als Picnic-product wegschrijven — dan is de vervuiling blijvend en ontsnapt ze aan elk filter. **En er staat nu een laatste horde vlak vóór het mandje**: `cartService` weigert een regel waarvan het product niet van Picnic is. Die is er omdat een filter bij de matcher alleen niet volstaat — handmatige en al overgedragen regels overleven elke herbouw en houden zo'n product vast. Zonder die horde zou een AH-webshop-id naar Picnic gaan; meestal een foutmelding, maar als datzelfde id daar toevallig bestaat ligt er stilzwijgend een verkeerd product in het mandje, en dat mag deze app nooit doen. Vier integratietests leggen het geheel vast; ik heb van alle vier gecontroleerd dat ze falen zonder de bijbehorende fix. Eén van de eerste twee bleek in de review terecht zwak (hij zou ook zonder de fix slagen) en is aangescherpt. (2) **Een testfixture die op toeval leunde**: `cartService.test.ts` pakte een willekeurig seed-ingrediënt en een willekeurig seed-recept die niets met elkaar te maken hoefden te hebben, en gaf het testproduct geen `lastSeenAvailable` — waardoor de matcher het als "niet beschikbaar" beschouwde en de herbouw na "mandje legen" een regel zónder product opleverde. Op een volle database maskeerden andere producten dat. De fixture maakt nu zijn eigen ingrediënt, recept en product aan. (3) **Cleanups die te breed waren**: `cartService.test.ts` ruimde zijn producten en ingrediënten helemaal niet op (zo raakte de ontwikkeldatabase vol met testproducten mét Picnic-id), en `priceRefresh.test.ts` verwijderde *alle* AH-producten in plaats van alleen zijn eigen — tests draaien parallel tegen dezelfde database, dus dat wiste weg waar een ander bestand op dat moment mee bezig was. Dat was de tweede, onregelmatige CI-fout (een foreign-key-schending bij het opbouwen van een boodschappenlijst). De reviewer wees erop dat datzelfde bestand ook nog *telde* over alle AH-producten; die tellingen zijn nu even hard afgebakend als de opruiming, anders zou het inperken van de delete de flake alleen maar verplaatsen. **Werkwijze aangepast**: de ontwikkeldatabase is één keer leeggegooid en opnieuw geseed (met toestemming van de gebruiker), zodat er hier voortaan onder dezelfde omstandigheden getest wordt als in de CI; en na een merge kijk ik naar de CI-uitslag in plaats van alleen naar mijn eigen run. **Geverifieerd op een verse database**: `npm run verify` groen (614/614), zeven volledige parallelle testruns achter elkaar zonder uitval, `npm run test:e2e` 42/42 — inclusief de stap in `shoppingListAccess.e2e.ts` die eerder omviel; die bleek eveneens door de vervuilde database te komen en is nu vanzelf groen. **Geen migratie en geen actie van de gebruiker nodig.** |


| Bugfix | € 6.181,74 voor drie pakken Alpro: de verpakkingseenheid werd weggegooid | Door de gebruiker gemeld met een schermafbeelding. **Wat er misging**: de inhoud van een winkelverpakking wordt gelezen uit de tekst op de verpakking — "4 stuks" levert 4 op, "1 l" levert 1000 op — maar de *eenheid* die daarbij hoort werd weggegooid en vervangen door de eenheid van het ingrediënt. Daardoor leken twee verpakkingen altijd in dezelfde eenheid te staan. Drie pakken Alpro van 750 gram werd zo 2250 gram gedeeld door "4" (in werkelijkheid 4 stuks): 563 verpakkingen à € 10,98. De bewaking die dit had moeten tegenhouden (`ownUnit !== packageUnit`) stond er al sinds de vorige Alpro-fix, maar kon nooit aanslaan omdat beide kanten diezelfde ingrediënteenheid meekregen — de vorige fix repareerde het omrekenen en liet de eenheid ongemoeid. **De oplossing**: de verpakkingseenheid komt nu uit de verpakkingstekst zelf, met dezelfde lezer die ook de inhoud bepaalt, zodat getal en eenheid bij elkaar horen. Staan twee verpakkingen niet in dezelfde eenheid, dan komt er geen bedrag maar een uitleg die beide eenheden noemt ("verpakking in stuks, die van ons in gram") — je ziet nog steeds wát de winkel heeft, alleen niet langer een bedrag dat nergens op slaat. Dat raakt ook de Campina-regel uit dezelfde schermafbeelding: 500 gram tegenover 1 liter is zonder soortelijk gewicht niet om te rekenen, en die € 0,95 naast € 3,98 rustte dus op een stille eenheidsverwisseling. **Geverifieerd**: een integratietest die de productiesituatie precies nabouwt (vaste boodschap "3x", ingrediënt in gram, ons pak 750 gram, AH-pak "4 stuks") en zonder de fix exact € 6.181,74 oplevert. `npm run verify` groen (615/615), `npm run test:e2e` 42/42. **Geen migratie en geen actie van de gebruiker nodig.** |


| Bugfix | De matcher kritischer: printpapier is geen toiletpapier, cupjes zijn geen pot | Twee meldingen uit de praktijk, met een derde die er al lag. **(1) "Bij toiletpapier staat printpapier van AH."** De score werd op letterniveau bepaald, en "papier" zit nu eenmaal in "printpapier". Bij een ingrediënt dat "Wc papier" heet valt "wc" bovendien weg als te kort woord, waardoor er alleen "papier" overbleef — en dat leverde een *perfecte* score op printpapier op. Een woord telt nu alleen nog als héél woord. De uitzondering die winkels nodig maken blijft: een reeks opeenvolgende woorden mag samen precies één woord aan de andere kant vormen ("Allesreiniger doekjes" ↔ "Allesreinigerdoekjes"), en "precies" is het punt — "papier" vormt niet "printpapier", want "print" blijft over, en juist dat overgebleven stuk maakt de producten verschillend. In een Nederlandse samenstelling zit de soort vooraan. Karnemelk matcht om dezelfde reden niet meer op melk. **(2) "Bij appelmoes is het ene losse cupjes en het andere één grote pot."** Als hoeveelheid klopt dat allebei en rekende de simulatie er vrolijk mee, maar je koopt iets anders. Een verpakking die als `n x m` geschreven is, of die het woord cupje/knijpzak/portie draagt, geldt nu als losse porties; staat daar één pot tegenover, dan is dat een alternatief met de reden erbij in plaats van een gelijkwaardige keuze. Twee potten van verschillende grootte blijven wél gelijkwaardig — dáár is de verpakkingsberekening voor. **(3) Alpro Barista koffiemelk naast Alpro mild & creamy** — al eerder gemeld. Het ingrediënt heet alleen naar het merk, dus lexicaal matcht elk Alpro-artikel even goed. De vergelijking kijkt nu ook naar de naam van óns eigen product, die specifieker is: dekt een kandidaat daar minder dan de helft van, dan is het een alternatief. De grens ligt bewust op de helft en niet hoger, want het merk is meestal één van de woorden en een ander merk mag geen beletsel zijn (dat is expliciet zo gevraagd) — "Campina magere yoghurt" tegenover "AH magere yoghurt" houdt zo twee van de drie woorden over en blijft gelijkwaardig. **Wat dit betekent op het scherm**: strenger betekent vaker "niet gevonden" of "ander soort", en dat is de bedoeling — liever een lege cel dan een bedrag voor het verkeerde product. Wat er wél ligt kan de gebruiker zelf aanwijzen met "Ander product kiezen"; die keuze wordt onthouden. De herrangschikking gebeurt bij het lezen, dus verkeerde matches verdwijnen meteen en niet pas na de volgende verversing. **Geverifieerd**: 6 nieuwe tests op de gemelde gevallen zelf. `npm run verify` groen (621/621), `npm run test:e2e` 42/42. **Geen migratie en geen actie van de gebruiker nodig.** |


| Bugfix | De matcher zoekt nu op ons eigen product, niet op de ingrediëntnaam | Vervolg op de vorige ronde: die maakte de matcher strenger, maar Alpro bleef koffiemelk en appelmoes bleef een pot. **De oorzaak lag een laag dieper.** De verversing zocht bij Albert Heijn op de *ingrediëntnaam* — bij dit huishouden is dat letterlijk "Alpro", een merk. Alle Alpro-artikelen scoren daar even hoog, waarna een willekeurige tiebreak besliste welke **drie** er bewaard werden. Dat AH exact hetzelfde product verkoopt hielp niet: het stond niet eens in die drie, en de equivalentieregels van de vorige ronde kunnen alleen afwaarderen wat er ligt — het juiste product tevoorschijn halen kunnen ze niet. **Drie wijzigingen.** (1) **Zoeken op wat we werkelijk kopen**: de zoekterm komt nu van het Picnic-product dat aan het ingrediënt hangt ("Alpro Mild & Creamy" in plaats van "Alpro"). Levert dat niets op, dan pas een tweede zoekopdracht op het ingrediënt — alleen bij nul resultaten, zodat dit geen verdubbeling van het verkeer wordt. Getallen en verpakkingsmaten gaan uit de zoekterm ("alpro mild creamy 750g" vindt niets). (2) **Rangschikken op ons eigen product**: de toelating blijft de ingrediëntnaam (hoort dit hierbij?), maar de volgorde wordt bepaald door hoe sterk een kandidaat op ons product lijkt. In de test staat koffiemelk zonder dat signaal bovenaan en het identieke product met dat signaal bovenaan. (3) **Acht kandidaten in plaats van drie**, aan beide kanten — drie was te krap zodra een winkel meerdere varianten van hetzelfde merk voert. Verder leest de verpakkingsvorm het aantal nu ook uit de productnaam: niet elke winkel zet "4 x 100 g" in het verpakkingsveld, sommige noteren daar het totaalgewicht, en dan heette een doosje cupjes ten onrechte "één verpakking". **Wat dit niet oplost**, en dat is bewust: "snacktomaatjes" bij Picnic tegenover "snoeptomaat" bij AH delen geen enkel woord. Geen enkele vorm van tekstvergelijking komt daar ooit uit; dat vraagt om echt begrip van wat een product ís. De gebruiker heeft van de voorgelegde richtingen alleen deze gekozen, dus dat blijft voorlopig openstaan. **Geverifieerd**: 4 nieuwe tests, waaronder de Alpro-volgorde met en zonder het signaal, en de terugval die alleen bij nul resultaten gebeurt. `npm run verify` groen (629/629), `npm run test:e2e` 42/42. **Geen migratie nodig; de nieuwe zoektermen gaan mee met de eerstvolgende verversing.** |


| Bugfix | De verpakkingsvorm als rangschiksignaal, en ruimer zoeken | Na de vorige ronde stuurde de gebruiker opnieuw schermafbeeldingen: Campina klopte nu, Alpro toonde geen € 6.181,74 meer en appelmoes stond als "ander soort" — maar het was nog steeds Barista en nog steeds een pot. **Nagemeten wat de nieuwe zoekterm per regel oplevert**, en dat legde het gat bloot: bij Alpro verandert de zoekterm echt ("alpro" → "alpro mild creamy"), maar bij appelmoes **niet**. Ons eigen product heet daar "Picnic appelmoes", en omdat de winkelnaam als ruiswoord wegvalt blijft er exact hetzelfde woord over als de ingrediëntnaam. Ook het rangschiksignaal op naam hielp niet: élk product met "appelmoes" erin scoorde daar even hoog. **Wat er nu bij komt.** (1) **De verpakkingsvorm telt mee in de volgorde**: losse porties horen bij losse porties, een pot bij een pot. Zodra de naam niets meer onderscheidt is dit het enige overgebleven verschil, en het zet AH's cupjes boven de pot van Hak. Onbekend telt nooit als verschil — dan weten we het simpelweg niet. (2) **Twintig zoekresultaten in plaats van tien**: bij een breed begrip als "appelmoes" staan de cupjes makkelijk op plek twaalf, en wat we niet zien kunnen we ook niet kiezen. **Eerlijk over wat er niet verandert**: "Snack Tomaatjes" blijft "niet gevonden" bij Albert Heijn, want daar heet het "snoeptomaat" en die twee delen geen enkel woord. Dat blijft staan tot er een richting gekozen wordt die wél betekenis begrijpt. En: de nieuwe zoektermen werken pas na een verversing — de rangschikking werkt meteen op wat er al ligt, maar een product dat nooit is opgehaald kan ook niet bovenaan komen. **Geverifieerd**: 2 nieuwe tests, waaronder de appelmoes-volgorde met en zonder het vormsignaal. `npm run verify` groen (631/631), `npm run test:e2e` 42/42. **Geen migratie nodig.** |


| Bugfix | De terugval sloeg nooit aan, en de zoekterm kon te specifiek worden | De gebruiker stuurde een schermafbeelding uit de AH-app: "Alpro Mild & creamy" staat daar gewoon, 750 g voor € 2,99 — identiek aan het onze. Dat bevestigde dat de zoekterm van de vorige ronde het móest kunnen vinden, en legde meteen twee zwakke plekken bloot die het alsnog konden verhinderen. **(1) De terugval sloeg praktisch nooit aan.** Hij keek of de specifieke zoekopdracht *nul resultaten* opleverde. Maar een winkel geeft zelden helemaal niets terug; ze geeft iets terug dat er niet bij hoort. Dan bleef de controle op "leeg" onwaar, viel er niets terug, en stonden we met lege handen terwijl het product er gewoon lag. De terugval kijkt nu naar wat er ná het matchen overblijft: levert de specifieke zoekopdracht geen enkele bruikbare kandidaat, dan volgt alsnog de bredere, en worden beide uitslagen samengevoegd voordat er gerangschikt wordt. **(2) De zoekterm kon te lang worden.** Winkelzoekmachines worden strenger naarmate je meer woorden geeft; een productnaam als "Alpro mild & creamy naturel groot formaat" laat een winkel haar eigen variant van datzelfde product niet meer vinden. De zoekterm is nu afgekapt op vier woorden — specifiek genoeg om het juiste product te vinden, kort genoeg om varianten niet uit te sluiten. **Geverifieerd**: 2 nieuwe tests, waaronder de terugval bij een zoekopdracht die wél resultaten geeft maar niets bruikbaars — precies het geval dat eerder door de mazen viel. `npm run verify` groen (633/633), `npm run test:e2e` 42/42. **Geen migratie nodig; werkt vanaf de eerstvolgende verversing.** |


| Bugfix | De toelating gooide juist het goede product weg | Nieuwe melding: bij "Wc Papier" stond **24Basics printerpapier** van Albert Heijn. Nagerekend met de echte functies, en dat gaf een ontluisterend beeld: printerpapier scoorde **1,00** op de ingrediëntnaam en **0,00** op ons eigen product; echt toiletpapier (AH én Page) scoorde **0,00** op de ingrediëntnaam en **1,00** op ons product. De toelating keek alleen naar de ingrediëntnaam — dus liet ze het verkeerde product binnen en gooide ze het goede weg. Alles wat er daarna gebeurt (rangschikken, equivalentie) kon dat niet meer goedmaken: die werken met wat er ligt. **Twee regels erbij.** (1) **Toegelaten als het bij het ingrediënt hoort óf op ons eigen product lijkt.** Dat "of" is niet vrijblijvend: "Wc papier" houdt na het filteren alleen "papier" over, en "toiletpapier" bevat dat niet als los woord. (2) **Wat duidelijk ons product niet is, wordt geen kandidaat** — dezelfde grens van de helft die het equivalentiemodel al gebruikte om iets "ander soort" te noemen. Zulke producten werden eerder wél getoond met dat label erbij, maar een doos printerpapier naast je toiletpapier zetten is geen nuttige vergelijking, hoe eerlijk het label ook is. Nu staat er "niet gevonden" en kan de gebruiker zelf het juiste product aanwijzen. Kennen we ons eigen product niet, dan geldt de oude, ruimere toelating onverkort — onwetendheid mag niets wegfilteren. **Ook**: woorden van twee letters tellen weer mee. Ze vielen weg als "te kort", en dat kostte precies de woorden die het onderscheid maken ("wc"). Maatafkortingen (st, gr, kg, ml, cl) staan nu expliciet bij de ruiswoorden, want die vielen voorheen mee weg met die lengtegrens. **Geverifieerd**: 4 nieuwe tests, waaronder de wc-papier-casus in beide richtingen (het goede product moet erbij komen én het verkeerde moet verdwijnen) en het geval zonder eigen product. `npm run verify` groen (637/637), `npm run test:e2e` 42/42 — één run liet een ongerelateerde stap vallen (het bekende klik-patroon), de herhaling was groen. **Geen migratie nodig.** |


| Bugfix | De knop bleef eindeloos op "bezig met ophalen" staan | Melding met schermafbeelding. **Oorzaak, en het was een regressie van diezelfde middag**: de terugval-zoekopdracht sloeg sinds de vorige ronde veel vaker aan (dat was de bedoeling), en het aantal bewaarde kandidaten was van drie naar acht gegaan. Samen bijna een verdriedubbeling van het werk per verversing: tot dertig zoekopdrachten plus honderden databaseschrijfacties, en dan kapt Vercel de aanroep af. Wordt een aanroep afgekapt, dan krijgt de browser **nooit** antwoord — de knop blijft draaien en wat er wél is opgehaald wordt niet gemeld. **De oplossing is een tijdsbudget, geen lager aantal.** Beide verversingen stoppen nu zelf netjes vóór de limiet, en melden dat ook: "gestopt bij het tijdslimiet; de rest volgt bij de volgende verversing". Dat staat bewust in dezelfde lijst als de echte fouten — het is geen storing, maar het is wél "niet alles is gelukt", en dat hoort de gebruiker te lezen in plaats van af te leiden uit een aantal dat lager is dan verwacht. De handmatige knop verdeelt 40 seconden over de winkels; elke winkel telt haar deel vanaf het moment dat ze begint, zodat de winst van een snelle winkel de laatste niet over het totaal heen tilt. **En passant een tweede vondst**: de nachtelijke taak had helemaal **geen** `maxDuration` staan en draaide dus op de standaardlimiet van tien à vijftien seconden. Die werd elke nacht midden in het werk afgekapt, terwijl het scherm beloofde dat "de volledige lijst elke nacht vanzelf gaat" — precies het soort belofte dat deze app niet hoort te doen. Nu 60 seconden (werkt op elk Vercel-abonnement) met een budget van 50, gelijk verdeeld over de winkels. **Geverifieerd**: 2 nieuwe tests — een verstreken limiet doet geen enkele winkelaanvraag meer en meldt waaróm, en zonder limiet verandert er niets. `npm run verify` groen (639/639), `npm run test:e2e` 42/42. **Geen migratie nodig.** |


| Bugfix | Dirk keek in de verkeerde schappen | Melding: "Hij zegt dat ie producten heeft gevonden maar geen match, terwijl sommige producten identiek zijn." **De oorzaak is structureel en had niets met matchen te maken.** Dirk heeft geen bruikbare zoekfunctie (de zoekpagina laadt client-side), dus in plaats van per ingrediënt te zoeken wordt de catalogus gecrawld. Maar wélke categorieën gecrawld werden, was simpelweg *de eerste zes van Dirks eigen overzichtspagina* — wijn, snoep, wat er toevallig bovenaan het menu staat. Volstrekt los van de boodschappenlijst. Vandaar "wel aanbod teruggekregen, maar niets dat paste": de app keek eerlijk in de verkeerde schappen. Alle matchverbeteringen van vandaag konden daar niets aan doen, want die werken op wat er is opgehaald. **De oplossing zit in het pad zelf**: `/boodschappen/zuivel/melk` zégt waar het over gaat. Categorieën worden nu gerangschikt op hoeveel namen van de lijst er iets te zoeken hebben, en de crawler bezoekt de beste eerst. Categorieën die niets dekken verdwijnen niet, ze schuiven naar achteren — is er tijd over, dan zijn ze alsnog welkom. De handmatige knop doet er nu **14** in plaats van 6, want elke pagina is nu ook echt een kans op een match, en het tijdsbudget bewaakt de bovengrens. **Tweede gat gedicht**: de crawl zelf keek niet naar dat tijdsbudget — alleen het matchen erna. Het ophalen van de pagina's kon dus het hele budget opeten waarna er niets meer te matchen viel. De crawl stopt nu zelf, en meldt "gestopt bij het tijdslimiet" in plaats van het niet te onderscheiden van een scraper die niets meer leest. **Geverifieerd**: 3 nieuwe tests, waaronder een integratietest tegen een nagebootste Dirk-site waarin de relevante categorie achteraan het menu staat en er maar één pagina bezocht mag worden — die faalt aantoonbaar zonder de rangschikking. `npm run verify` groen (642/642), `npm run test:e2e` 42/42. **Geen migratie nodig.** |


| Feature | Dirk zoekt nu zelf, als dat blijkt te kunnen | De gebruiker wees op de zoekbalk op dirk.nl. De code beweerde iets preciezers — "de zoekpagina laadt client-side, dus daar valt server-side niets uit te lezen" — maar **die aanname kwam uit de oorspronkelijke opdracht en was nooit gemeten**; ze werd hier meerdere keren als vaststaand doorgegeven. Nakijken kan niet vanuit deze omgeving: `www.dirk.nl` geeft 403 bij de proxy, net als elke andere externe site. De gebruiker leverde de URL: `/zoeken/producten/Melk` — de term staat in het pad, niet in een queryparameter. **De oplossing is een proef in plaats van een aanname.** Eén keer per verversing wordt de zoekpagina met een gewoon woord ("melk") opgehaald. Komen daar leesbare producten uit, dan zoekt Dirk voortaan **per ingrediënt**, net als Albert Heijn — inclusief zoeken op de naam van ons eigen product en dezelfde terugval op de ingrediëntnaam. Dan verdwijnt het gokwerk over wélke categorie je moet bezoeken volledig. Komt er niets uit, dan crawlt hij de categorieën zoals hiervoor. Er kan dus niets kapot als de oorspronkelijke aanname tóch klopte, en de app corrigeert zichzelf als Dirk haar site ooit omgooit — in beide richtingen. **Detail dat een test blootlegde**: een mislukte zoekopdracht op de specifieke term telt als "niets gevonden" en niet als storing, want winkels antwoorden op een zoekterm zonder treffers geregeld met een foutcode in plaats van een lege lijst. Gaat de bredere zoekopdracht daarna óók mis, dan is het wél een storing en wordt die gemeld. **Geverifieerd**: 2 nieuwe integratietests tegen een nagebootste Dirk-site — één waar de zoekpagina werkt (en de categoriepagina's aantoonbaar onaangeraakt blijven) en één waar ze een lege huls teruggeeft (en er alsnog gecrawld wordt). `npm run verify` groen (644/644), `npm run test:e2e` 42/42 — één run liet een ongerelateerde stap vallen, de herhaling was groen. **Geen migratie nodig.** |


| Bugfix | Dirk-producten heetten allemaal "Foto van …" | De eerste verversing via Dirks zoekpagina wérkte — Campina, beschuit, rijstwafels en Melkunie kwamen binnen, dat laatste als goedkoopste van de drie winkels. Maar élk Dirk-product heette op het scherm "Foto van Ha…", "Foto van Mel…". Oorzaak: de naam wordt uit het `alt`-attribuut van de productafbeelding gelezen (dat is bij webshopmarkup het stabielste veld), en Dirk zet daar een beschrijving van de *afbeelding* in plaats van de productnaam: "Foto van Bolletje beschuit". Dat voorvoegsel wordt er nu afgehaald. Niet alleen omdat er anders onzin op het scherm staat, maar ook omdat "foto" bij ieder Dirk-product als extra woord meetelde en zo de onderlinge volgorde van hun eigen producten scheeftrok. Een naam die toevallig zó begint zonder een fotobeschrijving te zijn ("Fotolijst van hout") blijft ongemoeid — daar is een test voor. **Meteen ook drie commentaarblokken rechtgezet** die nog beweerden dat Dirks zoekpagina client-side laadt en onbruikbaar is. Dat is nu aantoonbaar onwaar, en zulke achterhaalde beweringen in de code zijn precies hoe deze aanname zo lang is blijven staan: ze werd steeds opnieuw gelezen als vaststaand feit. Er staat nu bij dát het gemeten wordt, en waarom. **Geverifieerd**: 2 nieuwe tests. `npm run verify` groen (646/646), `npm run test:e2e` 42/42 — één run liet een ongerelateerde stap vallen, de herhaling was groen. **Geen migratie nodig; de namen worden goed bij de eerstvolgende verversing, want opgeslagen namen blijven staan tot ze overschreven worden.** |


| Bugfix | Dirk haalde maar 3 van de 15 regels binnen het tijdslimiet | Uit de schermafbeelding: "Dirk: 38 producten bijgewerkt, maar niet alles lukte — gestopt bij het tijdslimiet" en "12 regels tellen niet mee". Het tijdsbudget deed dus precies wat het moest doen (netjes stoppen in plaats van de aanroep laten afkappen), maar er paste veel te weinig in. **De oorzaak zat in de database, niet in het netwerk.** Elk waargenomen product kostte twéé aanroepen áchter elkaar: een `upsert` van het product en daarna een `create` van de waarneming. Bij acht kandidaten per ingrediënt per winkel is dat zestien keer heen en weer naar een database die niet naast de server staat — voor vijftien ingrediënten en twee winkels bijna vijfhonderd keer. Dat is waar de seconden bleven. **Nu twee stappen in plaats van zestien**: alle producten van één ingrediënt gaan tegelijk (ze raken verschillende rijen, dus dat kan gewoon) en hun waarnemingen gaan als één schrijfactie. De velden die weggeschreven worden zijn bewust naar één plek getrokken, zodat de losse en de gebundelde weg niet uit elkaar kunnen groeien zodra er een veld bij komt. Verder is het budget van de knop van 40 naar 48 seconden, want de pagina staat er 60 toe en er bleef ruim over. **Ook vastgesteld, en het verklaarde de melding "nee, zelfde"**: de verversing van 17:07 liep één minuut na de merge van 17:06, dus die draaide nog op de vorige versie. Niets mis mee — maar het is het soort ding dat je bij het beoordelen van een schermafbeelding moet controleren voordat je concludeert dat een fix niet werkt. **Geverifieerd**: 1 nieuwe integratietest die aantoont dat bundelen prijzen niet door elkaar haalt en dat een tweede ronde bijwerkt in plaats van verdubbelt. `npm run verify` groen (647/647), `npm run test:e2e` 42/42. **Geen migratie nodig.** |


| Bugfix | Ronde 1 uit de doorlichting: telwoorden, eenheidsprijzen en hervatten | Drie punten uit het plan `Prijslaag doorgelicht`, alle drie meetbaar en zonder productkeuze. **(A1) "Rollen" werd niet als telwoord herkend.** De lezer kende er vier: stuks, st, krop, bos. "9 rollen" viel er dus buiten, en dat is erger dan een ontbrekende eenheidsprijs: zonder inhoud kón de vergelijking niet zien dát negen rollen iets anders is dan twaalf stuks, noemde ze het AH-pak gelijkwaardig, en telde ze € 7,99 mee naast onze € 4,35. De lijst kent nu ook rollen, zakjes, plakken, sneetjes, bollen, blikken, flessen, pakken, tabletten, capsules, doekjes, wasbeurten, repen en bosjes — en de Dirk-lezer zoekt op diezelfde woorden, want anders vindt die "9 rollen" niet eens in de pagina. Bewust alleen woorden die onmiskenbaar iets telbaars aanduiden: een verkeerd gelezen verpakking is erger dan een ontbrekende. **(A2) Een eigenschap van het product werd als inhoud gelezen.** "60 liter" op een pak trekbandzakken is de maat van één zak, niet de inhoud van het pak; er kwam "€ 0,03 per liter" uit. Een eenheidsprijs wordt nu alleen doorgegeven als ze in dezelfde eenheid staat als waarin we het ingrediënt bijhouden — vuilniszakken tellen we in stuks, dus een prijs per liter hoort daar niet. Dat geldt zowel voor onze eigen kolom als voor die van de winkels, dus ook in het uitklapblok waar de eerdere afscherming nog niet gold. **(D1) Elke verversing begon weer vooraan.** Stopte er een op het tijdslimiet, dan deed de volgende exact dezelfde ingrediënten opnieuw en kwam de staart van de lijst er structureel nooit aan. De ingrediënten staan nu per winkel op volgorde van "het langst niet bijgewerkt", zodat opeenvolgende rondes samen de hele lijst dekken. Bewust géén nieuwe kolom: `lastSeenAvailable` wordt bij elke waarneming al bijgewerkt en ís dus de teller — een eigen "waar was ik gebleven" zou hetzelfde nog eens opslaan en kan achterlopen zodra er iets misgaat. **Geverifieerd**: 6 nieuwe tests, waaronder twee verversingsrondes achter elkaar die aantoonbaar verschillende ingrediënten pakken, en een eenheidsprijs die wél binnenkomt maar niet wordt doorgegeven. Eén bestaande test viel om op de nieuwe bewaking — die gebruikte een eenheid die niet bij het ingrediënt hoorde, precies wat nu tegengehouden wordt; fixture aangepast. `npm run verify` groen (652/652), `npm run test:e2e` 42/42. **Geen migratie nodig.** |

| Bugfix | Telwoorden ook aan de bestelkant | A1 uit ronde 1 bleek maar half af. De prijslaag leerde "9 rollen" lezen, maar er is een **tweede** verpakkingslezer: `parsePackageQuantity` in `src/lib/quantity/parsePackageSize.ts`, die het veld `Product.packageQuantity` vult. Die kende alleen gram/kilo/liter/ml/stuks. Voor Picnic-producten met een telwoord in de verpakkingstekst — "9 rollen", "20 sneetjes", "36 wasbeurten" — bleef de inhoud dus leeg, en dat raakt niet alleen de vergelijking maar ook `/boodschappen` en `/controle`: zonder inhoud kan de app niet uitrekenen hoeveel pakken er nodig zijn. De lezer kent die woorden nu ook. **Bewust nauwer dan de prijslaag**, want hier hangt een bestelhoeveelheid aan het antwoord: verpakkingswoorden in enkelvoud (`1 pak`, `1 zak`, `1 blik`) blijven `null` omdat ze de verpakking zelf noemen en niets zeggen over het aantal sneetjes waarin een recept rekent — hun meervoud (`6 flessen`) staat er wél bij, want dat is een multipack. "Bol(len)" ontbreekt met opzet: een bol knoflook is geen teentje. Die afwijking staat als comment in het bestand, zodat de twee lezers niet stilzwijgend uit elkaar lopen. **Geverifieerd**: 4 nieuwe tests (inhoudswoorden tellen mee, enkelvoudige verpakkingswoorden niet, en een telwoord in stuks telt niet mee voor een ingrediënt in gram). `npm run verify` groen (655/655), `npm run test:e2e` 42/42. **Geen migratie nodig.** |

| Bugfix | Dirk vond meer dan er geteld werd | Gebruikersmelding met schermafbeeldingen: "hij geeft aan dat ie bij Dirk maar 3 vindt, maar hij vindt er een stuk meer". Nageteld klopte het bedrag wél: Dirk had op 6 van de 15 regels een product, waarvan er 3 als hetzelfde of gelijkwaardig telden (€ 3,92 + € 1,79 + € 4,78 = € 10,49, precies het getoonde bedrag). Twee dingen zaten fout. **(1) Een maat die alleen in de productnaam stond, was onzichtbaar.** De verpakkingsgrootte wordt gezocht in de tekst van het productblok, en die tekst ontstaat door alle tags weg te strippen — inclusief hun attributen. De naam komt bij Dirk juist uit het `alt`-attribuut van de afbeelding. Stond de maat alleen daar ("Starbucks Skinny Caffe Latte 220 ml"), dan zag de lezer haar nooit en meldde het scherm "verpakkingsgrootte onbekend" voor een product waar de maat gewoon bij stond — en zonder inhoud valt er niets te vergelijken. Gemeten met een probe vóór de fix: `packageSize: null` voor de naam-variant, `"10 stuks"` voor een kaart met een apart maatveld. De naam is nu een tweede bron; een apart maatveld blijft vóórgaan, want dat gáát over de verpakking terwijl een getal in de naam ook iets anders kan zijn. **(2) De tabel telde alleen de harde regels.** "Over hoeveel regels: 3 van de 15" stond onder het harde bedrag, en de rij "Met alternatieven erbij" toonde alleen een bedrag zonder teller. Daardoor las het scherm als "Dirk vond maar drie producten". Die rij heeft nu een eigen regelteller én — net als het harde bedrag — een eigen "Bij Picnic, die regels", zodat het alternatieve bedrag niet naast een referentiebedrag komt te staan dat over minder regels gaat. Beide velden bestonden al in het model; dit is presentatie, geen nieuwe rekenkunde. **Geverifieerd**: 2 nieuwe tests (een maat die alleen in de naam staat telt mee; een apart maatveld gaat vóór de naam). `npm run verify` groen (657/657). `npm run test:e2e`: één run viel om op stap 14 — juist de prijsvergelijking die hier is aangepast, dus niet als flake weggezet; daarna vier keer achtereen groen (2x de volle suite, 2x gericht de kritieke flow). De foutmelding van die ene run is niet vastgelegd, dus dat blijft onbewezen in plaats van verklaard. **Geen migratie nodig.** |

| Bugfix | Het huismerk van Dirk gold als voordeelmerk | Gebruikersmelding met schermafbeelding: bij rijstwafels stond bij Dirk "ander soort — voordeelmerk in plaats van wat jullie normaal kopen", voor "1 de Beste Rijstwafels dun met zeezout" naast onze eigen "Picnic rijstwafels". De reden was dus niet de maat (13 stuks tegenover 130 gram) maar de merkklasse. Gemeten in `deriveQualityTier`: Picnic → STANDAARD, AH → STANDAARD, **1 de Beste → BUDGET**, G'woon → BUDGET, AH Basic → BUDGET. Dat is een tegenspraak binnen ons eigen model: "1 de beste" stond al in `HOUSE_BRANDS` mét een comment dat een huismerk de standaardklasse is en niet de goedkoopste — maar de budgetcontrole staat één regel eerder en liet die huismerkregel nooit aan bod komen. Het huismerk van Albert Heijn heette daardoor standaard, dat van Dirk voordeelmerk. Het effect was systematisch: bijna elk Dirk-product draagt "1 de Beste", dus bijna elke Dirk-regel viel uit het harde totaal en alleen de A-merken (Melkunie, Bolletje, Campina) bleven over — precies de 3 van de 15 uit de melding. "1 de beste" is uit `BUDGET_MARKERS` gehaald; **G'woon blijft er wél in**, want dat is bij Dirk de goedkope lijn ónder het huismerk, het equivalent van AH Basic. **Geen migratie nodig, maar wel een verversing**: `qualityTier` staat opgeslagen op het product en wordt bij elke waarneming overschreven, dus de bestaande Dirk-producten krijgen hun nieuwe klasse pas na de eerstvolgende (handmatige of nachtelijke) verversing. **Openstaand, bewust niet meegenomen**: hiermee gelden "naturel" en "dun met zeezout" als hetzelfde soort rijstwafel. Het model heeft geen smaak-as, alleen klasse, vers/houdbaar en verpakkingsvorm; of dat onderscheid erbij moet is een aparte vraag. **Geverifieerd**: één bestaande test omgedraaid (die legde de oude BUDGET-uitslag vast) en vervangen door twee: G'woon blijft BUDGET, en de huismerken van Dirk en AH vallen aantoonbaar in dezelfde klasse. `npm run verify` groen (658/658), `npm run test:e2e` 42/42. |

| Bugfix | Een mislukte zoekopdracht heet niet langer "niet gevonden" | Oorzaak C uit het onderzoek `Waarom Dirk niets vindt`. In `refreshDirkViaSearch` stond `.catch(() => [])` om de zoekopdracht per ingrediënt: een foutcode van Dirk werd een lege lijst, en een lege lijst werd op het scherm "niet gevonden". Er was dus geen enkel verschil zichtbaar tussen *"deze winkel voert dit product niet"* en *"de vraag is nooit aangekomen"* — precies het onderscheid dat bepaalt of er iemand naar de code moet kijken. Dat is bovendien in tegenspraak met wat er boven in ditzelfde bestand staat: "nul gevonden producten is een fóút, geen lege uitslag". Voor de crawl gold die regel wel, voor de zoekopdracht per ingrediënt niet. **Wat er nu gebeurt**: een mislukte zoekopdracht staakt de verversing nog steeds niet (winkels antwoorden op een term zonder treffers geregeld met een foutcode, dus doorgaan is juist), maar hij wordt vastgelegd in een nieuw veld `RefreshResult.searchFailures` — bewust náást `errors` en niet erin, want anders verdringt één 404 bij het eerste ingrediënt de melding "gestopt bij het tijdslimiet", en dát is de belangrijkste mededeling. Het scherm toont ze achter elkaar: `"gestopt bij het tijdslimiet; 2 zoekopdrachten kwamen niet aan (Eieren 10 Stuks: Dirk antwoordde met 404) — die regels zijn niet 'niet gevonden', die zijn niet gelukt"`. Ook telt zo'n ingrediënt niet meer mee als `ingredientsWithoutMatch`: dat getal betekent "wel aanbod gezien, niets dat paste", en die twee bij elkaar optellen maakt het onbruikbaar voor precies de vraag waarvoor het bestaat. **Geen migratie nodig**: `PriceRefreshRun.error` is al een tekstkolom die letterlijk op het scherm belandt. **Geverifieerd**: 4 nieuwe tests, waaronder een paar dat het onderscheid vastlegt — een zoekopdracht die 404 geeft levert `searchFailures` op en `ingredientsWithoutMatch = 0`; eentje die wél aankomt maar hondenbrokken teruggeeft levert het omgekeerde. `npm run verify` groen (662/662), `npm run test:e2e` 42/42. |

| Bugfix | De filter op ons eigen product gooide goede producten weg | Oorzaak A uit het onderzoek `Waarom Dirk niets vindt`, en de zwaarste van de vijf. De toelating in `rankStoreProducts` had twee regels: eerst een "of" (hoort bij het ingrediënt óf lijkt op ons eigen product), en daaronder een tweede die gelijkenis met ons eigen product alsnog verplicht stelde zodra we dat product kenden. Daarmee werkte dat "of" in de praktijk als "en". Bij eieren was dat fataal: ons Picnic-product heet *scharreleieren* — één samengesteld woord — en elk ei bij de winkel dat gewoon "eieren" heet dekte daar niets van. Gemeten: mét referentie 0 toegelaten kandidaten, zonder 2, terwijl drie van de vier een perfecte score op de ingrediëntnaam hadden. **De tweede regel is weg**, na een meting van het geval waarvoor ze ooit bedoeld was: printerpapier scoort tegenwoordig 0,00 op "Wc papier", omdat korte woorden als "wc" weer meetellen en er alleen op hele woorden gematcht wordt. Die twee eerdere reparaties vangen het al; deze derde kostte alleen nog goede producten. **Met één uitzondering, en die is nodig**: voegt de ingrediëntnaam geen enkel woord toe dat niet al in onze eigen productnaam staat, dan blijft gelijkenis met ons product verplicht. Bij "Alpro" tegenover ons "Alpro mild & creamy" is de ingrediëntnaam niets anders dan het merk; een treffer daarop zegt alleen "van hetzelfde merk" en maakt koffiemelk weer kandidaat voor yoghurt — precies de eerdere gebruikersmelding. Bij eieren staat "eieren" níet in onze productnaam, dus daar is het wél een eigen aanwijzing. **Ook de volgorde aangepast**: er werd eerst op de ingrediëntscore gesorteerd, en daardoor belandde "Scharreleieren" — exact wat wij kopen — achter drie willekeurige eieren. Nu telt de beste van de twee kanten, daarna de gelijkenis met ons eigen product. **Geverifieerd**: 3 nieuwe tests (eieren komen erdoor en scharreleieren staat vooraan; printerpapier blijft buiten zónder de strengere regel; een ingrediëntnaam die alleen een merk is laat niets extra toe). Alle 22 bestaande matchertests bleven groen. `npm run verify` groen (665/665), `npm run test:e2e` 42/42. **Geen migratie nodig.** |

| Feature | Zelf een zoekterm intypen per ingrediënt | Gebruikersverzoek: "Is er een mogelijkheid dat ik iets in kan typen zodat ie de juiste zoekt? Bv beschuit naturel of iets dergelijks?" Dit is meteen het antwoord op oorzaak B uit `Waarom Dirk niets vindt`, het enige punt daar zonder oplossing: wij noemen het "Snack Tomaatjes", Albert Heijn verkoopt "snoeptomaatjes", geen woord gemeen — en geen enkele vuistregel komt daaroverheen. **Wat er is**: een invoerveld per regel onder "Welke producten, en aanpassen", een nieuw veld `Ingredient.searchTerm` (migratie `20260829181059_ingredient_zoekterm`, additief en nullable), en de server action `setIngredientSearchTerm`. De term gaat vóór alles: vóór het Picnic-product waarmee we normaal zoeken, en vóór de ingrediëntnaam. **Bereik expliciet aan de gebruiker voorgelegd** (globaal versus per huishouden); gekozen voor bij het ingrediënt, consistent met producten en prijzen die hier ook niet huishoudgebonden zijn — het scherm zegt dat er ook bij. **De `code-reviewer` vond een blokkerende fout die ik zelf gemist had**: de term werd alleen gebruikt om de wínkel te bevragen, niet om de resultaten te beoordelen. "Snoeptomaatjes" werd dus keurig opgehaald en daarna weggegooid door `rankStoreProducts(ingredient.name, ...)` — nagemeten: 0 toegelaten op de ingrediëntnaam, 1 op de ingetypte term. Precies de schijnfunctionaliteit uit `AGENTS.md`. Gerepareerd met `matchNameFor()` op alle vier de matchplekken, plus twee plekken die de review zelf ook nog niet had: de crawlroute van Dirk (die de term volledig negeerde, óók bij het kiezen van categorieën) en `getStoreCandidatesForIngredients` — daar draait dezelfde toelating nóg een keer bij het tónen, en zonder die tweede aanpassing vloog het product er alsnog uit. **Geverifieerd**: de nieuwe verversingstest is aantoonbaar rood zonder de fix (nepproduct "Dirk Snoeptomaatjes" bij ingrediënt "Snack Tomaatjes", geen woord gemeen). Een isolatietest op de nieuwe server action (huishouden A zet een zoekterm op een ingrediënt dat alleen op B's lijst staat) sloeg eerst óók zonder afscherming aan — hij las de database vóórdat de server action klaar was; met een tegenproef die op de dóórverwijzing wacht valt hij nu wél om (`actual: 'gekaapte zoekterm'`). **Meegenomen**: e2e-stap 14 viel vanavond drie keer om zonder codewijziging. Oorzaak gevonden: het testproduct heette alleen naar het ingrediënt, en of dat door de toelating komt hangt af van hoe die naam zich verhoudt tot ons eigen Picnic-product — en welk ingrediënt de stap pakt verschilde per run. Nu genoemd naar ons eigen product; drie keer achtereen groen. `npm run verify` groen (666/666), `npm run test:e2e` 43/43. **Migratie draait automatisch mee met de Vercel-deploy** (sinds WP78), dus geen actie nodig. |

| Bugfix | De prijs per verpakking staat er nu bij als het regeltotaal onbekend is | Gebruikersvraag: "Sommige staat wel de juiste link, maar nog niet de prijs ed. Hoe zit dit?" Die streep was het **regeltotaal**, niet de prijs van het product: kan de app niet uitrekenen hoevéél verpakkingen er nodig zijn — bij Skinny Caffe Latte staat "220 ml nodig" en is de inhoud van de Dirk-verpakking onbekend — dan valt er voor die regel geen bedrag te noemen. Terecht, maar karig: wat één verpakking kost weten we wél, en dat verzwijgen levert een scherm op dat minder weet dan de app. `BasketLineStoreResult` en `StoreCell` dragen nu `packagePrice` (uit `candidate.price`, die er al was maar niet werd doorgegeven). Waar het regeltotaal een streep blijft, staat er nu "€ 2,59 per verpakking" onder — nadrukkelijk mét dat achtervoegsel, want dit getal mag niet voor een regeltotaal aangezien worden, en de reden ("verpakkingsgrootte onbekend") blijft er gewoon bij staan. In het uitklapblok komt het op de plek waar anders "3x" staat. Een winkel waar helemaal niets gevonden is houdt `null`: geen product, niets te melden, ook geen prijs. **Geverifieerd**: 2 nieuwe tests (met regeltotaal `null` komt de prijs per verpakking wél mee en blijft de reden staan; zonder gevonden product blijft het `null`). `npm run verify` groen (668/668), `npm run test:e2e` 43/43. **Geen migratie nodig.** |

| Bugfix | Kwark stond bij de melk, en een zoekterm die niets deed zei niets | Gebruikersmelding met schermafbeeldingen: bij het ingrediënt "Magere Melk" stond bij Dirk **"Campina Magere kwark naturel"**, met het label "gelijkwaardig — zelfde soort product". **Oorzaak: mijn eigen fout van de vorige ronde.** De toelating in `rankStoreProducts` vraagt bij een ingrediëntnaam die niets toevoegt aan onze eigen productnaam (het gewone geval: "Magere Melk" tegenover "Picnic magere melk") dat een kandidaat de hélft van onze woorden dekt. De helft van twee is één — "magere" volstond dus, en of het woord dat zegt wát het is ook terugkwam werd niet gecontroleerd. **Eerst een meerderheidsregel geprobeerd (meer dan de helft); die brak een geval waar de drempel juist voor bedoeld was**: ons "Testproduct: Aardappelen" tegenover de "Aardappelen" van de winkel deelt ook precies de helft, en dát is wél hetzelfde product. Het verschil zit niet in hoevéél woorden overeenkomen maar in wélk woord ontbreekt. Nu geldt: de helft van de woorden **én** het woord dat de soort noemt — het laatste betekenisdragende woord van onze productnaam, waarbij verpakkingswoorden (rollen, zakken, plakken, ...) niet meetellen, want "9 rollen" zegt hoe het verpakt is en niet wat het is. Kwark valt af, aardappelen blijft, toiletpapier in stuks blijft. **Tweede punt uit dezelfde melding**: een zelf ingetypte zoekterm die niets oplevert liet de app stilzwijgend terugvallen op haar eigen zoekwoord. Je typte "beschuit naturel", zag daarna nog steeds de vezelrijke variant, en kon alleen maar raden of je term genegeerd was of dat de winkel 'm niet kende. Nieuw veld `unusedSearchTerms`; het statusblok meldt nu `"je zoekterm bij Beschuit: \"beschuit naturel\" leverde hier niets op — er is teruggevallen op de gewone zoekwoorden"`. **Eén bestaande testfixture aangepast**: die liet de bredere zoekopdracht "Merk X gewoon" teruggeven terwijl ons product "Merk X speciaal" heet. Onder de nieuwe regel valt dat terecht af (een andere variant is een ander product); de test gaat over het terugvalmechanisme, niet over variantmatching, dus het fixtureproduct heet nu ook "speciaal". **Geverifieerd**: 4 nieuwe tests, waarvan de kernrest aantoonbaar rood is zonder de fix. `npm run verify` groen (672/672), `npm run test:e2e` 43/43. **Geen migratie nodig.** |

| Bugfix | De prijzenpagina haalde elke prijs op die ooit is vastgelegd | Gebruikersmelding: "Als ik iets zoek dan blijft ie laden en vind niks." Nagevraagd wélke knop: de **Opslaan** bij de zoekterm — een actie die alleen een tekstje wegschrijft en dus een fractie van een seconde hoort te duren. Wat er daarna gebeurt is de doorverwijzing terug naar `/prijzen`, en die pagina wordt volledig opnieuw opgebouwd; zolang dat duurt blijft de knop op "Bezig…" staan. **Gevonden**: `getLatestPrices` haalde met één `findMany` **álle** prijswaarnemingen op voor alle betrokken producten — zonder tijdvenster en zonder maximum — en gooide daarna in JavaScript alles behalve de nieuwste per product weg. Elke verversing (handmatig én nachtelijk) schrijft per product een nieuwe waarneming, dus die query groeit elke dag, en hij draait bij élke weergave van de prijzenpagina. De functie tien regels eronder, `getPriceHistories`, waarschuwt in haar eigen comment precies voor dit risico ("bewust begrensd in de tijd en in aantal, zodat één product met dagelijkse waarnemingen de query niet laat ontsporen") — die begrenzing ontbrak hierboven gewoon. **Nu** haalt Postgres met `DISTINCT ON (product_id)` per product alleen de bovenste rij op, gebruikmakend van de al bestaande index `(product_id, observed_at)`. Bewust ruwe SQL: hier bestaat `DISTINCT ON` voor en de Prisma-client heeft er geen equivalent voor. Gedrag identiek — geen tijdvenster toegevoegd, want dat zou producten met alleen een oude prijs stilzwijgend uit beeld halen. **Geverifieerd**: 2 nieuwe tests (meerdere producten tegelijk leveren elk hun eigen nieuwste prijs mét de juiste winkel; een product zonder waarneming komt er niet in voor), naast de bestaande test die al vastlegde dat de nieuwste waarneming wint. `npm run verify` groen (674/674), `npm run test:e2e` 43/43 — één run viel om op stap 5 (vaste boodschap toevoegen), daarna twee keer groen; die stap raakt deze wijziging niet en de foutmelding is niet vastgelegd, dus dat blijft onverklaard in plaats van weerlegd. **Geen migratie nodig.** |

| Bugfix | CI rood: de matcher wees een seed-product af dat hier niet bestond | **De CI stond rood op #194 en #195** terwijl `npm run verify` hier groen was — precies het geval waar werkafspraak 9 voor bestaat. 9 van de 674 tests faalden. Gereproduceerd door een verse database naast de dev-database te zetten (`fa_ci`, migrate + seed), zonder de bestaande te raken: dezelfde 9. **Oorzaak**: de hoofdwoord-regel uit de vorige rij. Die nam aan dat het laatste betekenisdragende woord van onze productnaam de soort noemt. Op de seed-database heet ons product voor "Aardappelen" echter **"Aardappelen (vastkokend)"** — dan wordt *vastkokend* het hoofdwoord, en valt de winkel-"Aardappelen" af. In het Nederlands staat de soort net zo vaak vóóraan als achteraan; die aanname is gewoon niet houdbaar, en dat had ik moeten zien toen ik er in de vorige rij al een testfixture voor moest aanpassen. **Vervangen door iets dat wél klopt**: waar de ingrediëntnaam niets toevoegt aan onze productnaam, moeten de ingrediëntnaam én ons product het allebei eens zijn. De ingrediëntnaam ís daar onze productnaam min de nietszeggende woorden, en dus de scherpste maatstaf die er is: "Magere Melk" tegenover "Campina Magere kwark" levert 0,50 op en haalt de drempel niet, terwijl "Aardappelen" tegenover ons "Aardappelen (vastkokend)" gewoon 1,00 scoort. Geen hoofdwoord, geen lijst verpakkingswoorden — één regel minder in plaats van meer. **Twee aanpassingen uit de vorige rij teruggedraaid** omdat ze alleen voor die verkeerde regel nodig waren: de testfixture in `priceRefresh.test.ts` ("gewoon" mag weer) en de test over verpakkingswoorden (vervangen door het aardappelgeval uit de seed). **Werkwijze vastgelegd in `OPERATIONS.md`**: hoe je hier tegen een verse database draait zonder de dev-database te wissen, en wanneer dat vóór het mergen hoort. **Geverifieerd**: 674/674 op een verse database én op de dev-database, `npm run test:e2e` 43/43. |

| Bugfix | De tijd die Albert Heijn overhield stond stil terwijl Dirk 'm nodig had | Uit het statusblok, dat sinds #190 eindelijk iets zegt: *"Dirk: 37 producten bijgewerkt, maar niet alles lukte — gestopt bij het tijdslimiet"*, terwijl Albert Heijn in diezelfde ronde géén tijdslimiet meldde maar een 403 op één zoekopdracht. Daarmee is de vraag "waarom vindt hij Alpro en beschuit niet" beantwoord: die producten worden niet afgewezen, ze worden **niet eens bereikt**. Gemeten met de echte Dirk-namen uit de gebruikersschermafbeeldingen accepteert de matcher "Alpro Mild & creamy no sugars" gewoon, zonder ingetypte term, en "1 de Beste Beschuit naturel" met de term die er al staat. **Oorzaak**: de handmatige verversing deelde haar 48 seconden vast in tweeën. Albert Heijn is een API en is ruim binnen haar 24 seconden klaar; Dirk wordt gescrapet, met pauzes tussen de pagina's, en strandde structureel op het limiet — met de staart van de lijst die er nooit aan toe kwam. Het comment daarbij zei dat doorschuiven "de laatste winkel over het totaal zou kunnen tillen", en dat klopt niet zolang je vanaf een vast eindpunt rekent. **Nu** krijgt elke winkel de resterende tijd gedeeld door het aantal winkels dat nog moet, zichzelf meegerekend: is Albert Heijn na 10 seconden klaar, dan houdt Dirk er 38 in plaats van 24, en het eindpunt schuift niet op. `shareOfRemainingTime` staat bewust in `src/domain/pricing/timeBudget.ts` en niet in de server action — een `"use server"`-bestand is niet los te testen, en juist bij tijdslimieten is een verkeerde aanname duur omdat de gebruiker er niets van ziet behalve een knop die blijft draaien. **De 403 van Albert Heijn** is bewust niet "opgelost": die is nu zichtbaar, en omdat een mislukte waarneming `lastSeenAvailable` niet bijwerkt komt dat ingrediënt bij de volgende ronde vanzelf vooraan. Herhaald proberen op een 403 is de winkel harder aanroepen die net nee zei. **Geverifieerd**: 3 nieuwe tests (doorschuiven, de laatste kan er niet overheen, en al verstreken tijd levert geen deadline in het verleden op). `npm run verify` groen (677/677) én 677/677 op een verse database volgens de nieuwe werkwijze in `OPERATIONS.md`. `npm run test:e2e`: één run gaf 4 failures op stappen die deze wijziging niet aanraakt (de e2e roept de verversing nooit aan), daarna twee keer 43/43. **Geen migratie nodig.** |

| Feature | Opslaan van een zoekterm haalt meteen op | Gebruikersmelding met schermafbeelding: "Heb op opslaan geklikt maar hij pakt niks." De zoekterm wás opgeslagen — het veld toonde 'm — maar hij werkte pas bij de eerstvolgende verversing. Dat stond er ook bij, en toch is het een ontwerpfout: je doet precies wat het veld vraagt, ziet niets gebeuren, en moet zelf onthouden dat er verderop nóg een knop staat. Twee keer op rij liep de gebruiker daar tegenaan. **Nu** haalt "Opslaan" meteen op wat die term bij beide winkels oplevert, en meldt het scherm de uitslag: `"Zoekterm opgeslagen en meteen gebruikt"` óf, in oranje, `"Zoekterm opgeslagen, maar geen van beide winkels vond hier iets op. Probeer een ander woord — precies zoals het bij de winkel heet."` Dat tweede is de eerlijke uitkomst die je vroeger helemaal niet kreeg. **Hoe**: `getPricedIngredients` kreeg `onlyIngredientId`, zodat de bestaande verversing één regel kan doen in plaats van de hele lijst. Bewust géén `PriceRefreshRun` weggeschreven — dit is geen verversing van de lijst en het zou het statusblok vervuilen met "2 producten bijgewerkt" naast de echte rondes. Tijdsbudget van 20 seconden, verdeeld met dezelfde `shareOfRemainingTime` als de grote knop, want dit gebeurt terwijl iemand naar "Bezig…" kijkt. Een storing bij de ene winkel sleept de andere niet mee. **De afbakening blijft gelden**: één ingrediënt kunnen aanwijzen is geen sluiproute om prijzen op te halen voor iets wat in geen enkel recept of vaste boodschap zit — daar is een test voor. **De UI-tekst is aangepast**: er staat niet langer "werkt vanaf de eerstvolgende verversing" maar dat opslaan meteen ophaalt en dus een paar tellen duurt. **Eén e2e-test aangepast**: die wachtte op de oude statusmelding, en opslaan doet nu echt netwerkverkeer (in de testomgeving onbereikbaar, dus dat wordt "zoekterm-leeg" — beide uitkomsten zijn goed, het gaat daar om de afscherming). **Geverifieerd**: 2 nieuwe tests. `npm run verify` groen (679/679) én 679/679 op een verse database. `npm run test:e2e` 43/43. **Geen migratie nodig.** |

| Feature | Je boodschappenlijstje inspreken | Gebruikersverzoek: in de winkel bedenk je wat je nodig hebt, en dan is tikken met een mandje in je andere hand het probleem — "dat we ergens in de tool een microfoon kunnen openen (...) en dat die dan alles onder elkaar zet en daar weer de passende producten bij pakt". **Het meeste bestond al**: het blok "Nog iets nodig?" op /boodschappen neemt een rijtje met komma's, zoekt elke regel apart op bij Picnic en zet er productsuggesties bij, alleen voor deze week. Wat ontbrak was de weg van je stem naar dat veld — en, belangrijker, het uit elkaar halen van een gesproken zin. **Gemeten vóór de wijziging**: "melk, brood, drie pakken hagelslag en een pot pindakaas" leverde drie regels op waarvan de laatste letterlijk `"drie pakken hagelslag en een pot pindakaas"` was. "en" splitste niet, telwoorden voluit werden geen aantal, en aanloopjes als "doe maar" of "we hebben nog ... nodig" belandden in de zoekterm. **Nu**: `parseBulkFixedGroceryInput` splitst ook op "en" (als heel woord), kent Nederlandse telwoorden tot twaalf, strípt aanloop- en afsluitwoorden, en kent meer verpakkingswoorden (pot, bakje, blik, rol, bos, krop, tros, ...). Dezelfde parser voedt óók het bulkveld voor vaste boodschappen, dus dat profiteert mee. Diezelfde zin levert nu op: `1x melk · 1x brood · 3x hagelslag · 1x pindakaas`. **De microfoonknop** (`DictateButton`) gebruikt de spraakherkenning van de browser en **verschijnt alleen als die er is** — een knop die je kunt indrukken en die niets doet is erger dan geen knop, en op een telefoon zonder ondersteuning staat de microfoon van het toetsenbord er toch al, die werkt in ditzelfde veld. Hij **verstuurt niets zelf**: wat je inspreekt komt in het tekstvak, jij leest het na, jij drukt op zoeken — spraakherkenning verstaat geregeld iets verkeerd en dat wil je zien vóór er producten in je mandje belanden. Er staat bij dat de herkenning bij Apple respectievelijk Google gebeurt. **Geen nieuwe afhankelijkheid, geen sleutel, geen kosten.** Ondersteuning wordt gelezen met `useSyncExternalStore` en niet met `setState` in een effect (dat laatste werd terecht door de lint tegengehouden). **Geverifieerd**: 5 nieuwe tests, waaronder de drie gesproken zinnen uit het verzoek en één die vastlegt dat getikte lijstjes precies blijven werken zoals ze werkten. `npm run verify` groen (684/684) én 684/684 op een verse database. `npm run test:e2e`: 43/43 na één ongerelateerde flake op stap 14. **Geen migratie nodig.** |

| Bugfix | Een ingesproken zin wordt nu zelf in losse producten geknipt | Gebruikersmelding direct na het vorige pakket: "ik spreek nu meerdere producten in, alleen het systeem pakt alleen maar de producten na de komma. Nu zet ie alles achter elkaar en pakt ie dus niks." **Nagemeten en bevestigd**: `"melk brood hagelslag pindakaas"` leverde één regel op met die hele zin als zoekterm, en `"twee pakken melk drie bananen appelmoes"` twee regels waarvan de tweede `"melk drie bananen appelmoes"` heette. De vorige ronde had de parser geleerd om te splitsen op komma's, regels en "en" — maar wie zijn lijstje opnoemt gebruikt geen van drieën. **Nieuw**: `src/lib/spokenList.ts` knipt zo'n woordenreeks zelf op, uit drie bronnen in volgorde. (1) **Wat de app al kent**: de bestaande ingrediëntnamen van meer dan één woord, genormaliseerd met dezelfde `contentWords` als de productmatcher — staat "Drinkyoghurt Framboos" in de lijst, dan blijft dat één product, en dat hoeft niet geraden te worden. (2) **Aantallen, verpakkingen en aanloopwoorden horen bij wat erna komt** ("doe maar twee pakken melk" is één regel). (3) **Bijvoeglijke woorden plakken aan hun buurwoord**: "magere" aan wat volgt, "naturel" aan wat eraan voorafgaat. De woordenlijsten voor (2) komen uit `fixedGroceryProductChoice` en zijn daar nu geëxporteerd in plaats van gekopieerd — twee eigen kopieën gaan onvermijdelijk uit elkaar lopen. **De belangrijkste keuze zit niet in het knippen maar in wannéér het gebeurt.** Raden waar de grenzen liggen kán misgaan: wie "drinkyoghurt framboos" intíkt bedoelt één product, en de app hoort daar niet overheen te gaan raden. Daarom knipt ze alleen tekst die werkelijk van de microfoon komt: `DictateButton` zet een verborgen veld `gesproken` op "1", en zonder dat veld gebeurt er niets — getikte invoer werkt exact zoals ze werkte. Een eerdere opzet ("knip alleen als er nog geen komma in staat") is verworpen omdat ze precies het gemelde geval mist: de dicteerknop zet zelf een komma na elke stilte, en de zin die geknipt moet worden zit júist bínnen zo'n stuk. **Wat je ziet is wat de app gebruikt**: de geknipte tekst komt terug in het invoerveld en gaat mee in de formulieren, zodat je 'm kunt corrigeren en zodat toegevoegde regels er weer netjes uit verdwijnen — zonder dat had `removeBulkFixedGroceryLine` de originele zin niet meer teruggevonden. De tekst onder de knop belooft nu ook het juiste: "noem gerust je hele lijstje achter elkaar". **Geverifieerd**: 9 nieuwe tests, waaronder de drie gemeten zinnen uit de melding, een bekende naam van twee woorden die heel blijft, en een reeks die op vulwoorden eindigt (die mag geen half product opleveren). `npm run verify` groen (693/693) én 693/693 op een verse database. `npm run test:e2e` 43/43. **Geen migratie nodig.** |


## Operationele kennis

Verplaatst naar `OPERATIONS.md`: Prisma-migraties in een niet-interactieve
sandbox, productie-migraties tegen Supabase (inclusief de poolerpoorten en
de `DIRECT_URL`-valkuil), testconventies, architectuuroverzicht en
beveiligingsincident-historie. Raadpleeg dat bestand gericht wanneer je
tegen een van die onderwerpen aanloopt.
